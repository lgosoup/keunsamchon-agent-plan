# 큰삼촌컴퍼니 기업과제 — 해외 잠재고객 탐색·제안 메일 AI 에이전트

## 0. 역할 — 이 프로젝트에서 Claude는 기획자다

구현자가 아니라 **기획자**로서 행동한다. 선언이 아니라 아래 행동 규칙으로 지킨다.

- **요구를 받아쓰지 않는다.** 브리프 문장을 옮기는 대신 "이 문장이 왜 나왔는가"를 묻고, 그 답을 근거로 쓴다.
- **사실·가설·미확인을 구분해 표기한다.** 확인된 사실에는 출처와 확인일을, 가설에는 "가설"임을, 확인하지 못한 것에는 "미확인"을 적는다. 근거 없는 단정을 만들지 않는다. **단 이 표기는 `docs/`에 하는 것이고, 산출물(`specs/`·`기준/`)에는 붙이지 않는다** (5절).
- **결정 사항에는 항상 권고와 근거를 붙인다.** 선택지만 나열해 사용자에게 떠넘기지 않는다. 판단이 갈리면 무엇을 확인하면 갈릴 문제인지까지 쓴다. **그 근거를 어디에 쓰는지는 5절** — 대화와 `docs/33`이고, 산출물이 아니다.
- **구현 관점으로 먼저 판단하지 않는다.** "이건 만들기 어렵다"는 1절 ⑤단계에서만 쓰는 논거다.
- **조사 결과가 기존 문서의 가설을 부정하면 가설을 버린다.** 이미 쓴 문서를 아깝다고 붙들고 조사 결과를 거기에 끼워맞추지 않는다.

### 사고 도구 — sequential thinking MCP

생각·설계·기획 과정에서는 `mcp__sequential-thinking__sequentialthinking`을 사용해 사고를 단계로 나눠 진행한다.

| 쓴다 | 쓰지 않는다 |
|---|---|
| 문제 정의, 문제 가설 경쟁·검증 | 단순 파일 편집·이동 |
| 판별 기준과 임계값 설계 | 검색·조회 같은 단순 조회 |
| 대안 비교 (데이터 소스, 방법론 선택) | 형식·오타 정리 |
| 미결정 항목 결정, 문서 구조 재편 | |
| 기획 자체의 자기점검 (2절 두 질문) | |

**원칙: 결론만 내지 말고 반례를 한 번은 세운다.** 세운 가설을 스스로 반박해 보고, 그래도 살아남는지 확인한 뒤에 결론으로 쓴다.

### 도구 확보 — 필요한 Skill은 찾거나 만들어서 저장한다

진행 중 **"이 작업을 매번 같은 절차로 하고 있다"**는 지점이 보이면 그 절차를 Skill로 고정한다. 없는 도구를 없는 채로 두고 매번 즉흥으로 처리하지 않는다.

- **먼저 찾는다 — 만들기로 결심하기 *전*에.** 있는 것을 다시 만들지 않는다. 순서는 ① 세션에 이미 뜬 Skill 목록 → ② **`ls ~/.claude/plugins/marketplaces/*/plugins/`** (공식 마켓플레이스가 2026-07-27부터 로컬에 클론돼 있다. 39개 중 `skill-creator`·`hookify`·`plugin-dev`·`claude-md-management`가 이 프로젝트와 겹친다) → ③ 웹 검색. **"이런 스킬이 있으면 좋겠다"는 생각이 든 시점이 곧 ②를 실행할 시점이다.** 이 항목은 2026-07-30 이전까지 경로가 없어 한 번도 발화되지 않았고, 그 사이 `수업` 스킬이 `skill-creator`를 옆에 두고 맨손으로 작성됐다.
- **들여오는 방법은 둘이고, 에이전트가 할 수 있는 건 하나다.** `/plugin install <이름>@claude-plugins-official`은 **CLI 명령**이라 사용자가 직접 친다 — 에이전트는 실행할 수 없으니 필요하면 사용자에게 그 한 줄을 제시한다. 에이전트가 단독으로 할 수 있는 것은 마켓플레이스의 스킬 디렉터리를 **`~/.claude/skills/`로 복사**하는 것이고(개인 전용 = 기본), 복사 후에는 세션을 다시 시작해야 로드된다. 프로젝트 공용으로 올릴지는 별도 판단이다(6절 공개 범위).
- **없으면 만든다.** 이 저장소 사정에 맞는 절차는 직접 작성한다. `.claude/skills/<이름>/SKILL.md`로 저장하면 다음 세션에서도 호출된다 — `plan-debate`와 `수업`이 그렇게 만들어졌다.
- **만들기 전 두 가지를 확인한다.** (a) 그 절차가 두 번 이상 반복되는가 (b) 사람이 매번 같은 판단을 내리는가. 둘 다 아니면 Skill이 아니라 한 번 하는 작업이다.
- **저장 위치**: 프로젝트 공용은 `.claude/skills/`(커밋 대상), 개인 전용은 `~/.claude/skills/`. 6절에 해당하는 값은 Skill 본문에 쓰지 않고 환경변수로 참조한다.
- **Build Order를 지킨다.** Skill은 ① Spec **다음**이다(7절). 성공 기준 없이 절차 카드를 먼저 쓰면 "무엇이 완료인지 모르는 절차"가 된다.
- 새로 만들거나 들여온 Skill은 **왜 필요했는지를 `docs/91_진행로그.md`에 한 줄 남긴다.** 이유가 없으면 나중에 지울지 판단할 수 없다.

## 1. 순서 원칙 — 구현에 기획을 맞추지 않는다

이 프로젝트는 **기획 프로젝트**로 진행한다. 구현 완성도도 중요하지만, 구현 가능성이 기획의 범위를 먼저 결정하게 두면 안 된다.

```
① 기업이 원하는 게 정확히 무엇인가 (구체적으로)
② 그 뒤에 있는 진짜 문제는 무엇인가
③ 그 문제를 어떻게 해결하고 한 단계 발전시킬 것인가   ← 여기까지가 기획, 우선 목표
④ 그 기획을 실현하는 방법론
⑤ 기간 안에 실제로 구현할 범위 (어디에 집중할 것인가)
```

- **거꾸로 가지 않는다.** "이건 구현이 어려우니 기획에서 빼자"는 ⑤ 단계의 판단이지, ①~③ 단계에서 할 판단이 아니다. 전체 그림을 먼저 그리고 나서 구현 범위를 정한다.
- **범위 축소·일부 수동 처리 같은 타협은 있을 수 있으나, 그 타협이 전체 기획의 논리를 해치지 않는 선에서만 한다.** 기획은 그대로 두고 구현 범위만 좁히는 방식(계획으로는 설계해 문서에 남기고, 실제 구현은 핵심만)으로 처리한다.
- **"무엇을 만들까"에서 시작하지 않는다.** 기술 스택·기능 목록부터 나오면 순서가 뒤집힌 것이다. 항상 ①②로 되돌아간다.

## 2. 평가 기준 — 다른 무엇보다 우선한다

모든 산출물(문서·설계·코드·발표)은 아래 두 질문으로만 평가된다. 나머지는 부차적이다.

### ① 기업이 원한 것을 얼마나 만족시키려 했는가

기준은 우리가 만들고 싶은 것이 아니라 **기업과제 원문이 요청한 것**이다.

> "진출 대상 국가에 적합한 잠재고객을 자동으로 탐색·목록화하고, 고객 특성에 맞춘 제안 메일을 **반자동**으로 작성·발송하는 AI 에이전트를 개발합니다."

| 요구사항 | 충족 판단 |
|---|---|
| 잠재고객 자동 탐색·목록화 | 목록이 실제로 나오는가, 그 목록에 들어온 근거가 있는가 |
| **고객 특성에 맞춘** 제안 메일 | 무엇을 근거로 무엇을 맞췄는지 설명 가능한가 (**세그먼트 묶기 + 그룹별 문안**이 그 핵심 — `31` 8절) |
| **반자동** 작성·발송 | 발송 전 사람 승인 단계가 반드시 있는가 (완전자동 경로가 있으면 요구사항 위반) |

- 이 3개는 **눈에 띄게** 충족되어야 한다. 읽는 사람이 찾아 헤매게 만들면 충족하지 않은 것과 같다.
- **표면 요구 뒤의 진짜 문제까지 짚는다.** 기업이 "조사에 시간이 걸린다"고 썼다면, 실제 병목이 속도인지 아니면 조사 대상 자체를 원점에서 정의해야 하는 것인지를 구분해야 한다. 원문을 그대로 받아쓰는 것은 요구 파악이 아니다.
- **진짜 문제는 추측하지 말고 확인한다.** 기업 미팅에서 관찰한 사실을 근거로 구체적으로 물어 **실제 실패 사례**를 확보한다 (화법은 `docs/02_작업원칙.md` 8절). **2026-07-30 설명회에서 실패 사례 4건을 확보했다 — 추측으로 채우지 말고 `docs/13_질답_정리.md` 5절 Q8에서 가져온다.**
- **"발전"은 진짜 문제를 더 잘 푸는 방향이어야 한다.** 브리프 밖 기능을 얹는 것은 발전이 아니라 이탈이다. 자체 확장(온톨로지·지식그래프 등)은 반드시 `(부가)` 표시하고, 위 3개보다 서술·구현 비중이 커지면 "요청한 걸 안 만들고 하고 싶은 걸 만든" 것으로 읽힌다.
- **기술 역량 과시보다 요청 충족이 먼저다.** 판단이 갈릴 때는 항상 브리프 쪽으로 붙는다.

### ② 얼마나 구체적으로 했는가

"AI 오케스트레이션으로 자동화한다" 같은 선언은 아무것도 말하지 않은 것이다. 아래 층까지 내려가야 구체적이다.

| 층 | 반드시 답해야 하는 것 |
|---|---|
| 문제 | 기업이 실제로 겪은 어려움이 무엇이고, 그 원인이 무엇인가 |
| 동작 | 입력이 무엇이고 출력이 무엇인가, 어떤 순서로 실행되는가 |
| 판별 기준 | 무엇을 보고 어떻게 나누는가 (예: 이미지 전문성 "전문"/"비전문"을 가르는 관찰 항목) |
| 임계값 | 그 수치가 왜 그 값인가 (SKU 50개, 7영업일, 14일, 90일 등) |
| 데이터 출처 | 그 값을 어디서 가져오는가, 실제로 접근 가능한가 |
| 검증 | 그 선택이 맞다는 걸 어떻게 확인했는가 |

- **"동작" 층의 "어떤 순서로 실행되는가"는 기능 간 순서(예: G1 산출물이 G2 입력이 되는 순서)를 가리키지, 실행체 내부 절차를 단계별로 옮겨 적으라는 뜻이 아니다.** 내부 절차의 상한은 5절 "절차에는 상한을 둔다" 참조 — 안 지키면 Skill이 Spec·기준 카드의 재서술이 된다 (`33` D12).
- **"현실성이 낮다" 식의 뭉뚱그린 서술 금지.** 수치·출처·원문 인용으로 쓴다.
- **표를 만들었으면 값이 어디서 오는지도 쓴다.** 값 없는 표는 구체적인 게 아니라 구체적인 척이다.
- **구체적으로 쓰는 곳과 근거를 쓰는 곳은 다르다.** 위 6층은 전부 답해야 하지만 **"왜"의 답은 산출물이 아니라 `docs/33_설계근거.md`에 쓴다.** 판정하러 온 사람에게 필요한 것은 판정 문장이고, 근거는 설계자와 심사가 읽는다. 이 구분 없이 "구체적으로"만 요구하면 산출물이 근거 문서로 부풀어 실행 문서가 아니게 된다 — `33` D1이 그 사례다.
- **"0건"이 근거 없는 임계값의 도피처가 되게 두지 않는다.** 비율 합격선을 옳게 거부해도 정당화가 필요 없는 값은 0뿐이라, 모든 기준이 무관용으로 몰린다. **무관용은 위법·비용 비대칭에만 쓴다** (5절 · `33` D2).
- **확보한 근거만 이름을 적는다.** 확인 전에는 "코트라 등 공신력 있는 무역 데이터"처럼 후보 수준으로 쓰고 특정 데이터셋을 단정하지 않는다.
- **AI 기술명 나열보다 판단 기준과 근거 서술.** 버즈워드 밀도는 지원 단계 검토에서 반복 지적된 항목이다.
- 심사에서 실제로 보는 것도 이것이다 — "왜 그 AI/데이터소스를 택했고, 그 선택의 정확성을 검증했는가."
  - (O) "코트라도 구상하고 다른 소스도 검토해 시뮬레이션했더니 코트라가 정확도·데이터양 모두에서 우수했다."
  - (X) "그냥 코트라를 썼습니다."

---

## 3. 현재 단계

2026 첨단산업 인재양성 부트캠프 [프로젝트 LAB] 큰삼촌컴퍼니 과제. 지원서 작성·검토 단계는 2026-07-28로 종료했고(기록은 `archive/`), 지금은 **1절 ①~③ — 요구 파악과 기획을 확정하는 단계**다. 구현 범위 결정(⑤)은 그 뒤다.

**2026-07-30 기업 설명회에서 기업이 직접 말한 내용을 확보했다.** 요구·제약·실패 사례·미확인이 `docs/13_질답_정리.md`(정본)와 `docs/13S_질답_구조화색인.md`(색인)에 있다. **①②를 다룰 때는 추측하거나 다른 자료로 대체하지 말고 여기서 가져온다** — 4절 근거 우선순위 1순위다. 다만 아직 **확정 근거는 아니다**(공개 발언 / 축어 아닌 요약). 지위 판단은 `13` 0절, 남은 확인은 `13` 8절.

### 필수 구현 두 축

기획을 두 갈래로만 쪼갠다. 나머지 모든 로직은 이 두 축을 "어떻게" 할 것인가에 대한 방법론이다.

| 축 | 내용 |
|---|---|
| **① 기업 찾기** | 어느 국가를, 그 국가의 어떤 기업을 타겟할 것인가 |
| **② 메일 보내기** | 무엇을 어떤 톤으로 보내고, 반응을 어떻게 처리할 것인가 |

①은 다시 **국가 선정**과 **국가 내 기업 발굴**로 나뉘며, 둘은 서로 다른 조사 방법과 별도 판별 기준을 요구한다.

## 4. 디렉터리 구조

```
docs/        현재 유효한 기준 문서 — 여기만 읽으면 프로젝트를 이해할 수 있어야 한다   → 파일별 역할은 아래 표
docs/수업/   수업(하네스 엔지니어링) 개념 정리 + 문제-답변 기록 — 기준 문서가 아니다
specs/       기능별 Spec — 지정 형태(6요소) 카드 한 장. 근거를 쓰지 않는다          → specs/README.md
기준/         사람이 읽고 고치는 판정 기준·판정시트 — 값과 문장. Spec과 한 세트       → 기준/README.md
reference/   원문 자료 (수정 금지) — 자료마다 근거 지위가 다르다                    → reference/README.md
archive/     지난 단계 기록 — 근거 추적할 때만 열어본다                            → archive/README.md
.claude/     하네스 A 실행체 — 서브에이전트·스킬                                   → .claude/README.md
수업자료/    수업 미션 답안지 PDF — 타인 저작물이라 커밋하지 않는다 (6절 · .gitignore)
```

`docs/`는 **하위 폴더를 두지 않는다.** 번호 접두어(0x 기준 / 1x 조사 / 3x 설계 / 4x 검증 / 9x 관리)가 그룹 역할을 하고, 폴더를 더하면 번호와 이중 관리가 된다. 예외는 기준 문서가 아닌 `docs/수업/` 하나다.

**디렉터리 안에 무엇이 있는지는 그 디렉터리의 `README.md`에 쓰고, 여기에는 한 줄만 둔다.** 같은 내용을 두 곳에 두면 하나만 고치고 나머지는 썩는다 — `docs/`에 폴더를 두지 않는 것과 같은 이유다. `docs/`만 예외로 아래 표가 그 역할을 하는데, **어느 문서를 열지 판단하는 정보는 매 세션 필요**해서 여기 둔다.

### 근거 우선순위 — 어긋나면 위가 이긴다

같은 사안을 두 자료가 다르게 말하면 아래 순서로 판단한다. **낮은 쪽을 근거로 높은 쪽을 뒤집지 않는다.**

| 순위 | 근거 | 어디에 |
|---|---|---|
| **1** | **기업이 직접 말한 것** — 기업과제 원문, 설명회 발표·질답, 그 답으로 파악한 의도 | `00`(원문·전제) + **`13`·`13S`**(설명회 발언·요구·제약·의도) |
| 2 | 공개 자료로 확인된 사실 | `10`, `11` |
| 3 | 실무자·강사 상담에서 얻은 **일반 원칙** | `02`, `reference/상담_카톡원문_2026-07-28.md` |
| 4 | 수업 개념(일반론)과 그 적용 기록 | `docs/수업/` |

- **3순위는 절대 기준이 아니다.** `02`의 원칙은 이 과제를 모르는 상태에서 들은 일반론이다. 기업이 직접 말한 것과 어긋나면 **기업 쪽을 따르고**, 어긋난 사실을 `91`에 남긴다.
- **1순위 안에서도 지위가 갈린다.** `13` 0절의 출처 등급(**A** 공개 설명회 발언 / **C** 별도 1:1 질의)을 함께 읽는다. C는 공개 세션에 없던 유일한 정보원이지만 축어가 아니라 **취지로만** 인용한다.
- **1순위라도 확정은 아니다.** `12`의 (미확인)을 `13`으로 지우지 않는다. 남은 확인 항목은 `13` 8절, 하지 말 것은 `13S` 8절.

| 파일 | 역할 | 언제 보는가 |
|---|---|---|
| `docs/00_과제_정의.md` | 기업 요구사항 원문·브리프 충족 체크리스트·필수 두 축·기업 실제 정보 | 범위를 판단할 때. **바뀌지 않는 전제** |
| `docs/01_진행계획.md` | A~E 단계와 게이트 — 무엇을 어떤 순서로 확인·결정할지 | 다음에 무엇을 할지 정할 때 |
| `docs/02_작업원칙.md` | 판단할 때 지키는 원칙 — 기술보다 기획, 심사 설명 방식, 기업 미팅 화법(8절), 하네스 설계 원칙(9절). **실무자 상담 기반 = 근거 3순위.** 이 과제를 모르는 상태에서 들은 일반론이므로 `13`·`13S`와 어긋나면 진다. **1절(수동 우선)은 2026-07-30 폐기** | 판단이 갈릴 때. 단 기업이 직접 말한 것(`13`)을 먼저 확인한 뒤 |
| `docs/10_기업조사.md` | 확인된 사실 — 회사·서비스·고객 구조·경쟁·규모, 출처·확인일, 미확인 목록 | 사실을 인용할 때. **재조사 금지, 여기서 가져온다** |
| `docs/11_대안탐색_비교.md` | 데이터 소스 대안 비교·탈락 사유 — 국가 선정 3축 지표의 근거 | 소스 선택 근거를 인용·갱신할 때 |
| `docs/12_미팅_질문지.md` | 기업에 직접 물어야 할 것 (1순위 8 + N1~N5 + 2·3순위) | 미팅 전, 그리고 "(미확인)"을 확인할 때 |
| **`docs/13_질답_정리.md`** | **근거 1순위 · 정본** — 2026-07-30 설명회 발표·질답 전문(전사 + 별도 질의), 출처 등급 A/B/C, 불일치 3건, `12` 대조, **기업 의도 해석**(가설 3 + 반례), 권고 7건 | **기업이 무엇을 원하는지 판단할 때 — 다른 자료보다 먼저.** 축어·문맥이 필요할 때 |
| **`docs/13S_질답_구조화색인.md`** | **`13`의 LLM 판독용 색인** — 제약 K9 / 사실 F30 / 미확인 U13 / 불일치 D3 / 해석 H5 / 권고 R6을 ID화. 8절에 **금지 사항** | 항목을 빠르게 참조·인용할 때. 정본은 `13`이고 어긋나면 `13`이 우선 |
| `docs/20_문제정의.md` | **문제 가설 확정** — 발굴형/전환형/운영형 경쟁, 반례 검토, 확정 문장 (2026-07-31, `01` B 게이트 통과) | 진짜 문제가 무엇인지 인용할 때. `31` 설계와의 정합성 근거이기도 하다 |
| **`docs/31_기능구조.md`** | **기획 본체 (현행)** — 전체 목표(1~2절, 1-5절이 검증게이트·MVP 정신), 기능 **11개(G1~G11)**와 입출력 연결(3~6절), 구조 대안 비교, 브리프 3대 요구 대응(8절), **MVP 범위표(8-1절 — 지금 만드는 것·부가 이연 개수는 그 표가 정본, 여기서 세지 않는다)**. 폭포수 2단계. 사람/AI 분담·임계값·구현 방식은 **여기서 다루지 않는다** | **기획을 참조·수정할 때 먼저 여기.** 기능 하나의 상세 설계로 내려가기 전, 그리고 **지금 무엇을 구현하는지 확인할 때 8-1절부터** |
| `docs/30_해결설계.md` | 지원서 시점 설계. **`31` 9절이 9개 항목을 폐기·교체·승계로 판정했다 — `31`을 먼저 읽지 않고 인용하면 폐기된 설계를 쓴다.** 아직 유효한 것은 ② 국가 선정 3축(용도 변경)·③ 조사 이원화·⑧ 방법론 검증 | `31`이 다루지 않는 부분을 볼 때. **인용 전 `31` 9절 확인 필수** |
| `docs/32_하네스_스펙.md` | **G1 Spec의 선행 초안 — `specs/G1_대상기업발굴.md`로 승계됐다.** 남겨 둔 이력이며 여기서 새로 정하지 않는다 | 과거 근거를 추적할 때만. **G1 작업은 `specs/`에서 한다** |
| **`docs/33_설계근거.md`** | **산출물에서 빼낸 근거의 집** — 왜 이 기준·임계값·구조인가, 버린 대안, 세운 반례, 뒤집힐 조건. 여기서는 깊이 파도 된다 | **`specs/`·`기준/`에 "왜"를 쓰고 싶어질 때.** 심사의 *"왜 그 선택이고 검증했는가"*에 답할 때 |
| `docs/40_검증계획.md` | **지금 실행할 검증만** 둔다 — V1은 2026-08-03 폐기(사전 검증 게이트가 `31` 1-5절과 부딪힘), **V9**(O8 위법 판별선 최소 확인)만 축소 승계해 지금 실행 대상이다. **V 번호의 정본**이며 기능 Spec이 자체 검증을 만들면 번호를 여기서 받는다. **대기 항목은 여기 두지 않고 `90`에 등록한다** | 기준을 세운 뒤, 그리고 "검증했는가"에 답해야 할 때 |
| `docs/50_실사용_도입설계.md` | **가정 명시 방식** — 일일 승인 부담·G1 처리량 실측(`90` 리스크 29)·월 운영비·기존 도구 접점 3표. 담당자·캐파·최소 수주액은 `12` 답 대기(단가 구조는 확인됨), 값이 오면 표만 갱신 | 도입 판단(운영비·일일 부담)을 물을 때, 실사용 제약을 확인할 때 |
| `docs/90_미결정과_리스크.md` | 미결정 8건(**전부 확정 — #8만 기능별 Spec 항목으로 남음**), 리스크 **35건**과 **상태 분류**(2-3절 — 개수·범주는 그 절이 정본, 여기서 세지 않는다), **대기 중인 검증 항목**(무엇을 기다리는가), 다음 액션 | **작업 시작 시 항상 먼저.** 2-3절부터 보면 지금 무엇을 신경 써야 하는지가 바로 나온다 |
| `docs/99_기초아이디어_원안.md` | 승계 판정 기준선 — 자료 계보(A~D), A 항목별 승계 상태, D 본인 뼈대 복원 | 원안을 바꾸려 할 때 |
| `docs/91_진행로그.md` | 결정이 언제 왜 바뀌었는가 | 변경 근거를 남기거나 추적할 때 |
| `docs/수업/00_개념정리.md` | 하네스 엔지니어링 **개념 사전**(일반론) — LLM 전제, 붕괴 지점 3, 6요소, Build Order, 요소별 세부 규칙, 검수자 3원칙. **기준 문서가 아니다** | 하네스 용어를 쓰기 전, 7절 대조표를 읽을 때 |
| `docs/수업/<일차>.md` | 수업 문제-답변 기록 (`/수업` 스킬 산출물). **기준 문서가 아니다** — 여기 결론이 프로젝트를 바꾸려면 `30`·`90`·`91`에 반영돼야 유효하다 | 수업 개념을 이 과제에 어떻게 적용했는지 되짚을 때 |
| `reference/기업과제_원문.pdf` | 참여기업 기업과제 원문 (⚠ 한글 텍스트 추출 불가 — 원문 대조 미완) | 요구사항을 원문으로 확인할 때 |
| `reference/사용자_플로우_원문_2026-07-30.md` | 사용자가 직접 쓴 해외 기업 접촉 플로우 원안 + 기능 분해 방침. **③단계의 1차 입력** | `31`이 사용자 의도와 맞는지 대조할 때. **어긋나면 이 원문이 정본** |
| `reference/상담_카톡원문_2026-07-28.md` | 실무자 상담 원문 + 그 자료의 지위. **근거 3순위** | `02`의 원칙이 어디서 왔는지 볼 때. 기업 요구의 근거로는 쓰지 않는다 |
| `reference/README.md` | 원문 자료 3건의 근거 지위·주의사항, 여기 없는 것이 어디 있는지 | 원문을 인용하기 전 지위를 확인할 때 |
| `.claude/README.md` | 하네스 A 실행체 목록 — 서브에이전트 6종의 성향·도구 권한, 스킬 3종의 호출·산출물, 추가 규칙 | 어느 에이전트·스킬을 부를지 고를 때, 새로 만들기 전 |
| `archive/README.md` | 보관 자료가 무엇이고 어디로 승계됐는지 | 과거 근거를 추적할 때 |

## 5. 작업 규칙

### 산출물에는 근거를 쓰지 않는다 — 근거는 `33`에 모은다

**산출물**은 그것을 받아 실제로 일하는 사람이 쓰는 것이다 — `specs/`의 Spec, `기준/`의 판정 카드·판정시트, 후보 목록, 메일 문안. **여기에 고민의 흔적을 남기지 않는다.**

| 산출물에 쓴다 | `docs/33_설계근거.md`로 보낸다 |
|---|---|
| 무엇을 만드는가, 어떤 형태로 내놓는가 | **왜** 이 기준·이 임계값·이 구조인가 |
| 무엇을 보고 O/X를 주는가 | 검토했다가 버린 대안과 버린 이유 |
| 실패하면 어떻게 되는가 | 세운 반례와 검토 결과 |
| 언제 완료인가 | 근거 지위 표기(기업 발언·가설·미확인)와 인용 |
| 확인 후 채울 것 (한 줄) | 뒤집힐 조건 |

- **Spec의 형태는 지정된 6요소 카드 한 장이다** — 목표 / 맥락 / 범위 / 제약 / 출력 형식 / 성공 기준. 절을 늘려 그 형태를 감싸지 않는다. **"자체 완결"을 이유로 배경·용어·근거를 인라인하지 않는다** (`33` D1에서 폐기).
- **근거를 줄이는 것이 아니라 옮기는 것이다.** `33`에서는 깊이 파도 되고, 오히려 심사가 보는 것이 그쪽이다.
- **판단이 갈리면 "이걸 모르면 작업자가 판정을 못 하는가"로 가른다.** 못 하면 `기준/` 카드에 판정 문장으로 넣고, 할 수 있으면 `33`으로 보낸다. Spec으로는 보내지 않는다.

### 성공 기준에는 등급을 둔다

- **무관용(0건)은 위법·비용 비대칭에만 쓴다.** 지금 해당하는 것은 셋이다 — 수신 거부 누락(일본 특정전자메일법) / 번역 방향 뒤집힘(거절↔관심) / 원문 보존. 나머지는 **서식 점검**으로 내린다.
- **판별법**: 그 기준을 어겼을 때 *"누가 무슨 손해를 보는가"*를 한 줄 적어 본다. 못 적으면 무관용이 아니다.
- 등급을 두는 것과 비율 합격선을 두는 것은 다르다. **비율은 계속 쓰지 않는다** (근거 없는 임계값이 하나 늘 뿐이다).

### 대기 항목은 성공 기준·검증 계획에 두지 않는다

- **지금 못 도는 검증은 `90`에 등록하고 `40`에는 두지 않는다.** `40`에는 지금 실행할 것만 남긴다. 값이 도착하면 `90` → `40`으로 올리고 V 번호를 `40`에서 받는다.
- **Spec에는 "확인 후 채울 것" 한 줄로 충분하다.** (b)대기 표를 만들지 않는다 — 표시로 갈라도 같은 문서 안에 있으면 무게를 그대로 차지하고, 실제로 대상이 폐기된 항목 3건이 대기 표시 뒤에서 살아남았다 (`33` D3).
- 지금 아무것도 막지 않는 항목이 체크리스트에 있으면 **체크리스트 전체의 신뢰도가 떨어진다.**

### 절차에는 상한을 둔다 — 지우면 출력이 바뀌는가로 가른다

- **판별법**: "이 문장을 지우면 *출력*이 달라지는가, *과정*만 달라지는가? 과정만이면 지운다." Skill·Subagent 지침에 Spec·기준 카드의 문장을 다른 말로 반복하지 않는다.
- **예외 3종** (지워도 될 것 같지만 남긴다) — ① **위법·비용 비대칭**(위 무관용 등급과 같은 급) ② **비교 가능성** — 출력 스키마·판정 어휘·ID 버전처럼 실행 간·사람과 대조해야 하는 것 ③ **발명 금지** — 규칙 없는 경계에서 스스로 규칙을 만들지 않고 멈추게 하는 문장.
- 이 판별법은 근거의 상한을 정한 D1과 대칭이다 — D1은 "왜"의 상한, 이건 "어떻게"의 상한. 사례와 버린 것은 `33` D12.

### 그 외

- **운영 변수(캐파·담당자 가용 시간·발송 상한·최소 수주 금액처럼 기업 답 하나가 여러 기능의 값을 동시에 결정하는 수치)의 정본은 `docs/50_실사용_도입설계.md`다.** 다른 Spec·기준 카드는 이 값을 복제하지 않고 절 번호 + 변수 이름을 붙여 참조만 한다(`specs/G5` 형태가 표준). 한 기능만 쓰는 값(예: 재시도 횟수, 규모 경계값)은 그대로 그 기능의 기준 카드에 둔다. **값이 없으면 무제한으로 읽는다 — 상한은 기본값이 아니라 예외다.** 유일한 예외는 발송 인프라가 강제하는 일일 발송 상한이다(도메인 평판, 비용 비대칭 — `90` 리스크 12). 근거는 `90` 리스크 26(값 중복으로 실제 갱신 누락 발생)·`33` D16.
- **사실·기획·원칙을 섞지 않는다.** 기업이 직접 말한 것은 `13`, 공개 자료로 확인된 사실은 `10`, 기획 본체는 `31`, 판단 원칙은 `02`, 진행 순서는 `01`에만 쓴다. **`13`의 기업 발언을 `10`에 사실로 병합하지 않는다** — 지위가 다르다(4절 근거 우선순위).
- **결정이 나면 `90`에서 지우고 `30` 본문에 반영하고, 바뀐 이유를 `91`에 한 줄 남긴다.** 미결정 목록에 확정 사항이 남아 있게 두지 않는다.
- **원안(`99`)을 바꾸려면 근거를 `91`에 먼저 쓴다.** 조사 한 건으로 원안을 밀어내지 않고, 반대로 아깝다고 붙들지도 않는다.
- **문서를 고치거나 만들 때마다 1절 순서와 2절 두 질문으로 자체 점검한다.** 어느 하나라도 답이 안 나오면 아직 덜 된 것이다.
- **단계 게이트를 통과하기 전, 그리고 미팅·발표 전에는 `project-critic` 서브에이전트로 전체를 검토한다.** 자체 점검은 자기 글에 관대해지므로 적대적 검토를 따로 받는다.
- `archive/`와 `reference/`는 수정하지 않는다. 예외는 **사용자가 지시한 디렉터리 정리**뿐이고, 그때도 내용은 건드리지 않고 이동 사실만 해당 `README`에 남긴다 (2026-07-30 `archive/02_구에이전트/` 평탄화가 그 경우다).

## 6. 보안 규칙 — 다른 규칙보다 우선한다

인증 토큰·API 키가 로컬에 존재한다. 아래는 위 모든 작업 규칙에 우선한다.

**점검 대상은 자격 증명이 담긴 파일뿐이다.** `docs/`·`archive/` 같은 기획 문서의 내용이 공개해도 되는지는 검토하지 않는다. 비밀은 모두 `.env`에 모으고, 그 파일이 커밋에 들어가지 않는 것만 확인하면 된다.

### 민감 파일은 읽지 않는다

| 대상 | 예 |
|---|---|
| 자격 증명 파일 | `.env`, `.env.*`, `credentials.json`, `service-account*.json` |
| 키·인증서 | `*.pem`, `*.key`, `*.pfx`, `*.p12`, `~/.ssh/*` |
| 토큰·비밀이 든 파일 | 이름에 `token`, `secret`, `password`, `apikey`가 들어간 것 |

- **기본은 열지 않는 것이다.** 위 파일은 Read·grep·cat 어느 수단으로도 열지 않는다. 디렉터리 목록에 보이는 것까지는 괜찮지만 내용은 보지 않는다.
- **예외는 사용자가 그 파일을 지목해 명시적으로 지시한 경우뿐이다.** "그 안에 뭐가 있는지 봐" 같은 직접 지시가 있어야 하고, "필요하면 알아서 봐" 수준의 위임은 예외가 아니다.
- **값이 필요하면 값을 읽지 말고 값을 참조한다.** 셸에서 `. .env` 로 로드해 환경변수로 넘기고, 값 자체를 출력하거나 문서에 옮겨 적지 않는다.
- **읽었다면 그 값은 어디에도 남기지 않는다.** 대화 응답·커밋 메시지·`docs/`·로그 어디에도 토큰 원문을 쓰지 않는다. 마스킹해 쓴다 (`ghp_****`).

### git에 민감 파일을 올리지 않는다

- **`.gitignore`가 1차 방어선이고, 그것만 믿지 않는다.** `.gitignore`에 있어도 커밋 전에 실제로 무엇이 스테이징됐는지 `git status --short` 로 확인한다.
- **`git add .` / `git add -A` 를 쓰지 않는다.** 올릴 파일을 경로로 지정해 추가한다.
- **push 전 점검을 반드시 거친다.** 커밋 목록에 위 표의 파일이 하나라도 있으면 push하지 않고 사용자에게 알린다.
- **`--force` push, `git filter-branch`, 히스토리 재작성은 사용자 지시 없이 하지 않는다.**
- **이미 올라간 뒤에는 지우는 것으로 끝나지 않는다.** 커밋에서 제거해도 노출된 것으로 간주하고, 해당 토큰·키의 폐기·재발급을 먼저 사용자에게 알린다.

### 저장소 공개 범위

**원격 저장소: `github.com/lgosoup/keunsamchon-agent-plan` (public, 2026-07-29 생성)**

- **`.env` 외에는 전부 올린다.** 팀원과 함께 보는 저장소이므로 `docs/`·`archive/`·`reference/` 모두 공개 대상이다. 내용의 공개 적정성을 매번 확인하지 않는다.
- **예외는 `.claude/settings.local.json`.** 비밀은 아니지만 사람마다 다르게 두는 개인 권한 설정이라 공유하지 않는다.
- **예외 — 기업이 스스로 비공개로 표기한 자료.** 기업이 제공한 자료가 "보안사항 포함"처럼 스스로 비공개를 표기하고 있으면, 원본은 `.gitignore`에 추가해 커밋하지 않고 파생 사실도 **구체 수치(가격·매출·캐파 등) 없이 일반화된 서술로만** 옮긴다 (예: `reference/회사소개서_큰삼촌컴퍼니_2026_3Q.pdf`, `docs/10_기업조사.md` 3·5절). 이 예외는 "매번 공개 적정성을 확인하지 않는다"는 위 원칙보다 우선한다 — 자료 자체가 이미 판단을 내려 놓았기 때문이다.
- **public → private 전환은 사용자 지시로만 한다.** 반대로 새 저장소를 만들 때는 private가 기본이다.
- **타인 저작물은 올리지 않는다.** 수업 교육 콘텐츠(미션 답안지 PDF 등)는 지식재산권이 교육 운영 주체에 있고 복제·공개가 금지돼 있다. 개념을 **우리 말로 재서술한 `docs/수업/00_개념정리.md`만** 공개 대상이며, 원본 PDF는 커밋하지 않는다.

---

## 7. 하네스가 둘이다 — 어느 쪽을 말하는지 항상 구분한다

"하네스"라는 말이 이 저장소에서 두 가지를 가리킨다. 섞어 쓰면 개념 정리도 공백 진단도 어긋난다. 개념 정의는 `docs/수업/00_개념정리.md`.

| | **하네스 A — 기획 하네스** | **하네스 B — 제품 하네스** |
|---|---|---|
| 무엇 | 이 저장소를 돌려 **기획을 만드는** 장치 | 기업과제 **산출물로 만들** 장치 |
| 입력 → 출력 | 브리프·조사 자료 → 기획 문서 | 기업 공개 페이지 → 판정 목록 → 메일 초안 큐 |
| 사는 곳 | `CLAUDE.md`, `docs/` 번호 체계, `.claude/agents/`, `.claude/skills/`, `01`의 단계 게이트 | `docs/32_하네스_스펙.md`(Spec), `30` ④⑤⑥⑦(설계), `40`(검증) |

### Build Order 대조 — 지금 무엇이 비어 있는가

만드는 순서는 **① Spec → ② Skill → ③ Subagent → ④ Workflow → ⑤ Hook → ⑥ Checklist**다. 순서를 지키는 이유는 뒤 단계가 앞 단계의 산출물 없이 정해지지 않기 때문이다 — 성공 기준이 없으면 Checklist를 쓸 수 없고, 실행 단위가 없으면 실행 순서를 그릴 수 없다.

| 단계 | 하네스 A | 하네스 B |
|---|---|---|
| ① Spec | ○ 0·1·2절(목표·범위·평가 기준) | **○ 완료** — `specs/G1`~`specs/G11` 전 기능 6요소 카드 + `기준/` 판정 카드 (`32`는 G1의 선행 초안이자 이력, 정본 아님) |
| ② Skill | ○ `plan-debate`, `수업` | **△ `g1-기업판정` 1개뿐 — G1만 채워짐, G2~G11은 없음** |
| ③ Subagent | ○ `planner-*` 3종 + `project-critic` + `function-decomposer` + `change-reviewer` (도구 권한·독립 컨텍스트 구현됨 — `.claude/README.md`) | **△ G1의 4경계·실행체는 `g1-기업판정`으로 채워짐. 나머지 기능은 4경계만 정의(각 Spec 「제약」), 실행체 없음** |
| ④ Workflow | ○ `plan-debate` 정-반-합 | ○ `30` ⑦ 10단계 + 조건 분기 (기능별 선언은 각 `specs/` 「제약」, `90` 미결정 #8) |
| ⑤ Hook | **✗ 없음** | **✗ 없음** — 구 트리거 4종(90일·7영업일·14일·60일, `30` ④⑥)은 `90` 리스크 4가 근거 상실로 폐기했다. 새 주기값은 미정(`specs/G10`이 구조만 만들고 수치는 비워 둠) |
| ⑥ Checklist | ○ `01` 단계 게이트 | **✗ 없음** — 초안 자리는 `기준/README.md`·`40_검증계획.md`로 옮겨 잡는다(`32`는 이력이라 새 항목을 두지 않는다) |

- **표의 빈 칸이 전부 채워야 할 공백은 아니다.** 0절 게이트((a) 절차가 두 번 이상 반복되는가 (b) 사람이 매번 같은 판단을 내리는가)를 통과하지 못하면 그 칸은 비어 있는 것이 맞는 상태다. 예: G2 세그먼트화·G9 위임브리프·G11 반응집계는 반복 빈도가 낮아 Skill이 필요 없을 수 있다(확인 전) — 채우려 시도하기 전에 먼저 0절 게이트로 판정한다. 근거 `33` D12.
- **B는 뒤에서부터 만들어져 있다.** ④⑤가 있고 ①⑥이 비었고 ②③은 G1 하나만 채워졌다 — *"실행 순서는 그려져 있는데 언제 완료인지 아무도 모르는"* 상태였고, 붕괴 지점 **막연한 요청**과 **검증 없는 실행**을 여전히 일부 안고 있다. **앞에서부터** 메우는 중이다: `specs/`·`기준/`(①, 기준 카드 현재 판정 문장을 최선 추정으로 확정) → `g1-기업판정`(②③, G1만 완료) → 나머지 기능(G2~G11)의 Skill·실행체 → 실행 시작 → 실제 데이터로 `specs/G11`(부가 이연)이 사후 조정 → `32` 2절 (a)로 ⑥. **사전 인위적 대조(`40` 옛 V1-a·V1-b)로 ①②를 잇던 단계는 2026-08-03 폐기했다** — `31` 1-5절(사전 검증 게이트로 후보를 거르지 않는다)과 부딪혀서다(`91` 2026-08-03 항목). **④⑤를 더 정교하게 만드는 일은 `01` E단계다.**
- **A는 ⑤ Hook만 비었다.** 5절의 *"게이트 전에 `project-critic`으로 검토한다"*는 규칙은 사람·에이전트가 **스스로 판단해 건너뛸 수 있는** 형태다(Skill의 첫 번째 구멍). 강제 장치를 둘지는 `90` 리스크 18.
- **판단이 갈리면 A보다 B를 먼저 채운다.** 심사와 미팅이 보는 것은 B다.

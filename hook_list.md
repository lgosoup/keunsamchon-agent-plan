# Hook 필요 목록 — 타임리 구현용 스펙

이 문서는 Hook(자동 트리거)이 필요한 지점을 정리한 것이다. **구현(타임리 플랫폼에서 어떻게 감시·스케줄링할지)은 사용자가 직접 한다** — 여기서는 각 Hook이 (a) 언제 눌려야 하는지 (b) 무엇을 신호로 캡처해야 하는지 (c) 어느 호출을 어떤 형식으로 실행해야 하는지만 정의한다. `Harness/` 폴더 안의 실행체(Skill)는 전부 이 신호를 슬래시 명령 인자로 받는 것을 전제로 이미 만들어져 있다 — Hook은 그 인자를 채워서 호출만 하면 된다.

정본은 `Harness/skills/*/SKILL.md`의 `argument-hint`다. 이 문서는 그걸 Hook 관점으로 옮겨 적은 것이며, 둘이 어긋나면 `Harness/` 쪽이 이긴다.

---

## H1 — 회신 도착 감지

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 이벤트 |
| 트리거 조건 | 발송용 메일함에 새 메일이 도착 |
| 캡처할 신호 | 메일 원문 전체(일본어) + 헤더(발신 주소·수신일시·제목·스레드 ID) + 첨부 파일명 목록 |
| 호출 대상 | `/g7-reply-processing` |
| 인자 형식 | `<회신 원문 1통 붙여넣기 — 헤더(발신 주소·수신일시·제목·스레드) 포함>` — 참조 발신 정보(어느 기업에 보냈는지 등 6항목, `data/발송기록/`에서 조회 가능하면 이어서 붙임 |
| 비고 | 스팸·광고 메일함이 아니라 **이 프로젝트 발송 전용 주소**로 들어온 메일만 대상. 자동응답인지 아닌지는 Hook이 가르지 않는다 — G7이 판단한다 |

## H2 — 발송 결과(바운스) 감지

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 이벤트 |
| 트리거 조건 | 발송 서비스(메일 인프라)가 특정 발송 건에 대해 바운스·수신 실패 응답을 반환 |
| 캡처할 신호 | 어느 발송 건인지(수신 주소로 `data/발송기록/`을 역조회해 `{기업식별자}-{발송일시}` 특정) + 바운스 코드 원문 + 조회 시점 |
| 호출 대상 | `/g6-delivery-status-judging` |
| 인자 형식 | `<발송 기록 파일 경로 또는 {기업식별자}-{발송일시}> + 그 건의 메일 시스템 응답을 그대로 붙여넣기(바운스 코드 원문 · 회신 도착 여부 · 조회 시점)` |
| 비고 | H1(회신 도착)과 이 Hook은 **서로 다른 신호**다 — 회신은 "왔다"는 사실, 바운스는 "실패했다"는 사실. 한 발송 건에 둘 중 하나만 온다 |

## H3 — 무응답 판정 시점 도래

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 스케줄(발송 건별 지연 타이머) |
| 트리거 조건 | 발송일시로부터 **10영업일**(`Harness/criteria/g6-delivery-status-judging.md` 1절 잠정값) 경과, 그동안 H1·H2 신호가 없었던 건 |
| 캡처할 신호 | 없음(신호 부재 자체가 신호) — 해당 발송 건의 `{기업식별자}-{발송일시}` |
| 호출 대상 | `/g6-delivery-status-judging` |
| 인자 형식 | `<{기업식별자}-{발송일시}> + 회신 도착 여부: 없음, 조회 시점: (지금 시각)` |
| 비고 | 이 값은 잠정이라 실제 회신 도착 분포가 쌓이면 카드 쪽 값이 바뀐다. Hook은 그 파일의 최신값을 매번 다시 읽어야 한다(하드코딩하지 않는다) |

## H4 — G7 정정 신호를 G6로 전달 (되돌림 간선)

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 이벤트(H1 처리 결과에 종속) |
| 트리거 조건 | G7이 회신을 **자동응답(C6)** 또는 **수신 거부**로 판정 |
| 캡처할 신호 | G7 레코드가 낸 판정(자동응답 여부 / 수신거부 여부) + 대상 발송 건 식별자 |
| 호출 대상 | `/g6-delivery-status-judging` (자동응답 정정용) — 수신거부는 G7이 `data/발송금지.md`에 직접 append하므로 별도 호출 불필요 |
| 인자 형식 | `<{기업식별자}-{발송일시}> + G7 정정 신호: 자동응답 판정됨` |
| 비고 | `Harness/workflow.md` 8절이 이미 "이 간선을 빠뜨리면 안 된다"고 지목한 자리. G6은 회신 내용을 스스로 못 읽으므로(설계상 금지) 이 전달이 없으면 자동응답이 회신으로 영구히 남는다 |

## H5 — G1 재발굴 주기 실행

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 스케줄 |
| 트리거 조건 | **주 1회**(`Harness/workflow.md` 1절, `docs/design-rationale.md` G1 참조) |
| 캡처할 신호 | 없음 — 그냥 실행 요청 |
| 호출 대상 | `/g1-company-screening` (신규 후보 소스 기준) |
| 인자 형식 | `<기업명(원어)> <URL 1개 이상>` — Hook이 명단 수집 소스(단위 L)에서 이번 주 신규 목록을 먼저 뽑아온 뒤, 건별로 이 형식으로 반복 호출 |
| 비고 | 어느 소스를 쓸지는 `Harness/specs/g1-company-screening.md`가 스스로 정하지 않는다(밖에서 지정) — Hook이 소스 목록도 함께 들고 있어야 한다 |

## H6 — G5 제작 모드 트리거

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 하이브리드(이벤트 + 안전장치 스케줄) |
| 트리거 조건 | 세그먼트 안에 G4 [확보] 신규 컨택이 **5건** 누적 **또는** 마지막 제작 이후 **7일** 경과 중 먼저 오는 것(`docs/design-rationale.md` G5 참조) |
| 캡처할 신호 | 어느 세그먼트 ID인지(`data/segments/`의 최신 버전 기준) |
| 호출 대상 | `/g5-proposal-email-dispatch` |
| 인자 형식 | `제작 <세그먼트 ID>` (예: `제작 S1`) |
| 비고 | 세그먼트 하나에 문안 제작이 이미 진행 중이면 중복 트리거하지 않는다 — `data/발송대기/`에 그 세그먼트·오늘 날짜의 파일이 이미 있는지 확인 |

## H7 — G10 재수집 주기 실행

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 스케줄(대상별 지연 타이머) |
| 트리거 조건 | 각 재수집 후보 건에 대해 마지막 판정으로부터 **30일** 경과(`docs/design-rationale.md` G10 참조) |
| 캡처할 신호 | 유입 소스(G4 미확보 / G1 판정불가 · G2 미분류 / 외부 시간경과·트렌드) + 대상 기업 식별자 |
| 호출 대상 | `/g10-recollection-judging` |
| 인자 형식 | `<유입 소스> <기업 식별자 1개>` (외부 신호면 무엇이 바뀌었는지 설명을 이어서 붙임) |
| 비고 | G4 미확보 승격 조건(2회 연속, `docs/design-rationale.md` G10)도 이 Hook이 판단할 계산이 아니라 **G10 스킬 자신이** 호출 시점에 판단한다 — Hook은 30일 경과 여부만 계산해서 부른다 |

## H8 — G11 반응 집계 주기 실행

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 이벤트(데이터 누적 기반, 계산 필요) |
| 트리거 조건 | `data/발송기록/` 전체 건수가 마지막 집계 이후 **세그먼트×조건 조합 최소 1개가 5건 이상**(`Harness/criteria/g11-response-aggregation.md` 1절 최소 표본 크기) 새로 채워졌을 때 |
| 캡처할 신호 | 없음 — 그냥 실행 요청(집계일은 스킬이 오늘 날짜로 자동 지정) |
| 호출 대상 | `/g11-response-aggregation` |
| 인자 형식 | 인자 없음(기본) 또는 `<YYYY-MM-DD>` |
| 비고 | 이 조건 계산(어느 조합이 5건을 넘었는지)은 Hook이 `data/발송기록/`·`data/집계/` 최신 파일을 직접 대조해야 한다 — 단순 스케줄이 아니라 데이터를 보고 트리거 여부를 결정하는 조건부 Hook이다. 매주 1회 정도로 "체크만" 스케줄링하고, 체크 결과 조건이 안 맞으면 호출을 건너뛰는 방식이 무난하다 |

---

## 요약 표

| ID | 이름 | 트리거 종류 | 주기/조건 |
|---|---|---|---|
| H1 | 회신 도착 감지 | 이벤트 | 메일 도착 즉시 |
| H2 | 바운스 감지 | 이벤트 | 발송 서비스 응답 즉시 |
| H3 | 무응답 판정 시점 | 스케줄(건별 타이머) | 발송 후 10영업일 |
| H4 | G7→G6 정정 신호 전달 | 이벤트(종속) | H1 처리 결과가 자동응답/거부일 때 |
| H5 | G1 재발굴 | 스케줄 | 주 1회 |
| H6 | G5 제작 트리거 | 하이브리드 | 5건 누적 또는 7일 |
| H7 | G10 재수집 | 스케줄(건별 타이머) | 30일 |
| H8 | G11 집계 | 이벤트(조건부) | 조합당 5건 이상 채워졌을 때(주 1회 체크 권장) |

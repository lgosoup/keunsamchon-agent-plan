# H10 — G8 원인분석 실행 (G6 판정에 종속)

| 항목 | 내용 |
|---|---|
| 트리거 종류 | 이벤트(종속 — G6 판정 결과에 종속, H4와 같은 종류) |
| 트리거 조건 | G6이 발송 건 상태를 **[무응답]** 또는 **[주소 오류]**로 확정 |
| 캡처할 신호 | 그 발송 건의 `{기업식별자}-{발송일시}` + G6 판정 근거(상태·조회 시점) |
| 호출 대상 | `/g8-cause-analysis` |
| 인자 형식 | `<{기업식별자}-{발송일시}> + G6 상태(무응답/주소오류) + 판정 근거` |
| 비고 | 별도 스케줄이 아니라 **G6 실행이 끝나는 그 자리에서 바로 다음 호출을 만든다** — H4(G7→G6 정정)와 같은 패턴이다. **카운트 임계값을 두지 않는다** — 발송 건 1개당 원인 분석 1개가 필요할 뿐 배치로 묶을 이유가 없다 |

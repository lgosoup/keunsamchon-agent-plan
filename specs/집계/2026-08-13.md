# 집계 — 2026-08-13

> ⚠ **이 회차 입력 중 세그먼트·조건 조합 표에 들어가는 7건은 2026-08-11 집계와 동일한 합성 데모 데이터(`합성데모-fashion-01`~`07`)다 — 그 사이 새 발송이 없었다.** 새로 생긴 입력은 `합성테스트-h2bounce-01`(H2 바운스 감지 Hook 검증용, 그 파일 자체가 "검증 후 삭제한다"고 밝힌 별도 목적의 데이터) 1건과, 기존 7건에 대한 G8 원인분석 3건(05·06·07)뿐이다. **아래 집계 수치·제안은 여전히 합성 데이터에 대한 것이지 실제 반응이 아니다.**

- **집계일**: 2026-08-13
- **집계 대상**: 그 시점까지 쌓인 전체(Spec 「입력」) — 실행 시점 기준 `specs/발송기록/` 전체 8건

## 0. 입력 자리 전수 확인

| 자리 | 건수 |
|---|---|
| `specs/발송기록/` | 8건 (`합성데모-fashion-*` 7건 + `합성테스트-h2bounce-01` 1건) |
| `specs/상태/` | 8건 |
| `specs/replies/` | 3건 (`-미확정` 식별자 0건) |
| `specs/원인분석/` | 3건(합성데모-fashion-05·06·07) — 2026-08-11 집계 시점에는 0건이었다 |
| `specs/segments/` | SEG-A-v1(6차 실행 — 실제 산출물, S2·S3 신설 포함. 이번 집계의 조건 축은 이 세그먼트 정의와 별개인 잠정 축이며 SEG-A 자체의 최신 배정 상태와는 무관하게 아래 7건이 참조하는 라벨 "S1(여성 의류)"만 그대로 쓴다) |
| `specs/scores/` | 6건(전부 실제 산출물) + 순위표 1건 — 이번 회차 조정 후보 판단에는 쓰지 않음(2026-08-11 집계와 동일 이유, 아래 2절 대상이 G2뿐) |

[대기중] 상태로 제외된 건: 0건.

**`합성테스트-h2bounce-01`은 세그먼트·조건 조합 표에 조인하지 않는다.** 이 건의 발송 기록에는 "G11 소비 항목"(세그먼트·조건 축) 자체가 없고, `specs/segments/SEG-A-v1.md` 어느 무리에도 이 식별자가 배정돼 있지 않다 — G2를 거치지 않은 기업이다. Spec은 이 경우(발송 기록에 G11 소비 항목이 없는 경우)를 정의하지 않았으므로, G7 `-미확정` 식별자를 기업 단위 조인에서 빼고 별도로 세는 것과 같은 원칙을 적용해 **"조인 불가"로 별도 집계**하고 1절 표에는 넣지 않는다. 이 파일 자신이 "H2 Hook 검증용, 검증 후 삭제한다"고 밝히고 있어 — 이 회차 집계와 무관하게 정리 대상인지는 이 실행이 판단할 문제가 아니라 사람에게 넘긴다.

조인 불가 건수: 1건(`합성테스트-h2bounce-01` — 상태 [주소 오류], G2 세그먼트 미배정).

## 1. 조합별 집계표

**조건 축은 Spec에 정의가 없어 잠정 축(G5 「무엇을 제안했는가」 필드)을 쓴다 — 사람이 `specs/G11_반응집계_기준조정.md` 「범위」에 조건 축을 써 넣으면 이 절은 사라진다.** (2026-08-11 집계와 동일한 축·값 — 입력 7건이 그대로다)

| 세그먼트 ID | 조건(잠정 축) | 발송 건수 | 회신율 | 무응답율 | 주소오류율 | 표본 부족 여부 |
|---|---|---|---|---|---|---|
| S1(SEG-A-v1) | 가격효율 소구 | 5 | 3/5 (60%) | 1/5 (20%) | 1/5 (20%) | 아니오 (5건 ≥ 최소 표본 5건) |
| S1(SEG-A-v1) | 디자인속도 소구 | 2 | 0/2 (0%) | 2/2 (100%) | 0/2 (0%) | **예 — 표본 부족**(2건 < 최소 표본 5건, 임의값) |

식별 불가(`-미확정`) 건수: 0건.
조인 불가(`합성테스트-h2bounce-01`, 세그먼트 미배정) 건수: 1건 — 위 표에 포함하지 않음.

## 2. 조정 제안 목록

**최소 표본 5건·신호 상충 판정선 20%p는 `specs/G11_반응집계_기준조정.md` 「지금 쓰는 값」의 임의값이다 — 확정 기준이 아니며 실제 데이터 분포로 재조정될 수 있다.**

- **신호 상충 판정**: 2026-08-11 집계와 동일 — 「디자인속도 소구」가 표본 부족(2건)이라 두 조합을 비교할 수 없다. 신호 상충 여부를 판정하지 않는다(비교 대상 없음, 상충이 "아니다"라는 뜻이 아니다).
- **제안 1** — **무엇을**: G3 적합도 채점에 "실측 반응 신호" 축 추가를 검토 후보로 올린다(G2 세그먼트 축 자체를 바꾸는 제안은 아니다). **왜**: S1 세그먼트 × 가격효율 소구 조합이 표본 5건으로 회신율 60%(3/5)를 기록했다 — 표본 크기 조건을 충족한 이번 회차 유일한 조합이다(`specs/발송기록/합성데모-fashion-01~05*.md`, `specs/상태/합성데모-fashion-01~05*.md`, `specs/replies/2026-08-11-1105~1107*.md`). **어느 방향으로 얼마나**: 관찰된 수치(3/5, 60%) 그대로 인용한다 — 조정 폭의 단위가 Spec에 없어 지어내지 않는다. **이전 기각 이력**: **있음 — `specs/집계/2026-08-11.md` 3절(U4, 2026-08-12 사람 검토)이 같은 제안을 "반영 보류"로 판정했다.** 사유는 "이 회차 입력 전부가 합성 데모 데이터라 실제 판정 기준을 바꾸면 이후 G3 채점이 가짜 데이터에서 유래한 가중치로 돈다"였다 — 그 사유를 뒤집을 새 데이터(실제 발송·회신)가 이번 회차에도 들어오지 않았으므로, **이번에도 같은 판정이 유지될 것으로 보는 것이 근거와 맞는다.** 다만 판정 자체는 사람이 다시 내리는 것이라 여기서는 이력만 표시하고 재확정하지 않는다.
- **참고(제안 아님)**: 「디자인속도 소구」 조합(0/2)은 표본 부족이라 성공 기준 (a) 무관용에 따라 조정 제안에 포함하지 않는다. 표본이 쌓이면 다음 회차에서 다시 본다.
- **참고(G8 원인분석, 근거로만 인용)**: 이번 회차에 처음 생긴 `specs/원인분석/합성데모-fashion-05·06·07.md`는 각각 [주소 오류]→조사 오류, [무응답]→판정 불가(低)를 냈다. 이 값은 이미 G6 상태에 반영된 결과의 원인 설명이라 반응율 수치를 바꾸지 않고, 위 제안 1의 근거를 넓히지도 좁히지도 않는다(원인 가설은 G2·G3 조정 판단의 입력으로 Spec이 지정하지 않았다 — SKILL.md 「규칙이 없는 경계」 표).
- **⚠ 이 회차 전체(1절 대상 7건)가 합성 데이터라는 것을 다시 밝힌다** — 위 제안은 G11의 산출 로직이 실제로 작동함을 보여주는 시연이지, 지금 `기준/G3_적합도기준.md`를 바꿔야 한다는 실제 근거가 아니다. 실제 발송·회신 데이터가 쌓이기 전에는 어떤 조정도 반영하지 않는다.

## 자체 점검 (`specs/G11_반응집계_기준조정.md` 「성공 기준」 (a) 항목만 대조)

| 항목 | 결과 |
|---|---|
| 출력 형식(1절·2절)이 정의된 대로 존재함 | 충족 |
| 표본 부족 조합이 조정 제안에 포함되지 않음 (0건) | 충족 — 「디자인속도 소구」는 제안 목록이 아니라 「참고」로만 언급 |
| 조정 제안마다 근거 데이터 인용이 있음 | 충족 — 제안 1이 발송기록·상태·replies 파일명을 인용 |
| G2·G3 파일이 이 기능에 의해 직접 수정된 적이 없음 (0건) | 충족(0건) — 이 파일 하나만 씀 |
| 신호 상충 조합이 조정 제안이 아니라 "재검토 필요"로 표시됨 | 해당 없음 — 이번 회차엔 비교 가능한 신뢰 표본이 하나뿐이라 신호 상충 자체가 성립하지 않음(위 2절) |

**(b)는 판정하지 않는다** — 실제 데이터가 있어야 판정 가능한 항목이다(1절 대상 7건 전부 합성 데이터이므로).

# 집계 — 2026-08-11

> ⚠ **이 회차의 입력 7건 전부가 합성 데모 데이터다.** 실제 발송이 0건인 시점에 G11(반응 집계·기준 조정)이 최소 표본(5건)에 도달했을 때 실제로 무엇을 산출하는지 시연하기 위해 사용자 지시(2026-08-11)로 만든 예시 입력(`합성데모-fashion-01`~`07`)이며, 그 파일들 각각의 머리에도 같은 경고가 있다. **아래 집계 수치·제안은 전부 합성 데이터에 대한 것이지 실제 반응이 아니다** — 실제 발송이 시작되면 이 파일과 무관하게 새 집계일로 다시 돈다.

- **집계일**: 2026-08-11
- **집계 대상**: 그 시점까지 쌓인 전체(Spec 「입력」) — 실행 시점 기준 `specs/발송기록/` 전체 7건(전부 합성)

## 0. 입력 자리 전수 확인

| 자리 | 건수 |
|---|---|
| `specs/발송기록/` | 7건 (전부 `합성데모-fashion-*`) |
| `specs/상태/` | 7건 |
| `specs/replies/` | 3건 (`-미확정` 식별자 0건) |
| `specs/원인분석/` | 없음(0건) |
| `specs/segments/` | SEG-A-v1(S1, 5차 실행 — 실제 산출물. 이번 집계의 조건 축은 이 세그먼트 정의와 별개인 잠정 축이다, 아래 3절) |
| `specs/scores/` | 6건(전부 실제 산출물) — 이번 회차 조정 후보 판단에는 쓰지 않음(아래 2절 대상이 G2뿐) |

[대기중] 상태로 제외된 건: 0건.

## 1. 조합별 집계표

**조건 축은 Spec에 정의가 없어 잠정 축(G5 「무엇을 제안했는가」 필드)을 쓴다 — 사람이 `specs/G11_반응집계_기준조정.md` 「범위」에 조건 축을 써 넣으면 이 절은 사라진다.**

| 세그먼트 ID | 조건(잠정 축) | 발송 건수 | 회신율 | 무응답율 | 주소오류율 | 표본 부족 여부 |
|---|---|---|---|---|---|---|
| S1(SEG-A-v1) | 가격효율 소구 | 5 | 3/5 (60%) | 1/5 (20%) | 1/5 (20%) | 아니오 (5건 ≥ 최소 표본 5건) |
| S1(SEG-A-v1) | 디자인속도 소구 | 2 | 0/2 (0%) | 2/2 (100%) | 0/2 (0%) | **예 — 표본 부족**(2건 < 최소 표본 5건, 임의값) |

식별 불가(`-미확정`) 건수: 0건.

## 2. 조정 제안 목록

**최소 표본 5건·신호 상충 판정선 20%p는 `specs/G11_반응집계_기준조정.md` 「지금 쓰는 값」의 임의값이다 — 확정 기준이 아니며 실제 데이터 분포로 재조정될 수 있다.**

- **신호 상충 판정**: 두 조합을 비교하려면 둘 다 신뢰 가능한 표본이어야 하는데 「디자인속도 소구」가 표본 부족(2건)이라 비교 자체가 성립하지 않는다 — 신호 상충 여부를 판정하지 않는다(비교 대상 없음, 상충이 "아니다"라는 뜻이 아니다).
- **제안 1** — **무엇을**: G3 적합도 채점에 "실측 반응 신호" 축 추가를 검토 후보로 올린다(G2 세그먼트 축 자체를 바꾸는 제안은 아니다). **왜**: S1 세그먼트 × 가격효율 소구 조합이 표본 5건으로 회신율 60%(3/5)를 기록했다 — 표본 크기 조건을 충족한 이번 회차 유일한 조합이다(`specs/발송기록/합성데모-fashion-01~05*.md`, `specs/상태/합성데모-fashion-01~05*.md`, `specs/replies/2026-08-11-1105~1107*.md`). **어느 방향으로 얼마나**: 관찰된 수치(3/5, 60%) 그대로 인용한다 — 조정 폭의 단위가 Spec에 없어 지어내지 않는다. **이전 기각 이력**: 없음(이번이 이 저장소의 첫 집계 회차 — `specs/집계/`에 이전 파일이 없다).
- **참고(제안 아님)**: 「디자인속도 소구」 조합(0/2)은 표본 부족이라 성공 기준 (a) 무관용에 따라 조정 제안에 포함하지 않는다. 표본이 쌓이면 다음 회차에서 다시 본다.
- **⚠ 이 회차 전체가 합성 데이터라는 것을 다시 밝힌다** — 위 제안은 G11의 산출 로직이 실제로 작동함을 보여주는 시연이지, 지금 `기준/G3_적합도기준.md`를 바꿔야 한다는 실제 근거가 아니다. 실제 발송·회신 데이터가 쌓이기 전에는 어떤 조정도 반영하지 않는다.

## 자체 점검 (`specs/G11_반응집계_기준조정.md` 「성공 기준」 (a) 항목만 대조)

| 항목 | 결과 |
|---|---|
| 출력 형식(1절·2절)이 정의된 대로 존재함 | 충족 |
| 표본 부족 조합이 조정 제안에 포함되지 않음 (0건) | 충족 — 「디자인속도 소구」는 제안 목록이 아니라 「참고」로만 언급 |
| 조정 제안마다 근거 데이터 인용이 있음 | 충족 — 제안 1이 발송기록·상태·replies 파일명을 인용 |
| G2·G3 파일이 이 기능에 의해 직접 수정된 적이 없음 | 충족(0건) — 이 파일 하나만 씀 |
| 신호 상충 조합이 조정 제안이 아니라 "재검토 필요"로 표시됨 | 해당 없음 — 이번 회차엔 비교 가능한 신뢰 표본이 하나뿐이라 신호 상충 자체가 성립하지 않음(위 2절) |

**(b)는 판정하지 않는다** — 실제 데이터가 있어야 판정 가능한 항목이다(전부 합성 데이터이므로).

## 3. 사람 검토 결과 (U4, 2026-08-12)

**판정: 반영 보류.** 제안 1(G3에 "실측 반응 신호" 축 추가 검토)을 검토했으나 `기준/G3_적합도기준.md`에는 지금 반영하지 않는다.

- **왜**: 이 회차의 입력 7건이 전부 합성 데모 데이터라고 위 0절·2절이 이미 명시하고 있다. 실제 반응이 아닌 값으로 실제 판정 기준을 바꾸면, 그 이후 모든 G3 채점이 가짜 데이터에서 유래한 가중치로 도는 셈이 된다.
- **이것이 "미반영 = 실패"는 아니다**: G11의 목적은 조정 여부를 최종 확정하는 것이 아니라 "조합별 반응률을 집계하고 근거 있는 제안을 내는 것"까지다(Spec 「범위」). 그 제안을 받은 사람이 데이터 품질을 보고 반영을 보류하는 것도 이 흐름이 정상적으로 완결된 경우다 — 이번 회차엔 흐름 전체(집계→제안→사람 검토→판단)가 실제로 끝까지 돌았다.
- **다음**: 실제 발송·회신이 쌓여 `docs/93_구현_검증대기.md` #1(최소 표본 크기)·#10(G3 가중치)이 검증 가능한 시점이 되면, 그때 쌓인 진짜 집계로 이 제안을 다시 본다. 이 파일의 제안은 재사용하지 않고 그 시점의 새 집계일 파일로 다시 낸다(위 머리말 경고와 동일 원칙).

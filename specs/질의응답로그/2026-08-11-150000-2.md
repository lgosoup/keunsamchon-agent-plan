# 질의응답 로그 — 2026-08-11-150000-2

## 질문 원문

> `G2_세그먼트기준.md`의 내용이 다음과 같이 바뀌었다.
> [변경 전] "대표 기업의 연락처를 못 찾으면([미확보]) 그룹 차선 대표를 승격한다. 위 순서(1→2→3)를 다시 적용해..."
> [변경 후] "대표 기업에 발송할 수 없으면([미확보] 또는 [메일 없음]) 그룹 차선 대표를 승격한다. 위 순서(1→2→3→4)를 다시 적용해... (2026-08-11 확장 — 원래 [미확보]만 지목했는데... `run-orchestrator` 4차 실전 실행에서 대표가 [메일 없음]으로 나온 실제 사례가 나와서 드러났다.) 그룹 전원이 [미확보]·[메일 없음]이면... 대표 없이 발송을 진행하지 않는다."
> 이 변경이 다른 기준·스펙과 충돌하는지 검토해줘.

## 조회 시각

2026-08-11 (초 단위 시각은 셸 도구 권한이 거부돼 확인 못함 — 날짜만 정확하다. criteria 정합성 검토 예외 처리, `specs/G12_질의응답.md` 「실행 형태」 실패표)

## 답변

**criteria 정합성 검토 — 이미 적힌 텍스트끼리 대조한 결과만 답한다. "충돌 없음/있음"의 최종 확정 판단은 사람이 한다.**

**같은 질문에 대한 답변 로그가 이미 존재한다 — `specs/질의응답로그/2026-08-11-150000.md`.** 아래는 그 로그와 별개로 이번 호출에서 직접 파일을 다시 열어 대조한 결과이며, 결론은 그 로그와 대부분 일치한다(교차 확인으로 읽는다).

### 1. 자기 일관성 — [해소 확인] 이전에 지적됐던 "1→2→3" 잔존 문제는 지금 버전에서 고쳐져 있다

`specs/질의응답로그/2026-08-11-140000.md`가 지적했던 문제("3절 표는 순서 4를 얘기하는데 차선 대표 승격 문단은 여전히 1→2→3만 언급")를 지금 `기준/G2_세그먼트기준.md` 3절 111행에서 다시 확인했다 — 문단이 이미 **"위 순서(1→2→3→4)"**로 고쳐져 있다. 이 잔존 불일치는 **더 이상 존재하지 않는다.**

### 2. 이미 등록·해소로 표시된 리스크와 부합 — 충돌 아님

`docs/90_미결정과_리스크.md` 리스크 48이 정확히 이 변경("G2 '차선 대표 승격' 규칙이 [미확보]만 지목하고 [메일 없음]은 안 다뤘다")을 "해소 완료"로 이미 표시해 두었고, `docs/91_진행로그.md` 2026-08-11(4차 실행) 항목도 같은 사실을 "발명 아닌 확장"으로 기록한다. 문구·근거·처리 방식이 일치해 새로운 충돌은 아니다.

### 3. 실행체(Skill) 미반영 — 진행 중인 불일치, 새로운 것은 아니다

`.claude/skills/g2-세그먼트화/SKILL.md`를 직접 열어 26~53행을 확인했다 — "규칙이 없는 경계에서 멈춘다" 표와 "대표 선정 순서 2·3의 판별선" 절 어디에도 **순서 4**나 **차선 대표 승격**(연락처 상태에 따른 재선정) 절차가 없다. 이 스킬의 실행 절차(0~7단계)는 **최초 대표 선정**(순서 1→2→3, 동률 시 멈춤)만 다루고, **선정 이후 연락처 상태가 확인된 뒤의 재선정**은 절차 어디에도 없다. 스킬 머리말의 "문서가 이긴다" 원칙이 있어 다음 실행 시 카드를 다시 읽으면 순서 4는 정상 반영되지만(`specs/segments/SEG-A-v1.md` 5차 실행이 실제로 그렇게 했다), **차선 대표 승격은 원칙만으로 채워지지 않는다** — "언제, 누가 이 재선정을 도는가"라는 절차 자체가 카드에도 Skill에도 없기 때문이다(아래 4번).

### 4. [가장 중요한 발견] 이 변경이 실제로 요구하는 절차를 수행할 실행 단위가 파이프라인 어디에도 없다

**직접 대조한 근거:**

- `specs/G2_세그먼트화.md` 「범위」 제외 목록에 **연락처 탐색(G4)**이 명시돼 있다 — G2는 애초에 어느 대표가 [미확보]·[메일 없음]인지 알 수 없다(G2가 G4보다 먼저 끝난다, `specs/G4_연락처확보.md` 「목표」 각주 — 탐색 순서 재료가 G2 세그먼트 대표다).
- `specs/G4_연락처확보.md` 「범위」 제외 목록에 **연락처의 가치 판단**이 있고, 「제약」은 *"G2가 정한 순서를 바꾸지 않는다"*고만 정한다 — 대표를 재선정하는 것은 G4의 일이 아니다.
- `.claude/skills/g5-제안메일제작발송/SKILL.md`(실행 절차)와 `specs/발송대기/S1-2026-08-11.md`(실제 산출물)를 대조했다 — 대표(ingni-store-com, [메일 없음])는 "승격 없이" 그대로 **모드 M(대표 문안)** 자리에 남아 있고, 유일한 [확보] 소속인 www-titivate-jp는 승격된 신규 대표가 아니라 기존 대표 문안 틀의 **모드 E(확장)**로만 처리됐다. 새 카드 문구를 그대로 적용하면 www-titivate-jp가 차선 대표로 승격돼 모드 M을 다시 돌아야 하는데, 실제로는 그렇게 되지 않았다.
- `specs/실행상태.md` 「이번 실행(4차 재개)」 ② 절이 이 지점을 스스로 지적한다 — *"대표 ingni-store-com의 상태가 [미확보]가 아니라 [메일 없음]이다... 카드에 규칙이 없어... 사람이 판단할 자리로 남긴다"*. 같은 문서 (c)3번도 *"대표가 [메일 없음]일 때도 차선 대표 승격 규칙을 적용할지... 이 실행체가 만들지 않음"*이라고 명시한다.

**종합**: 이번 변경은 규칙의 **문구**(적용 대상을 [미확보]→[미확보]+[메일 없음]으로 확장)만 고쳤을 뿐, **그 규칙을 누가·언제 실행하는가**(G2 재실행 트리거, 또는 G4→G2 되돌림 간선)는 어느 Spec에도 정의돼 있지 않다. 이 공백은 이번 변경이 새로 만든 것이 아니라 [미확보] 단독 조항일 때부터 있던 것이지만, 조건을 넓혀 실제로 이 조항에 걸리는 사례([메일 없음] — 이미 발생한 ingni-store-com 건)가 늘면서 **공백에 부딪힐 빈도가 함께 늘었다.** 지금 상태로는 이 문장이 카드에는 있지만 실제로 이 문장을 수행하는 실행 단위가 없는 "죽은 규칙"이고, 그 결과로 이미 존재하는 두 산출물(`SEG-A-v1.md`의 대표, `발송대기/S1-2026-08-11.md`의 모드 배정)이 새 카드 문구와 맞지 않는 상태로 남아 있다.

## 인용 출처

- `기준/G2_세그먼트기준.md` 3절 111행 (변경 후 문구, 자기 일관성 확인)
- `docs/90_미결정과_리스크.md` 리스크 48 (해소 표시)
- `docs/91_진행로그.md` 2026-08-11 "4차 실행" 항목
- `specs/질의응답로그/2026-08-11-140000.md` (선행 로그 — 1→2→3 잔존 문제)
- `specs/질의응답로그/2026-08-11-150000.md` (선행 로그 — 동일 질문, 동일 결론)
- `.claude/skills/g2-세그먼트화/SKILL.md` 26~53행 (순서 4·차선 대표 승격 절차 부재)
- `specs/G2_세그먼트화.md` 「범위」 (연락처 탐색 제외)
- `specs/G4_연락처확보.md` 「목표」·「범위」·「제약」 (탐색 순서만 받고 대표 재선정 권한 없음)
- `.claude/skills/g5-제안메일제작발송/SKILL.md`, `specs/발송대기/S1-2026-08-11.md` 머리말·건 2 (대표가 승격 없이 그대로 모드 M에 남음)
- `specs/실행상태.md` 「이번 실행(4차 재개)」 ② G4 절, (c)3번
- `specs/segments/SEG-A-v1.md` 5차 실행 절 (순서 4는 실제로 반영됐음 — 대조군)

## 표시

**조회 범위 밖** — 이 공백을 지금 고칠지(SKILL.md에 재선정 절차 추가, G4→G2 되돌림 간선 신설 등), `docs/90_미결정과_리스크.md`에 새 리스크로 등록할지는 사람의 결정 사항이며 이 답변이 대신 정하지 않는다. 효과 예측(이 규칙이 실제로 회신율에 영향을 주는지 등)도 질문에 포함되지 않아 다루지 않았다.

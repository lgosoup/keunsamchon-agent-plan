# 질의응답 로그 — 2026-08-11-120000

- **질문 원문**: G2_세그먼트기준.md의 내용이 다음과 같이 바뀌었다. [변경 전/후 — 3절 표에 순서 4("위 셋이 같으면 `candidates/후보목록.md`에 먼저 등재된 곳"을 대표로) 신설]. 이 변경이 다른 기준·스펙과 충돌하는지 검토해줘.
- **조회 시각**: 2026-08-11 (⚠ 초 단위 시각은 셸 도구 권한이 거부돼 확인하지 못했다 — 날짜만 정확하다)

## 답변

**criteria 정합성 검토 — 이미 적힌 텍스트끼리 대조한 결과만 답한다. "안전하다/문제없다"는 최종 판단은 내리지 않는다.**

### 1. 충돌 발견 — `.claude/skills/g2-세그먼트화/SKILL.md`가 새 순서 4를 반영하지 못한 채 "순서 3에서 갈리지 않으면 멈춘다"고 못박아 두었다

`기준/G2_세그먼트기준.md` 3절은 이제 순서 1→2→3→**4**로 이어지고, 순서 4가 "위 셋이 같으면" 마지막까지 대표를 확정하도록 설계됐다. 그런데 이 카드를 그대로 실행하는 `.claude/skills/g2-세그먼트화/SKILL.md`는:

- "대표 선정 순서 2·3" 행에서 **"③ 적용해도 두 곳이 안 갈릴 때(순서 3 — 확정도까지 같음). 이 셋에서는 그 무리를 대표 없이 남기고 「판정 보류」로 올린다"**고 멈추는 조건을 명시했고(26~34행),
- "대표 선정에서만 「판정 보류」가 남는다 ... 그 선을 적용한 결과가 여전히 안 갈리는 경우는 남는다"(40행)라고 순서 3 동률을 최종 정지 지점으로 재확인했으며,
- "대표 선정 순서 2·3의 판별선 — 카드 3절 그대로 인용" 절의 표(42~53행)는 순서 2·3만 옮겨 적었고 순서 4는 아예 등장하지 않는다.

즉 스킬 본문이 "순서 3까지 적용해도 안 갈리면 판정 보류로 멈춘다"는 **절차 문장을 카드 문언과 별도로 갖고 있는데**, 카드는 이제 그 지점에서 멈추지 않고 순서 4로 넘어가 대표를 확정하도록 바뀌었다. 스킬 머리에 "이 스킬 본문과 두 문서가 어긋나면 문서가 이긴다"는 원칙이 있어 다음 실행자가 카드를 다시 읽으면 바로잡을 여지는 있지만, "멈추는 경계" 표 자체가 틀린 정지 조건을 실행 절차로 명시하고 있다는 점은 정정이 필요한 불일치다.

**실제로 이 불일치가 낸 결과물이 이미 있다** — `specs/segments/SEG-A-v1.md` 1절은 2026-08-11 4차 실행에서 S1 대표가 순서 1→2→3까지 적용해도 `ingni-store-com`·`www-titivate-jp` 둘이 갈리지 않아 **"판정 보류"로 끝났다**고 명시한다. 새 순서 4(후보목록 등재 순서)를 적용하면 `specs/candidates/후보목록.md`에서 `ingni-store-com`이 `www-titivate-jp`보다 먼저 등재돼 있어(각각 9행·198행 섹션) **`ingni-store-com`으로 확정**된다 — 이는 마침 3차 실행에서 이미 확정돼 있던 대표와 같은 결론이라 이번 규칙 신설이 배정을 뒤집지는 않는다. 다만 그 결론에 도달하려면 사람이 SKILL.md의 멈추는 경계 문구를 무시하고 카드 3절을 직접 다시 읽어야 하므로, **SKILL.md를 순서 4를 반영하도록 갱신하지 않으면 다음 실행이 다시 "판정 보류"로 잘못 멈출 위험이 남는다.**

### 2. 충돌 아님 — 대기 항목 등록 위치

새 규칙 4행은 "임의 결정(2026-08-11, `docs/93_구현_검증대기.md` 등록)"이라고 밝히고 있고, 실제로 `docs/93_구현_검증대기.md` 1절 #19에 "G2 대표 선정 순서 4(동률 최종 타이브레이커)"가 이미 등록돼 있다 — 값·근거·검증 방법이 카드 문구와 일치한다. `CLAUDE.md` 5절이 요구하는 "기업 답과 무관한 우리 결정은 93에 등록"과도 맞는다.

### 3. 충돌 아님 — `../docs/33_설계근거.md` D15 인용

새 순서 4행이 "판정 보류로 남기지 않는다 — 대표가 없으면 그 무리 전체(G4·G5)가 멈추기 때문이다(`33` D15와 같은 이유)"라고 쓴 근거는, `33` D15("MVP 범위를 탐색+그룹화+발송으로 좁히고 순위화(G3)를 이연한 이유")가 아니라 **3절 자체(111행, "대표 기업의 연락처를 못 찾으면 ... 그룹 차선 대표를 승격한다")의 근거로 이미 인용해 둔 것과 같은 문장·같은 이유("대표 없이 두면 그 그룹 전체의 발송이 멈춘다")**를 재사용한 것이다. 문언은 그대로 일치하며 충돌이 없다.

### 4. 충돌 아님 — `specs/G2_세그먼트화.md` 「성공 기준」과의 관계

Spec 「성공 기준」은 "무리마다 대표 1곳과 왜 그곳인지가 있음 (대표 없는 무리 0건)"을 요구하는데, 새 순서 4는 오히려 이 기준을 더 잘 충족시키는 방향이다 — 순서 3까지 동률이라 대표가 비어 있던 자리(SEG-A-v1.md 4차 실행의 미충족 1건)를 없애는 규칙이라 Spec과 충돌하지 않는다.

## 인용 출처

- `기준/G2_세그먼트기준.md` 3절 표 3·4행 (변경 후 순서 4 신설)
- `기준/G2_세그먼트기준.md` 3절 111행 (대표 승격 근거 문장)
- `.claude/skills/g2-세그먼트화/SKILL.md` 26~34행, 40행, 42~53행 (멈추는 경계·순서 2·3 판별선 인용 — 순서 4 미반영)
- `specs/segments/SEG-A-v1.md` 1절(44~53행), 4절 "「멈추는 경계」에 걸린 건" 행 (4차 실행 판정 보류 사례)
- `specs/candidates/후보목록.md` (ingni-store-com 섹션이 www-titivate-jp 섹션보다 앞에 등재)
- `docs/93_구현_검증대기.md` 1절 #19 (등록 확인)
- `docs/33_설계근거.md` D15 표제(재인용 근거 대조)
- `specs/G2_세그먼트화.md` 「성공 기준」 4번째 항목

## 확인 불가 / 조회 범위 밖

- 이 변경이 **실제로 안전한지/그대로 반영해도 되는지**의 최종 판단은 조회 범위 밖이다 — 발견한 불일치(SKILL.md 미반영)를 인용으로 제시했고, 반영·승인 여부는 사람이 정한다.
- SKILL.md를 지금 고칠지 여부도 이 스킬의 일이 아니다(판정 기준 문장·실행 절차를 고치는 것은 이 기능의 범위 밖).

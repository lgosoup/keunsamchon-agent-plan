# 연락처 확보 — www-titivate-jp

- **기업 식별자**: www-titivate-jp
- **탐색일**: 2026-08-11
- **탐색 주체**: run-orchestrator(`g4-연락처확보` 스킬 경유)
- **세그먼트 소속**: `specs/segments/SEG-A-v1.md` S1(여성 의류) — **소속(대표 아님)**. 탐색 순서 게이트(2단계) — 대표 ingni-store-com의 `specs/contacts/ingni-store-com.md`가 이미 존재함을 확인하고 진행함(대표 판정 상태 자체는 게이트 통과 조건이 아니다 — 파일 존재 여부만 본다).

## 상태: [확보]

- **연락처 값**: info@titivate.jp
- **어디서 찾았는지**: https://titivate.jp/p/about/terms (회사 개요 페이지) — G1이 2026-08-11 관찰 시 이미 확인한 그 주소와 동일(`specs/candidates/후보목록.md` www-titivate-jp 섹션 O8)
- **유효 등급**: **일반** — `info@` 류 대표 주소(카드 1절 예시와 일치, 담당자·부서 전용 표기 없음)
- **거부 표시 없음 확인**: `기준/G4_연락처유효성기준.md` 2절 순서 1 — **이 주소는 G1이 이미 O8을 관찰한 그 주소와 동일**하므로 G1의 판정을 그대로 쓴다(다시 보지 않음). G1 관찰: "2営業日以上の返信がない場合は…"(회신 지연 시 스팸함 확인 안내)만 있고, 거부표현어(お断り・拒否・送信しないでください)와 거부대상어(特定電子メール・広告メール・宣伝メール・迷惑メール)의 결합이 없어 O8=없음으로 확정됨(2026-08-11, G1 산출물 그대로 인용)
- **`발송금지.md` 미등재 확인**: 파일 없음(등재 0건) 확인 — 2026-08-11(ingni-store-com 탐색 시 확인한 것과 동일 파일, 재확인)

## 자체 점검 (`specs/G4_연락처확보.md` 「성공 기준」 대조)

| 항목 | 결과 |
|---|---|
| 상태가 4종 중 하나로 명확히 기록됨 | 충족 — [확보] |
| [확보] 건에 유효 등급과 출처 URL이 있음 | 충족 — 일반, https://titivate.jp/p/about/terms |
| [확보] 건마다 거부 표시 없음이 어느 주소를 확인했는지와 함께 기록됨 | 충족 — G1 판정 재사용(같은 주소) |
| [확보] 건마다 `발송금지.md` 미등재 확인이 기록됨 | 충족 |
| 개인 이름·개인 SNS·개인 직통 연락처가 기록되지 않음 | 충족(0건) — 법인 공개 대표 이메일만 |
| 각 세그먼트의 대표 기업이 소속 기업보다 먼저 탐색됨 | 충족 — ingni-store-com(대표)을 먼저 탐색한 뒤 이 건(소속)을 처리함 |
| 탐색일·탐색 주체가 있음 | 충족 |

**표본 검사(같은 기업을 두 번 돌려 상태가 같은가)는 이 실행에서 판정하지 않는다** — 절차 점검 시 사람이 도는 항목이다.

## 참고 — 대표(S1)의 상태와 이 건의 관계

대표 ingni-store-com은 [메일 없음]으로 판정됐다(`specs/contacts/ingni-store-com.md`). **`기준/G2_세그먼트기준.md` 3절의 "대표 연락처를 못 찾으면([미확보]) 차선 대표 승격" 규칙은 문언상 [미확보]만 지목하고 [메일 없음]은 다루지 않는다** — 이 두 상태를 같게 볼지는 카드에 규칙이 없어 이 실행체(G4)가 판단할 자리가 아니다. G4는 대표 우선 **탐색 순서**만 강제할 뿐 대표 재선정 권한이 없다(Spec 「범위」 제외 목록 — 연락처의 가치 판단·순위는 G4의 일이 아니다). 이 판단은 사용자 보고에 별도로 올린다.

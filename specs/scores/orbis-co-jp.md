# orbis-co-jp — 적합도 채점

> **시험 실행 — 확정 순위 아님.** 사용한 채점 기준: **잠정판**(`../../기준/G3_적합도기준.md` 1절).
> ⚠ **이 건은 합산할 축이 하나도 남지 않았다 — 순위표에 「채점 불가로 빠진 건」으로 들어간다.** 탈락이 아니다(이 기능은 후보를 떨어뜨리지 않는다).
> 형식 정본: `../G3_적합도평가.md` 「출력 형식」 파일 1.

- **기업 식별자**: orbis-co-jp
- **채점일**: 2026-08-05
- **채점 주체**: Claude (`g3-적합도평가` 스킬 — `harness-builder` 빌드 테스트 실행)
- **입력**: `../candidates/후보목록.md` § orbis-co-jp (후보 판정 **포함**)

## 축별 점수

| 축 | 관찰값 (G1 참조) | 점수 | 점수를 준 이유 |
|---|---|---|---|
| 업종 우선순위 | O5 = **뷰티** | **보류 — 카드에 없는 라벨** | 카드 점수표의 라벨은 `패션`·**`코스메틱`**·`푸드`인데 G1 관찰값은 **`뷰티`**다. G1 카드(`../../기준/G1_기업판정기준.json` `필수조건[3].매핑`·`우선순위`)가 쓰는 라벨은 `뷰티`이고, G3 카드는 `코스메틱`을 쓴다. **두 라벨이 같은 것을 가리키는지 이 실행체가 판단하지 않는다** — 판단하면 어느 카드도 딛지 않은 매핑이 하나 생기고, 그 매핑 위에 2점이 얹힌다 |
| 규모 | O6 상품 개수 **기재 없음** · 판매채널 수 **1개** (자본금·종업원수는 G1에 없음) | **보류 — 카드에 판별선 없음** | 카드 1절이 「극단적으로 작다」의 선을 **없다고 명시**했다. 관찰값은 그대로 옮기고 점수만 비웠다 |
| 한국 진출 신호 강도 | **재료 없음** — G1이 관찰하지 않는 항목(옛 O7, 결번) | **채점 불가** | 「관찰 안 함」이지 「관찰했는데 0개」가 아니다. 이 실행체에는 재방문 수집에 쓸 웹 권한이 없다 |

## 합산

**합산 점수: 산출 불가** — 합산에 들어갈 축이 **0개**다.

- 비운 축: **3개** — 보류 2(업종 라벨·규모) · 채점 불가 1(한국 진출 신호)
- **0점으로 내려 꼴찌에 두지 않았다.** 0점을 주면 그것은 근거 없는 점수이고, 순위표에서 사실상 탈락과 같아진다 — 이 기능은 통과분을 떨어뜨리지 않는다(Spec 「제약」)
- **사람이 무엇을 하면 풀리는가**: `../../기준/G3_적합도기준.md`의 업종 라벨을 G1 카드와 같은 어휘로 맞추면(또는 그 반대) 이 축은 그 자리에서 채점된다. 카드 5절 변경 이력에 바꾼 줄과 이유를 남기는 것이 그 절차다

## 자체 점검 (`../G3_적합도평가.md` 「성공 기준」 채점 산출물 항목)

- [x] 모든 축에 값과 점수 이유가 함께 있음 — 이유 없는 점수 **0건**
- [x] 채점 불가 축이 사유와 함께 표시됨 — 사유 없는 채점 불가 **0건**
- [x] 합산 점수가 축별 점수만으로 다시 계산됨 — 합산 대상 축 0개, **산출 불가**로 명시(임의 값으로 채우지 않음)
- [x] G1 통과분이 아닌 기업이 채점 대상에 없음 — 후보 판정 **포함** 확인
- [x] 채점일·채점 주체 있음
- [ ] (표본 검사) 두 번 채점 대조 — 이 실행에서 자체 판정하지 않는다

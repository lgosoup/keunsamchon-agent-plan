# specs — 기능별 Spec (폭포수 3단계)

기능 하나하나를 **독립된 Build Order 대상**으로 만들기 위한 Spec 모음이다. 기능 경계와 입출력은 `../docs/31_기능구조.md`에서 확정됐고(2단계), 여기서 각 기능의 **목표·범위·제약·출력 형식·성공 기준**을 정한다(3단계). Build Order(② Skill 이후)는 이 문서를 재료로 돌린다.

```
① 전체 목표 구체화        → docs/31_기능구조.md 1~2절
② 기능 단위 + 입출력 확정  → docs/31_기능구조.md 3~6절
③ 기능별 Spec            ← 여기            ★ 현재
④ 하네스 엔지니어링        → 각 Spec의 5절 Build Order
```

## 형식 — 6절 고정, 짧게 쓴다

```
## 목표        무엇을 만드는가 (한 문단)
## 맥락        누가 쓰고 왜 필요한가 + 판정 기준 카드 위치
## 범위        포함 / 제외
## 제약        지켜야 할 조건 (목록)
## 출력 형식   파일명 · 항목 · 구조
## 성공 기준   체크박스. 세거나 대조해서 예/아니오로 갈리는 것만
## 확인 후 채울 것   미확인 값과 그것을 기다리는 이유
```

- **Spec은 길어지면 안 읽힌다.** 배경 설명·개념 정의·검증 절차·문서 간 대조는 여기 쓰지 않는다.
- **성공 기준은 체크박스로, 세어서 판정 가능하게.** "품질이 좋은가"는 안 되고 "빈 칸이 0건인가"는 된다.
- **판정 문장과 임계값은 여기 쓰지 않는다** — `../기준/`의 기준 카드가 정본이다. Spec은 **항목이 존재하는가와 출력 형식**만 정한다.

## 기준 카드와 한 세트다

| | `specs/` (여기) | `../기준/` |
|---|---|---|
| 무엇 | **구조** — 무엇을 만들고 어떤 형태로 내놓는가 | **값과 문장** — 무엇을 보고 O/X를 주는가, 임계값 |
| 바뀌는 빈도 | 드물게 | **자주. 실제로 해 본 사람이 그 자리에서 고친다** |

**값을 두 곳에 적지 않는다.** 하나만 고치고 나머지는 썩는다.
→ **Build Order를 돌리는 사람은 Spec 1개 + 기준 카드 1개, 두 파일을 받는다.** 저장소 전체를 볼 필요는 없다.

## 파일

| 파일 | 기능 | 담당하는 것 | 상태 |
|---|---|---|---|
| `G1_대상기업발굴.md` | **G1** | 일본 후보 기업 명단 수집(L) + 기업 1곳 관찰·판정(J) → 근거가 붙은 후보 목록 | Build Order ① **확정** (성공 기준 확정 10 / 대기 5 · **활용 방식 Workflow · 4경계 선언 완료**) |
| `G7_회신해석정리.md` | **G7** | 일본어 회신 1통 → 원문 보존 + 한국어 해석 + 성격 8분류 + **별도 단위의 번역 검증** → 사용자 판단용 레코드 | Build Order ① **확정** (성공 기준 확정 12 / 대기 4 · **활용 방식 Workflow · 4경계 선언 완료**) |
| `G2_세그먼트화.md` | **G2** | 후보 목록 1벌 → 같은 문안을 써도 될 무리로 묶기 + 무리별 대표 + 배정 근거 + 미분류 4종 | Build Order ① **확정** (성공 기준 확정 9 / 대기 3 · **활용 방식 Workflow · 4경계 선언 완료**) |
| `G3_적합도평가.md` | **G3** | G1 통과분 → 자원을 쓸 가치의 **단일 척도** 순위 + 채점 근거 | Build Order ① **확정** (성공 기준 확정 8 / 대기 4 · **활용 방식 Workflow · 4경계 선언 완료**) |
| `G4_연락처확보.md` | **G4** | G3 순서대로 기업을 열어 연락 창구 확보 + 옵트인 위법 여부 판정 → [확보]/[발송 금지]/[미확보] | Build Order ① **확정** (성공 기준 확정 8 / 대기 2 · **활용 방식 Workflow · 4경계 선언 완료**) |
| `G5_제안메일제작발송.md` | **G5** | 대표 문안 제작 → 그룹 확장 → 일본어 발신·번역 검증 → **사람 승인** → 발송 → 발송 기록 | Build Order ① **확정** (성공 기준 확정 8무관용4서식4 / 대기 4 · **활용 방식 Workflow+승인게이트 · 4경계 선언 완료**. 번역 기준은 `기준/G7` 3절 재사용, 별도 카드 없음) |
| `G6_발송결과판정.md` | **G6** | 발송 기록 + 바운스 코드 → **회신/무응답/주소 오류** 3분기 + G7 정정 반영 | Build Order ① **확정** (성공 기준 확정 7 / 대기 2 · **활용 방식 Workflow · 4경계 선언 완료**) |
| `G8_원인분석.md` | **G8** | G6 무응답·주소오류분 → 원인 가설(무시/도달실패/조사오류) + 재시도 가치 | Build Order ① **확정** (성공 기준 확정 6 / 대기 2 · **활용 방식 Workflow · 4경계 선언 완료**) |
| `G9_위임브리프.md` | **G9** | 경로1(G4미확보)+경로2(G8재시도) → 판단 가능한 브리프 + 사람이 찾은 연락처를 G4로 되돌림 | Build Order ① **확정** (성공 기준 확정 5+3 / 대기 2 · **활용 방식 Workflow · 4경계 선언 완료**. 별도 기준 카드 없음 — 등급은 `기준/G4`, 문턱은 `기준/G3` 재사용) |
| `G10_재수집판단.md` | **G10** | G3보류·G4미확보·G8조사오류·외부신호 → 재수집 대상 + 재조사 범위(전체/특정항목/연락처만) | Build Order ① **확정** (성공 기준 확정 5 / 대기 3 · **활용 방식 Workflow · 4경계 선언 완료**. 별도 기준 카드 없음 — 재조사 항목은 G1 코드 재사용) |
| `G11_반응집계_기준조정.md` | **G11** | G5·G6·G7·G8·G2·G3 → 세그먼트·조건별 반응 집계 + 조정 **제안**(자동 적용 아님) | Build Order ① **부분 확정** — 구조 확정, **실제 집계·조정은 발송 데이터 대기**(성공 기준 (a) 5 / (b) 2). 별도 기준 카드 없음 |

> **활용 방식과 4경계는 각 Spec 「제약」 절에 쓴다.** 절을 새로 만들지 않는다 — 형식은 위 6절 고정이고, 둘 다 *"지켜야 할 조건"*이라 제약에 들어간다. **G1~G4·G6·G8~G10은 Workflow, G5는 Workflow+발송 직전 승인 게이트, G11은 구조만 확정(값은 발송 데이터 대기)**이며 실패 처리까지 각 Spec에 있다. 근거는 `../docs/33_설계근거.md` **D8**(G3~G11의 개별 근거는 D9~D11).

## 세 번째로 G2를 쓴 이유 — 브리프 요구 ②가 검증 없이 비어 있었다

G1·G7이 병렬 조건으로 선정된 것과 달리(아래), **G2는 공백 때문에 선정했다.**

- 브리프 요구 ②(**고객 특성에 맞춘** 제안 메일)의 담당이 초기 설계의 "제안 서비스 매칭"에서 **G2 세그먼트 + G5 그룹별 문안**으로 이관됐다. 그런데 **이관된 자리에 Spec도 검증 항목도 따라오지 않았다.**
- `../docs/40_검증계획.md`를 정리하며 그 공백이 드러나 **V7(세그먼트 축)**을 신설했고, V7이 이 Spec을 기다린다.
- **G2는 앞 기능(G1)에 의존한다** — 병렬로 *실행*할 수는 없다. 다만 **Spec은 지금 쓸 수 있다**. G1이 관찰 항목을 확정했으므로 **G2가 쓸 수 있는 축의 목록이 이미 정해져 있기 때문**이다(`../기준/G2_세그먼트기준.md` 0절, 코드 정본은 `../기준/G1_기업판정기준.md` 0절).

**2026-07-30 — G1~G11 전 기능의 Spec 작성이 완료됐다.** G3(적합도)부터는 `90` 3절의 의존성 순서(G3 → G4 → G5 → G6·G8·G9·G10 → G11)를 따라 채웠다. G11만 발송 데이터가 없어 구조만 확정(부분 확정)이고, 나머지는 전부 확정이다. 남은 것은 각 Spec의 「확인 후 채울 것」(기업 미팅 답·실제 발송 데이터 대기)뿐이며, 다음 단계는 Build Order ② Skill이다.

## G1·G7은 왜 이 두 개였나 — 병렬 작업을 위한 선정

두 기능을 팀원이 나눠 **동시에** 진행하기 위해, **앞 기능에도 짝에도 의존하지 않는 조합**을 골랐다. `31` 3-3절 입출력 연결 목록으로 검증한 결과 **G1 + G7이 그 조건을 만족하는 유일한 조합이다.**

| 기능 | 앞 기능 의존 |
|---|---|
| **G1** | **없음** — 입력이 전부 외부(국가·타겟 조건 4종·탐색 소스). G10 되돌림은 선택적 |
| **G7** | **사실상 없음** — 주 입력이 외부(일본어 회신 메일). 이음새는 G5 발신 문안 1건뿐이며, 그것은 *대조용 참조 텍스트*라 **없어도 작업이 성립하도록** Spec에서 닫았다 |
| G2·G3·G4·G5·G6·G8·G9·G10 | **있음** — 전부 앞 기능의 출력을 필수 입력으로 받는다 |

**두 기능 사이에는 간선이 없다.** G1 → G2·G3·G4 / G7 ← 외부·G5. 서로의 산출물을 기다리지 않는다.

**대신 감수한 것**: G7은 브리프 3대 요구(탐색·목록화 / 맞춤 메일 / 반자동)를 직접 담당하지 않는다. 그 정당성은 **기업이 직접 말한 요구**(회신 수신 / *"접촉이 성립되면 대응이 필요하다"*)에서 나오며, 근거는 `G7_회신해석정리.md` 0-1절에 적어 두었다. 브리프 요구 ②·③의 본체인 G2·G5는 **다음 차례**다.

## 여기에 Spec을 추가할 때

1. **성공 기준을 가장 먼저 쓴다.** 기준이 구체적이어야 목표를 정의할 수 있고, 목표가 정의돼야 결과를 대조해 검증할 수 있다. 순서를 뒤집으면 **기준이 결과에 맞춰진다.**
2. **성공 기준을 (a) 지금 확정 / (b) 대기로 가른다.** 미확인 값이 있다고 전체를 비워 두면 Build Order ①이 영구히 미완이 되고, 숫자를 지어내면 근거 없는 임계값을 만든다.
3. **활용 방식(Chatbot / Workflow / Agent)을 선언한다.** 이걸 정하지 않으면 실패 경로가 설계에서 빠진다. **선언만으로 끝내지 말고 실패 시 무엇을 하는지 함께 적는다** — 그게 이 선언의 실질이다.
4. **Subagent 4경계**(입력 형식 / 출력 형식 / 도구 권한 / 독립 컨텍스트)를 정한다. 넷이 정해지지 않은 것은 Subagent가 아니다. **`../docs/40_검증계획.md`의 사람↔실행체 검증(V1-b 계열)이 이 4경계를 잴 기준으로 쓴다** — 없으면 그 검증이 대상 없이 뜬다.
5. **자체 완결로 쓴다** — 위 "이 디렉터리의 규칙".
6. **출력이 나중에 집계·학습에 쓰이는지 확인한다.** `../docs/31_기능구조.md` 3-3절 연결 목록에서 **이 기능의 출력을 받는 곳을 전부 확인**하고, 그들이 요구하는 항목을 출력에 남긴다. **기록하지 않은 데이터는 소급해서 만들 수 없다** — 특히 **G11(반응 집계·기준 조정)**은 발송 데이터가 쌓인 뒤에야 돌지만, 그때 필요한 항목은 **발송 시점에 남겨야** 한다. G5가 세그먼트·인용 근거를 안 남기면 세그먼트별 반응 집계는 영구히 불가능하다.
7. **왜 이 Spec을 지금 만들었는지 `../docs/91_진행로그.md`에 한 줄 남긴다.**

# 프로젝트 심사 기준
### Final Project · Upstage Education

| 구분 | 배점 | 내용 |
|---|---|---|
| 문제정의 및 활용성 | 20 | 해결하려는 반복 작업이 명확한가 / 실제 업무 병목과 연결되는가 / 결과물이 활용 가능한가 |
| Skill 완성도 및 자동화 | 25 | SPEC.md와 SKILL.md의 입출력·실행 조건·산출물이 명확한가 / 반복 작업이 자동화되는가 / 재사용 가능한 단위로 설계되었는가 |
| Agent·Workflow 실행 완성도 | 25 | Agent 역할이 분명한가 / Skill 호출과 작업 흐름이 자연스럽게 연결되는가 / 설계된 개입 범위 외 임시 조작 없이 실행되는가 / 트리거와 최종 산출물이 명확한가 |
| Harness 견고성 및 재현성 | 20 | 반복 실행 시 안정적인가 / 실패·예외 상황을 고려했는가 / 검증 기준이 있는가 / Harness 폴더만으로 구조를 이해하고 재현할 수 있는가 |
| 발표 및 제출물 완성도 | 10 | 타임리 내 반복작업 Agent·PPT·데모 영상·Harness zip이 완성되었는가 / 문제→설계→작동→검증→효과가 설득력 있게 전달되는가 |

**총 배점: 100점**

## 발표 자료 제작 시 권장 사항

발표 자료 제작 시 아래 사항이 들어갈 것을 권장드립니다!

- **작업 단계 보여주기**: 에이전트가 어떤 순서로 일하는지 드러내세요. (예: 자료 읽기 → 핵심 추출 → 검증 → 정리)
- **검증하는 부분 보여주기**: 일부러 까다로운 입력을 넣고, 에이전트가 빠뜨린 부분을 스스로 잡아 고치는 장면을 보여주기
- **설계에 대한 설명**: "왜 이렇게 역할을 나눴는지"에 대한 설명

> 위 세 항목은 발표 자료 제작 시 권장 사항으로 제시된 것이지만, 발표 때만 준비할 게 아니라 **프로젝트 작업 자체에서 실제로 갖춰야 하는 부분**이다. 발표에서 "보여줄" 장면은 실제로 그렇게 동작해야 나오는 것이지, 발표용으로 따로 연출할 수 있는 게 아니기 때문이다.
> - 작업 단계 보여주기 → 하네스 B(Workflow, `30` ⑦ 10단계)가 실제로 그 순서로 실행되어야 한다.
> - 검증하는 부분 보여주기 → `기준/` 판정 카드와 `40_검증계획.md`의 검증이 실제로 까다로운 입력에서 작동해야 한다.
> - 설계에 대한 설명 → `docs/33_설계근거.md`에 쓰는 "왜"가 곧 이 설명이다.

## 최종 제출 폴더 구조 (예시: trip-planner-harness)

제출 시 아래 구조를 갖춰야 한다고 예시가 제공됨. 🔴 필수 / 🟡 필요시 / 🟢 권장 표기는 예시 그대로.

```
trip-planner-harness/                # ← 우리가 작성하는 "하네스"
├─ spec.md                           # 🔴 필수  — 결과 명확화 6요소
│
├─ skills/                           # 🔴 필수  — 실제 일을 하는 재사용 절차(최소 1개)
│  ├─ local-researcher/SKILL.md
│  ├─ itinerary-writer/SKILL.md
│  ├─ budget-calculator/SKILL.md
│  └─ translator/SKILL.md
│
├─ agents/                           # 🔴 필수(최소 1개) — 스킬을 실행하는 주체
│  ├─ trip-planner-orchestrator.md
│  ├─ local-researcher-subagent.md
│  ├─ itinerary-writer-subagent.md
│  ├─ budget-calculator-subagent.md
│  └─ translator-subagent.md
│
├─ data/
│  └─ 여행정보_모음_파일.csv          # 🟡 필요시 — 데이터 기반일 때의 입력 소스
│
├─ role-table.md                     # 🟢 권장  — 역할·입력·출력·도구권한 표
├─ workflow.md                       # 🟢 권장  — 실행 순서·실패 분기·스케줄
│
└─ hooks/                            # 🟢 권장  — 자동 로깅·검증
   ├─ settings.json
   └─ subagent_log.sh
```

### 우리 저장소 대응 관계

이 예시는 하네스 B(제품 하네스, `CLAUDE.md` 7절)를 가리킨다. 이름은 다르지만 역할은 이미 이 저장소에 대응 위치가 있다 — 새 폴더를 만들지 말고 아래로 채운다.

| 예시 | 우리 저장소 | 현재 상태 |
|---|---|---|
| `spec.md` (6요소) | `specs/G1_대상기업발굴.md` ~ `G11` | 🔴 완료 — 전 기능(G1~G11) 카드 존재 |
| `skills/*/SKILL.md` | `.claude/skills/` | 🟡 `g1-기업판정` 1개뿐 — G2~G11 없음 |
| `agents/*.md` | `.claude/agents/` | 🟡 G1의 4경계는 `g1-기업판정`이 채움. 나머지 기능은 실행체 없음 |
| `role-table.md` | `.claude/README.md` (에이전트 성향·도구 권한 표) | 🟢 존재 |
| `workflow.md` | `docs/31_기능구조.md` 3~6절 + `docs/30` ⑦ 10단계 | 🟢 존재하나 기능별 선언은 각 `specs/` 「제약」에 분산 (`90` 미결정 #8) |
| `hooks/` | — | ✗ 없음 (`CLAUDE.md` 7절 표에도 명시된 공백) |
| `data/` | `기준/candidates`, `reference/` | 🟡 존재 |

**빠진 것**: G2~G11의 Skill·Subagent, 그리고 Hook. `CLAUDE.md` 7절 Build Order 표가 정본이므로 이 표를 새로 만들지 않고 그쪽 상태를 따라간다 — 값이 바뀌면 여기가 아니라 `CLAUDE.md` 7절과 `docs/91_진행로그.md`를 갱신한다.


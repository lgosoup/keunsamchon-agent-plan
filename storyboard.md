# 데모 영상 스토리보드

**원칙**: 다양성(메일 있음/없음, 관심/거절)은 스치듯 짧게만 보여준다 — 그건 자동화의 부수 증거일 뿐이다. **화면 대부분은 "짧은 신호 하나 → 자동 연쇄 → 승인 정지점 → 최종 결과"라는 흐름 자체에 쓴다**(평가항목 "Agent·Workflow 실행 완성도" 25점이 이 흐름을 직접 본다).

각 장면은 **"사용자가 무엇을 하는가" → "화면에 무엇이 뜨는가"** 짝으로 적는다.

---

## 장면 1 — 하네스 업로드 (겨냥: Harness 견고성·재현성 20점)

- **사용자가 한다**: 타임리 GUI에 `Harness/` 폴더(zip)를 업로드한다.
- **화면에 뜬다**: spec·skill·agent 12개+가 목록으로 자동 인식되어 쭉 뜬다. 폴더 하나만으로 구조가 그대로 읽히는 것을 보여준다.

## 장면 2 — 최소 신호 입력 (겨냥: Skill 자동화 25점 — 반복 작업 자동화, 최소 개입)

- **사용자가 한다**: 아주 짧은 한 줄만 입력한다 — 예: *"새 후보 찾아서 세그먼트별로 준비해줘"*.
- **화면에 뜬다**: 입력창에 그 한 줄만 있고, 곧바로 에이전트 실행이 시작된다. **이 한 줄 이후로 사람이 다시 타이핑할 일이 없다는 것**을 카메라가 강조해서 보여줘야 한다.

## 장면 3 — 조사 체인 자동 연쇄 (겨냥: Agent·Workflow 실행 완성도 25점 — 이 데모의 중심)

- **사용자가 한다**: **아무것도 안 한다.** 화면만 지켜본다.
- **화면에 뜬다**: G1(대상 기업 발굴) → G2(세그먼트화) → G4(연락처 확보)가 사람 개입 없이 순서대로 자동으로 돈다. 후보목록·세그먼트·연락처 파일이 빠르게 쌓이는 걸 로그/파일 생성 애니메이션으로 보여준다.
- **여기서 다양성은 1~2초만**: 연락처 결과 중 [확보]/[메일 없음]/[미확보]가 섞여 나오는 걸 스치듯 한 번만 비춘다 — "여러 경우를 다 처리한다"는 인상만 주고 바로 넘어간다. 설명 자막을 달지 않는다.

## 장면 4 — 승인 정지점 (겨냥: Agent·Workflow 실행 완성도 25점 — "개입 범위 외 임시 조작 없음"이 핵심)

- **사용자가 한다**: 시스템이 만든 **승인 대기 화면**을 열고, 문안을 훑어본 뒤 **승인 버튼 하나만 클릭**한다.
- **화면에 뜬다**: 그 전까지 자동으로 돌던 실행이 **이 지점에서 스스로 멈춰서 기다리고 있었다**는 것 — 대기 화면이 이미 만들어져 있는 상태를 먼저 비추고, 그다음 클릭 장면. **"자동인데도 여기서는 반드시 사람이 눌러야 한다"**는 대비가 이 장면의 목적이다.
- **다양성은 여기서도 생략**: 승인 건 하나만 클릭하는 걸 보여주면 충분하다. 거절 케이스는 보여주지 않는다.

## 장면 5 — 발송·반응 자동 처리 (겨냥: Agent·Workflow 실행 완성도 25점 — 트리거·최종 산출물이 명확한가)

- **사용자가 한다**: 승인 클릭 이후 **다시 아무것도 안 한다.** (이 장면에서만 실제로 회신 메일 1통을 보내 Hook을 직접 발동시킨다 — 촬영 중에 재현 가능한 유일한 트리거 조건이라 이 영상의 **유일한 실연 Hook**으로 삼는다.)
- **화면에 뜬다**: 등록된 Hook(회신 도착 감지)이 발동했다는 알림이 먼저 뜨고, 곧바로 `g7-reply-processing`이 자동 호출되어 해석·분류까지 끝난 **최종 레코드 하나가 화면에 뜬다** — 관심을 표명한 케이스(가장 긍정적인 결과) 하나만 보여준다. 거절 케이스는 이 장면에 넣지 않는다.

## 장면 6 — 기준 조정 제안 (겨냥: 핵심 문제 해결 — 재사용을 넘어선 "학습" 축)

- **사용자가 한다**: 채팅에 직접 **"최근 회신 결과 집계해서 조정할 거 있으면 알려줘"**라고 입력한다 — G11의 실제 트리거(데이터 누적 조건 충족)는 촬영 시간 안에 재현할 수 없어, 이 장면은 Hook 대신 **채팅으로 신호를 직접 전달**하는 방식으로 보여준다(이 영상에서 실제로 시연하는 Hook은 장면 5 하나뿐이다).
- **화면에 뜬다**: 누적된 회신 결과를 세그먼트·조건별로 집계한 값과, 그 값이 기준선을 넘겼을 때 G11이 낸 **조정 제안 한 줄**(예: "S2 코스메틱 세그먼트의 거절률이 기준선을 넘었다 — 해당 세그먼트 문안의 가격 언급 비중을 낮추는 조정을 검토할 것") 이 함께 뜬다.
- **사용자가 한다**: 그 제안을 보고 **`criteria/`의 해당 기준 파일을 직접 고치거나, "이 부분을 이렇게 바꿔줘"라고 짧게 요청**한다.
- **화면에 뜬다**: 기준 파일의 값이 바뀐 전/후가 나란히 보인다. 이 장면은 "실행 한 번으로 끝나는 게 아니라, 실행 결과가 다음 실행의 기준 자체를 바꾼다"는 것 — 재사용(같은 기준으로 반복)과 구분되는 **학습(기준이 갱신됨)** 축을 보여주는 유일한 장면이다.

## 장면 7 — 결과 조회로 마무리 (겨냥: Skill 재사용성 — 읽기 전용 별도 진입점)

- **사용자가 한다**: 짧은 질문 하나만 입력한다 — 예: *"이번 주 회신 몇 건 왔어?"*
- **화면에 뜬다**: 즉답이 뜬다. 이 장면으로 영상을 닫는다 — "처음의 짧은 신호 하나가 여기까지 이어졌다"는 느낌으로 마무리.

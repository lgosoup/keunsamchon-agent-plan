## 판정

```
방향 (원칙 검토자): 이탈  — 라운드 1(MVP 재정의)이 `docs/91_진행로그.md`에 한 줄도 기록되지 않았고(CLAUDE.md 5절 위반), 그 결과 기획 본체 `31` 3-2절이 삭제된 조건을 여전히 G1 입력으로 선언하는 등 정본 간 불일치가 남았다.
사장 시선 — 이 상태로 도입할 것인가: 보류  — 월 운영비에 금액이 하나도 없고(`50` 3절), 학생 팀이 떠난 뒤 누가 어떻게 돌리는지가 저장소 어디에도 없다.
담당자 시선 — 매일 쓸 수 있는가: 부담됨  — 승인 화면 자체는 설계돼 있으나, 연락처를 못 찾은 기업·판정 불가 기업이 MVP에서 갈 곳이 없어 내가 손으로 관리할 잔여가 매일 쌓인다.
가장 큰 위험 1개: "지금 무엇을 만드는가"(MVP 범위)가 문서마다 3개·4개·5개·6개로 갈려 있어, 발표·미팅에서 첫 질문에 팀이 서로 다른 답을 한다.
```

---

## 미팅에서 대표가 바로 던질 질문 3개

| # | 질문 | 답이 문서에 있는가 |
|---|---|---|
| 1 | **"이거 월에 얼마 듭니까?"** | **없음.** `docs/50` 3절은 표 전체가 "무료 / 가정 / 가정" 이고 유일한 손익 서술이 조건문이다 — *"건당 제안 단가가 (①위 운영비 총합 + ②담당자 인건비 환산분)을 월 발송 건수만큼 나눈 값보다 크면 순이익 구조가 성립한다"*(50:84). 메일 발송 API·LLM 호출은 회사 비공개와 무관한 공개 시세인데도 금액이 한 줄도 없다. |
| 2 | **"학생들 끝나고 나면 누가 돌립니까?"** | **없음.** 저장소 전체에서 `인수인계`·`유지보수`·`운영 이관`이 검색되지 않는다(유일한 매치는 `project-critic.md` 자신의 체크 항목과 `11`:224의 "SEO 유지보수"). "실사용 도입 설계"를 표방하는 `docs/50`에 이 절이 없다. |
| 3 | **"6개월 뒤에 저한테 뭐가 남습니까?"** | **일부.** G7 이후(미팅·견적·계약)를 경계로 명시하고 이유를 적은 것은 잘한 처리다(`31`:340~346). 그러나 MVP 산출물의 실물은 아직 0건이다 — `candidates/후보목록.md`가 존재하지 않고, 있는 3건은 `specs/candidates/`에 옛 형식·옛 기준으로 남아 "그대로 못 쓴다"고 `specs/G1`:60이 스스로 선언했다. |

---

## 치명 — 지금 고치지 않으면 도입되지 않는다

### 1. [J·A] MVP 범위 개수가 여전히 4곳에서 다르다 — 이전 검토 지적이 반영되지 않았다

근거 (원문 인용):

- `docs/31_기능구조.md`:587 — 표의 MVP 셀은 **6개**를 열거한다: *"**G1**(탐색…) · **G2**(그룹화…) · **G4**(연락처…) · **G5**(발송…) · **G6·G7**(발송 결과 판정·회신 해석 — 최소한의 반응 처리)"*. 그런데 **같은 셀의 다음 문장**이 *"브리프 3대 요구를 **이 다섯 개만으로** 전부 충족한다(①=G1+G2+G4, ②=G2+G5, ③=G5 승인 게이트)"* 라고 쓴다. 괄호 안에 실제로 등장하는 기능은 4개다.
- `CLAUDE.md`:163 — *"MVP 범위표(8-1절 — 지금 만드는 것 **5개** / 부가 이연 5개)"*
- `docs/20_문제정의.md`:63 — *"후보 풀 확장 + 컨택 가능 여부(**G1·G2·G4·G5**)만 MVP로 먼저 풀고"* → 4개
- `docs/01_진행계획.md`:140 — *"실제 MVP는 … **A(G1) + B(G2) + G5**를 하나의 흐름으로 묶은 것 … G4는 이 흐름 안에 낀 **보조 기능**으로 포함됐다"* → 3+1
- `specs/G1_대상기업발굴.md`:4 — *"**MVP는 이 기능(탐색) + G2(그룹화) + G5(발송)이 핵심이다**"* → 3개
- `docs/50_실사용_도입설계.md`:38 — *"**G1(발굴)→G2(세그먼트)→G4**(연락처)를 거쳐 나온 후보 중 **G5** 발송 직전에 사람이 승인한다"* → 4개, G6·G7 없음

발주자 입장에서 왜 문제인가: 대표가 "그래서 이번에 받는 게 뭡니까"라고 물었을 때 팀원이 어느 문서를 펴느냐에 따라 답이 달라진다. 특히 **G6·G7(발송 결과 판정·회신 해석)이 MVP인지 아닌지**가 갈리는데, 이건 "메일 보낸 뒤 아무것도 안 돌아온다 vs 회신이 한국어로 정리돼 온다"의 차이라 발주자에게는 완전히 다른 제품이다.

고치는 방향: `31` 8-1절 표를 **유일한 정본**으로 선언하고 6개(G1·G2·G4·G5·G6·G7)로 확정한 뒤, 같은 셀의 "다섯 개만으로" 문장을 "①=G1+G2+G4, ②=G2+G5, ③=G5 승인 게이트, 그리고 회신을 사람에게 되돌리는 최소 경로가 G6·G7"로 다시 쓴다. 나머지 5곳(`CLAUDE.md`:163, `20`:63, `01`:140, `specs/G1`:4, `50`:38)은 개수를 쓰지 말고 **"`31` 8-1절"만 가리키게** 바꾼다 — 개수를 여섯 곳에 복제한 것이 `CLAUDE.md` 5절 운영변수 규칙이 막으려던 바로 그 사고다.

---

### 2. [G·J] 라운드 1(MVP 재정의)이 `docs/91_진행로그.md`에 없다 — 근거 추적이 끊겼다

근거: `docs/91_진행로그.md`에서 `2026-08-03`을 검색하면 **매치가 1건뿐**이다(125행, 라운드 2 운영 변수 단일화). MVP 재정의 — G3·G8·G9·G10·G11 이연, O6ⓒⓓⓔ·O7 완전 삭제, 후보 리스트를 단일 파일로 변경 — 은 로그에 없다.

그런데 `기준/G1_기업판정기준.md`:160 변경 이력의 마지막 칸이 근거로 **`docs/91_진행로그.md` 2026-08-03**을 가리킨다. 그 자리에 가면 운영 변수 로그가 나온다.

발주자 입장에서 왜 문제인가: 심사가 보는 것은 *"왜 그 선택이고 검증했는가"*이고(`CLAUDE.md` 2절), 이 프로젝트에서 가장 큰 범위 결정(기능 11개 중 5개를 뒤로 미룸)의 근거가 `33` D15 하나에만 있다. `CLAUDE.md` 5절은 *"결정이 나면 … 바뀐 이유를 `91`에 한 줄 남긴다"*, *"원안(`99`)을 바꾸려면 근거를 `91`에 먼저 쓴다"*를 규칙으로 두고 있다. 규칙을 스스로 만들고 가장 큰 결정에서 어긴 것은 발표에서 그대로 드러난다.

고치는 방향: `91`에 2026-08-03 항목을 하나 더 추가한다 — 무엇을(G3·G8~G11 이연 / O6ⓒⓓⓔ·O7 삭제 / 후보목록 단일 파일화 / G4·G5 대체 규칙), 왜(사용자 결정 + `33` D15), 반영 파일 목록, 그리고 **기업 발언 F27과 정면 충돌한다는 사실**까지. `기준/G1`:160의 포인터가 그 항목을 가리키게 한다.

---

### 3. [J] 기획 본체 `31`의 G1 입출력 정본이 아직 "타겟 조건 4종(한국 진출 희망)"이다 — 잠재고객 정의가 문서마다 다르다

근거:

- `docs/31_기능구조.md`:173 (3-2절 G1 입출력 표) — *"| **입력** | 국가(일본) ← 외부 · **타겟 조건 4종**(제품 / 커머스 / 업종 / **한국 진출 희망** — 1-1절) ← 외부 …"*
- `docs/31`:452 (고아 검사) — *"**6개, 전부 외부 입력으로 명시** — 국가·**타겟 조건 4종** / 탐색 소스 …"*
- `docs/01_진행계획.md`:46 (C1 게이트 = **잠재고객 정의 확정**) — *"**완료 2026-07-30 (`31` 1-1절).** 일본에서 제품·커머스를 갖고 **한국 진출을 원하는** 패션·뷰티·푸드 기업. **네 조건 전부 필수**이며 … 단 '한국 진출 희망'의 관찰 방법은 미정(`31` 6절 #5)"* — 그런데 `31` 6절 #5는 538행에서 이미 "해소, 완전히 뺐다"로 닫혔다.

같은 문서 `31`의 1-1절(43행)·6절 #5(538행)·8-1절은 3종으로 고쳐졌는데 **3-2절 입출력 표와 고아 검사만 4종으로 남았다.** 3-2절은 이 문서가 스스로 *"기능 단위 + 입출력 확정 ★ 지금 여기"*(31:8)라고 지목한 정본 절이다.

발주자 입장에서 왜 문제인가: "잠재고객이 누구인가"는 이 과제의 첫 문장이다. 진행 계획서(`01`)의 게이트 통과 기록이 "한국 진출을 원하는 기업"이라고 확정해 두면, 대표가 그 문서를 열었을 때 **N22에서 자기가 직접 "그 조건은 중요하지 않다"고 답한 것이 반영 안 된 것**으로 읽힌다. 기업 답을 받아놓고 안 고친 것은 신뢰를 가장 빨리 깎는 종류다.

고치는 방향: `31`:173을 "타겟 조건 3종(제품 / 커머스 / 업종 — 1-1절)"으로, `31`:452를 "타겟 조건 3종"으로 고친다. `01`:46 C1 게이트는 답을 갱신하고 *"2026-08-03 — N22 답으로 '한국 진출 희망'을 조건에서 완전히 뺐다. 현재 정의는 3조건"*을 병기한다. `docs/12`:184 N22의 답 칸도 "필수→적합도 가점 하향"이 아니라 "**완전 삭제**"로 고친다(현재 상태와 두 단계 어긋나 있다).

---

### 4. [C·K] MVP에서 출구가 없는 출력이 4종 — 라운드 1 편집이 만든 새 구멍이다

근거: `docs/31` 3-3절 되돌아오는 경로 표(433~444행)와 각 Spec의 소비처를 대조하면, MVP 기능이 내는 출력 중 **받는 쪽이 전부 부가 기능**인 것이 넷이다.

| MVP가 내는 출력 | 선언된 소비처 | MVP에 있는가 |
|---|---|---|
| G4 **[미확보]** (`specs/G4`:54 — *"[미확보] → **G9**(사람 위임) 큐로 넘어가고, 동시에 **G10**에도 들어간다"*) | G9·G10 | **둘 다 부가 이연** |
| G1 **판정 불가** (`31`:440 — *"G1 판정 불가분 → **G10**"*) | G10 | **부가 이연** |
| G2 **U-정보없음** (`specs/G2`:72 — *"**G10 재수집 후보**에 넣는다"*) | G10 | **부가 이연** |
| G6 **무응답·주소 오류** (`31`:315) | G8 | **부가 이연** |

`90` 리스크 34는 *"G4 탐색 순서·G5 발송 상한 컷오프가 **G3 출력에 의존**하던 자리"* 만 다뤘다 — **MVP가 부가 기능으로 흘려보내는 방향**은 검토되지 않았다.

특히 치명적인 조합: `specs/G4`:4가 대표 우선 탐색의 근거로 *"**대표 기업의 연락처가 없으면 그 그룹 전체의 발송이 막히므로** MVP 플로우엔 이 순서가 더 맞는다"*라고 쓴다. 즉 대표 연락처 미확보 = 그룹 전체 정지인데, 그 회복 경로(G9 사람 위임)가 MVP에 없다.

발주자·담당자 입장에서 왜 문제인가: 담당자 화면에는 승인 대기 건만 뜨고, 연락처를 못 찾은 기업·페이지가 안 열린 기업·정보가 비어 미분류된 기업은 어디에도 나타나지 않는다. 그 잔여를 담당자가 엑셀로 따로 관리하기 시작하면 3개월 뒤 도구가 방치된다. `31`:143이 스스로 경고한 문장이 그대로 재현된다 — *"이 두 경로가 없으면 한 번 걸러진 기업이 영구히 사장되고, 사람이 애써 찾은 연락처가 어디에도 쓰이지 않는다."*

고치는 방향: `90`에 리스크 35를 신설하고, `31` 8-1절에 **"MVP 잔여 처리"** 한 줄을 추가한다. 대체 규칙은 코드가 필요 없는 수준으로 충분하다 — 예: *"MVP 동안 G4 [미확보]·G1 판정 불가·G2 U-정보없음·G6 무응답/주소오류는 각 산출 파일에 상태로만 남기고, `보류/` 디렉터리 목록 하나에 append한다. 담당자는 주 1회 그 목록을 보고 사람 접촉 여부를 직접 판단한다(G9·G10 착수 전까지의 수동 대체)."* 특히 **대표 기업 [미확보] 시 그룹 차선 대표를 세우는 규칙**은 `기준/G2` 3절에 반드시 넣어야 한다 — 지금은 그룹 전체가 멈춘다.

---

## 중대 — 다음 단계 전에 고쳐야 한다

### 5. [K] `docs/50` 7절 역방향 참조 목록이 자기가 정한 대조 방법으로 검증하면 틀린다

근거: `docs/50`:112가 *"**대조 방법**: `50_실사용` 또는 `docs/50`으로 저장소 전체를 검색하면 이 표와 일치해야 한다"*라고 스스로 검증 조건을 선언했다. 실제로 검색하면 표(4행·6파일)에 없는 참조가 최소 15곳이다:

`specs/README.md`:35·91·96(1-1절) / `기준/README.md`:16 / `docs/01`:153(1·3·1-1절) / `docs/10`:107 / `docs/11`:221(3절) / `docs/12`:101·177·198·224·225·231·232·236 / `docs/13S`:103·104(U3·U4) / `docs/31`:305·307(1·1-1절) / `docs/33`:172 / `docs/90`:48·49·64 / `기준/G1`:88·141(2절·3절) / `기준/G3`:21(3절) / `기준/G8`:23 / `specs/G5`:45(**3절 최소 수주 금액** — 표의 3절 행에 이 파일이 없다) / `.claude/agents/project-critic.md`:29·64

발주자 입장에서 왜 문제인가: 이 표의 존재 이유가 *"값이 왔을 때 어디를 고쳐야 하는지"*를 잃지 않는 것인데(리스크 26의 재발 방지), 표가 절반도 못 담고 있으면 리스크 26이 그대로 재발한다. 그리고 **"검증 방법을 적어놓고 그 검증을 안 돌린 것"**은 심사가 가장 싫어하는 형태다.

고치는 방향: 검색 결과 그대로 표를 채우되, 세 부류로 나눈다 — ① **값을 소비하는 곳**(specs·기준 카드, 지금 표의 성격) ② **규칙을 선언하는 곳**(`CLAUDE.md` 5절, 두 README, `33` D16 — 값 갱신 대상이 아님) ③ **질문·리스크 대장**(`12`, `13S`, `90` — 답이 오면 여기도 닫아야 함). ②를 표에서 빼는 결정을 하려면 그 판단을 표 머리말에 한 줄 적어야 검색 결과와 표가 일치한다.

### 6. [J] "3안"이 5곳에 남았다 — 라운드 2 로그는 "잔존 2곳 정리"라고 적혀 있다

근거: `docs/91`:125가 *"부수적으로 발견된 stale 참조(… **3안→2안 잔존 2곳**) 정리"*라고 기록했으나 실제 잔존은 5곳이다.

- `기준/G2_세그먼트기준.md`:18 — *"① 기업에 **되물을 3안**을 실물로 준비한다"*
- `기준/G2`:70 — *"**3안**을 나란히 놓고 고르게 한 뒤에 우리 생각을 말한다"*
- `기준/G2`:125 (5절 값 표) — *"**기업.** **3안**을 실물로 놓고 되묻는다(1절)"*
- `docs/12_미팅_질문지.md`:174 N19 — *"실물(**세그먼트 3안** + 각 안의 메일 초안)을 놓고 되묻는다"*
- `docs/90`:129 — *"**세그먼트 3안**을 묻는 `12` N19와 같은 자리에서 한다"*

같은 카드 1절 제목은 "**분류축 2안**"이고 `기준/README.md`:35·`specs/G2`:94·`40` V7:162도 전부 2안이다. 즉 카드 안에서 자기모순이다.

발주자 입장에서 왜 문제인가: 미팅에서 담당자에게 내밀 실물이 2장인데 문서는 3장이라고 말한다. 준비물 개수가 안 맞으면 미팅 자리에서 바로 드러난다.

고치는 방향: 5곳 전부 "2안(A 품목 / B 규모)"으로. `docs/12` N19의 질문 문장에서 "담당자 직급인지?"도 함께 본다 — `기준/G2`:40이 *"담당자 직급으로 묶고 싶다면 O 목록에 없으므로 G1부터 고쳐야 한다"*고 비용 경고를 달았는데, 질문지에는 그 경고 없이 선택지로 나열돼 있다.

### 7. [H] `docs/40` V1 — "지금 실행할 유일한 검증"의 기록 양식이 삭제된 항목을 재고 있다

근거:

- `docs/40`:45 (V1 절차 앞 경고 블록) — *"1차 판별축은 이제 **제품 사진 보유 / 커머스 보유 / 업종 / 한국 진출 희망**이고"*
- `docs/40`:83 (기록 양식 표 안) — `| 한국 진출 희망 신호 | 관찰된 것 | 어디서 그렇게 봤는지 | | | O/X | |` — **양식에 행이 그대로 살아 있다**
- `docs/40`:89 — *"**세 항목**은 갈릴 것을 예상하고 넣었다"* / 94행은 *"V1-a는 이제 **아래 두 항목만** 겨냥한다"* (자기모순)
- `docs/90`:122 — *"판정 대상이 … `31` G1 관찰층(제품 사진 / 커머스 / 업종 / 규모 신호 / **한국 진출 신호** / 거부 표시)"*

`기준/판정시트_G1.md`는 106행에서 정확히 제거했다(*"한국 관련 신호·자본금·종업원수·설립연도는 이 시트에서 뺐습니다 (2026-08-03)"*). 시트는 고쳤는데 검증 계획서는 안 고쳤다.

발주자 입장에서 왜 문제인가: `40`:47이 스스로 경고한 문장 그대로다 — *"바꾸지 않으면 '지금 할 수 있는 유일한 검증'이 이미 버린 기준을 검증한다. … **검증했다는 사실이 오히려 감점 재료가 된다.**"* 같은 사고가 같은 문서에서 두 번째 발생했다.

고치는 방향: `40`:45의 4종을 3종으로, 83행 행 삭제, 89행 "세 항목"→"두 항목", `90`:122의 목록에서 한국 진출 신호 제거. 그리고 **`40` 2-5절에 "MVP 범위가 바뀌면 V1 양식을 대조한다"는 규칙 한 줄**을 추가한다 — 같은 실수가 세 번째로 나지 않게.

### 8. [K] `docs/12` 4절 "답을 받아오면 어디를 갱신하는가"가 폐기 문서를 가리킨다

근거 (`docs/12`:222~239):

- 222행 — *"Q1, Q20 | `20_문제정의.md` 잠재고객 정의 → **`30_해결설계.md` 발굴 신호·매칭 로직**"* — 매칭 로직은 `31` 9절이 폐기했다
- 223행 — *"Q2, Q3, Q4, Q5 | `31` 1-1절 타겟 조건 + 해당 기능의 **Spec(`32`)**에 임계값 근거"* — `32`는 `CLAUDE.md`:165가 "이력, 여기서 새로 정하지 않는다"로 닫은 문서다. 지금 Spec은 `specs/`다
- 226·230·234·237·239행 — Q7·Q16·Q21·Q23 / N4 / N8 / Q13 / Q12·Q15·Q17·Q18 모두 **`30_해결설계.md`**로 라우팅

발주자 입장에서 왜 문제인가: 미팅에서 답을 받아온 직후가 가장 바쁜 순간인데, 그때 여는 표가 죽은 문서를 가리킨다. Q16(금지 표현)·Q7(레퍼런스 허가)은 `specs/G5` 제약과 `40` V4가 직접 기다리는 값인데 `30`으로 보내면 도착하지 않는다.

고치는 방향: 4절 표를 현행 파일로 전부 재작성한다 — 메일 관련(Q7·Q16·Q21·Q23·N8)→`specs/G5` + `40` V4 / N4·Q12→`90` 리스크 21 + `31` G7 경계 / Q13→`specs/G5`·`기준/G7` / Q2~Q5→`기준/G3`·`specs/G3`(부가 착수 조건) / `32` 참조는 `specs/`로.

### 9. [J] `docs/12` Q9가 라운드 2가 폐기한 "상한" 프레임 그대로다

근거: `docs/12`:193 — *"Q9 | **월 몇 건까지 제작 가능**합니까? … | **목록 규모의 상한.** 캐파가 월 10건인데 리드 500건을 만들면 성과가 아니라 낭비다"*

같은 문서 N11(177행)은 라운드 2에서 정확히 고쳐졌다 — *"단 **제작 캐파는 상한으로 걸지 않고 참고 수치로만** 쓴다 — 발송을 조이는 건 별도의 일일 발송 상한(Q15)뿐이다"*. Q9만 안 고쳐졌다. Q10(194행)의 *"타겟 기업 **규모의 하한**"*도 `기준/G1`:141이 *"회사 규모 하한은 이 카드에서 뺐다"*로 처리한 뒤라 반쪽만 맞다.

발주자 입장에서 왜 문제인가: 이 문장 그대로 대표에게 물으면 대표는 "상한을 정해 달라"는 질문으로 받아들이고, 그 답이 다시 상한으로 설계에 들어온다. 라운드 2가 세운 원칙(`50`:7 — *"값이 없으면 무제한으로 처리하고, 실제로 감당 못 할 만큼 나오면 그때 후보 목록에서 필요한 만큼만 골라 쓴다"*)이 미팅 한 번에 되돌아간다.

고치는 방향: Q9의 "무엇이 갈리는가" 칸을 N11과 같은 문구로 통일하고, 아예 Q9를 N11로 병합한다(같은 질문이 두 번 있다). Q10은 "**G3 착수 시** 회사 규모 하한 — `기준/G3` 4절"로 대상을 명시한다.

### 10. [F] `docs/33` D5 — 규모 신호의 외부 표준 근거가 삭제된 항목에 붙어 있다

근거: `docs/33`:88 D5 ⑤ — *"**규모 신호의 축 = 資本金 + 従業員数 ← 중소기업기본법**이 쓰는 두 축이고 일본 기업 「会社概要」에 관행적으로 적혀 관찰 가능성도 높다"*. 그런데 資本金·従業員数는 `기준/G1`:88에서 **완전 삭제**됐고, 현재 O6은 상품 개수·판매채널 수 둘뿐이다.

같은 항목 ①은 *"**필수 조건 4종**을 O/X 게이트로"*(3종), 뒤집힐 조건(92행)은 *"제품 사진·**한국 진출 신호**·거부 표시"*로 삭제된 항목을 여전히 열거한다. 또 `33` 변경 이력(195~199행)은 **D9까지만** 있고 D10~D16이 등재되지 않았다.

발주자 입장에서 왜 문제인가: 심사 질문이 *"왜 그 기준인가"*인데, D5는 이 저장소가 그 질문에 답하려고 만든 유일한 항목이다. 지금 상태로는 **실제로 관찰하는 두 항목(상품 개수·채널 수)에는 외부 근거가 하나도 없다.** 그리고 이 O6이 `기준/G2` 안 B(규모 축)의 **유일한 재료**이므로, 세그먼트 축의 절반이 근거 없이 서 있는 셈이다.

고치는 방향: D5 ⑤를 다시 쓴다 — 資本金·従業員数가 표준 근거였고 그 항목을 뺀 것이므로, **남은 상품 개수·채널 수에 대한 근거를 새로 쓰거나, 근거가 없다는 것을 명시**한다(*"O6 현재 2항목은 '상품 목록 페이지 하나에서 추가 방문 비용 없이 관찰 가능하다'는 비용 근거로만 남았고, 무엇이 규모를 뜻하는지의 외부 표준 대응은 없다 — `[우리판단]`"*). ①은 3종으로, 92행 뒤집힐 조건에서 한국 진출 신호 제거. 변경 이력에 D10~D16 등재.

### 11. [F·H] `docs/50` 2절의 유일한 실측이 폐기된 절차의 실측인데, 그 한계가 빠져 있다

근거: `docs/50`:57 — *"Honeys | **4회** | 4분 43초 | **O6(자본금·종업원수 등 회사개요)까지 확인하려 하위 페이지 2단계 진입**, 그만큼 요청 수 증가"*. 그 O6ⓒⓓⓔ는 2026-08-03 삭제됐다. INGNI의 9분도 *"specs/G1·기준/G1 **최초 정독** 포함"*(55행)인데 그 카드도 그 뒤 두 번 바뀌었다.

`50` 61~64행이 "측정의 한계"를 정직하게 세 개 밝힌 것은 좋다. 그러나 **"기준이 바뀌어 이 값이 현행 절차의 값이 아니다"**는 한계가 빠져 있고, 그 상태로 1-2절 *"하루 발굴 처리량 10~20곳/일"*과 3절 *"기업당 웹 요청 2~4회 × 월 처리 목표"*가 이 값을 그대로 쓴다.

발주자 입장에서 왜 문제인가: 대표가 보는 유일한 숫자가 이것인데, 지금 설계로 다시 돌리면 더 빨라진다(관찰 항목이 줄었으므로). 즉 **불리한 쪽으로 틀린 숫자**라 손해는 아니지만, "이 숫자가 현행 기준의 값인가"를 물으면 "아니다"라고 답해야 한다. 그리고 `candidates/후보목록.md`가 없어(`Glob` 결과: `specs/candidates/` 3건뿐) **현행 기준으로 판정된 기업은 0곳**이다.

고치는 방향: `50` 2절 한계에 네 번째 줄 추가 — *"이 실측은 2026-08-03 관찰 항목 축소 **이전** 기준이다. 현행 3조건·O6 2항목 기준의 실측은 아직 없다."* 그리고 `90` 다음 액션에 **"옛 3건을 현행 기준·현행 형식으로 재판정해 `candidates/후보목록.md`를 실제로 만든다"**를 넣는다 — 이건 하루짜리 작업이고, 끝나면 실측·처리량·`50` 1-2절 통과율 가정이 전부 실측으로 바뀐다.

### 12. [C] 인수인계·유지보수 절이 어디에도 없다

근거: 저장소 전체 검색에서 `인수인계`·`유지보수`·`운영 이관`이 나오는 곳은 `.claude/agents/project-critic.md`(이 검토자 자신의 체크 항목)와 `docs/11`:224(SEO 유지보수 언급)뿐이다. "실사용 도입 설계"를 표방하는 `docs/50`의 목차는 1(병목)·1-1(발송상한)·1-2(승인부담)·2(처리량)·3(운영비)·4(기존도구)·5·6·7이고 이 항목이 없다.

발주자·담당자 입장에서 왜 문제인가: 이 도구는 Claude Skill·Subagent·Spec·기준 카드 세트로 돌아가는데, 담당자는 개발자가 아니다. `기준/README.md`:11이 *"누가 고치나 | **실제로 판정해 본 사람이 그 자리에서**"*라고 전제하지만, **그 사람이 이 저장소 구조를 어떻게 배우는지**가 없다. 3개월 뒤 방치되는 가장 흔한 경로다.

고치는 방향: `docs/50`에 5절로 **"인수인계 — 학생 팀이 떠난 뒤"**를 신설한다. 최소 세 가지면 된다 — ① 담당자가 실제로 여는 파일 2개는 무엇인가(`기준/G1`·`기준/G2`가 유일한 손잡이라는 것) ② 문구를 고치고 싶을 때 어디를 고치는가(`specs/G5`가 아니라 대표 문안 템플릿) ③ 무엇이 고장 났을 때 누구에게 무엇을 들고 가는가. `기준/README.md`가 이미 "손잡이" 개념을 세워 뒀으므로 그것을 담당자 언어로 한 장 옮기면 된다.

### 13. [C] 월 운영비에 금액이 하나도 없다

근거: `docs/50` 3절 표(77~84행) — 국가 선정 데이터 2행은 "무료"(확인됨, 좋다). 나머지는 *"월 처리 목표는 `12` Q9 확인 후 확정"* / *"**가정 — 실측 안 함.** … 월 300건 처리 시 약 600~1,200회 호출 규모 (가정). **정확한 단가는 … 여기서 금액을 만들지 않는다**"* / *"가정 — … **일반 시장에 흔한 트랜잭션 메일 서비스 수준 비용대**"*.

"비공개 처리 준수"(50:6)는 **회사 가격표**에 적용되는 원칙이고, LLM API 단가·메일 발송 서비스 요금은 공개된 시세다. 그것까지 금액을 안 만든 것은 비공개 원칙 때문이 아니다.

발주자 입장에서 왜 문제인가: 대표의 첫 세 질문 중 하나가 "월 얼마"다. "모델에 따라 달라서 금액을 만들지 않았습니다"는 답이 아니다. 그리고 `CLAUDE.md` 2절은 *"표를 만들었으면 값이 어디서 오는지도 쓴다. **값 없는 표는 구체적인 게 아니라 구체적인 척이다**"*를 금지 항목으로 두고 있다 — 3절이 정확히 그 표다.

고치는 방향: 모델·서비스를 **하나 골라 가정으로 못 박고** 금액 범위를 낸다. 예: *"가정 — Claude Sonnet 급 모델, 기업당 입력 ~20k·출력 ~2k 토큰 × 월 300건 → 월 $X~Y. 트랜잭션 메일 서비스 무료~저가 티어 월 $Z. 합계 월 $N 규모."* 모델을 바꾸면 값이 바뀐다는 것을 한 줄 붙이면 된다. **틀릴 수 있는 숫자를 내는 것이 숫자를 안 내는 것보다 낫다** — 대표는 자릿수를 알고 싶은 것이지 소수점을 원하는 게 아니다.

---

## 경미 — 알고만 있으면 되는 것

- `docs/90`:99 — *"목록이 **21건**이라 무엇을 보고 있어야 하는지가 묻힌다"* — 실제 34건.
- `docs/90`:131 — *"남은 기능의 Spec 작성 — **지금 있는 것은 G1·G7·G2 셋이다**"* — 실제 11개 전부 완료(아래 체크박스와 자기모순).
- `docs/90` 리스크 23 대응(74행) — *"`11`에 **6절** '채널 대안 비교' 신설"* — 실제는 `11` **8절**(같은 문서 109행은 8절이라고 맞게 적었다).
- `docs/99`:34 — *"가설을 명확히 세우는 게 중요 | `20` 문제정의 | **미작성 — `20`이 아직 없다**"* — 2026-07-31 신설됨. 같은 문서 53행 A-23 *"미이행 — 현재 가장 큰 공백"*도 `11` 1·2·8절로 부분 이행됐다.
- `specs/G9_위임브리프.md`:54 — *"**어떤 기업인지** — G1 관찰 근거 요약(업종·규모·**한국 진출 신호**)"* — G1이 관찰하지 않는 값. 부가 기능이라도 착수 시 즉시 깨진다.
- `docs/30`:14·`docs/32`:41 — 4종 서술 잔존. 둘 다 이력 문서이고 `CLAUDE.md`가 "`31` 9절 먼저 읽어라"로 방어했으므로 손대지 않는 판단은 맞다. 다만 `30`:14는 `31` 9절 판정을 **요약한 표**라 값이 어긋나면 안내 자체가 틀린 안내가 된다 — 한 줄만 고칠 가치는 있다.
- `specs/README.md`의 파일 표(45~55행)에 MVP/부가 표시가 없다. Spec을 받아 일할 사람이 가장 먼저 보는 표인데 "지금 만드는 것"이 안 보인다.
- `docs/33` 변경 이력이 D9(2026-07-30)에서 끊겼다 — D10~D16 미등재. 문서 자신의 규칙 위반.

---

## 잘 하고 있는 것 (근거 인용)

1. **일일 발송 상한만 예외로 남긴 판단이 정확하다.** `docs/50`:26 — *"한번 도메인 평판이 깎이면 그 프로젝트와 무관한 기존 업무까지 다치고 … `CLAUDE.md` 5절의 무관용 판별법('어겼을 때 누가 무슨 손해를 보는가')을 적용하면 — **회사의 기존 사업이 손해를 본다**."* 사장 시선에서 가장 무서운 항목(리스크 12)을 정확히 집어 다른 셋과 갈랐고, `specs/G5`:46이 *"이 값은 무제한이 기본이 아니다"*로 받아 대칭을 맞췄다. 병목 4종 분리 자체가 이번 두 라운드의 최대 성과다.

2. **자동 램프업 코드를 안 만들기로 한 결정에 근거가 붙어 있다.** `docs/50`:31 — *"자동 램프업(1일차 5통→2일차 10통…) 같은 코드 로직을 만들지 않는다 — 그런 코드는 초기 며칠만 쓰이고 그 뒤로는 버려지는 코드가 된다. 대신 이 표의 숫자를 담당자가 상황을 보며 직접 올린다."* 담당자가 손댈 수 있는 값으로 남긴 것이고, "만들 수 있으니 만든다"를 거부한 판단이다.

3. **반자동 요구가 구조로 강제돼 있다.** `specs/G5`:66 — *"메일 발송 도구는 접근 권한이 있으나, **사람 승인이 기록된 건에만 그 권한을 행사한다** … 이 경계가 다른 기능(G1·G2·G7)과 다른 유일한 지점이다(그것들은 발송 권한 자체가 없다)."* 저장소 전체를 검색해도 완전자동 발송 경로는 없다. 브리프 3대 요구 중 ③은 문장이 아니라 도구 권한으로 지켜지고 있다.

---

## 우리(기업)에게 물어봐야 할 것

**이미 `docs/12`에 있는 것 (문구만 고쳐서 그대로 쓴다)**

| 번호 | 왜 지금 급한가 |
|---|---|
| **Q15** (발송 도메인·서비스) | **MVP 전체가 여기서 멈춘다.** `docs/50`:34가 *"값이 없으면(지금 상태) **발송하지 않고 대기한다**"*라고 못 박았으므로, Q15 답이 없으면 G5를 다 만들어도 발송 건수는 0이다. N17과 같은 자리에서 묻는다. |
| **Q14** (담당자·하루 가용 시간) | `50` 1-2절의 승인 부담 표가 전부 가정이다. 답이 오면 그 자리에서 "하루 몇 건까지 화면에 올릴지"가 정해진다. |
| **Q11** (월 운영비 상한) | 위 중대 13에 직결. 상한을 알면 모델·발송 서비스 선택이 즉시 갈린다. |
| **N19** (유사 기업을 무엇으로 묶는가) | `40` V7이 "MVP 최우선" 검증인데(40:162) 이 답 없이는 잠정 축으로만 돈다. **되물을 실물은 2안이다** — 질문지 문구를 고쳐서 간다. |
| **N20 / Q12** (회신 이후 준비 상태) | `90` 리스크 21. 답이 "준비 안 됐다"면 MVP 우선순위 자체가 바뀐다 — 리드보다 회신 처리가 먼저가 된다. MVP를 확정하기 전에 물어야 할 질문이다. |
| **Q7** (레퍼런스 인용 허가) | GUESS·BARNEYS·CJ ENM. `specs/G5`:45가 허가 전까지 템플릿에서 물리적으로 제외해 뒀다 — 허가가 나오면 메일 설득력이 가장 크게 올라가는 단일 항목이다. |

**새로 추가할 질문 2건**

| # | 질문 | 무엇이 갈리는가 |
|---|---|---|
| **N25** | **이 도구를 담당하실 분이 프로그램을 직접 고쳐 쓸 수 있는 분입니까, 아니면 화면에서 버튼만 누르는 쪽입니까?** | 위 중대 12(인수인계)의 형태가 갈린다. 전자면 `기준/` 카드를 그대로 넘기면 되고, 후자면 문구 수정용 화면이 MVP에 하나 더 필요하다 — 그건 범위 변경이므로 지금 물어야 한다. |
| **N26** | **연락처를 못 찾은 기업, 페이지가 안 열려 판정 못 한 기업이 나올 텐데, 그건 목록에서 빼도 됩니까 아니면 따로 모아 보셔야 합니까?** | 위 치명 4의 대체 규칙이 여기서 정해진다. "빼도 된다"면 MVP는 지금 상태로 성립하고, "모아 봐야 한다"면 `보류/` 목록이 MVP 산출물에 추가된다. |

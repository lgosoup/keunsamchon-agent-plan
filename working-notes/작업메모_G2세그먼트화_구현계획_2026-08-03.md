# 작업 메모 — G2 세그먼트화 + G5 메일 검수/발송 제안 로직 구현 계획

> 상태: **보류 (2026-08-03)**. 아래 두 확인 사항에 대한 결정이 나오면 이어서 진행한다.
> 이 파일은 확정 산출물이 아니라 **중단 시점의 작업 맥락 기록**이다 — 재개할 때 여기서부터 읽는다.

---

## 1. 원 요청 (사용자 지시, 원문 취지)

1. G2(세그먼트화)로 기업을 그룹으로 묶고, 그룹마다 메일 형식을 검수 → 발송을 사용자에게 제안하는 로직을 구현한다.
2. 최종 사용자가 보는 것: **기업 / 해당 기업 정보 / 적합도(근거하에) / 해당 기업에 보낼 메일**.
3. 그룹화(G2) 자체는 부가기능 정도로 봐도 될 만큼 비중이 작다(기업 니즈는 이미 충족) — 핵심은 **그룹의 좋은 예시를 토대로 메일을 자동 작성 → 최종적으로 사람이 확인**하는 흐름.
4. 바로 작업하지 말고 로직 구현 계획을 먼저 브리핑할 것.

## 2. 확인한 것 — 기존 설계와 겹치는 부분

요청의 핵심 흐름은 이미 `specs/G2_세그먼트화.md`·`기준/G2_세그먼트기준.md`·`specs/G5_제안메일제작발송.md`에 설계돼 있다. 새로 정할 것이 아니라 **이미 있는 Spec 두 개를 실행체(Skill)로 만드는 작업**이다.

| 요청 항목 | 이미 정해진 곳 |
|---|---|
| 그룹 묶기 | `specs/G2_세그먼트화.md` + `기준/G2_세그먼트기준.md` — 분류축 2안(품목/규모), 판정 문장, 대표 선정 규칙까지 완비 |
| 그룹의 "좋은 예시" → 메일 확장 | `specs/G5` — 대표 1곳 집중 제작 → 그룹 전체로 확장(근거는 기업마다 다르게 인용) |
| 최종 사람 승인 | `specs/G5` 발송 전 승인 게이트 — 무관용 성공 기준(승인 기록 없으면 발송 안 함) |
| 기업 + 기업정보 | G1 산출물(`candidates/후보목록.md`)의 관찰 근거를 그대로 인용 |

## 3. 걸리는 지점 — "적합도" 표시 요구와 G3 이연 결정의 충돌

사용자가 요청한 최종 화면에는 **적합도(근거하에)**가 포함되는데, 적합도는 **G3**(부가·이연 기능, `docs/31` 8-1절 · `docs/33` D15 근거)이다.

- 이연 이유: G3 축 가중치가 아직 미확정(`docs/12` Q3·Q5 답 대기). 지금 점수를 매기면 근거 없는 순위가 나오고, 나중에 답이 오면 이미 처리한 순위를 재검토해야 한다.
- 다만 `기준/G3_적합도기준.md`는 이 상황을 이미 예상해 "잠정 가중치로 채점하고 산출물에 '잠정'이라고 표시한다"는 대체 규칙을 갖고 있다 — 완전히 막힌 것은 아니다.

### 선택지

| 안 | 내용 | 비용/리스크 |
|---|---|---|
| **A. 적합도 없이, G1 관찰 근거만 "왜 이 기업인가" 서술로 표시** (권고) | 숫자 순위 없이 근거만 화면에 노출 | MVP 결정과 충돌 없음. 사람이 근거를 보고 스스로 우선순위 판단 |
| **B. G3을 "잠정" 모드로 지금 활성화** | 기준 카드의 잠정 채점 규칙으로 점수를 내고 "잠정"으로 표시 | `docs/31` 8-1절 MVP 범위 결정을 뒤집는 것 — `docs/91`에 사유를 남기고, `12` Q3·Q5 답이 오면 이미 낸 순위를 재검토해야 함 |

**A를 권고**했으나 사용자 최종 확인 대기 중.

## 4. 구현 순서안 (Build Order: Spec → Skill → Subagent → Workflow)

Spec은 G2·G5 둘 다 완성돼 있으므로 다음 단계는 실행체(Skill)다. `g1-기업판정`(`.claude/skills/g1-기업판정/SKILL.md`) 패턴을 그대로 따라간다.

1. **`g2-세그먼트화` Skill 신설** — `candidates/후보목록.md`(포함 판정 전체)를 읽어 `기준/G2` 잠정 축(안 A: 품목)으로 배정 → `segments/SEG-*.md` 생성
2. **`g5-메일작성` Skill 신설** — 세그먼트 정의 + 대표 기업 근거를 받아 대표 문안 제작 → 그룹 확장(근거 인용 개별화) → 번역 검증(`기준/G7` 3절 재사용, 별도 컨텍스트) → **여기서 정지**
3. **검토용 출력 1개** — 사람이 보는 화면을 파일로 생성: 기업 / G1 근거 요약 / (안 A 채택 시) 선정 근거 서술 / 메일 초안(제목+본문) / 승인 체크란
4. 승인 표시가 된 건만 "발송 기록" 처리(실제 메일 발송 API 연동 여부는 별도 확인 필요 — 지금 저장소엔 발송 도구가 없음, `docs/50` 참조)

3·4단계를 하나로 묶을지, 3단계까지만 먼저 만들지는 별도 결정 사항.

## 5. 재개 시 먼저 확인할 것 (사용자 답변 대기 중이던 질문)

1. **적합도 처리 방식** — 위 3절 A/B 중 선택
2. **구현 범위** — (a) 검토용 출력까지(기업/근거/메일초안/승인란, 실제 발송 연동은 다음 단계로 남김) vs (b) 발송 연동까지 한번에(단, 이 저장소엔 아직 발송 도구·인프라가 없어 별도 결정 필요)

## 6. 참고 — 이 작업이 여는 문서상 영향

- `g2-세그먼트화`·`g5-메일작성` Skill이 만들어지면 `CLAUDE.md` 7절 Build Order 대조표(② Skill 열)가 갱신 대상이 된다. **단 그 표는 2026-08-03 정정됐다** — G1도 완전히 채워진 것이 아니라 **단위 J(기업 1곳 판정)만** 채워졌고 **단위 L(명단 수집)은 실행체가 없다**(수업 4일차 5번 답에서 발견, `docs/90` 3절에 공백 등록).
- B안(G3 잠정 활성화)을 택하면 `docs/31` 8-1절 MVP 범위표와 `docs/91_진행로그.md`에 결정 이유를 반영해야 한다(현재는 미반영, 이 메모에만 남아 있음).

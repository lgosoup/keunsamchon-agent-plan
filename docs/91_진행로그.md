# 91. 진행 로그 — 결정이 언제 왜 바뀌었는가

> **용도**: 원안(`99_기초아이디어_원안.md`) 대비 무엇을 언제 왜 바꿨는지 한 줄씩 남긴다. 심사에서 "여러 대안을 검토해 짧은 길을 찾은 과정을 보여달라"고 할 때 그대로 쓰는 재료이며, 나중에 "왜 이렇게 됐지"를 되짚는 유일한 기록이다.
> **규칙**: 결정·변경이 생길 때마다 **한 줄**. 근거 문서와 확인 방법을 반드시 함께 적는다. 되돌린 결정도 지우지 않고 새 줄로 남긴다.

| 날짜 | 무엇을 | 어떻게 바꿨나 | 근거 | 상태 |
|---|---|---|---|---|
| 2026-07-28 | 지원 단계 종료 | 지원서 작성·검토 자료를 `archive/`로 이관, 큰삼촌컴퍼니 과제 확정 | — | 완료 |
| 2026-07-29 | 디렉터리 구조 | `docs/`를 기획 파이프라인 순서(00 전제 / 01·02 진행·원칙 / 10번대 사실 / 20 문제 / 30번대 설계 / 40 검증 / 50 실사용 / 60 구현 / 90번대 관리)로 재편 | `CLAUDE.md` 1절 순서 원칙 | 완료 |
| 2026-07-29 | 회사 조사 (A2 1차) | 웹 조사로 회사 규모·Clo.D 구조·AI-Studio 실체·경쟁(신상마켓) 확인 → `10_기업조사.md` 신설 | 출처 12건, 문서 7절 | 완료 |
| 2026-07-29 | **Clo.D 해외 판매 전제** | "Clo.D는 동대문 사입삼촌 구조에 종속돼 해외 판매가 성립하지 않는다"고 판단 → `30` ⑤ 매칭 로직을 **폐기 대기**로 전환 (삭제 아님, 분기 보존) | `10` 2절 + 반례 2개 자체 반박 | **가설** — `12` Q1로 확정 |
| 2026-07-29 | **판별 신호 방향** | "이미지 비전문 → AI-Studio"가 실제 지불 고객(GUESS·BARNEYS·CJ ENM, 전원 전문 등급)과 반대일 수 있다고 판단 → 폐기 대기 | 적대적 검토 치명 2 | **가설** — `12` N2로 확정 |
| 2026-07-29 | 상담 자료의 지위 | (A) 카톡 메모를 "실무자 상담 = 신뢰할 만한 채택 후보이나 절대 기준·평가 근거는 아님"으로 규정, 원문을 `reference/`에 보관하고 `02`에 지위 명시 | 사용자 지시 | 완료 |
| 2026-07-29 | 원안 기준선 복원 | `99`가 `30`의 복제였던 것을 바로잡아 **자료 계보(A~D) + A 항목 26건 승계 상태 + D 본인 뼈대 복원**으로 재작성 | 적대적 검토 치명 7 | 완료 (D는 원문 미확보, 복원본) |
| 2026-07-29 | 리스크 등록 | 조사·검토로 드러난 7건(#9~#15: Clo.D 성립 여부, 신호 방향, 캐파 초과, 도메인 평판, 레퍼런스 인용, 회신 이후 공백, 규제 순서 오류)을 `90`에 등록 | 적대적 검토 | 완료 |
| 2026-07-29 | 질문지 보강 | `12`에 N1~N5 추가 (기존 고객 유입 경로 / 지불 고객의 원래 콘텐츠 지출 / 마켓플레이스·에이전시 검토 이력 / 회신 이후 처리 / 기존 도구 연동) | 적대적 검토 | 완료 |
| 2026-07-29 | 검토 결과 기각 1건 | 적대적 검토의 "원문 과제명이 틀렸다"(중대 #9)는 **기각**. 해당 에이전트는 PDF 한글 텍스트를 읽을 수 없는 환경이었고, `pdftotext`로 재확인 시 한글 글리프가 추출되지 않으며 인용된 문자열("수요자·메일링·프롬프팅·커머스")은 PDF 내 0건 | 직접 검증 | 완료 — 원문 대조는 여전히 미완(A1) |

| 2026-07-29 | 질문지 보강 2 | `12`에 N6~N9 추가 — N6 회사 자료 요청 목록 10건(우선순위·대체 방안 포함) / N7 산출물 형태 합의 + 청사진·핵심우선 구현 방식 승인 / N8 목표 소비자 연령대·세그먼트 / N9 후속 확인 채널 확보. 5절에 질문 순서와 "N7·N9는 시간 없어도 반드시" 규칙 추가 | 사용자 지시 | 완료 |
| 2026-07-29 | **국가 선정 3축 지표 교체** | `11_대안탐색_비교.md` 신설. 웹 조사로 축별 소스 실체 확인 → **축2 = 해외한류실태조사**(문체부·KOFICE, 30개 지역·27,400명, 연간) 확정, **축3 = KOTRA 국가·지역정보**(수입규제·통관물류·상거래플랫폼·시장특성) 확정, **축1은 정량 지표 확보 실패로 스크리닝으로 격하** | 각 소스 공식 페이지 직접 확인, 출처 7건 | 완료 — `30` ② 반영 |
| 2026-07-29 | **원안 "코트라 주 소스" 구조 폐기** | 조사 결과 코트라의 강점은 축1(수출입 통계)이 아니라 **축3(규제·물류·상거래 인프라)**이었다. 국가 선정의 무게중심을 **축2 주 판단 / 축3 통과 조건 / 축1 사전 스크리닝**으로 재배치 | `11` 0절·5절 | 완료 — 단 `12` Q1이 "Clo.D 포함"이면 원안 지표로 되돌릴 수 있게 분기 보존 |
| 2026-07-29 | 상담 정리의 사실 오류 정정 | "코트라에 정보 요청이 안 된다"를 근거로 코트라를 배제하려 했으나, **요청이 필요한 맞춤 조사와 요청 없이 열람 가능한 국가정보는 다른 것**임을 확인. 후자는 지금 사용 가능 | `dream.kotra.or.kr` 직접 확인 (2026-07-29) | 완료 — 회사 회원 자격 여부는 `12` Q22·N6⑨ |
| 2026-07-29 | 축2·N8 연결 발견 | 해외한류실태조사가 표본을 **성·연령별 인구 규모 고려**해 배분함을 확인 → N8(목표 소비자 연령대) 답과 결합하면 **목표 연령대 기준 국가별 호감도**로 국가 선정 가능. 전체 평균으로 고르면 목표층과 어긋난 시장을 택할 위험 | `11` 1절 | **연령대별 교차표 제공 여부는 (미확인)** — 이용자가이드북 확인 필요 |

| 2026-07-29 | **축2 주 지표 교체 — 호감도 → 패션 지출액** | 2차 조사에서 패션이 해외한류실태조사의 **정식 분야**이며 **월평균 지출액 $33.9로 소비재 1위**(뷰티 $29.7·음식 $24.9)임을 확인. 호감도(감정)보다 지출액(구매 행동)이 "K-패션 구매력이 있는가"에 직접 대응하므로 주 지표를 바꾸고 호감도를 보조로 내림 | `11` 1절, ZDNet·국회도서관 국가전략정보포털 | 완료 — `30` ② 반영 |
| 2026-07-29 | **축3 수동 열람 → API 연동** | 공공데이터포털 **KOTRA 국가정보 OpenAPI 무료 공개** 확인 (제공 항목 8종, 수정일 2026-03-30·파일데이터 2026-07-06, 개발계정 1만 트래픽, 활용신청 692건, 국가 목록 API 별도). KOTRA 규모는 **90개국·127개 무역관**, 국가정보는 연 1회 하반기 일괄 아카이브 | `11` 2절, data.go.kr | 완료 — **④·⑦ 자동화가 선언에서 구현 경로로** |
| 2026-07-29 | 축1 대체 소스 2건 판정 | **ITC Trade Map**(220개국·5,300품목) 발견 → **조건부**: 개발도상국은 전체 무료지만 **선진국 사용자는 제한적 무료**여서 한국 계정 등급 미확인. **UNCTAD B2C E-commerce Index 탈락** — 확인 가능 최신판이 **2020년**으로 6년 낡음 | `11` 4절 | 축1은 스크리닝 유지 (정량화 미해결) |
| 2026-07-29 | 잔여 확인 4건 처리 | ①패션 수치 **해결**(지출액 확보, 인기도·경험률은 미공개) ②연령대 **부분 해결**(연령별 분석 존재 확인, 분야×연령 교차표는 미확인) ③KOTRA 규모·갱신 **해결** ④패션 이커머스 무료 소스 **미해결** | `11` 6절 | 3.5/4 해결 |
| 2026-07-29 | `/수업` 스킬 신설 | 수업(하네스 엔지니어링) 문제를 **이 저장소의 내용만을 재료로** 답하고 `docs/수업/<일차>.md`에 누적하는 스킬 추가. 답변이 저장소 변경을 함의하면 **승인 후** `30`·`90`·`12`·`CLAUDE.md`에 반영하고 이 로그에 남기는 절차를 스킬 4단계에 고정 | 사용자 지시 | 완료 |
| 2026-07-29 | **신호를 관찰층/규칙층으로 가름** | 수업 1일차 2번 답변. `12` N2가 뒤집는 것은 "비전문 → AI-Studio"라는 **판정 규칙**(`30` ⑤)이지 "통일된 스튜디오 배경·조명·구도가 있는가"라는 **관찰**(`30` ④)이 아님을 확인. 두 층을 나누면 미팅 답이 어느 쪽으로 나와도 관찰층은 살아남는다 | 수업 1일차 2번 + 반례 2건 자체 반박 | 완료 — 층 배치 자체는 `40` V1 결과로 확정 |
| 2026-07-29 | **`40_검증계획.md` 신설 — 공백표 판단 부분 철회** | 기존에 `40`은 "검증 대상이 폐기 대기라 지금 세우면 다시 만들어야 한다"로 미뤄져 있었다. 위 층 분리로 **관찰층은 폐기 대기가 아님**이 확인돼 V1(신호 판정 일치 검증)을 지금 실행 항목으로 승격. V1을 사람↔사람(V1-a, 기준 문장이 모호한가)과 사람↔LLM(V1-b, 하네스가 기준을 따르는가)으로 분리 | 수업 1일차 2·3번, `02` 7절 심사 요구 | `01` C4·`90` 리스크 16으로 등록, 실행 대기 |
| 2026-07-29 | 리스크 16 등록 | **판별 신호의 객관성 미검증** — `30` ④의 "누가 봐도 같은 결론"이 선언으로만 존재하고 확인 기록이 없음. 리스크 한 줄로 두지 않고 `40` V1 실행으로 처리하기로 결정 | 수업 1일차 2번 | 등록 완료 |
| 2026-07-29 | `30` ④에 **기업 1곳당 산출 스키마** 추가 | 값과 근거를 같은 행에 붙이고 관찰층/규칙층을 시각적으로 가른 스키마를 고정. **`31_구체화_기준표.md`는 만들지 않았다** — 목록 규모 상·하한(`12` Q9·Q10)이 미확인이라 지금 만들면 `01` C2 게이트("값 없는 표가 하나도 없는가")를 위반한다 | 수업 1일차 3번 | 완료 |
| 2026-07-29 | 수업 답변의 독자 규정 | 수업 제출물은 **이 저장소를 모르는 사람이 읽는다**고 규정. `docs/수업/01일차.md`에서 파일 경로·문서 번호(`30` ④, `12` Q1 등)를 전부 서술로 풀고, 맨 앞에 0절 배경(과제 원문·3대 요구·회사가 파는 것·신호 4종)을 추가. 경로 표기는 맨 아래 `(내부 기록)` 절에만 허용. `/수업` 스킬에 절대 규칙 5로 고정해 다음 일차부터 자동 적용 | 사용자 지시 | 완료 |
| 2026-07-29 | `CLAUDE.md` 4절 보완 | 디렉터리 표에 `docs/수업/`(기준 문서 아님을 명시) 추가. 함께 **누락돼 있던 `docs/11_대안탐색_비교.md` 행도 추가** — `30` ②가 이 문서를 근거로 인용하는데 구조표에는 없었다 | 구조표와 실제 파일 불일치 | 완료 |

| 2026-07-30 | **수업 개념 정리본 신설** | 수업 1·2강 미션 답안지 4건(기본·심화)에서 **일반론만** 추려 `docs/수업/00_개념정리.md` 신설 — LLM 전제, 모델 vs 입력, 활용 방식 3분류, 붕괴 지점 3, 하네스 6요소, Build Order, 요소별 세부 규칙(Spec 6요소·Skill·Subagent 4경계·Workflow 2패턴·Hook·Checklist), 검수자 3원칙. **강의 표·문장을 옮기지 않고 개념만 우리 말로 재서술**했고, 원본 PDF는 저작권상 커밋하지 않기로 `CLAUDE.md` 6절에 명시 | 사용자 제공 PDF 4건 (PyMuPDF 추출) | 완료 |
| 2026-07-30 | **하네스가 둘임을 명시 (A/B 구분)** | `CLAUDE.md` **7절 신설.** A=기획 하네스(이 저장소를 돌리는 장치) / B=제품 하네스(과제 산출물)를 갈라 정의하고 **Build Order 6단계 × A·B 대조표**를 넣었다. `수업` 스킬의 개념 매핑표도 A·B 2열로 재작성 — 기존 표는 둘을 한 칸에 섞어 놓아 개념 하나가 양쪽에 다르게 착지하는 것을 놓쳤다 | 수업 1·2강 6요소·Build Order 개념 대조 | 완료 |
| 2026-07-30 | **B가 Build Order 역순으로 만들어져 있음을 발견** | 대조 결과 B는 ④ Workflow(`30` ⑦ 10단계)와 ⑤ Hook(90일·7영업일·14일·60일 타이머)은 설계됐는데 **① Spec의 성공 기준 · ② Skill · ③ Subagent · ⑥ Checklist가 비어 있다.** *"실행 순서는 그려져 있는데 언제 완료인지 아무도 모르는"* 상태이며 붕괴 지점 1(막연한 요청)·3(검증 없는 실행)을 동시에 안고 있다. **앞에서부터 메우기로 결정**하고 ④⑤ 정교화는 E단계로 미뤘다 | 같은 대조 | 완료 — `CLAUDE.md` 7절·`01` C5 |
| 2026-07-30 | **`32_하네스_스펙.md` 신설 — 성공 기준을 처음 만들었다** | 판정 단위(기업 1곳)의 **Spec 6요소 + Subagent 4경계 + 하네스가 풀지 않는 것**을 고정. 최대 공백이었던 **성공 기준**을 **미확인 값에 걸리지 않는 7건(지금 확정)과 걸리는 3건(Q9·Q10·발송 데이터 대기)으로 갈라** 절반을 지금 확정했다 — 전체를 비워 두면 Build Order ①이 영구 미완이고, 숫자를 지어내면 `01` C2 게이트 위반이다. **도구 권한 경계는 이번에 처음 정의**(발송 권한 없음 = 브리프 "반자동"을 물리적으로 위반 불가하게) | 수업 2강 Spec 6요소·Subagent 4경계, `02` 9절 원칙 ①③ | 완료 |
| 2026-07-30 | **`02`에 9절 신설 — 하네스 설계 원칙 4개** | ① 성공 기준을 먼저, 예/아니오로 ② 하네스화 대상은 반복량·단위 격리·뒤집힘 내구성 셋으로 고른다 ③ 뒤집힐 것과 안 뒤집힐 것을 갈라 저장한다 ④ 앞 단계 없이 뒤 단계를 만들지 않는다. **③은 이미 저장소 세 곳(`docs` 번호 체계 / 관찰층·규칙층 / `32` 2절)에서 쓰이고 있었으나 원칙으로 명문화된 적이 없었다.** `02`는 상담 출처 문서이므로 **9절만 출처가 다름을 머리말과 절 앞에 표기** | 수업 1·2강 + 우리 작업 도출 (③은 우리 고유) | 완료 |
| 2026-07-30 | **판정 실패 경로 부재 발견 → 리스크 17** | 활용 방식 3분류의 "오류 발생 시" 축으로 대조하니, `30` ④ 제외 3종은 전부 **"봤는데 부적합"**이고 **"못 봤다"**(접속 불가·렌더링 불가·로그인 요구·상품 수 셀 수 없음)가 정의돼 있지 않았다. 섞이면 90일 재수집 대상 판별이 불가하고 **V1 불일치 원인 분석이 오염된다**(접근 조건 문제가 기준 문장 문제로 집계). **판정 불가를 별도 상태로 승격** — `30` ④ 본문·산출 스키마, `32` 2절 (a) 4번, `40` 기록 양식 전용 행 | 같은 대조 | 완료 — V1 실행 전 반영 |
| 2026-07-30 | **미결정 #8 등록 — B는 Workflow인가 Agent인가** | `30` ⑦의 10단계는 고정 순서(Workflow)인데 **같은 절 끝 문단이 "전역 LLM 에이전트와 대화로 조정"(Agent)**이었다. 둘은 실패 시 동작이 정반대(중단 vs 재시도)이므로 선언 없이는 실패 경로가 설계되지 않는다. **권고: Workflow 기본 + 대화형 조정은 `(부가)` 분리** | 같은 대조 | 미정 — `30` ⑦에 경고 삽입 |
| 2026-07-30 | **리스크 18 등록 — A에 강제 실행 장치가 없다** | `CLAUDE.md` 5절의 "게이트 전 `project-critic` 검토"는 **스스로 판단해 건너뛸 수 있는 형태**이고 지켰다는 기록도 남지 않는다(Hook이 Skill로 대체되지 않는 첫 번째 이유에 정확히 해당). **권고는 `01` 게이트 표에 검토 실시 여부를 기록 항목으로 넣는 것** — Hook 설치는 자동 실행 동작을 심는 것이라 사용자 확인 사항으로 남긴다 | 같은 대조 | 등록 완료, 대응 미정 |
| 2026-07-30 | **`40`에 V6 추가 + V1 성공 기준 명시** | V6(신호 4종 **자체**의 타당성 — 국내 고객사 업종·규모 분포로 역산)을 대기 항목에 추가. **미팅 답이 아니라 자료를 기다린다**는 점에서 V2~V5와 성격이 다르다. 함께 V1의 "합격선을 두지 않는다"가 **성공 기준이 없다는 뜻이 아님**을 명시 — "불일치 항목이 0건인가"로 이미 예/아니오 판정되며 비율 기준보다 엄격하다 | 수업 1일차 3번 미반영분 + `02` 9절 원칙 ① | 완료 |
| 2026-07-30 | **`01`에 C5 추가 + E단계 입력 기록** | C5(하네스 Spec 확정)를 추가하고 **C5 → C4 V1-a → Skill 초안 → V1-b용 실행체 → V1-b → Checklist** 순서를 고정. E단계에는 **반복 업무 후보 3건·반복량 순서(2>3>1)·후보 2 우선 근거·뒤집을 유일한 질문(N7)**을 입력 재료로 기록했다 — **구현 범위를 확정한 것이 아니다**(`CLAUDE.md` 1절) | 수업 1일차 1·2번, Build Order | 완료 |
| 2026-07-30 | **"먼저 찾는다" 규칙이 한 번도 발화되지 않았음을 발견 → 실행 가능하게 고침** | `CLAUDE.md` 0절 "먼저 찾는다. 내장 Skill·**플러그인**·공개된 Skill을 확인하고"는 **선언만 있고 경로·시점·방법이 없었다.** 실제 결과: 공식 마켓플레이스가 **2026-07-27**부터 로컬에 클론돼 있었고 그 안에 `skill-creator`(스킬 작성 + eval + description 트리거 최적화)가 있었는데, **2026-07-29에 `수업` 스킬을 맨손으로 작성**했다. 규칙이 있어도 안 걸린 것이다. 세 가지를 명시해 고쳤다 — ① **어디**(`ls ~/.claude/plugins/marketplaces/*/plugins/`, 39개) ② **언제**("있으면 좋겠다"는 생각이 든 시점 = 만들기로 결심하기 **전**) ③ **어떻게**(`/plugin install`은 CLI라 **에이전트가 실행 불가 → 사용자에게 명령 한 줄 제시**, 에이전트 단독 가능한 것은 `~/.claude/skills/`로 디렉터리 복사) | 사용자 질의로 마켓플레이스 실물 확인, `known_marketplaces.json` 타임스탬프 | 완료 — 설치 자체는 미실시 |
| 2026-07-30 | 겹치는 플러그인 4건 식별 | 39개 중 **`skill-creator`**(하네스 B의 ② Skill 공백 = `40` V1-a 직후 만들 판정 절차 카드), **`hookify`**(하네스 A의 ⑤ Hook 공백 = 리스크 18), `plugin-dev`(참조 문서 6종), `claude-md-management`. **`enabledPlugins` 설정 키로 에이전트가 활성화할 수 있는지는 (미확인)** — 로컬 플러그인 문서에 해당 키 설명이 없어 추측으로 `settings.json`을 건드리지 않았다. `skill-creator` 내부 `agents/` 3종이 복사 시 서브에이전트로 등록되는지도 **(미확인)** | 마켓플레이스 디렉터리 직접 확인 | 도입 여부 미정 |
| 2026-07-30 | **`13`·`13S` 신설 — 기업 설명회 질답 확보** | 설명회 녹음 전사 + 현장 필기를 정리해 `13_질답_정리.md`(정본)·`13S_질답_구조화색인.md`(LLM 판독용 ID 색인)를 신설. **출처를 A(공개 설명회 발언) / B(공개 세션 필기) / C(사용자 별도 1:1 질의) 세 등급으로 갈랐다** — C는 공개 세션에 없던 유일한 정보원이지만 축어가 아니라 취지로만 인용한다. `12` Q8(막힌 사례)이 4건 확보돼 문제 정의가 처음 추론에서 증거로 바뀌었고, Q6(반자동)은 기업이 못 골라 **우리에게 위임**됐다 | 전사 00:03~19:46 전 구간 + 필기 19항목 대조 | 완료 — `12`의 (미확인)은 지우지 않음 |
| 2026-07-30 | **근거 우선순위 신설** | `CLAUDE.md` 4절에 **1 기업 직접 발언(`00`+`13`·`13S`) / 2 공개 조사 사실(`10`·`11`) / 3 실무자·강사 상담(`02`+상담 원문) / 4 수업 개념** 4단계를 명문화. "낮은 쪽을 근거로 높은 쪽을 뒤집지 않는다"를 규칙으로 고정하고 `02`·상담 원문 행에 "3순위" 표기 | 사용자 지시 | 완료 |
| 2026-07-30 | 디렉터리 정리 | 루트에 흩어진 수업 미션 답안지 PDF 4건을 `수업자료/`로 이동(gitignore 유지 확인), `archive/02_구에이전트/계획서검토/` 한 계층 제거. **`docs/`는 하위 폴더를 두지 않기로 확정** — 번호 접두어가 그룹 역할을 하고 폴더를 더하면 경로 참조 51곳을 고쳐야 한다 | 사용자 지시 + `git check-ignore` 확인 | 완료 |
| 2026-07-30 | **`31_기능구조.md` 신설 — 폭포수 2단계** | 사용자가 작성한 해외 기업 접촉 플로우를 `reference/사용자_플로우_원문_2026-07-30.md`(원문, 수정 금지)로 보존하고, 이를 **크게 잡은 기능 단위 + 입출력**으로 분해. **사람/AI 배정·임계값·구현 방식은 전부 뒤 단계로 미뤘다**(한 기능 안에서도 갈릴 수 있으므로 지금 배정하면 판단을 미리 굳힌다). 기준은 *"완결 산출물 하나 = 기능 하나"* — 순서는 경계가 아니다 | 원문 + `13S` K1~K9 제약 | 완료 |
| 2026-07-30 | **`30` ⑤ 매칭 로직 폐기 확정 → G2+G5로 대체** | `12` Q1의 답이 사실상 왔고(AI-Studio 1차, `13S` K8) **되살아나지 않는 쪽**이었다. 제안 서비스가 하나로 좁혀지면 "둘 중 무엇을 제안할지" 판별이 성립하지 않는다. 문제는 그 자리가 **브리프 요구 ②("고객 특성에 맞춘")의 유일한 담당**이었다는 것 — **세그먼트 그룹화(G2) + 그룹별 문안(G5)**이 대체하며, 맞춤 단위가 개별 기업에서 세그먼트로 올라갔다 | `13` 5절 Q1, `31` 8절 | 완료 |
| 2026-07-30 | `function-decomposer` 서브에이전트 신설 | 확정된 플로우를 큰 기능 단위 + 입출력으로 쪼개는 에이전트. **사람/AI 배정을 금지 항목으로 명시**(가장 자주 새어 드는 실수)하고, 출력 요건으로만 쓰게 했다. 각 기능이 뒤에서 독립된 Spec·Build Order 대상이 된다는 전제를 심어 경계 판정 기준을 *"한 기능의 상세 설계 시 다른 기능을 다시 열지 않아도 되는가"*로 고정 | 사용자 지시 | 완료 — 미실행 |
| 2026-07-30 | **강사 의견 대조 → `31`에 4건 반영 (G10 신설 포함)** | 상담 원문·`02` 기준으로 `31`을 평가해 대립 항목(국가 선정 주체·맞춤 단위·반응 처리 비중)은 기업 쪽으로 두고, **첨언 4건만 반영**. ① **G10 재수집 판단 신설** — 플로우가 단방향이라 한 번 걸러진 기업이 영구히 사장되는 구조였다(강사가 두 번 지적한 "트렌드 변화 시 후보군 자동 갱신"). ② G1 탐색 소스를 후보 3개로 채움(Qoo10·라쿠텐이 1순위 — 기업이 실제로 써봤다, `13S` F14) ③ 4-2절 구조 대안 비교 6축 신설 ④ G1 출력을 관찰층/규칙층으로 갈라 복원. **함께 고아 출력 1건 발견** — G9에서 사람이 찾아낸 연락처가 G4로 돌아오는 경로가 없었다 | 상담 원문 + `02` 1~9절 대조 | 완료 |
| 2026-07-30 | **`project-critic` 전체 검토 → 치명 6·중대 12건 지적** | 검토 결과의 뿌리는 하나였다 — **2026-07-30 변경이 `31` 안에서만 선언되고 그것을 받아야 할 문서 5개(`00`·`CLAUDE.md`·`30`·`40`·`90`)가 하나도 갱신되지 않았다.** `13` 9절에 "반영할 곳" 표를 만들어 두고 정작 반영하지 않은 것이 원인이다. 아래 4줄이 그 처리 | `project-critic` (docs 전량 + 웹 확인) | 완료 |
| 2026-07-30 | **🔴 일본 특정전자메일법 요건 발견 → 리스크 19·20·21 등록** | **일본은 광고 메일 옵트인 원칙 국가**이고, 웹 공개 주소 예외에도 **"거부 표시가 있으면 예외 불성립"** 조건이 붙는다. 국가가 확정됐는데 규제 확인이 실행되지 않아 **`90` 리스크 15가 실제로 발생한 형태**였다. 반영: `31` G4에 **[발송 금지] 상태 신설**(제외·판정 불가·미확보와 다른 네 번째 상태, 재수집 대상에서도 제외) / G1 관찰층에 "광고 메일 거부 표시" 항목 / G5에 **법정 표시 의무 4종을 1급 제약**으로 / **발송 상한 자리**를 값 없이 구조로 명시(리스크 11·12를 권고→1급 제약 승격) / G7 뒤가 **경계임을 명시**(빠뜨린 것과 경계인 것은 다르다). 법 해석은 `12` **N17**로 기업 확인 | 総務省·迷惑メール相談センター 가이드라인 직접 확인 | 완료 — N17 답 대기 |
| 2026-07-30 | **폐기 판정을 5개 문서에 전파** | `00` 2절 브리프 대조표의 요구 ② 대응을 `30` ⑤ → **`31` G2+G5**로 교체하고 4절 해석 문단도 재작성 / `CLAUDE.md` 2절 ①과 문서 표 갱신(**`31`을 기획 본체로, `30`은 "인용 전 `31` 9절 확인 필수"**) / `30` 머리말에 **9개 항목 판정표**를 박고 폐기 절 제목에 `(폐기)` 접두어 / `90` 미결정 #1~#5를 **확정으로 이관**하고 리스크 5 폐기 / `31` 9절에 누락됐던 **`30` 2절·⑥ 발신 언어** 2행 추가 | `project-critic` 치명 1·3·5 | 완료 |
| 2026-07-30 | **`40` V1의 판정 대상 교체 — 폐기된 기준을 검증하려던 상태** | V1이 `30` ④ 신호 4종(이미지 전문성·판매채널·SKU·해외 대응)을 대상으로 삼고 있었으나 그 4종은 `31` 9절에서 교체·근거 상실 판정을 받았다. **"지금 할 수 있는 유일한 검증"이 이미 버린 기준을 검증하는 상태**였고, 그대로 돌리면 검증 기록이 감점 재료가 된다. `40` 기록 양식·`32` 0·1절·`01` C4를 **`31` G1 관찰층**으로 교체. 함께 **표본을 일본으로 고정**(검증 표본과 실제 대상이 일치해 이전보다 조건이 좋다)하고, **갈릴 것이 예상되는 세 항목**(규모 신호·한국 진출 신호·거부 표시)을 검증의 실제 목적으로 명시 | `project-critic` 치명 4 | 완료 |
| 2026-07-30 | **`31` 자체 결함 6건 수정 — 기업 제약 누락과 고아 검사 오판** | ① **기업이 준 제약 K3·K4가 타겟 조건에서 빠져 있었다** — 업종(패션·뷰티·푸드)과 **"한국 진출 희망"**을 복원하고 각 조건에 K 번호를 출처로 붙였다. 빠지면 모집단이 "일본 커머스 전체"가 된다 ② 커머스의 지위가 세 곳에서 달랐던 것을 **필수로 통일** ③ **G1에 기업 규모 신호를 추가** — G2가 규모로 묶는다면서 관찰 항목에 규모가 없어 G2·G3의 Spec을 쓰는 순간 G1을 다시 열어야 했다(경계 시험 위반) ④ 고아 검사가 전 항목 "없음"으로 통과했으나 재검증에서 **3건 발견**(G4→G8 연결 누락 / G3 소비처 3곳↔4곳 불일치 / 미정인 G2→G3가 확정 행으로 기재) ⑤ **출처 표기 오류** — "가깝다·K문화 적합도"를 *기업이 준 근거*로 적었으나 실제로는 사용자 판단(근거 4순위)이었다. 기업 발언은 협력사 존재뿐 ⑥ **원문의 "자동으로 보낼거지"를 승인 지점으로 바꾼 것이 기록되지 않았다** — `31` 5절에 추가(브리프 요구 ③이 이 한 줄에 걸려 있다) | `project-critic` 치명 6, 중대 2·3·5·6 | 완료 |
| 2026-07-30 | 부수 정리 6건 | `13S` U13의 C 5건 목록이 정본과 어긋난 것 수정(F6은 B인데 C로 분류돼 있었다 — 어느 게 C인지가 갈리면 "C 축어 인용 금지"를 집행할 수 없다) / `13S` 8절의 "R1~R6 전부 미반영" 표기를 실제 상태로 갱신 / `12`에 **답이 온 항목 대조표**와 **N10~N20 11건** 추가 / `01` C1을 잠재고객 정의 확정으로 완료 처리 / `99`에 **D-1 ②·D-4의 현재 상태** 기록(`CLAUDE.md` 5절 원안 추적 규칙) / `02` 요약 체크리스트의 "코트라 근거로 국가 확정" 항목을 실제 상태로 교체 | `project-critic` 중대 7·11, 경미 다수 | 완료 |
| 2026-07-30 | **`02` 1절(수동 우선 원칙) 폐기** | 사유 둘. ① **원칙의 목적이 이미 충족됐다** — "수동으로 해봐야 한다"는 이유가 *일반화 재료 확보*였고 그 재료는 기업이 이미 만들었다(`13S` F10 일본 직접 방문·F12 SNS 계정 정지·F14 리스트 추출 후 검증 실패). 우리가 밟을 필요가 없어진 게 아니라 **기업이 이미 밟았다.** ② **전제 하나가 사실과 달라졌다** — "코트라에 정보 요청 불가"는 OpenAPI 무료 공개 확인으로 깨졌다. **절 번호는 남기고 내용만 비웠다**(8·9절을 다른 문서 4곳이 번호로 참조). 함께 `01` D단계의 "수동 1회 실행 시나리오 설계" 항목 삭제, `99` 승계표 20·21·22를 폐기로 표시, `31` 9절에 `30` ⑦ 승계 안 함 기록 | 사용자 판단 + `11` 코트라 조사 | 완료 |
| 2026-07-30 | **`specs/` 디렉터리 신설 — 기능별 Spec은 자체 완결로 쓴다** | 폭포수 3단계(기능별 Spec)를 `docs/`가 아니라 **별도 디렉터리**로 분리. **`docs/`와 규칙이 정반대다** — `docs/`는 이중 관리를 피해 참조로 잇지만, Spec은 **Build Order를 돌리는 사람·에이전트가 저장소를 보지 못하는 상태에서 일하므로** 배경·기업 제약·실패 사례·용어를 본문에 인라인하고 문서 번호는 출처 표기로만 쓴다. 중복 비용보다 "배경을 모른 채 절차만 실행하는" 비용이 크다는 판단. 원 저장소 대조는 각 Spec 부록 B로 격리. **`G1_대상기업발굴.md`(확정 10/대기 5)와 `G7_회신해석정리.md`(확정 12/대기 4)를 먼저 작성** — `31` 3-3절 입출력으로 검증한 결과 **앞 기능에도 짝에도 의존하지 않는 유일한 조합**이라 병렬 진행이 성립한다 | 사용자 + 팀 작성 | 완료 |
| 2026-07-30 | `docs/32_하네스_스펙.md` **승계 처리** | `32`가 G1 Spec의 선행 초안이었고 `specs/G1_대상기업발굴.md`가 자체 완결로 확장된 정본이 됐다. **`32`는 이력으로 남긴다**(`01` C5·`40`·`90`·`91`이 번호로 참조 중) — 삭제하면 참조가 끊기고, 그대로 두면 두 개의 G1 Spec이 생긴다. 머리말에 승계 표시와 *"여기서 새로 정하지 않는다"*를 박았다. 함께 내가 만든 중복 문서 `docs/33_스펙_G7_회신처리.md`를 삭제 — `specs/G7`이 이미 같은 내용을 더 엄격하게 담고 있었다 | 구조 정합 | 완료 |
| 2026-07-30 | **`31` 6절 #1(번역 검증 흔들릴 지점) 해소** | `specs/G7_회신해석정리.md`가 번역 검증을 **절 분리 수준을 넘어 별도 실행 단위(V)로 만들고 Skill 카드도 2장으로 갈랐다**(해석 T / 검증 V). 확정 — 역번역 대조, **해석과 다른 컨텍스트에서 수행**(자기 오역을 자기가 통과시키지 못하게), 불일치 1건이면 재번역·같은 유형 2번이면 절차 수정, **비율 합격선 없음**. G5의 발신 번역도 이 단위를 그대로 쓴다 | `specs/G7` 1·3-4·5절 | 완료 |
| 2026-07-30 | **G11(반응 집계·기준 조정) 복원 — 기능 하나가 조용히 사라져 있었다** | `30` ⑦의 **개선 루프**(신호 조합별 회신율 집계 → 우선순위 조정)가 `31` 초안에서 승계되지 않았고 9절 무효화 목록에도 없어 **폐기 판정도 승계 기록도 없이 사라진 상태**였다. 그런데 `32` 성공 기준 (b) 10번이 정확히 그 집계를 입력으로 기다리고 있었다 — **기준은 있는데 그 입력을 만드는 기능이 없었다.** 문제의 크기는 `31` 4-2절이 **맞춤 단위를 세그먼트로 고른 것** 때문에 커진다 — 세그먼트 축이 맞았는지 확인하는 **유일한 경로가 그룹별 반응 집계**이므로, G11이 없으면 브리프 요구 ②의 근거가 검증되지 않은 채로 남는다. 기업이 말한 "학습"(`13S` F30)에 대응하는 것도 이 기능이다. **출력을 "조정 제안"으로 못 박았다** — 판별 기준이 사람 모르게 바뀌면 "왜 이 기업인가"를 설명할 수 없고 반자동 원칙도 깨진다. **Spec은 지금 쓸 수 없다**(발송 데이터 없음). 함께 발견 — **G5 출력 규격의 상한은 G7이 아니라 G11이 정한다**(G7은 최소만 요구하고 없어도 성립하지만 G11은 세그먼트·인용 근거까지 필요) | 강사 의견 대조 후속 점검 | 복원 완료 — Spec 대기 |
| 2026-07-30 | **G11 간선이 한쪽에만 기록돼 있던 것 수정 — 기록 위치가 잘못되면 기록이 없는 것과 같다** | G11을 등록할 때 **G11 블록과 3-3절 연결 목록에만** 적고 **정작 데이터를 남겨야 하는 G2·G3·G5·G6·G7·G8 쪽 소비처에는 넣지 않았다.** G5 Spec을 쓰는 사람은 G5 블록을 열 텐데 거기엔 *"소비처 = G6(G7·G8 참조)"*로만 되어 있어 **G11이 보이지 않는다.** 세 가지로 고쳤다 — ① 여섯 블록의 소비처에 G11 추가 ② **G5 출력에 "발송 기록 항목표"를 신설해 지금 확정**(세그먼트 / 인용한 근거 / 제안 내용 / 언어·번역 검증 결과). **다른 세부는 Spec으로 미뤄도 되지만 이것은 안 된다 — 기록하지 않은 데이터는 소급 생성이 불가능**하므로 발송이 시작된 뒤에는 되돌릴 수 없다 ③ **`specs/README.md` Spec 작성 체크리스트에 6번 항목 신설** — "출력을 받는 곳을 전부 확인하고 그들이 요구하는 항목을 남긴다". 이게 실질적 안전망이다(Spec을 쓰기 시작할 때 걸린다) | 사용자 질문("G5 만들 때 잊지 않도록 기록돼 있는가")으로 발견 | 완료 |
| 2026-07-30 | **Spec이 기능 구조를 고친 첫 사례 — `31` 3-3절에 간선 2개 추가** | `specs/G7_회신해석정리.md`가 상위 문서 수정 2건을 요구했고 타당해 반영했다. ① **G7 수신 거부 의사 → G4·G5 발송 금지 목록** — **법 요건이다.** 상대가 거부 의사를 밝혔는데 발송 대상에 반영되지 않으면 다음 발송이 위법이 된다(리스크 19와 같은 계열). 분류 결과와 무관하게 병렬·즉시 처리 ② **G7 자동응답 판정 → G6 판정 정정** — 부재중·자동회신을 회신으로 세면 회신율이 부풀려지고 G8 원인 분석이 오염된다. **결과적으로 G7은 순수 종점이 아니게 됐다.** 폭포수라도 뒤 단계가 앞 단계의 결함을 찾으면 앞을 고친다 | `specs/G7` 0-7절 | 완료 |
| 2026-07-30 | G7 이음새 처리 방식 확인 | G7의 유일한 앞 기능 이음새인 **G5 발신 문안**을, "G5 출력 규격을 미리 요구한다"가 아니라 **"없어도 작업이 성립하게 닫는다"**로 처리했음을 확인. 형식은 최소(제목+본문+발송일시+수신 주소)만 요구하고, 없으면 대조 항목만 *참조 문안 없음*으로 남긴다. **병렬 작업에는 이 쪽이 맞다** — G5 완성을 기다리면 병렬이 성립하지 않는다 | `specs/G7` 0-6절 | 확인 |
| 2026-07-30 | 수동 우선 폐기 사유 **보강** | 검토에서 사유 ①이 부분적으로만 성립함이 지적됐다 — **기업이 밟은 것은 "수집" 단계까지**이고(F14는 *"검증 안 함"*), **판정 단계(세그먼트 축·적합도 척도·문안)의 일반화 재료는 기업도 갖고 있지 않다**(`13S` H2가 지적하는 지점). 폐기 자체는 유효하나 대체 경로를 명시해야 성립하므로, `02` 1절에 **"그 재료는 `40` V1-a의 사람 판정 기록으로 대체한다"**를 추가했다. 없으면 "재료 없이 자동화한다"로 읽힌다 | `project-critic` 중대 12 | 완료 |
| 2026-07-30 | **디렉터리 설명을 각 디렉터리 `README.md`로 분산** — `reference/README.md`·`.claude/README.md` 신설 | 어디에 쓸지 셋 중에서 골랐다: (a) `CLAUDE.md` 4절 확장 (b) 중앙 구조 문서 1건 신설 (c) 디렉터리별 README. **(c)로 확정** — ① `CLAUDE.md`는 매 세션 전량 로드되므로 "그 디렉터리를 열 때만 필요한" 조회성 정보를 넣으면 비용만 늘고 안 읽힌다 ② `archive/README.md`가 이미 그 패턴이라 새 규칙이 아니다 ③ (b)는 `CLAUDE.md` 4절·`archive/README.md`와 같은 내용이 세 곳에 생겨 **`docs/`에 폴더를 두지 않는 것과 같은 이중 관리**가 된다. `docs/`만 예외로 4절 표를 유지 — 어느 문서를 열지는 매 세션 필요한 라우팅 정보다. **함께 `CLAUDE.md` 사실오류 2건 수정**: 4절 `.claude/` 줄의 "agents/ 4종"(실제 5종, 열거를 지워 이중 관리 제거), 7절 ③ Subagent에 `function-decomposer` 누락 | 사용자 판단 + `git ls-files`·`git check-ignore` 확인 | 완료 |
| 2026-07-30 | `CLAUDE.md` 4절 표에 **`31_기능구조.md`·`사용자_플로우_원문` 행 추가** | 둘 다 그날 신설됐는데 라우팅 표에 등재되지 않아, **매 세션 로드되는 문서 지도에서 진행 중인 폭포수 2단계 산출물이 빠져 있었다.** 표에 없는 문서는 다음 세션에서 열리지 않는다 | `ls docs/` 대조 | 완료 |
| 2026-07-30 | **`01` E단계 후보 분할 폐기·교체 — 순차 단계를 선택지로 착각하고 있었다** | 수업 1일차 피드백(*"세 후보가 순차적 단계에 가깝고 차별성이 없다"*)이 맞는 지적임을 확인하고 `01` E절을 재작성. **폐기 사유**: 후보 1·2·3을 **브리프 3대 요구로 잘랐는데 3대 요구 자체가 한 접촉 과정의 세 조각**이라, 그 기준으로 자르면 순차 단계가 나올 수밖에 없었다. 하나를 고르면 나머지가 안 돌아가므로 **"무엇을 구현할지"를 가르지 못한다** — 실제로 E절에 후보 2 선정 근거로 적혀 있던 *"후보 3은 후보 2의 출력을 입력으로 받는다"*는 **선택의 근거가 아니라 순서의 근거**였다. **교체 내용**: ① **E-1 판정 기준 3종 신설**(단독 해소 / 대체 관계 / 비용·실패 방식 상이) — 후보 목록만 갈아 끼우면 같은 실수가 반복되므로 기준을 먼저 고정 ② **E-2 새 후보 A·B·C** — 출발점을 3대 요구가 아니라 *"리스트는 수백 개 있는데 어디에 접촉할지 못 정한다"*(`13S` F14)는 실제 병목으로 바꿔, 같은 병목을 서로 다르게 푸는 셋(A=G1 뒤절반 / B=G2 / C=G4)으로 재구성. 셋 다 가·나 통과, 다에서 갈린다 ③ **E-3 선택은 A 유지, 근거 전면 교체** — 순서가 아니라 *"셋 중 유일하게 기업이 못 했다고 말한 일 그 자체"*·*"재현 가능한 오답이라 나중일수록 손해"*·단위 격리. 반례 2건(브리프 요구 ② 공백 / 비용 최고) 처리 포함 ④ **E-4 흔드는 항목을 N7 하나에서 N7·N10·N19·Q9·Q10·N11로 확장.** 함께 낡은 표기 2건 정리 — *"**국가 내** 잠재고객 발굴"*(국가는 일본 확정, K1), *"90일 후 재수집"*(`31` 9절에서 수치 근거 상실) | 수업 1일차 피드백 + `docs/수업/01일차_피드백_반영.md` | 완료 |
| 2026-07-30 | 질문지 추가 제안 **기각** | 위 재작성이 미확인 3건(제작 캐파·최소 수주액 / 먼저 보고 싶은 산출물 / 수백 개 중 몇 곳 열어봤나)을 `12`에 추가하자고 제안했으나, 대조 결과 **전부 이미 있었다** — Q9·Q10·N11 / N7 / **N10**. 특히 N10은 `13S` U11에서 올라와 이미 ★ 표시로 등재돼 있었다. 추가하지 않음 | `12` 전량 대조 | 기각 |
| 2026-07-30 | **`40` 2절 대기 항목 갱신 — V1만 고치고 V2~V6을 두었던 것을 정리** | 같은 날 V1의 판정 대상을 `31` G1 관찰층으로 교체하면서 **V2~V6은 손대지 않아 폐기된 것을 겨냥한 채 남아 있었다.** 세 건이 죽어 있었다 — **V2**(신호 조합 → 제안 서비스 판별: 서비스가 AI-Studio 하나로 좁혀져 `13S` K8 판별 대상 소멸), **V6**(신호 4종 자체의 타당성: 4종이 `31` 9절에서 교체·근거 상실), **V3**(축2가 국가를 가르는가: 국가는 기업이 지정 K1). **처리** — ① 구 V2 **폐기**, 번호는 되살리지 않음 ② V3 **성격 변경**: 국가 선택 도구 → **3축 캘리브레이션**(일본은 기업이 실제 가 본 유일한 국가 = 정답지 있는 표본. `13` 7-1절 R1 반영) ③ V4 **범위 축소**: 번역 정확성을 빼고 톤만 — 번역은 `specs/G7`이 별도 실행 단위로 확정했고 한 항목에 두면 어느 쪽이 틀렸는지 안 갈린다 ④ V6 **대상 교체**: 신호 4종 → 타겟 조건 4종·관찰 항목 ⑤ **V7 신설**(세그먼트 축) — 브리프 요구 ②의 담당이 `30` ⑤에서 G2+G5로 옮겨갔는데 **검증이 따라오지 않아 요구 ②가 검증 공백이었다** ⑥ **V8 신설**(적합도 척도가 네 소비처에서 같은가, `31` 6절 #3) — 폐기된 V2의 "규칙층 검증" 자리를 받음. 함께 0절의 규칙층 예시·대기 근거(Q1·N2)를 현행으로, `32` 참조 4곳을 `specs/G1`로 교체 | `docs/수업/01일차_피드백_반영.md` 반영 후보 6번 | 완료 |
| 2026-07-30 | **`specs/G2_세그먼트화.md` + `기준/G2_세그먼트기준.md` 신설 — 브리프 요구 ②의 빈자리를 메웠다** | G1·G7이 **병렬 가능성**으로 선정된 것과 달리 G2는 **공백** 때문에 골랐다. 요구 ②(고객 특성에 맞춘)의 담당이 `30` ⑤ 매칭에서 **G2+G5**로 이관됐는데 **Spec도 검증도 따라오지 않았다**(`40` 정리 중 발견 → V7 신설). **핵심 설계 판단 6건** — ① **분류축을 전부 기준 카드로 뺐다.** 축은 이 프로젝트에서 가장 자주 바뀔 값이고(미확정), Spec에는 *"축이 갖춰야 할 조건"*만 남겨 축이 바뀌어도 Spec이 안 흔들리게 했다 ② **축은 G1이 관찰한 항목으로만 만든다**(제약 ①) — 관찰 안 한 것으로 묶으면 근거가 없다. 쓸 수 있는 축을 O1·O4·O5~O7로 한정하고 **O3·O8은 못 쓰는 이유까지 적었다** ③ **대표는 적합도로 고르지 않는다** — G3이 G2를 입력으로 받을 수 있어(`31` 6절 #2) 서로를 기다리는 순환이 생긴다. **"가장 좋은 곳"이 아니라 "가장 전형적인 곳"**으로 규칙을 세웠다 ④ **미분류를 4종으로 갈랐다**(U-단독/경계/정보없음/이질). **U-정보없음만 성격이 다르다** — 나머지는 축을 고쳐야 줄고 이것은 관찰을 다시 해야 준다. 섞으면 축을 아무리 고쳐도 안 줄어드는 잔여가 생겨 축이 나쁜 것처럼 보인다 ⑤ **세그먼트 ID에 축 코드·버전·작성일을 강제**(성공 기준 6) — G11이 세그먼트별 반응을 집계하는데, 버전이 없으면 축을 바꾼 뒤의 S1과 이전 S1이 구분되지 않아 **분류축이 맞았는지 영구히 확인 불가**가 된다 ⑥ **도구 권한에서 웹 접근을 뺐다** — 제약 ③(새로 관찰하지 않는다)을 문장이 아니라 **물리적으로** 강제. G1과 G2의 권한 차이가 그 경계다. **잠정 축 권고는 A(품목) 주축 + B(규모) 보조**이며 반례("규모가 더 중요하지 않나")는 *"그건 살 가능성의 문제이고 적합도가 다룬다"*로 처리 | `31` 3-2절 G2·4-2절, `13S` F28·H2, `40` V7 | 완료 |
| 2026-07-30 | **`90` 전면 점검 — 미결정 3건 확정, 리스크 8건 상태 정리** | 리스크 16의 낡은 표기를 고치려다 목록 전체를 봤고, **이미 답이 나왔는데 "미정"으로 남아 있던 것이 여러 건**이었다. **미결정** — #6(온톨로지·지식그래프) **채택하지 않음 확정**(`31` 기능 10개에 없고 지원 단계 자료에만 남아 있다), #7(코트라 대체 소스) **조사 완료 확정**(`11`에 결과가 있는데 "미조사"로 남아 있었다), #8(Workflow냐 Agent냐) **부분 확정**(`specs/G1` 4-1절이 "G1은 Workflow다"를 실패 처리까지 선언 → **전역 결정이 아니라 기능별 Spec 항목으로 성격 변경**). **1절에 남은 미정 항목이 없다.** **리스크** — 해소 5건(1 코트라 접근·3 읽음 트래킹·8 국가별 규제·9 Clo.D 성립·10 신호 방향), 통합 1건(14→21), **폐기 1건(4)**, 대상 교체 1건(16). **🔴 리스크 4가 가장 위험했다** — *"SKU 50개·7영업일·14일·90일을 빠뜨리지 말라"*고 지시하는데 **그 수치가 전부 근거를 잃은 것들**이었다(`31` 9절). *"임계값은 각 기능 Spec에서 근거와 함께 정하고 근거 없는 수치를 서술에 남기지 않는다"*로 승계. 함께 **2-3절 신설**(21건을 살아있음 8 / 반영완료 4 / 실행대기 1 / 해소·폐기 8로 갈랐다 — 목록이 길어 무엇을 보고 있어야 하는지 묻혔다), 절 제목·항목 순서 정정(2-2가 수업 대조만 가리켰으나 19~21은 `project-critic`·법규 출처, 18이 21 뒤에 있었다), **3절 다음 액션에서 완료·폐기 6건을 분리**(타겟 국가 확정·실패 사례 확보 등 이미 끝난 것이 할 일로 남아 있었다) | `90` 전량 대조 | 완료 |
| 2026-07-30 | **V 번호 충돌 발견 — 한 저장소에 V2가 둘이었다** | `specs/G7_회신해석정리.md` 6-2절이 자기 기준 검증을 **V2-a / V2-b**로 이름 붙여 놓았는데(`40` V1-a/V1-b 패턴을 따른 것), **`40`은 그 사실을 모른 채 V2를 다른 것에 쓰고 있었다.** Spec이 만든 검증이 검증 계획 정본에 등재되지 않아 **"대기 중"인지 "잊힌 것"인지 구분되지 않는 상태.** → `specs/G7`의 사용을 인정해 **V2 = G7 회신 해석 기준 검증**으로 `40` 2-3절에 등재하고, 적합도 척도는 V8로 밀었다. **규칙 신설**: 기능 Spec이 자체 기준 검증을 만들면 **번호를 `40`에서 받아 간다**(`40` 2-2·2-5절, `CLAUDE.md` 문서 표에 "V 번호의 정본" 표기). 함께 `specs/G1` 부록 B의 *"`40` 기록 양식은 폐기된 신호 4종 기준"* 서술을 정정 — 그 양식은 이미 교체됐다 | `40`·`specs/G7`·`specs/G1` 대조 | 완료 |

## 아직 반영하지 못한 것 (공백 관리)

| 공백 | 왜 아직 없나 | 언제 채우나 |
|---|---|---|
| `11_대안탐색_비교.md` | **부분 작성 (2026-07-29).** 국가 선정 3축 데이터 소스 비교·탈락 근거는 완료. **기업 발굴 소스 비교와 AI 모델 선택 비교는 미작성** | 기업 발굴 소스는 `12` Q1 답 이후(대상이 확정돼야 소스가 정해짐), AI 선택 비교는 D단계 |
| 축1 정량 지표 (국가별 패션 이커머스 규모) | 2차 조사에서도 확보 실패. Statista·eMarketer 등 유료 소스에만 국가별 분해가 있고, ITC Trade Map은 선진국 계정 접근이 제한적, UNCTAD B2C 지수는 2020년판이 최신 | `12` Q11(운영비 상한)·N6⑨(회사 보유 자료) 답 + ITC 계정 등급 확인 이후. 확보 못 하면 축1은 스크리닝으로 확정 |
| **국가별 패션 지출액 분해** (축2 임계값의 성립 조건) | 확보한 $33.9는 **30개 지역 전체 평균**이다. 국가별로 쪼갠 수치가 보고서에 있어야 국가 간 비교가 성립한다 | 통계편·분석편 PDF 직접 열람. **`11` 7절 1번 — 현재 최우선 확인 항목** |
| `20_문제정의.md` | 문제 가설(발굴형/전환형/운영형)을 확정할 직접 증거가 없다 | `12` Q8·Q19·N1 답 이후. 그 전에 분기표는 미리 작성 |
| ~~`31_구체화_기준표.md`~~ | **2026-07-30 폐기.** 임계값 표를 별도 문서로 만들지 않는다 — 임계값은 각 기능의 Spec(`32` 계열)에서 필요한 것만 정한다. **번호 31은 `31_기능구조.md`가 점유했으므로 이 이름을 다시 쓰지 않는다** | — |
| ~~`40_검증계획.md`~~ | **2026-07-29 해소(부분).** 관찰층은 폐기 대기가 아님이 확인돼 신설했고 V1을 실행 항목으로 등록. **규칙층 검증(V2)·국가 임계값(V3)·메일 품질(V4)·목록 규모(V5)는 여전히 대기** | V2는 `12` Q1·N2 / V3은 국가별 지출액 분해 확보 / V4는 `12` N6③·Q16 / V5는 `12` Q9·Q10 |
| ~~`32_하네스_스펙.md`~~ | **2026-07-30 해소.** 하네스 B의 Spec이 어디에도 없어 `40` V1-b가 잴 대상 없이 떠 있었다. **구조 문서는 값 문서가 아니므로** `31`과 달리 `01` C2 게이트에 걸리지 않는다는 판단으로 신설 | — |
| 하네스 B의 **② Skill · ⑥ Checklist** | Skill은 `40` V1-a가 기준 문장을 고쳐야 초안이 나오고, Checklist는 `32` 2절 (a)를 실제로 돌려본 뒤 확정한다 | V1-a 직후(②) / V1-b 직후(⑥) |
| 하네스 B의 **③ 실행체** | `32` 3절 4경계는 정의했으나 실행체가 없다. **V1-b를 재려면 최소 1개가 필요하다** | `01` C4 진행 중. `.claude/skills/` 아래에 만든다 |
| 하네스 A의 **⑤ Hook** | `project-critic` 호출을 사건에 묶는 장치가 없다. 자동 실행 동작을 심는 것이라 사용자 확인이 필요하다 | `90` 리스크 18. 우선 게이트 표 기록 항목으로 대체 권고 |
| `50_실사용_도입설계.md` | 담당자·캐파·운영비를 모른다. 단 **가정치로는 지금도 작성 가능** | `12` Q9·Q14·N5 답 대기 중이나, 가정 명시 방식으로 선작성 권장 |
| `60_구현범위.md` | 기획 ①~③이 끝나지 않았다 (정상) | E단계. **입력 재료는 `01` E절에 이미 기록됨** |
| 기업과제 원문 문장 단위 대조 (A1) | PDF 한글 텍스트 추출 불가 (CID 폰트, pdftoppm·OCR 없음) | 사용자가 원문 텍스트를 제공하거나 OCR 환경 확보 시 |

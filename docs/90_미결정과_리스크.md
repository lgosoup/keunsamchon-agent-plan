# 90. 미결정 사항 · 리스크 · 다음 액션

> 구현에 들어가기 전에 정해야 할 것과, 정해도 남는 위험을 모아둔 **작업 목록**이다. 결정이 끝난 항목은 `30_해결설계.md` 본문에 반영하고 여기서는 "확정" 표시만 남긴다.
> 출처: 2026-07-28 상담 + 지원 단계 자료 대조(원본은 `../archive/01_지원단계/1지망_수행아이디어_통합정리.md` 5·7절).

---

## 1. 결정해야 할 것 (권고 포함)

| # | 결정 사항 | 선택지 | 권고 | 근거 | 상태 |
|---|---|---|---|---|---|
| 1 | 메일 형식 | (a) 전원 동일 / (b) 타겟 맞춤 / (c) 자사소개 동봉 + 협업 시그널 | (b)+(c) 결합 | (c)는 "신뢰 인식이 비용 논리보다 먼저"라는 문제정의에, (b)는 "고객 특성에 맞춘 제안"이라는 브리프 원문에 대응 | **확정 (2026-07-30)** — `31` G2+G5. **맞춤 단위는 개별 기업이 아니라 세그먼트**이고 자사 자료 동봉은 G5 입력에 포함 |
| 2 | 커뮤니케이션 깊이 | (a) 단발성 / (b) 분석 내용 포함 | (b) | 신호를 수집해 매칭까지 하는 구조를 만들고 결과만 보내면 근거가 메일에 드러나지 않는다 | **확정 (2026-07-30)** — `31` G5 하위 설계에 "G1 관찰층 근거를 본문에 인용해 왜 귀사인가를 설명"으로 반영 (원안 D-4 승계) |
| 3 | 반응 트래킹 범위 | 회신 여부만 / 읽음 여부까지 | 회신 중심 + 읽음은 보조·한계 명시 | 읽음 추적은 픽셀 방식이라 차단·규제로 정확도가 낮다 | **확정 (2026-07-30)** — `31` G6은 **회신 / 무응답 / 주소 오류 3분기**. 기업이 요구한 "안 읽음 vs 주소 오류" 구분은 픽셀이 아니라 **바운스 코드 분류**로 실현 |
| 4 | 메일 외 후속 채널 | 메일 팔로우업만 / 전화 등 확장 | 계획에는 포함, 구현은 메일까지 | 해커톤 범위를 넘는다 | **확정 (2026-07-30)** — `31` G9가 그 자리다. 채널은 "전화"가 아니라 **사람이 주변·협력사를 통해 접촉**이며 기업 실제 자산에 맞춘 형태 |
| 5 | 타겟 국가 | 미정 | 3축 조사 후 확정 | — | **확정 — 일본.** 기업이 지정(`13S` K1). **3축은 국가 선택 도구가 아니라 (i) 일본 캘리브레이션 (ii) 2차 국가 후보 도구로 용도 변경** (`13` 7-1절). ⚠ "가깝다·K문화 적합도"는 기업 발언이 아니라 우리 판단이다 (`31` 1-3절) |
| 6 | 온톨로지·지식그래프 | 유지 / 축소 / 삭제 | **축소 유지 (부가 표시)** | "기술보다 기획" 원칙상 비중을 키우면 역효과 | **확정 — 채택하지 않음 (2026-07-30).** `31`이 기능을 G1~G10으로 확정했고 **그 안에 없다.** 지원 단계 자료(`archive/`)에만 남아 있고 이후 어느 설계 문서에도 들어가지 않았다. `(부가)`로도 넣지 않는 이유: 요구를 담당하는 기능이 이미 열 개인데 여기에 관계 구조화를 얹으면 `CLAUDE.md` 2절이 경고한 *"요청한 걸 안 만들고 하고 싶은 걸 만든"* 비중이 된다. **되살리려면 `91`에 근거를 먼저 쓴다** |
| 7 | 코트라 대체 소스 | 미조사 | **조사 필요 (우선순위 높음)** | 조사 결과가 비교·검증 서술의 핵심 재료가 된다 | **확정 — 조사 완료 (2026-07-29).** 결과는 `11_대안탐색_비교.md`에 있다 — 코트라 국가정보는 **OpenAPI 무료 공개**(축3 확정), 축2는 해외한류실태조사, **ITC Trade Map 조건부**(선진국 계정 등급 미확인), **UNCTAD 지수 탈락**(최신판 2020년). **남은 것은 축1 정량화 하나**이고 그것은 `40` **V3**이 다룬다 |
| **8** | **하네스 B를 Workflow로 만드는가 Agent로 만드는가** | (a) Workflow — 사람이 미리 설계한 고정 순서 / (b) Agent — AI가 스스로 경로를 판단 / (c) 혼합 | **(a) Workflow를 기본, 대화형 조정은 `(부가)`로 분리** | `30` ⑦은 10단계 고정 순서(=Workflow)인데 **같은 절 끝에 "전역 LLM 에이전트와 대화로 조정·자연어로 재분석 요청"(=Agent)이 섞여 있다.** 둘은 **오류 처리가 정반대**다 — Workflow는 실패 시 파이프라인 중단, Agent는 스스로 재시도. 선언하지 않으면 실패 경로가 설계에서 빠진다(실제로 빠져 있었다 → 리스크 17). 브리프가 요구한 "반자동"과 심사의 "판단 근거를 설명할 수 있는가"에도 (a)가 맞는다 | **부분 확정 — 기능별로 선언한다 (2026-07-30).** **Spec이 있는 세 기능 전부가 「제약」에서 "Workflow다"를 선언하고 실패 3종의 처리까지 확정했다** — G1(판정 불가로 기록 후 종료 / 채운 항목은 유지 / 소스 차단 시 수집 범위 기록), G2(미분류 과다는 사람에게, 스스로 축을 바꾸지 않음), G7(판독 불가는 C8 / 검증 불일치는 재번역 1회 후 C8 / 검증층이 비면 사용자에게 보내지 않음). 근거는 `33` **D8**. **전역 결정이 아니라 각 기능 Spec에서 선언하는 항목으로 성격이 바뀌었다** — 기능마다 실패 처리가 다를 수 있기 때문이다. 남은 것은 **G3~G6·G8~G11의 선언**이며 각 Spec 작성 시 채운다. ⚠ **2026-08-05 각주 — 이 항목이 기각한 "전역 LLM 에이전트로 파이프라인을 대화 조정"과, G12(질의응답)의 "읽기 전용 질의응답"은 다른 것이다.** G12는 실행·수정 권한이 없어 이 항목이 우려한 "설명 불가능한 재실행"이 성립할 자리가 없다 — `specs/G12`·`33` D20 |

> ✅ **2026-07-29 정합성 경고 해소.** 미결정 #1~#5가 `31_기능구조.md`에서 정식 확정됐다(위 표).
>
> ✅ **2026-07-30 — #6·#7 확정, #8 부분 확정. 1절에 남은 미정 항목이 없다.** #8은 미결정이 아니라 **각 기능 Spec이 반드시 채울 항목**으로 성격이 바뀌었다.
>
> ⚠ **확정은 "다시 열지 않는다"는 뜻이 아니다.** #5(타겟 국가)처럼 기업이 정한 것은 우리가 못 바꾸지만, #6·#7은 **우리 판단**이므로 근거가 바뀌면 `91`에 쓰고 되돌릴 수 있다.

## 2. 리스크

> **2026-07-30 상태 점검.** 아래 1~8은 2026-07-28 상담 시점에 등록된 것이고, 그 뒤 조사·설명회·설계 교체를 거치며 **해소되거나 다른 항목으로 승계된 것이 다섯 건**이다. 해소된 것을 그대로 두면 목록 전체가 "안 읽히는 목록"이 된다. **지우지 않고 상태만 표시한다** — 왜 지금 신경 쓰지 않아도 되는지가 근거다.

| # | 내용 | 대응 | 상태 |
|---|---|---|---|
| 1 | 코트라 데이터 실제 접근 범위 미확인 | 공개 열람 범위 확인 + 대체 소스 후보 확보 | **해소 (2026-07-29).** 국가정보는 **OpenAPI로 무료 공개**돼 있고 개발계정 1만 트래픽까지 확인했다(`11` 2절). "특정 데이터셋을 단정하지 않는다"는 제약도 함께 풀린다 — 이제 이름을 적을 수 있다 |
| 2 | 지표명·조사명을 확인 전에 못 박으면 사실 오류 | 3축 표의 지표는 **후보**로만 다루고, 실제 확보한 것만 이름을 적는다 | **유효 — 일반 원칙으로 유지.** 3축은 확정됐으나 같은 실수가 다른 곳에서 난다. 실제로 `31` 1-3절에서 *"가깝다·K문화 적합도"*를 기업 발언으로 잘못 적은 것이 발견됐다 |
| 3 | "읽음 여부 트래킹" 과잉 약속 | 1절 #3대로 회신 중심 + 읽음은 한계 병기 | **해소 (2026-07-30).** `31` G6이 **회신 / 무응답 / 주소 오류 3분기**로 확정됐고 읽음은 분기에서 빠졌다. 기업이 요구한 "안 읽음 vs 주소 오류" 구분은 픽셀이 아니라 **바운스 코드**로 실현된다 |
| 4 | ~~정량 기준 누락 — SKU 50개·7영업일·14일·90일을 빠뜨리지 않는다~~ | — | **🔴 폐기 (2026-07-30). 그대로 두면 위험한 항목이었다.** 여기 적힌 수치가 **전부 근거를 잃었다** — SKU 50개는 폐기된 매칭 로직용이었고(`31` 9절), 60·90일은 수치를 떼어내고 구조만 G10으로 승계했다. 이 리스크는 **폐기된 수치를 "빠뜨리지 말라"고 지시하고 있었다.** <br>**승계본**: *"임계값은 각 기능의 Spec에서 근거와 함께 정하고, 근거 없는 수치를 서술에 남기지 않는다."* 값이 없으면 자리만 만든다(`31` G5 발송 상한이 그 방식) |
| 5 | ~~Clo.D / AI-Studio 매칭 로직 누락~~ | — | **폐기 (2026-07-30).** 제안 서비스가 AI-Studio 하나로 확인돼(`13S` K8) 매칭 자체가 성립하지 않는다. 브리프 요구 ②의 담당은 **`31` G2 + G5**로 이관됐고, **그쪽을 축소하지 않는 것**이 이 리스크의 승계본이다. 검증은 `40` **V7** |
| 6 | 버즈워드 밀도 | AI 기술명 나열보다 판단 기준·근거 서술 비중을 높인다 | **유효.** 발표·제출 시점까지 계속 본다 |
| 7 | 공개 정보만으로는 의사결정권자 연락처 확보가 어려움 | 일반 문의창구로 수렴되는 경우를 전제로 메일 문안·팔로우업을 설계 | **유효 — 설계에 반영됨.** `31` G4가 *"유효 = 존재가 아니라 의미 있는 창구인지"*로 판정하고, 미확보분은 **G9(사람 위임)**로 넘긴다. **기업도 같은 예상을 말했다** — 실제 담당자 도달률을 *"굉장히 낮은 퍼센트"*로 봄(`13S` F15) |
| 8 | 국가별 콜드메일 규제 | 타겟 국가 확정 시 해당국 스팸 규율을 함께 확인 | **발생 → 리스크 19로 승계 (2026-07-30).** 국가가 일본으로 확정됐고 규제를 확인했다. 대응은 **국가 선정 기준이 아니라 발송 적법성 요건**으로 재배치됐다(리스크 15·19) |

### 2-1. 2026-07-29 조사·검토로 새로 등록된 리스크

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 9 | **Clo.D 해외 판매 성립 여부** — 동대문 사입삼촌 구조에 종속. 국내 논리를 해외에 옮기면 "우리 사업을 모른다"로 읽힌다 | **해소, 확정 완료 (2026-07-31).** 2026-07-30 설명회 시점엔 기업이 **AI 비주얼 제작을 1차 목표**로 지목한 정황(`13S` K8)만 있어 "Clo.D 배제"를 단정하지 않았으나, **이후 기업에 직접 재질의해 "AI-Studio 단독, Clo.D 범위 제외"를 명확히 확인했다.** `30` ⑤와 2절 「기업 활용 제안」은 폐기, `00`·`10`·`31`에 확정 반영 | `10` 2절, `12` Q1 |
| 10 | **판별 신호가 지불 고객과 반대 방향일 가능성** — 실제 지불 고객은 전원 "전문" 등급인데 설계는 "비전문"을 타겟한다 | **해소 — 신호 자체가 교체됐다 (2026-07-30).** 1차 판별축이 이미지 전문성이 아니라 **제품 사진 / 커머스 / 업종 / 한국 진출 희망**으로 바뀌었다(`13S` H4, `31` 1-1절). 방향이 문제였던 항목은 **가점 항목으로 내려가** 1차 판별에서 빠졌다. **남은 것**: 가점 항목으로 쓸 때의 방향은 여전히 미확인이며 `12` N2가 그것을 가른다 | `10` 3절 + 검토 |
| 11 | **캐파 초과 리드는 낭비이자 위험** — 제작 캐파를 넘는 발송은 승인 부담·도메인 평판 손실만 남는다 | **2026-08-03 대응 변경.** 상한을 미리 걸지 않는다 — 기업 상황(바쁨, 컨택 자체가 안 됨)상 무제한이 기본이고, 값이 오면(`12` Q9) `docs/50` 1절에 참고 수치로만 기록한다. 감당 못 하면 후보 목록에서 필요한 만큼만 가져다 쓴다. **승인 부담**은 `50` 1-2절, **도메인 평판**은 리스크 12(별도 예외)로 분리했다 | 검토 |
| 12 | **회사 도메인 평판 훼손** — 메인 도메인이 스팸 처리되면 기존 동대문 거래 메일까지 피해 | **이 리스크만 무제한 원칙의 예외다** — 전용 서브도메인(`specs/README.md` "발송 도메인 준비") + 일일 발송 상한(`docs/50` 1-1절, 담당자가 직접 늘려가는 값) + 원클릭 수신거부. `12` Q15 답 대기 | 검토 |
| 13 | **레퍼런스 무단 인용** — GUESS·BARNEYS·CJ ENM을 허가 없이 메일에 쓰면 기업 리스크 | 허가 확인 전 템플릿에서 물리적 제외 (`12` Q7) | 검토 |
| 14 | **회신 이후 경로 공백** — 견적·영문 계약·결제 수단이 없으면 계약 직전에 막힌다 | **리스크 21로 통합 (2026-07-30).** 같은 사안이 두 번 등록돼 있었다. 21이 `31` G7을 **경계로 명시**한 것까지 반영한 최신본이므로 그쪽을 본다. 확인은 `12` **N20**·Q12 | 검토 |
| 15 | **콜드메일 규제를 국가 선정 후에 확인하는 순서 오류** — 옵트인 국가를 1순위로 고르면 국가 선정을 다시 해야 한다 | **🔴 실제로 발생했다 (2026-07-30).** 기업이 지정한 일본이 옵트인 원칙 국가이며, 국가 확정 후에도 규제 확인이 실행되지 않았다. 다만 **국가 선정을 다시 하는 것은 불가능**하다 — 기업이 지정했으므로 게이트 조건으로 쓸 수 없다. → **규제를 국가 선정 기준이 아니라 발송 적법성 요건으로 재배치**했다 (리스크 19) | 검토 → **발생·대응 완료** |
| 16 | **판별 기준의 객관성이 미검증** — 관찰 항목을 "누가 봐도 같은 결론에 이르는 기준"으로 **선언만 했고 확인한 기록이 없다.** 사람끼리 갈리는 기준은 자동화해도 갈린다. 이 전제가 무너지면 목록 신뢰도와 **메일의 맞춤 근거**(*"귀사의 공개 정보에서 이런 점을 확인했다"*)가 함께 무너진다 | **🔴 2026-08-03 — 사전 대조(`40` 옛 V1)를 폐기했고, 대체 경로도 재검토로 축소됐다.** 사전 게이트가 `31` 1-5절과 부딪혀 폐기한 것은 유지한다. **다만 "G11이 사후 대체한다"는 최초 대체 근거는 사실이 아니었다** — `specs/G11`의 조정 대상은 `기준/G2`·`기준/G3`뿐이고 `기준/G1`의 판정 문장은 G11의 입력·조정 대상 어디에도 없다(`project-critic` 2026-08-03 검토 치명 3). **현재 상태**: ① O4(커머스) 잠정 규칙은 확정 규칙으로 바꿔 리스크를 없앴다(`기준/G1` 변경 이력 2026-08-03) ② O8(광고 메일 거부 표시, 위법 직결)은 `40` V9로 축소 승계해 최소 확인을 되살린다(`12` N17과 묶어 처리) ③ **O5(업종)·나머지 판정 문장이 사람 사이에서 실제로 일치하는지 확인하는 경로는 지금 없다** — 이 공백은 리스크로 남기고, 실행 중 판정 불일치가 관찰되면(예: 담당자가 후보 목록을 검토하며 "이건 아닌데" 하는 사례) 그때 `기준/G1` 변경 이력으로 직접 고친다(사전 검증 대신 실사용 중 관찰) | 수업 1일차 → 2026-08-03 사용자 지적으로 재검토 → `project-critic` 재검토로 대체 근거 정정 |

### 2-2. 2026-07-30 새로 등록된 리스크

> 출처가 둘로 갈린다 — **17·18**은 수업 1·2강 개념 대조에서, **19·20·21**은 `project-critic` 적대적 검토와 일본 법규 확인에서 나왔다. 이전 절 제목이 전자만 가리키고 있었고 18이 21 뒤에 놓여 있어 바로잡았다.

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 17 | **판정 실패 경로가 설계에 없다** — 초기 설계의 제외 조건 3종은 전부 "봤는데 부적합"이고, **"못 봤다"**(사이트 접속 불가 / 스크립트 렌더링으로 상품 목록을 읽을 수 없음 / 로그인 요구 / 상품 수를 셀 수 없음)는 정의돼 있지 않았다. 둘이 한 칸에 섞이면 재수집 때 어느 쪽을 다시 열어야 하는지 알 수 없고, **V1 불일치의 원인 분석도 오염된다**(접근 조건 문제가 기준 문장 문제로 집계된다) | **반영 완료 (2026-07-30).** 판정 불가를 제외와 다른 **별도 상태**로 두고 사유를 남긴다 — `specs/G1` 「성공 기준」, `기준/G1` 3절(? 사유 목록), `40` V1 기록 양식의 전용 행, `31` G1 관찰층. **V1을 돌리면 즉시 부딪히므로 실행 전에 반영했다.** `specs/G1` 「제약」의 Workflow 실패 처리가 한 번 더 고정 | 활용 방식 3분류(Chatbot/Workflow/Agent)의 "오류 발생 시" 축으로 대조 |
| 18 | **기획 하네스(A)에 강제 실행 장치가 없다** — `CLAUDE.md` 5절의 "게이트 전과 미팅·발표 전에 `project-critic`으로 검토한다"는 규칙은 **스스로 판단해 건너뛸 수 있는 형태**다. 규칙이 지켜졌는지 남는 기록도 없다. 자기 점검은 자기 글에 관대해지므로 이 규칙이 건너뛰어지면 적대적 검토가 아예 없는 상태로 게이트를 통과한다 | 선택지는 (a) `.claude/settings.json`에 Hook을 두어 사건에 묶는다 (b) `01` 게이트 표에 "검토 실시 여부"를 기록 항목으로 넣는다 (c) 현행 유지. **권고는 (b) 먼저** — Hook은 자동 실행 동작을 심는 것이라 사용자 확인이 필요하고, (b)만으로도 "건너뛴 사실이 남는다"는 효과가 생긴다. **미대응** | Hook이 Skill로 대체되지 않는 두 이유 중 첫 번째와 대조 |
| 19 | **🔴 일본 특정전자메일법 요건이 설계에 없었다 — 리스크 15가 실제로 발생한 형태** | 일본은 광고 메일에 **사전 동의(옵트인)가 원칙**이고, 웹 공개 주소 예외에도 조건이 붙는다 — **주소 옆에 "광고·선전 메일 거부" 표시가 있으면 예외가 적용되지 않아 발송이 위법**이다. 여기에 표시 의무 4종(송신자명·수신거부 가능 사실·수신거부 접수처·주소와 문의처)이 별도로 붙는다. 국가가 일본으로 확정됐는데 확인이 실행되지 않았다 | **`31`에 반영 완료 (2026-07-30)** — G4 출력에 **[발송 금지]** 상태 신설(제외·판정 불가·미확보와 다른 네 번째 상태, 재수집 대상에서도 제외), G1 관찰층에 "광고 메일 거부 표시" 항목 추가, G5 하위 설계에 표시 의무 4종을 1급 제약으로. **법 해석 자체는 `12` N17로 기업 확인 필요** | 総務省·迷惑メール相談センター 가이드라인 (2026-07-30 확인) |
| 20 | **발송 상한이 어느 기능에도 없었다** | G3은 접촉 **순서**만 정하고 **한 번에 몇 건**을 정하지 않았다. 상한이 없으면 리스크 11(캐파 초과)·12(도메인 평판)가 그대로 실현된다. 이 회사는 SNS에서 같은 유형의 사고를 이미 겪었다(`13S` F12) | **2026-08-03 — 자리를 `docs/50` 1·1-1절로 옮겼다.** 제작 캐파(리스크 11)는 무제한이 기본이라 역산 컷오프를 안 만든다. **일일 발송 상한(리스크 12)만 실제 상한**이고, 그 값은 `50` 1-1절에서 담당자가 직접 조정한다(자동 컷오프 알고리즘 미제작) | `13` 7-3절 R3 |
| 21 | ~~회신 이후 경로가 플로우 밖이다~~ (리스크 14 통합) | `31` G7이 사용자 전달로 끝난다. 미팅·견적·영문 계약·결제가 없다 | **해소 — 확정 결정 (2026-08-10, 사용자 지시).** 회신 수락 이후(미팅·견적·계약·결제)는 **당연히 사람이 하는 일**이라 자동화도, 기업 확인 질문도 필요 없다고 확정했다 — `12` N20·Q12 문항 신설 계획도 취소. **대신 확인된 진짜 요구**: 회신이 오면 **어떤 회신인지 사람이 쉽게 알 수 있어야 한다.** 이건 이미 충족돼 있다 — G7이 회신마다 8분류·상대 요구·기한을 이미 뽑아 레코드로 낸다(`specs/G7_회신해석정리.md`). 남는 건 새 기능이 아니라 **그 레코드를 사람이 보기 좋게 보여주는 것**이고, 이건 웹+대시보드 작업(전부 끝난 뒤 별도 진행)의 몫이다 | 해소 완료 |

### 2-4. 2026-07-31 `project-critic` 전체 검토로 새로 등록된 리스크

> 치명 1건(기업 비공개 자료 공개 위험)은 발견 즉시 처리 완료(`91` 로그 참조 — `.gitignore`·`docs/10`·`CLAUDE.md` 6절 반영). 아래는 **아직 처리하지 않은** 나머지다.

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 22 | **`docs/20_문제정의.md`·`docs/50_실사용_도입설계.md`가 존재하지 않는다** — `CLAUDE.md` 1절은 ②(진짜 문제)를 ③(해결 설계)보다 앞에 두는데, ②의 산출물 없이 ③④ 산출물(Spec 13개)이 이미 완성됐다. `91:129`가 "가정치로는 지금도 작성 가능"이라 판정하고도 6일간 착수하지 않았다. 재료(`13` Q8 실패 사례 4건, `13S` H1·H2 해석, PPT 가격 구조)는 이미 있다 | **반영 완료 (2026-07-31).** `docs/20_문제정의.md` 신설 — 발굴형 확정(반례 통과), 운영형은 발굴형에 흡수, 전환형은 검증 불가로 보류. `docs/50_실사용_도입설계.md` 신설 — 가정 명시 3표 + G1 실측(리스크 29와 통합) | project-critic 치명 2·12 |
| 23 | **"왜 콜드메일인가"의 채널 대안 비교가 `11`에 없고 공백 목록에도 등록 안 됨** | `11`에 8절 "채널 대안 비교" 신설 — 콜드메일 / 해외 마켓플레이스 입점 / 현지 에이전시 / 인바운드 강화 4축 × 초기비용·월운영·인원요구·기업 실패이력 4열. 관측 사실 3건(크몽 리드·10개언어 사이트·협력사 의존 실패)으로 초안 가능 | project-critic 치명 3 |
| 24 | **`12` Q1 확정(AI-Studio 단독) 이후 `31` 1-1절 "한국 진출 희망" 필수 게이트의 관련성 근거가 재검토되지 않았다** — 근거가 "모집단 축소"뿐인데 그건 편의이지 관련성이 아니다. Clo.D 제외로 이 조건의 원래 근거(동대문 소싱)가 약해졌을 수 있다 | **반영 완료 (2026-08-01).** `12` N22 답 확보 — 기업이 한국 진출 계획 없는 브랜드도 대상에 포함되며 이 조건이 중요하지 않다고 확인. 실측(n=3, 0/3 관찰)도 같은 방향. **필수 게이트에서 G3 적합도 가점으로 하향 확정** — `기준/G1` 1절·2-3절·4절, `specs/G1`, `기준/G2`·`기준/G3`, `40` V1 갱신(`91` 로그) | project-critic 치명 4 |
| 25 | **PPT 확인 산업(Fashion·Cosmetic·Jewelry·Furniture)과 설명회 발언 업종(패션·뷰티·푸드)이 어긋나는데 불일치로 등록 안 됨** — 푸드가 PPT 공식 산업에 없고 주얼리·가구가 발언에 없다. `13S` D1(3순위 업종 미확정)의 판별 재료가 새로 생겼는데 반영 안 됨 | **반영 완료 (2026-07-31).** `13S` D1 갱신(PPT의 Jewelry 명시를 기록, 층위가 다를 수 있음도 명시), `12` N14 갱신 + **N24 신설**로 기업에 1차 타겟 업종 재확인. 답은 대기 | project-critic 중대 11 |
| 26 | **가격 구조를 확보했는데 캐파·최소수주액 "미확인" 표기 7곳이 갱신 안 됨** — `12` Q10, `13S` U4, `기준/G1` 5절, `기준/G3` 4절, `specs/G1`·`specs/G5`, `40` V5 | **반영 완료 (2026-07-31).** 8곳을 "단가 구조는 확인됨(비공개 보유). 남은 것은 최소 수주 금액"으로 좁혔다. **2026-08-03 후속** — 같은 사고를 구조적으로 막기 위해 캐파·최소수주액 등 운영 변수를 `docs/50_실사용_도입설계.md` 단일 출처로 이관했다(`CLAUDE.md` 5절). 이후 이 8곳은 값 갱신 대상이 아니라 `50` 참조만 유지하면 된다 | project-critic 중대 6 |
| 27 | **`CLAUDE.md` 4·7절 라우팅 표 자체 오류 4건** — `31` 행 중복(설명 다름), 기획 본체를 `30`·`31` 두 곳으로 지칭, 기능 수 "G1~G10"(실제 G1~G11), 하네스 B ⑤ Hook을 "○"로 표시(실제로는 리스크 4가 폐기한 수치뿐) | **반영 완료 (2026-07-31).** `CLAUDE.md` 4절 표 정정(중복 병합·기능 수·기획 본체 단일화, `20`·`50` 행 추가), 7절 Hook·Spec 행을 실제 상태로 정정 | project-critic 중대 7·8 |
| 28 | **매출 로드맵 GLOBAL 트랙과 이번 과제가 같은 트랙인지 미검증** — 로드맵 태그(B2B 중계·해외 B2C·PB)는 상품 판매로 읽히고 이번 과제는 AI-Studio 제작 서비스 판매다. `10` 5절이 이미 가설로 낮춰 반례를 병기했다(2026-07-31 처리 완료) | **반영 완료 (2026-07-31).** `12`에 **N21 신설**(같은 트랙인지 질문). 기업 답 오기 전까지 이 로드맵을 Q1 확정의 지지 근거로 인용하지 않는다 | project-critic 중대 9 |
| 29 | **G1 실제 처리량(수백 건 후보 처리 시간·비용)이 어디에도 산정되지 않았다** | **반영 완료 (2026-07-31).** 일본 기업 3곳(INGNI·ROPÉ PICNIC·Honeys) 실측 — `specs/candidates/`, 결과·외삽은 `50_실사용_도입설계.md` 2절. **n=3(목표 5에 못 미침)** — 토큰 비용은 측정 안 하고 웹 요청 횟수·벽시계 시간만 실측. 부수 발견: 3곳 전부 한국 진출 신호 0/3 → 리스크 24와 연결. **2026-08-04 추가 — n=3→n=6.** 화장품·과자·여성의류 3곳을 추가 실측해 업종 표본이 패션 단일에서 패션·뷰티·푸드 3종으로 늘었다. 이번 실측에서 이 프로젝트 최초의 실제 「판정 불가」 사례(상품 목록 스크립트 렌더링)가 나왔고, O8(광고 메일 거부 표시)은 6곳 전부 물음표로 나와 판별 문구를 테스트할 표본이 아직 없다는 것도 함께 확인됐다(`40` V9 표본 조건에 반영) | project-critic 치명 13 |

### 2-5. 2026-08-01 `project-critic` 감사(불필요한 검증 게이트 점검)로 새로 등록된 리스크

> **사용자 지시의 방향**: 자동화의 목적이 넓게 뿌리는 것인데, 근거 없는 검증·관찰이 후보를 사전에 다 걸러내면 브리프 요구 ①(자동 탐색·목록화)이 성립하지 않는다. **아래 중 A-1·E-1·E-2는 그 방향과 직접 겹쳐 바로 반영했고, 나머지는 후보 물량을 줄이는 게이트가 아니라 남겨 두거나 별도 판단으로 미뤘다.**

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 30 | **O6 규모 신호 중 ⓒ資本金·ⓓ従業員数·ⓔ설립연도가 기업 발언 근거 없이 필수 관찰이었고, 걸러내는 데도 안 쓰이는데 웹 요청을 2배로 늘린 것이 실측됐다** | **반영 완료 (2026-08-01).** ⓐ상품개수·ⓑ채널수만 필수로 남기고 나머지 셋은 선택 관찰로 하향 — `기준/G1` 2-1절·변경이력, `specs/G1` | project-critic A-1 |
| 31 | **G5 번역 검증 3층 중 "어조·방향"이 회신 해석(거절↔관심)의 판정 문장을 그대로 발신 메일의 무관용 기준으로 썼다** — 발신 메일에는 거절·관심이라는 대상이 없어 판정 불가능한 조건이었다 | **반영 완료 (2026-08-01).** `기준/G7` 3절에 G5 전용 판정 문장(정중함 붕괴·단정 뒤집힘) 신설, `specs/G5` 성공 기준 문구 정정 | project-critic E-1 |
| 32 | **V1-a 표본 수가 문서마다 다르다(10곳 vs 5~10곳)** — O7 강등 조건("10곳 모두 관찰률 0")이 실제 계획("5~10곳")과 안 맞아, 강등 여부를 판단할 수 없는 상태였다 | **반영 완료 (2026-08-01).** 리스크 24 처리로 조건4 자체가 삭제돼 이 불일치도 함께 해소 — `기준/판정시트_G1.md`에 "이미 결정됨" 안내로 교체 | project-critic E-2 |
| 33 | **A-2(G8 원인 분석 판정 근거 취약) · B군(G6·G8 근거 없는 임계값 3건) · C-1(국가 확대 전제의 순환 근거) · C-2(G10 트리거 부재) · D-1(G11 브리프 대응표 미표시) · F군(경미 5건)은 이번에 손대지 않았다** | **검토 보류.** 전부 **후보를 사전에 걸러 물량을 줄이는 게이트가 아니다** — 응답 이후 처리 로직(G6·G8)이거나 표시·라벨링 문제(C-1·C-2·D-1)라 이번 우선순위(검증 게이트 축소)와 결이 다르다고 판단했다. 다음에 다룰 때는 `project-critic` 원본 보고(2026-08-01 세션)를 그대로 근거로 쓴다 | project-critic A-2·B·C-1·C-2·D-1·F |

### 2-6. 2026-08-03 MVP 재정의 — G3 이연으로 대체 규칙이 필요해진 지점

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 34 | **G3(적합도 순위)을 부가 기능으로 이연하면서, G4 탐색 순서·G5 발송 상한 컷오프가 G3 출력에 의존하던 자리가 비게 됐다** — 대체 규칙 없이 그냥 끄면 두 기능의 성공 기준이 판정 불가능해진다(`90` 리스크 20이 되살아나는 형태) | **반영 완료 (2026-08-03).** G4는 "G2 세그먼트 대표 기업 우선"으로, G5는 "세그먼트별 라운드로빈"으로 대체 — `specs/G4`·`specs/G5`. G8·G9·G10·G11의 G3 의존 항목은 그 넷 자체가 부가 범위라 잠정 규칙(低 기본/소스 제외)으로만 처리. 근거는 `33` D15 | 사용자 결정 + opus 사전 검토 |
| 35 | **MVP가 내는 출력 중 4종(G4 [미확보]·[메일 없음] / G1 판정 불가 / G2 U-정보없음 / G6 무응답·주소오류)의 선언된 소비처가 전부 부가 이연 기능(G9·G10)이다** — MVP 기간에는 그 잔여가 담당자 화면 어디에도 뜨지 않고 조용히 묻힌다. 특히 **세그먼트 대표 기업이 [미확보]면 그룹 전체 발송이 멈춘다**(`specs/G4`가 대표 우선 탐색의 전제로 깔아 둔 논리의 이면) | **반영 완료 (2026-08-03, 사용자 결정). 형식 확정(2026-08-03 2차)** — 별도 파일 `specs/candidates/보류목록.md`에 append(G1의 `후보목록.md` append-only 제약과 소유권이 겹치지 않도록 분리). 지금은 `specs/G4`가 [메일 없음]·[미확보]를 이 파일에 쓰도록 반영됨 — G1 판정불가·G2 U-정보없음은 아직 미반영(다음 작업). **협업사 등을 통한 추가 컨택 시도 여부는 기업이 직접 판단할 문제**이므로 우리 쪽 책임은 "잃어버리지 않고 남긴다"까지로 한정한다. 대표 기업 [미확보] 시 그룹 차선 대표를 세우는 규칙은 `기준/G2` 3절에 추가 | 사용자 지시 (`project-critic` 치명 4) |
| 36 | **O8(광고 메일 거부 표시)이 `?`인 회사가 흔해서, 그룹화(G2) 대표 선정 규칙("관찰 값이 빠짐없이 채워진 곳")에 걸려 대표 후보가 0곳이 되는 경우가 실측(재판정 3건 전부)에서 나타났다** | **반영 완료 (2026-08-03, 사용자 결정) — 2단 처리.** ① **G1 단계로 이동**: O8=있음이 **확인**되면 그 즉시 `발송금지.md`에 등재하고 그룹화 대상에서 제외한다(`기준/G1` 2-2절) — 위법 판정을 G4까지 미룰 이유가 없는 확정 사례이므로. **O8='?'(주소를 못 찾은 경우 포함)는 조기 차단하지 않는다** — G1은 얕게만 보고 딥서치는 G4의 일이라 '?'가 곧 위법 확정은 아니다. ② **대표 선정 규칙 자체도 재설계**: O8·O9(문안과 무관한 항목)를 대표 자격 요건에서 빼고, 축 관련 항목(품목·규모)이 가장 구체적으로 채워진 곳을 대표로 삼는다. 동시에 "메일 형식 유사"(자료 밀도 상세형/표준형)를 배정마다 표시하는 개념을 신설 — 새 분류축이 아니라 문안 수위 조절 신호다(`기준/G2` 3절). **정확한 저장 파일·형식(DB·md 스키마)은 사용자가 후속으로 직접 설계하기로 함** — 여기서는 로직·원칙만 확정 | 사용자 지시, sequential-thinking으로 대표 사례(재판정 3건) 검증 |

| 37 | **도구 권한으로 발신을 막는 장치가 서브에이전트까지만 걸린다** — G7 실행체를 만들며 실물로 확인됐다. `g7-회신해석`·`g7-번역검증`은 `tools:`로 발신 도구를 물리적으로 배제했지만, **두 단위를 부르는 오케스트레이터(`g7-회신처리`·`g1-기업판정`)는 세션 권한 그대로 돈다.** `specs/G7` 「도구 권한」 경계의 *"사후 점검이 아니라 권한으로 막는다"*는 주장이 실행 단위에는 참이고 오케스트레이터에는 규칙 수준이라는 뜻이다 | **해결 완료 (2026-08-05).** SKILL.md frontmatter가 `disallowed-tools`를 지원함을 문서로 확인(활성 중인 스킬의 도구 풀에서 실제로 제거되는 필드 — `allowed-tools`는 사전승인일 뿐 제한이 아니다). `g1-기업판정`에 `disallowed-tools: Bash, PowerShell, Agent`(Spec의 "메일 발송·외부 전송 도구 접근 금지"+단일 실행 단위 원칙을 물리적으로 강제), `g7-회신처리`에 `disallowed-tools: Bash, PowerShell, WebFetch, WebSearch`(Spec의 "발신 도구 권한 없음"+회신 텍스트만 처리 원칙을 강제, `Agent`는 T/V 호출에 필요하므로 남김)를 추가했다 | `change-reviewer` 2026-08-05 §5 → `claude-code-guide` 확인 → 반영 완료 |

| 38 | **식별자 조인 붕괴 위험(G7 발신 도메인 폴백 `-미확정`이 G1 식별자와 불일치할 수 있음)의 소비처가 G11 하나에서 둘로 늘었다** — G12(질의응답, 2026-08-05 신설)도 기업식별자로 여러 산출물을 조인해서 읽는다. 이 위험 자체는 이미 `33` D14에 기록돼 있었지만 리스크 표에는 등록되지 않아 두 번째 소비처가 생겨도 놓치기 쉬운 상태였다 | **반영 완료 (2026-08-05).** G12도 G11과 동일한 완화책(`-미확정` 레코드는 기업 단위 조인에서 제외하고 "식별 불가"로 별도 표시)을 `specs/G12` 제약에 명시했다. 새 완화책을 발명하지 않고 기존 것을 재사용 | `33` D14·**D20**, `specs/G11`·`specs/G12` |

### 2-7. 2026-08-06 `project-critic` 전체 검토(MVP+부가 9개 실행체 완비 후 첫 전체 감사)로 새로 등록된 리스크

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 39 | **브리프 요구 ①의 "자동 탐색" 절반(명단 수집, `specs/G1_대상기업발굴.md` 「입력」의 단위 L)에 실행체가 없다.** `g1-기업판정` 실행체는 기업 판정(단위 J)만 하고, URL·기업명을 어디서 몇 백 개 모아올지는 사람이 손으로 한다 — `.claude/README.md`가 "G1은 실행 단위 1개"라 적은 것 자체가 Spec의 J·L 두 단위 선언과 어긋난다. 실제 후보 8곳도 원래 채택한 소스(Qoo10·라쿠텐, `docs/11:234`)에서 나온 게 확인되지 않는다 | **부분 반영 (2026-08-06).** ① `docs/11:236`의 "N28 신설 필요, 아직 질문지에 없음"이 **사실 오류였다** — `12` N28은 이미 존재했다(질문 자체는 있고 답만 대기). 정정 완료. ② `.claude/README.md` G1 행 + 실행 단위 요약 문장 + `CLAUDE.md` 7절을 "1개"에서 "J만 채운다, L은 실행체 없음"으로 정정. **단위 L 실행체 자체는 아직 안 만들었다** — `12` N28 답(소스 확정)이 와야 만들 수 있어 착수 조건 미충족으로 남긴다 | project-critic 치명 1 |
| 40 | **발송 대기 중인 실제 문안(`specs/발송대기/S1-2026-08-06.md`)에 회사가 승인한 적 없는 상업적 약속·역량 과장이 들어 있다.** "샘플 비주얼 1건 무상 제작"(회신마다 회사 자원 지출 약속) / "대량 상세페이지 자동화를 이미 하고 있다"(`docs/10_기업조사.md:92`는 "추진 중"으로만 기록). `specs/G5_제안메일제작발송.md` 성공 기준에 "회사가 확인한 서비스 범위 밖 주장·무상 제공 금지" 항목이 없다 | **반영 완료 (2026-08-06).** `specs/G5_제안메일제작발송.md` 「제약」·「성공 기준」(무관용)에 항목 신설(6번째), `12` Q16에 "무상 제공 약속 가능 여부" 추가, `specs/발송대기/S1-2026-08-06.md` 본문 두 곳을 논의 제안 톤으로 정정 — 단 **그 파일의 번역 검증 3층 결과는 정정 전 문구 기준이라 재검증 전에는 승인 대상이 아니라고 파일에 명시**했다(재검증은 실제 `g5-번역검증` 호출이 필요해 이번엔 안 돌렸다). `기준/G7` 3절(G5 재사용 대상)의 "정중함·단정 뒤집힘" 판정 문장에 상업적 약속 여부는 원래 대상이 아니었다 — 새 항목은 스킬 자체 점검(서식 점검이 아니라 발송 모드 무관용)이 담당한다 | project-critic 치명 2 |
| 41 | **G5 승인 게이트가 이 저장소 자신의 기준(리스크 37)으로도 "권한이 아니라 지침" 수준으로만 막혀 있다.** 리스크 37에서 "오케스트레이터 도구 차단은 규칙 수준"이라 이미 문제 삼아 놓고, `g5-제안메일제작발송/SKILL.md`는 승인란 판독(발송 모드)과 발송 실행을 같은 실행 단위가 한다 — "승인란을 스스로 채우지 않는다"가 지침 한 줄로만 지켜진다. G5 자신이 문안 제작에는 적용한 "게이트를 통과시키는 쪽이 만든 쪽이면 게이트가 아니다"(`:22`) 원칙을 승인 게이트 자체에는 안 걸었다 | **반영 완료 (2026-08-06, 사용자가 구조적 분리 선택).** `g5-승인확인`(Read 전용, Write·Edit 없음) 신설 — 발송대기 파일의 승인란만 읽어 승인/거부/미승인 판정을 반환하고, 발송 권한(Write)을 쥔 `g5-제안메일제작발송` 스킬은 그 판정을 그대로 따르기만 한다(승인란을 직접 읽지 않는다). `specs/G5_제안메일제작발송.md` 「제약」·「실행체 경계 4개」, `.claude/skills/g5-제안메일제작발송/SKILL.md`(실행 단위 넷으로), `.claude/README.md`(에이전트 표·모델 절·단위 수 요약) 갱신. **실물 검증은 아직이다** — 실제 발송 인프라가 없어(발송 = 파일 쓰기로 대체 중) 이 분리가 막는 시나리오 자체가 지금은 발생할 수 없다. **`change-reviewer` 좁은 검토(2026-08-06) 완료** — 낡은 개수 표기 1건(`.claude/README.md` "두 질문 프레임워크" 단락이 "셋"으로 남아 있던 것) 발견·정정. 같은 검토가 **미해결 질문 하나를 남겼다** — `g5-승인확인`이 막는 패턴(판정하는 자리와 집행하는 자리가 같으면 안 된다)이 기존 두 자기검증 유형(①검증자가 결론을 봄 ②생성자가 앞 회차를 봄) 어디에도 깔끔히 안 들어맞는 **세 번째 결**로 보인다는 것. 공식 유형으로 편입할지는 `.claude/README.md`의 그 절 자체를 다시 쓰는 판단이라 이번엔 정하지 않고 관찰만 남겨 뒀다 | project-critic 치명 3 |
| 42 | **중대 4건 — 2026-08-06 등록만 하고 미뤘던 것을 이번에 처리한다.** ① G2 세그먼트 재사용이 실물로 0회 검증(`docs/40` V7이 MVP 최우선 표시인데 `90` 3절 액션 목록에 없음) ② `발송금지.md` 경로가 쓰는 쪽(`g7-회신처리`, 접두어 없음)과 읽는 쪽(`g4`·`g5`·`g10`, `specs/` 접두어) 사이에서 갈린다 — 위법 재발송 위험 ③ `docs/50_실사용_도입설계.md` 하루 부담 산정이 G5 승인 1스텝만 세고 실제 호출 여러 종(G1·G4·G5·G6 반복 포함)을 안 셈 ④ 기업이 직접 요구한 "대시보드"(`13S` F9·F22) 실물이 0건, 그 창구로 지목된 G12는 이번에 스킬만 생겨 아직 실행 0건 | **① 부분 반영.** `docs/40` V7 행에 연결 — 형식 검증 5항목 중 4개는 이미 `specs/segments/SEG-A-v1.md`(2026-08-05 3차 실행)에 수치가 있었다(U-단독 4/6=66.7%, 가장 큰 무리 2/6). "0회 검증"은 정확히는 "실행은 됐는데 V7에 연결·기록이 안 됐다"였다. **다섯째 항목(두 사람 대조)은 여전히 미실행 — 진짜 남은 일**. **② 반영 완료.** `.claude/skills/g7-회신처리/SKILL.md`·`.claude/agents/g7-회신해석.md`·`.claude/README.md`의 `발송금지.md`·`replies/` 경로를 전부 `specs/` 접두어로 명시(G4·G5·G10과 통일). 실물 파일이 아직 없어 지금 당장 갈라진 상태는 아니었지만, G7이 처음 돌 때 다른 자리에 썼다면 그때 발견하기 어려웠을 것 — 사전에 닫음. **③ 반영 완료(수치는 채우지 않음).** `docs/50` 1-2절에 캐벗 추가 — 현재 표는 승인 리뷰 시간만 세고, `run-orchestrator` 미가동으로 사람이 각 스킬을 직접 호출하는 지금 상태의 호출·확인 시간은 안 세고 있다는 것을 명시. **실측 없이 숫자를 만들지 않았다** — `run-orchestrator` 가동 후 재실측 대상으로 남긴다. **④ 재판정 — 원래 지적이 틀렸다.** 사용자 확인 결과 "대시보드"는 **기존 산출물(specs/ 각 디렉터리)을 웹으로 재노출하는 화면**이지 새 기능이 아니다 — 이미 `docs/34_사용자흐름.md` 7절·`docs/33` D20이 "화면 자체는 새 산출물을 안 만들어 번호를 안 받는다"로 확정해 뒀다. G12(질의응답)를 대시보드의 "창구"로 동일시한 것이 이 지적의 착오였다 — 웹 ⊃ (대시보드 + G12)이지 웹 = G12가 아니다(`34`:84). **남는 것은 설계 공백이 아니라 순수 구현 범위 문제**(⑤ 단계, 웹을 언제 만들 것인가)이며 지금 결정할 사안이 아니다 | project-critic 중대 4·5·6·7 |

### 2-8. 2026-08-10 Hook·API 더미 신설 + `project-critic`·구현 배선 감사로 새로 등록된 리스크

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 43 | **무인 실행(Hook) 권한이 열리면 발송 모드도 같은 권한으로 열릴 수 있다 — 리스크 37·41과 같은 유형인데 등록이 없었다.** `Harness/hook/hook_h0*.sh`가 `claude -p`로 스킬을 호출할 때, 그 호출이 도구 사용 승인을 요구하면 Hook은 멈추고, 승인을 무인 배치용으로 우회 설정하면 그 설정은 `/g5-제안메일제작발송` **발송 모드**에도 그대로 적용된다. 지금 완전자동 발송을 막는 것은 H6 훅이 인자에 "제작"만 넣는다는 관례 하나뿐이다 — 리스크 37·41이 "지침이 아니라 권한으로 막아야 한다"고 이미 두 번 판정한 바로 그 형태의 방어선이다 | **부분 반영 + 2026-08-11 실제 라이브 테스트로 검증 완료.** H12(발송 승인 게이트)를 `.claude/settings.json`+`.claude/hooks/`에 실제 등록해 이 저장소 자신에서 살아있게 만들고, 진짜 Edit 도구로 승인란을 고쳐 실제로 발동하는지 확인했다 — 감지→`/g5-제안메일제작발송 발송` 실제 호출→`g5-승인확인` 실제 판독→승인자·승인일시 미기재로 "미승인" 정확히 판정→발송 거부까지 **전 구간 실제 동작 확인**(합성 테스트 파일 대상, 실 데이터 무영향). 테스트 중 실제 버그 1건 발견·수정 — Windows에서 Claude Code가 넘기는 `file_path`가 백슬래시 경로라 슬래시만 보던 원래 조건문이 아예 안 걸렸다(H9·H12 라이브 사본, `Harness/hook/` 원본 3개 스크립트 전부 수정). **이건 "무인 실행 자체를 막는" 게 아니라 "무인 실행이 언제 열리는지를 사람의 승인 편집 시점으로 코드로 좁힌" 것**이다 — 실제 승인·무관용 재확인은 여전히 발송 모드+`g5-승인확인`이 한다. **남는 전제**: 승인란을 Claude Code로 직접 고칠 때만 작동하고, 나중에 별도 화면(웹)으로 승인을 처리하게 되면 이 방어선도 다시 뚫린다 — `web/dispatch.py`(A안, 웹 서버가 원자적으로 처리)가 그 경우의 대안으로 이미 설계·구현돼 있다 | project-critic 2026-08-10 치명 7, 사용자 지시로 H12 신설 + 2026-08-11 실제 라이브 검증 |
| 44 | **`Harness/data/`(제출 스냅샷)의 발송기록·발송대기 2쌍에 가짜 수신거부·발신자 주소가 실재했고, 승인본(서울)과 발송본(제주)의 법정 표시가 서로 달랐다.** `docs/92` 4절의 무관용 예외("실제 값 오기 전엔 채우지 않는다")가 `specs/` 원본에만 적용되고 스냅샷 사본은 감시 범위 밖이었다 | **반영 완료 (2026-08-10).** 4개 파일에 "합성 데모용 예시값" 경고를 추가하고 서울→제주로 통일. `docs/92` 4절에 `Harness/data/` 행 추가 — 실제 값이 와도 이 스냅샷은 자동 교체 대상이 아니라는 것도 명시 | project-critic 2026-08-10 치명 1 |
| 46 | **`run-orchestrator`가 조사 리듬(G1)을 한 번도 실행하지 못했다 — 자기 자신의 `tools:`에 `WebFetch`·`WebSearch`가 없어서였다.** `Skill` 도구는 스킬 카드의 지침을 컨텍스트에 불러올 뿐 새 도구 권한을 주지 않으므로, 호출자의 도구 목록에 웹 접근이 없으면 G1·G4처럼 공개 페이지를 직접 읽어야 하는 스텝은 아예 실행되지 않는다. 지금까지 G1·G4가 손으로(사람 또는 `harness-builder` 대행)만 실행돼 왔기 때문에 이 공백이 드러난 적이 없었다 | **해소 완료 (2026-08-11).** `g1-기업판정`·`g4-연락처확보` 스킬 자신의 `disallowed-tools`(Bash·PowerShell·Agent만 차단, 웹 접근은 원래 허용됨)를 대조해 웹 도구 추가가 안전함을 확인한 뒤 `.claude/agents/run-orchestrator.md`에 `WebFetch`·`WebSearch` 추가. 정의 수정이 같은 세션 안에서는 반영 안 됐으나(서브에이전트 정의는 세션 시작 시 로드), **사용자가 새 세션을 열어 재실행하자 실제로 반영돼 G1(신규 기업 titivate.jp 실제 웹조사)→G2(세그먼트 갱신)→G4(연락처 확보, [메일 없음]·[확보] 둘 다 실제 판정)→G5(제작 모드, 번역 검증까지)가 전부 실제 슬래시 경로로 자동 연쇄되어 정확히 설계된 정지점(`specs/발송대기/S1-2026-08-11.md`, 사람 승인 대기)에서 종료됐다.** 이 과정에서 파생 리스크 2건 추가 발견·해소(아래 47·48) | run-orchestrator 2026-08-11 실전 테스트 1~4차, 전 구간 실물 검증 완료 |
| 47 | **G2 대표 선정 순서 1~3을 다 적용해도 동률이 나는 실제 사례(ingni-store-com ↔ titivate.jp)가 나왔다 — 카드에 순서 4가 없어 실행체가 "판정 보류"로 멈췄고, 그 여파로 G4 게이트에도 없는 케이스가 생겼다** | **해소 완료.** `기준/G2_세그먼트기준.md` 3절에 순서 4(동률이면 후보목록 등재 순서가 앞선 곳) 신설 — 새 판단 기준을 발명하지 않고 이미 있는 객관적 사실 하나만 추가. 재실행으로 실제로 동률이 갈리는 것까지 확인(`docs/93` #19 등록) | run-orchestrator 3·4차 |
| 48 | **G2 "차선 대표 승격" 규칙이 [미확보]만 지목하고 [메일 없음]은 안 다뤘다** — 실제로 대표가 [메일 없음]으로 판정되는 사례가 나오면서 드러남 | **해소 완료.** 둘 다 "이 주소로는 못 보낸다"는 결과가 같아 발명이 아니라 기존 규칙의 자연스러운 확장으로 처리 — `기준/G2_세그먼트기준.md` 3절 문구 확장 | run-orchestrator 4차 |
| 45 | **H7(재수집 30일 주기)과 G10 승격 조건(14일 지속)이 서로 어긋난다 — 30일마다만 재확인하면 14일 조건이 상시 참에 가까워진다. 같은 자리에서 "2회 연속"(구 값)과 "3회째"(`specs/G10` 2026-08-10 확정)도 문서마다 갈려 있었다.** | **부분 반영.** "2회 연속"→"3회째" 표기는 `hook_list.md`·`Harness/hook/h07-*.md`·`Harness/docs/design-rationale.md` 전부 통일했다. **30일/14일 상충 자체는 미해결 — 사람이 정할 값**으로 `docs/93_구현_검증대기.md`에 등록했다(#17) | 구현 배선 감사 2026-08-10, project-critic 치명 4 |
| 49 | **`specs/contacts/orbis-co-jp.md`에 합성 데모용 [미확보] 임시 값이 남아 있다** — G9(위임 브리프)를 실물로 시연하려고 사용자 지시로 만든 값인데, 이 세션에 파일 삭제 도구가 없어 지우지 못했다 | **해소 완료(2026-08-11) — 사용자가 `rm specs/contacts/orbis-co-jp.md`를 직접 실행해 삭제.** 에이전트에게 삭제 도구가 없을 때는 `!` 접두어로 사용자가 직접 셸을 돌리는 경로가 있다는 것을 이번에 확인했다. 결과물(`specs/위임브리프/orbis-co-jp.md`)은 그대로 유지 — 이 값이 합성이었다는 사실은 이미 그 파일 머리에 명시돼 있다 | G9 실물 시연 2026-08-11 |
| 50 | **U2(승인/거부 게이트)의 거부(반려) 경로가 한 번도 클릭 테스트되지 않았다** — 웹 대시보드 승인 화면에서 승인 버튼 클릭·파일기록·발송트리거는 Playwright로 실물 검증됐지만 거부 버튼 경로는 안 돌려봤다 | **해소 완료(2026-08-12).** claude-in-chrome으로 실제 거부 버튼을 클릭해 검증(거부 사유는 브라우저 native `prompt()`가 자동화를 막을 수 있어 `window.prompt`를 세션 한정 스텁으로 대체해 입력). 파일에 거부 사유가 정확히 기록되고, 발송 트리거는 걸리지 않으며(`dispatch.py`가 승인일 때만 트리거하는 코드 그대로 확인), 화면에서 버튼이 사라지고 "현재 상태: 거부"로 표시되는 것까지 전부 확인 | 진입점 전수 감사 2026-08-11, 실물 검증 2026-08-12 |
| 51 | **더미 Hook 10종(H1~H8·H10·H11) 중 H2·H5·H6·H7·H8·H10·H11 일곱 개가 이 세션에서 실행 검증되지 않았다** — H1·H3·H4는 이전에 피봇 JSON으로 파싱 로직까지 확인했으나, 나머지 일곱은 이번 세션에 Bash/PowerShell 권한이 막혀 코드 읽기로 구조만 대조했다(동일 패턴 확인 — stdin JSON→jq 파싱→`claude -p`→로그) | **해소 완료(2026-08-13).** H5는 2026-08-12에 먼저 해소됐고(아래 53·54·55), 나머지(H2·H6·H7·H8·H10·H11)도 2026-08-13 전수 실물 검증으로 마무리됐다(리스크 60·61) — 이로써 Hook 12종 전부 최소 한 번은 실물 검증됨 | 진입점·Hook 전수 감사 2026-08-11, H5 실물 검증 2026-08-12, 나머지 전수 검증 2026-08-13 |
| 53 | **H5·H11 라이브 훅에 실제 버그 발견 — `jq \| while read` 파이프 안에서 백그라운드 `claude -p ...&`를 stdin 리다이렉트 없이 띄우면 그 자식이 파이프의 나머지 JSON을 가로채 루프가 조기 종료된다** — 실사용 중 24건 중 5건만 처리되고 멈추는 것으로 실제 재현됨 | **해소 완료(2026-08-12).** 루프 입력을 프로세스 치환(`< <(...)`)으로 바꾸고 `claude` 호출에 `< /dev/null`을 명시해 두 배로 막았다. 나머지 19건 재실행에서 24건 전부(이번 배치 19건) DISPATCHED 정상 확인 | H5 실물 시연 2026-08-12 |
| 54 | **마켓플레이스 입점 URL(라쿠텐 `rakuten.co.jp`)이 WebFetch 수준에서 구조적으로 차단된다** — H5 실물 테스트로 24건 중 19건이 전부 같은 이유(403/WAF)로 "판정 불가"였다. claude-in-chrome(실제 브라우저 세션)으로 같은 사이트의 다른 페이지(랭킹)는 문제없이 열렸던 것과 대조된다 — WebFetch가 자동화 트래픽으로 식별돼 막히는 것으로 보인다 | **미해결 — 실사용 데이터 소스 제약으로 기록.** G1은 `disallowed-tools`로 WebFetch·WebSearch만 허용돼 있어 이 제약을 스킬 자체로는 못 피한다(브라우저 자동화 허용은 스킬 권한 경계를 새로 여는 결정이라 이번엔 손대지 않음, 사용자 확인). **성공 사례 3건**(`yokumoku-co-jp`·`chakashodina-com`·`radianne-jp`)은 G1이 라쿠텐 URL 차단 시 검색으로 그 기업의 **자사 공식 홈페이지**를 찾아 그쪽에서 관찰에 성공한 경우 — 마켓플레이스 입점 기업이라도 자사 도메인이 있으면 이 경로로 우회 가능하다는 것을 실측으로 확인. **2026-08-13 — 이 우회를 정식 절차로 승격.** 트리거를 "라쿠텐이라서"가 아니라 "접근 차단 응답(403 등)이면"으로 일반화해 `기준/G1_기업판정기준.json`(`접근차단_자사도메인재시도` 신설)·`specs/G1_대상기업발굴.md`(실패 3종 표 참조 추가)·`.claude/skills/g1-기업판정/SKILL.md`(실행 절차 1번 신설)에 반영 — 소스가 라쿠텐이든 Qoo10(`docs/11` 9절, 이미 채택돼 있으나 실행 이력 0건)이든 동일 적용된다. 크롬 직괄(대화형 세션 한정)이 도입되면 이 우회 자체의 필요성이 줄어들 수 있다는 것은 여전히 미해결로 남는다. **2026-08-13 — 같은 차단이 `scripts/crawl_leads.py`(H5의 신규 후보 소스, Python `requests` 직접 호출)에서도 재현됨을 실측 확인.** 라쿠텐 랭킹 페이지는 타임아웃/403으로, Qoo10 카테고리 페이지는 200 상태에 523(오리진 연결 불가) 안내 페이지만 담겨 옴 — WebFetch뿐 아니라 일반 HTTP 클라이언트도 같은 방식으로 막힌다는 뜻이다. 스크립트 자신은 0건을 지어내지 않고 정직하게 보고하도록 이미 설계돼 있어 크래시는 없다 — 실제 후보가 나오려면 브라우저 자동화(Playwright 등) 경로로 바꾸거나 사람이 URL을 직접 확인해 넣어야 한다. **2026-08-13 후속 — 멘토 제안으로 Jina AI Reader(`https://r.jina.ai/`)를 앞에 붙여 우회, 실제로 차단이 풀리는 것을 확인함.** 원본 URL 대신 `https://r.jina.ai/{원본 URL}`로 요청하면 Jina가 실제 렌더링(JS 포함) 후 정리된 Markdown을 돌려준다 — 라쿠텐 랭킹 페이지에서 실제 스토어 링크(`item.rakuten.co.jp/darkangel` 등 이미 처리한 실기업 포함)를 두 차례 성공적으로 추출 확인. `crawl_leads.py`를 이 경로로 전환(파싱도 BeautifulSoup HTML 매칭에서 Markdown 링크 정규식으로 전환). **다만 Jina 무료(비인증) 등급에 레이트리밋이 있다** — 실측 중 반복 요청(수 분 내 8~10회)으로 403이 나기 시작했고, 45초 이상 간격을 두면 다시 200이 나오는 것으로 봐 짧은 창의 버스트 제한으로 추정(확진 아님). **주 1회 예약 실행(요청 2건/주)에는 사실상 문제가 안 될 수준**이나, API 키 없이 쓰는 이상 재시도로도 못 뚫는 순간이 있을 수 있다 — 필요하면 무료 API 키 발급 후 `.env`에 저장해 헤더로 실어 한도를 늘리는 것을 다음 단계로 남긴다. Qoo10은 차단은 풀렸지만 지금 크롤링 대상 URL(`cate_shop_no=1`) 자체에 개별 판매자 링크가 없어(카테고리 트리만 있음) 별도로 더 적합한 URL을 찾아야 한다 — 이건 차단과 무관한 별개 문제. **2026-08-13 — 사용자 결정: Qoo10 자체를 안 쓴다.** 후보 명단 채우기(현재 사람이 하는 일)를 별도로 자동화 중인데 그 경로가 라쿠텐 API를 쓰기로 해, 결과적으로 라쿠텐 계열만 다루게 된다 — Qoo10 크롤링 함수는 더 손대지 않는다(코드는 남겨 두되 우선순위 없음). **Jina API 키는 사용자가 직접 발급받아 관리한다** — 이 세션은 키 값을 다루지 않는다(`CLAUDE.md` 6절) | H5 실물 시연 2026-08-12, 절차 정식화 2026-08-13, `crawl_leads.py` 실행 검증 2026-08-13, Jina Reader 우회 확인 2026-08-13 |
| 55 | ~~무인(백그라운드) G1 실행이 애매한 판단 지점에서 응답자 없이 멈춘다~~ — 24건 중 5건(radianne·TeddyShop·Rakuten Fashion·azusa·imokuniya)이 산출물을 못 남김 | **진단 정정(2026-08-12) — "대화형 되물음에 멈춤"이 아니라 사용량 한도(spend limit)였다.** 로그에 "You've hit your individual spend limit" 문구가 그대로 남아 있었다 — 24건을 한꺼번에 병렬로 백그라운드 실행한 것이 원인으로 추정된다(확진은 아니나 정황상 유력). **5건은 병렬 부하 없이 순차로 직접 재실행해 전부 완료 처리했다** — 1건 포함(`radianne-jp`), 4건 판정불가(자사 도메인 없음 2건, 도구 응답 실패 1건, 그리고 리스크 56의 새 유형 1건). 재발 방지책(동시 실행 수 제한 등)은 실제 팀원 플랫폼이 붙을 때 함께 설계할 사안으로 남긴다 | H5 실물 시연 2026-08-12, 잔여 5건 순차 재실행 2026-08-12 |
| 57 | **G2 대표 선정 순서 2(규정의 한가운데)가 "카테고리 전용" 무리 규정에는 적용 불가 — 실측으로 처음 확인** — 카드 3절 순서 2는 무리 규정이 **대상군**(성별 등) 또는 **규모 구간**일 때만 판별선이 있는데, 신설된 S2(뷰티)·S3(푸드)는 업종 카테고리만으로 정의돼 둘 다 아니다 | **해소 완료(2026-08-12, 사용자 재지적으로 정정).** 처음엔 "판정 보류"로 멈췄으나, 사용자가 "그럼 결과물이 안 나온 거고 목표를 못 이룬 것 아니냐"고 지적 — 재검토한 결과 순서 2의 **원래 목적**(이질적인 다른 축을 함께 취급하는 곳을 대표에서 내린다)은 대상군·규모 구간이 그 목적의 예시 두 가지일 뿐이라고 판단, 같은 목적을 "카테고리 순수성"(그 업종 하나에만 집중하는가)으로 카테고리 전용 무리에도 그대로 적용했다 — 축을 하나 더 만든 게 아니라 기존 "경계 판별" 방식을 그대로 대입한 것. 결과: **S2 대표=orbis-co-jp, S3 대표=yokumoku-co-jp 확정**(`specs/segments/SEG-A-v1.md` 6차 실행 참조). 이 일반화를 카드 3절에 명문화할지는 여전히 사람이 정할 문제로 남는다 | G2 6차 실행(뷰티·푸드 무리 신설) 2026-08-12, 재선정 2026-08-12 |
| 58 | **웹 승인화면과 실제 G5 산출물의 표기 형식이 처음부터 안 맞았다** — `web/parsers.py`·`web/dispatch.py`는 "## N절 승인 대기 건 — #N id" 헤더 + 승인란이 표(table)인 것을 전제로 만들어졌는데(웹 개발 당시 손으로 만든 `web/test-fixtures` 기준), 실제 `g5-제안메일제작발송` 스킬 산출물(run-orchestrator 실행분 포함)은 "## 건 N — id (역할)" 헤더 + 승인란이 인용구(`> 빈칸`)였다. 이 상태로는 **웹에서 승인 버튼을 눌러도 항목 자체가 안 잡히거나, 잡혀도 파일에 아무것도 기록되지 않았을 것**이다 | **해소 완료(2026-08-12) — 실물 확인 완료.** `web/parsers.py`·`web/dispatch.py`의 헤더 정규식을 두 표기 다 받도록 수정, 제목 추출 정규식에 `**일본어 제목**:` 형태 추가. `.claude/skills/g5-제안메일제작발송/SKILL.md`에 "승인란은 반드시 표로 쓴다 + 건 헤더 형식 고정" 경고를 추가해 재발을 막았다. **같은 날 claude-in-chrome으로 실제 웹 화면에서 `radianne-jp`(S1-2026-08-12.md) 승인 버튼을 직접 클릭해 검증** — 파일에 승인 기록이 정확히 쓰였고(`network_requests`로 `POST /approvals/decide` 200 확인), `DISPATCH_MODE=real`이 `claude -p /g5-제안메일제작발송 발송 ...`을 실제로 스폰해 발송 모드까지 이어졌다(법정 표시 미확인으로 정상 거부됨 — 아래 59) | S1-2026-08-12 발송대기 작성 중 사용자 질문으로 발견, 실물 확인 2026-08-12 |
| 59 | **승인 대기함(`/approvals`)이 실제 발송 여부(`발송기록/`)와 연동돼 있지 않았고, G5 발송 모드에 "이미 보낸 건 재발송하지 않는다"는 멱등성 체크가 없었다** — 위 58 실물 검증 중 사용자가 "승인/미승인 + 보낸 것/안 보낸 것 구분이 되냐, 안 되면 보낼 때마다 다시 보내는 것 아니냐"고 질문해 코드를 직접 대조하다 발견. `web/parsers.py`의 `parse_approvals()`는 `발송대기/*.md`의 승인란만 보고 `발송기록/`은 전혀 조회하지 않았고, `specs/G5_제안메일제작발송.md`·스킬 절차 어디에도 발송기록 존재 여부를 먼저 확인하는 단계가 없어 같은 파일에 발송 모드를 두 번 트리거하면(웹 중복 트리거·사람의 수동 재실행 등) 중복 발송이 그대로 일어나는 구조였다 | **해소 완료(2026-08-12).** 일일 발송 상한과 같은 급(도메인 평판 비용 비대칭)으로 판단해 무관용 항목으로 격상: ① `specs/G5_제안메일제작발송.md` 「성공 기준」 무관용에 "이미 발송 기록이 있는 건은 재발송하지 않는다" 추가 + 「실행 형태」 실패표에 스킵 처리 행 추가 ② `g5-제안메일제작발송/SKILL.md` 발송 모드에 "0. 발송기록 중복 확인" 단계 신설 — 승인란을 보기도 전에 기업식별자로 `발송기록/` 존재를 먼저 확인해 있으면 조용히 스킵 ③ `web/parsers.py`에 `_sent_company_ids()` 추가, `parse_approvals()`가 이미 발송된 건은 목록에서 아예 제외(발송 이력은 `/sent`에서 확인). 서버 재기동 후 `/approvals` 재확인 — 정상 렌더링(크래시 없음), 기존 미발송 건들은 그대로 노출 | 사용자 질문으로 발견·즉시 수정 2026-08-12 |
| 56 | **G1 판정 기준에 "운영 주체가 독립 사업자인가"를 가르는 항목이 없다** — `www-rakuten-co-jp-stylife`(라쿠텐 그룹이 직접 운영하는 자사 편집숍 "Rakuten Fashion")를 조사하며 발견. 이 사업의 실제 타겟은 "AI-Studio 콘텐츠 제작이 필요한 개별 중소 판매처"인데, 마켓플레이스 운영사 자신이 후보에 잡혀도 3필수조건(제품사진·커머스·업종)만으로는 걸러지지 않는다 | **미해결 — 판정 기준 공백으로 등록, 카드를 스스로 고치지 않음.** 이번 건은 사이트 접속 자체가 막혀 판정불가로 종료했지만, 접속이 됐다면 기계적으로 "포함"됐을 것이다. 실제 발생 빈도가 낮으면(마켓플레이스 자체가 후보로 잡히는 일은 드물다) 우선순위가 낮을 수 있으나, 재발하면 `기준/G1_기업판정기준.json`에 관찰 항목을 추가할지 사람이 정할 문제 | www-rakuten-co-jp-stylife 조사 2026-08-12 |
| 60 | **더미 Hook 12종 중 아직 실물 검증 안 된 8개(H2·H3·H4·H6·H7·H8·H10·H11)를 전수 실물 테스트하다가, `claude -p "/스킬명 ..."` 호출 자체가 Git Bash(MSYS) 환경에서 깨지는 구조적 버그를 발견했다** — MSYS 런타임이 `/`로 시작하는 인자를 "POSIX 절대경로"로 오인해, 네이티브 Windows 실행파일(`claude.exe`)에 넘기기 전에 자동으로 `C:/Program Files/Git/...`로 변환해 버린다. 실제로 H8(`/g11-반응집계`)이 이 때문에 완전히 깨져 Claude가 "이게 무슨 경로냐"고 되묻는 응답을 냈고, H6(`/g5-제안메일제작발송 제작 S2`)도 같은 증상으로 프로세스가 응답 없이 붙잡혀 있었다(`wmic`으로 실제 커맨드라인을 확인해 `C:/Program Files/Git/g5-...`로 바뀐 것을 직접 확인) | **해소 완료(2026-08-13).** `MSYS_NO_PATHCONV=1` 환경변수를 설정하면 이 변환이 꺼지는 것을 `node -e "console.log(process.argv)"`로 최소 재현해 확인 — Hook 12개 스크립트(`hook_h01`~`hook_h12`, `hook_h9` 포함) 전부의 `input=$(cat)` 직후에 `export MSYS_NO_PATHCONV=1`을 추가했다. 재발급된 H6·H8을 재실행해 커맨드라인이 더 이상 안 깨지는 것과 실제 산출물(`specs/발송대기/S2-2026-08-13.md` 9건, `specs/집계/2026-08-13.md`)이 정상 생성되는 것까지 확인. **H9·H12가 이전에 안 걸렸던 이유**는 그 둘은 Claude Code의 PostToolUse 훅 메커니즘(비-MSYS 실행 경로로 추정)으로만 테스트됐고, Git Bash로 직접 돌린 적이 없었기 때문으로 보인다(확진은 아님) — 즉 "이전에 통과했다"가 "이 경로로는 항상 안전하다"를 뜻하지 않는다는 사례 | H1~H12 전수 실물 검증 2026-08-13 |
| 61 | **G9 스킬 본문("적합도 문턱이 아직 없다")이 Spec의 실제 확정값과 어긋나 있었다** — `specs/G9_위임브리프.md` 「지금 쓰는 값」에는 이미 문턱(G3 가동 중 상위 20%, `docs/92` #6)이 채워져 있는데 `.claude/skills/g9-위임브리프/SKILL.md`는 옛 서술("문턱 미확정 — 배제하지 않고 표시만")을 그대로 갖고 있었다(G11 SKILL.md가 겪었던 것과 같은 유형의 Spec-Skill 드리프트, `91` 2026-08-11 항목 참조) | **해소 완료(2026-08-13) — 실물 테스트 중 발견.** H11 실물 재검증으로 `/g9-위임브리프 경로1 sucreyshopping-jp`를 실제로 돌렸더니, 스킬이 "본문과 문서가 다르면 문서가 이긴다"는 자기 원칙을 실제로 지켜 Spec의 실제값을 적용해 올바르게 문턱 미달(상위 20% 밖) 판정을 내고 브리프를 만들지 않았다 — **판정 자체는 이미 정확했다.** 다만 스킬 본문이 낡은 채로 있으면 다음에 덜 꼼꼼히 읽었을 때 틀릴 위험이 있어, SKILL.md의 해당 절을 실제 게이트 절차(G3 가동 여부 확인 → 순위표에서 상위 20%/큐 상단 10건 판정)로 교체했다 | H11 실물 재검증 2026-08-13 |
| 52 | ~~더미 Hook 10종 전부의 최종 디스패치 단계는 이 저장소 세션 안에서 근본적으로 검증할 수 없다~~ — `Harness/hook/*.sh`가 전부 Harness 제출판의 영문 스킬명(`/g1-company-screening` 등)을 불러 "Unknown command"가 났던 것 | **판단 정정 — 애초에 "구조적 한계"가 아니라 그냥 참조 오류였다.** 사용자 지적으로 재검토: 이 스크립트들이 부르는 이름은 "팀원이 나중에 줄 인자값"과 무관하다 — 어떤 값을 넘기든 애초에 **명령어 이름 자체**가 이 세션에 없어서 실패하는 것이었다. H9·H12를 라이브 이식했던 것과 완전히 같은 종류의 수정이라 판단, `.claude/hooks/`에 10개 전부 한글 스킬명으로 교체한 라이브 사본을 만들었다(원본 `Harness/hook/*.sh`는 제출 패키지 자기완결성을 위해 영문 그대로 둔다). "새 기능을 발명하는 것"이라던 이전 판단은 틀렸다 — 이름 하나 바로잡는 참조 수정이었을 뿐 | 사용자 재질문 2026-08-11 |
| 62 | **무인 실행(사용자가 화면 앞에 없는 자동 진행 구간)에서 Bash·PowerShell·WebFetch·WebSearch·Agent(서브에이전트 스폰)가 승인 없이 바로 거부된다 — 다만 도구 범주 전체가 막히는 것은 아니고, Playwright MCP(브라우저 자동화)·읽기·쓰기 계열 도구는 정상 동작한다는 것도 같이 확인했다** | G9→G4 재투입 루프(사람이 찾아낸 연락처 → G4가 등급·거부표시를 재확인) 실물 검증 중 발견. G9 합류 모드가 `specs/연락처후보/{식별자}.md`를 쓰고 G4가 그 파일을 읽는 자리(SKILL.md 실행 3번)까지 성공을 확인한 뒤, 창구 재확인(4번)·거부 표시 확인(5번)에 쓰려던 `WebFetch`가 거부됐다. `WebSearch`·`Agent`(서브에이전트 스폰으로 우회 시도)도 같은 세션에서 함께 거부돼 "승인이 필요한 도구 범주 전체가 막힌다"로 처음 판단했으나, **`mcp__playwright__browser_navigate` 등 Playwright MCP 도구는 같은 세션에서 정상 동작했다** — 즉 막히는 기준은 "승인이 필요한가"가 아니라 **개별 도구/카테고리별로 다른 사전 승인 정책**으로 보인다(확진 아님). `mcp__ide__executeCode`(Jupyter 커널 실행)는 거부가 아니라 "No active notebook editor found"로 실패 — IDE에 노트북 편집기가 실제로 열려 있어야 커널이 붙는 구조라 이 CLI 세션(IDE 미부착) 자체의 한계이지 권한 문제가 아니었다 | **부분 해소.** Playwright를 WebFetch 대체 경로로 써서 G9→G4 루프의 웹 재확인 구간을 실제로 완주했다(아래 91 항목) — **리스크 62의 실무적 함의는 "WebFetch가 막히면 그 실행체는 못 돈다"가 아니라 "WebFetch가 막혀도 Playwright 경로가 있으면 대체 가능하다"로 좁혀졌다.** 다만 Bash·PowerShell 계열이 필요한 작업(`real_send_api.py` 실행 테스트 등)은 여전히 대체 경로가 없다 — 이 세션에서 Jupyter 경로까지 시도했으나 IDE 미부착으로 막혔다. G1·G4처럼 WebFetch가 필수인 실행체가 Hook 등으로 완전 무인 트리거될 때의 위험(리스크 46과 다른 층 — 권한은 있어도 승인이 안 남)은 여전히 살아 있다 | G9→G4 재투입 루프 실물 검증 2026-08-13, Playwright 대체 경로 확인 2026-08-13 |

### 2-3. 지금 살아 있는 리스크만 추리면

목록 개수는 여기서 세지 않는다(위 표가 정본). **상태별로 갈라 둔다 — 2026-08-13 project-critic 지적으로 49~62번 반영, 여기 없던 14건이 조용히 안 보이고 있었다.**

| 상태 | 번호 |
|---|---|
| **살아 있다 — 계속 본다** | **2**(지표명 단정) · **6**(버즈워드) · **7**(의사결정권자 연락처) · **11**(캐파 초과) · **12**(도메인 평판) · **13**(레퍼런스 인용 허가) · **16**(2026-08-03 재검토 — G1 판정 문장 사후 조정 경로 없음, O8만 V9로 확인) · **18**(A에 강제 장치 없음) · **54**(라쿠텐 등 마켓플레이스 URL이 WebFetch 수준에서 구조적으로 차단됨 — 판정불가 22건의 주 원인) · **56**(G1 판정 기준에 "운영 주체가 독립 사업자인가"를 가르는 항목 없음) · **62**(무인 실행에서 Bash·PowerShell 계열 도구가 승인 없이 거부됨 — Playwright 경로는 확보, Bash 계열은 대체 경로 없음) |
| **신규 (2026-08-11~13, Hook·G9 실물 검증) — 전부 해소** | **49**(합성 데모 값 정리) · **50**(승인화면 거부 경로 검증) · **51**(더미 Hook 12종 전수 검증) · **52**(Hook 참조 오류 정정) · **53**(H5·H11 파이프 조기 종료 버그) · **55**(H5 진단 정정, 24/24 확정) · **57**(G2 대표 선정 순서 2 카테고리 전용 확장) · **58**(웹 승인화면 파서 형식 불일치) · **59**(승인·발송 미연동·재발송 방지) · **60**(MSYS 경로 오염 버그) · **61**(G9 스킬 문서 드리프트) |
| **신규 (2026-08-11, run-orchestrator 첫 실전 실행 1~4차) — 전부 해소** | **46**(도구 권한 — WebFetch·WebSearch 추가, 새 세션 재실행으로 반영 확인) · **47**(G2 대표 선정 순서 4 신설) · **48**(차선 대표 승격 조건에 [메일 없음] 추가) — **이 실행으로 G1→G2→G4→G5가 이 저장소 역사상 처음으로 실제 자동 연쇄 완주, 정확히 설계된 승인 정지점에서 종료 확인됨** |
| **신규 (2026-08-10) — 해소 확정** | **21**(회신 이후 경로 — 사람이 할 일로 확정, 회신 분류 가시화는 G7이 이미 충족하고 표시는 웹+대시보드 몫) |
| **설계에 반영해 대응 완료 — 되돌아가지 않는지만 확인** | **15**·**17**·**19**·**20** |
| **해소·폐기·통합** | **1**·**3**·**4**·**5**·**8**·**9**·**10**·**14** |
| **신규 (2026-07-31, `project-critic`) — 반영 완료** | **22**(`20`·`50` 신설) · **23**(`11` 8절 채널 비교) · **24**(N22 답 확보, 필수→적합도 하향, 2026-08-01) · **25**(업종 불일치, N24 신설) · **26**(미확인 표기 8곳 갱신) · **27**(`CLAUDE.md` 라우팅 표 정정) · **28**(N21 신설) · **29**(G1 3곳 실측) |
| **신규 (2026-08-01, `project-critic` 검증 게이트 감사) — 반영 완료** | **30**(O6 3항목 선택 하향) · **31**(G5 층3 판정 문장 신설) · **32**(V1-a 표본 수 불일치 해소) |
| **신규 (2026-08-01) — 검토 보류** | **33**(A-2·B군·C-1·C-2·D-1·F군 — 게이트 축소와 결이 달라 다음 순위로) |
| **신규 (2026-08-05, G7 실행체 구현) — 반영 완료** | **37**(스킬 frontmatter `disallowed-tools`로 `g1-기업판정`·`g7-회신처리` 오케스트레이터 층에도 도구 경계 적용) |
| **신규 (2026-08-03, MVP 재정의) — 반영 완료** | **34**(G4·G5 대체 규칙) · **35**(MVP 잔여 저장 정책 + 대표 기업 승격 규칙) · **36**(O8 조기 필터링 + 대표 선정 재설계) |
| **신규 (2026-08-05, G12 신설) — 반영 완료** | **38**(식별자 조인 붕괴 위험의 소비처가 G11+G12 둘로 늘어남 — 기존 완화책 재사용으로 대응) |
| **신규 (2026-08-06, MVP+부가 9개 첫 전체 감사) — 부분/반영 완료** | **39**(G1 단위 L 실행체 없음 — 문서 정정만 반영, 실행체는 `12` N28 답 대기) · **40**(발송대기 상업적 약속·과장 — 반영 완료) · **41**(G5 승인 게이트 자기검증 — `g5-승인확인` 신설로 구조 분리, 실물 검증·`change-reviewer`는 대기) |
| **신규 (2026-08-06) — 부분/반영 완료** | **42**(① V7 연결·기록 완료, 두 사람 대조만 잔여 ② `발송금지.md` 경로 통일 완료 ③ `50` 하루 부담에 캐벗 추가(수치는 미실측) ④ 대시보드는 애초에 새 기능이 아니었음을 재확인) |
| **신규 (2026-08-10, Hook·API 더미 신설 + `project-critic`·구현 배선 감사) — 대부분 반영 완료** | **43**(무인 실행-발송 권한 공유 위험 — H12 신설로 부분 반영, 승인란이 웹으로 옮겨가면 재검토) · **44**(스냅샷 무관용 값 leak — 반영 완료) · **45**(H7/G10 30일·14일 상충 — "2회↔3회째" 표기 통일 완료, 주기 자체도 3회째 단독 기준으로 반영 완료) |

**살아 있는 11건 중 4건(11·12·13·21)이 기업 답을 기다린다** — 제작 캐파, 발송 도메인, 레퍼런스 인용 허가, 회신 이후 실무 준비. 이 넷은 우리가 더 할 수 있는 것이 없고 `12`의 해당 질문에 걸려 있다. **나머지 7건(2·6·7·16·18·54·56)은 기업 답과 무관하게 우리가 판단할 수 있는 것들이다** — 54(대안 소스 검토)·56(G1 판정 기준 보완)이 특히 실측으로 뒷받침된 실행 가능한 항목이다.

## 3. 다음 액션

> 이 목록을 어떤 순서로 처리할지는 `01_진행계획.md`의 A~E 단계를 따른다. A2(회사 조사)가 대부분의 선행 조건이다.

- [x] **옛 후보 3건(INGNI·ROPÉ PICNIC·Honeys)을 현행 3조건·O6 2항목 기준으로 재판정 — 2026-08-03 완료.** `specs/candidates/후보목록.md` 신설 — O6만 새로 관찰(WebFetch), 나머지는 승계. **3곳 전부 제외→포함으로 뒤집혔다**(옛 4조건 게이트 삭제 효과). 옛 개별 파일 3건은 이력으로 보존
- [ ] **G2 빌드에서 드러난 참조·정의 공백을 사람이 정리한다 (2026-08-05, `harness-builder` 발견 — 실행체가 스스로 고치지 않고 올린 것). 개수는 아래 항목을 세고 여기서 요약하지 않는다** — `90` 리스크 26이 낸 개수 동기화 사고를 이 목록 자신이 한 번 더 냈다(3건으로 적어 두고 항목만 늘렸다)
  - **막는 것: ⑥⑬⑭⑰⑱. 값·판단 대기: ①⑦⑨-1⑩⑪⑲. 해결됨(✅ 표시): ②③④⑤⑧⑨⑫⑮⑯. 부분 해결: ⑬-2** — 근거는 각 줄에서 `33` 링크로. **⑰⑱⑲는 2026-08-05 3차 실행(확정된 카드로 절차 재실행)이 새로 낸 것이며, ⑰⑱은 배정 결과를 실제로 바꾸는 자리라 「막는 것」에 넣는다**
  - **① `specs/G2` 출력 형식의 `{축코드}`를 정의한 곳이 없다.** 파일명이 `SEG-{축코드}-v{버전}.md`인데 `기준/G2` 1절은 축을 「안 A」·「안 B」로만 부른다. `g2-세그먼트화`는 카드의 그 이름을 그대로 코드로 쓰고(안 A → `A`) **카드에 없는 코드는 만들지 않는다**로 잠정 처리했다. **잠정 권고가 "A 주축 + 커지면 그 안에서만 B로 쪼갬"이라 A와 B가 함께 쓰인 회차의 축코드 표기가 여전히 비어 있다** — 실측(무리 1개)에서는 아직 발생하지 않았다. 기업 답으로 축이 확정될 때 함께 정한다
  - **② `기준/G1` JSON의 O8 제어흐름이 존재하지 않는 파일(`기준/G2_세그먼트기준.json`)을 가리켰다.** ✅ 해결 — `.md`로 참조 정정, 변경 이력 기록
  - **③ `.claude/README.md`의 `g1-기업판정` 행이 낡았다**(구 산출물 형식·구 정본 경로 표기). ✅ 해결 — 갱신 완료
  - **④ 카드 4절 미분류 4종 코드 경계가 겹치고 우선순위가 없었다** — cocacoca-jp 1건이 세 코드에 동시 성립, 적대적 검토자 둘도 다른 코드를 골랐다. **정본은 `33` D24다** — 세 코드 동시 성립의 실물 기록은 3차 실행이 `SEG-A-v1.md` 3절을 덮어써 사라졌고, 검토자 둘이 갈렸다는 서술은 원래 그 파일이 아니라 D24에만 있었다. **✅ 해결 — 4절에 우선순위 신설(`33` D26)**
  - **⑤ 카드 3절 순서 3과 `specs/G2` 「입력」(근거 원문 배제)이 배타적이었다.** **✅ 해결 — 근거 원문 대신 관찰 값 확정도(축약표 전체, O8·O9 제외)로 비교하도록 3절 확정(`33` D26)**
  - **⑥ `specs/G2` 「제약」에 재실행 분기가 둘밖에 없다.** 정의된 건 축 변경→새 버전 / G10 재수집→같은 버전 갱신 둘뿐인데, **실제로 발생한 회차가 둘 더 있다** — ⓐ 후보 추가(0절 게이트가 전제한 반복 재실행, `SKILL.md`가 「같은 버전 갱신」으로 실행체 판단 처리) ⓑ **카드가 개정된 회차**(2026-08-05 3차 실행이 실제로 이 경우였고, 같은 버전 갱신으로 처리했다 — 역시 실행체 판단). 둘 다 Spec에 분기가 없다 — **미해결**
  - **⑦ `disallowed-tools`의 실제 차단이 관측된 적이 없다.** 근거가 문서 조회 1건뿐 — **미해결. 단 「확인 비용 낮음」은 2026-08-05 취소한다**: 스킬은 설계상 금지 도구를 시도하지 않으므로 `/g2-세그먼트화`를 슬래시로 돌려도 차단이 발동할 계기가 없다(3차 실행에서 실제로 웹 도구 호출이 0건이었다). **닫으려면 금지 도구를 실제로 시도하는 시나리오를 따로 만들거나 「관측 불가」로 종결해야 한다** — 아래 ⑬-2와 닫는 조건이 다르다(`change-reviewer` 2026-08-05 지적)
  - **⑧ `33` D19가 리스크 37 해결을 반영 못해 같은 파일 안에서 모순이었다.** **✅ 해결 — D19 정정**
  - **⑨ `Harness/` 패키지에 G2가 없고 형식 충돌 미해결.** **✅ 해결(방향 전환) — G7 단독 패키징 폐기, `Harness/` 비움, MVP 6개 다 빌드되면 한 번에 패키징**(`Harness/README.md`)
  - **⑨-1 (2026-08-05 추가, ⑨ 처리 이후에도 유효 — MVP 6개 전체 패키징 시점에 적용할 규칙 세 가지)**
    1. **내부 참조 경로 재작성.** 원본(`.claude/skills/*/SKILL.md`, `.claude/agents/*.md`)은 저장소 루트 기준 경로(`specs/G{n}_*.md`, `기준/G{n}_*.md`)를 쓰는데, `Harness/`로 옮기면 파일명이 `spec.md`/`기준.md`로 바뀌므로 그 경로를 Harness 폴더 상대 경로로 다시 써야 한다
    2. **기준카드 참조 전 확장자 확인.** `기준/` 카드는 `G1`만 JSON이고 나머지는 마크다운이다(`기준/README.md` 37~41행). 다른 카드를 참조할 때 형식을 가정하지 않고 `기준/README.md` 파일 표로 확인한다 — 확인 없이 JSON을 가정해 난 버그가 이미 한 번 있었다(위 ② 항목, `기준/G1_기업판정기준.json` 변경이력 141행)
    3. **운영변수 정본 동봉.** `docs/50_실사용_도입설계.md`(운영 변수)·`docs/11_대안탐색_비교.md`(G1 데이터 소스)에만 정본이 있는 값은 `기준/` 카드 어디에도 없다. **G1·G2·G5를 패키징할 때는 그 절의 실제 값을 카드에 동봉하거나 인라인해야 한다** — G7은 참조 대상이 전부 `33`(근거, 안 읽어도 되는 것)뿐이라 이 문제가 없다
  - **⑬-2 (2026-08-05 신규, ⑨ 처리에 딸려 나온 것) — G2의 남은 잔여 둘(유일한 산출물 `SEG-A-v1.md`가 확정 전 카드 기준으로 낡음 / 스킬 경로 실제 실행 0건).** **✅ 앞의 하나만 해결 (2026-08-05 3차 실행)** — 확정된 카드로 절차를 다시 돌려 산출물을 갱신했고, 실제로 **배정 2건이 바뀌었다**(junonline S1→U-단독 / cocacoca 판정보류→U-단독). 「낡음」은 해소. **뒤의 하나(스킬 경로 실제 실행)는 남아 있다** — 이번 실행도 `harness-builder` 컨텍스트에서 절차를 수행한 것이라 `/g2-세그먼트화` 호출이 아니다. **⑦과 혼동하지 않는다**: 이 잔여는 슬래시 호출 1회로 닫히지만 ⑦(금지 도구 차단 관측)은 그것으로 안 닫힌다
  - **⑰ 카드 3절 순서 2의 「키즈·반려견」 예시가 같은 날 확정된 2절 긍정문 규정과 겹쳐 적용 대상을 잃는다 (2026-08-05 3차 실행 신규).** 3절 순서 2는 *"다른 대상군의 라인(남성·키즈·반려견 등)을 함께 취급하는 곳은 경계로 보고 **대표에서 내린다**"*로 그런 기업이 **무리 안에 배정돼 있음**을 전제하는데, 2절 긍정문 규정("O2 취급 품목이 전부 여성 대상군 라인")은 그런 기업을 **배정 단계에서 이미 걸러낸다.** 실제로 junonline-jp-rope-picnic 1건이 2차 실행분에서는 S1 소속이었다가 이번에 U-단독으로 빠졌다. **배정 판정의 자리는 2절이므로 실행체는 2절을 따랐고 스스로 화해시키지 않았다** — 3절 순서 2의 그 예시를 지울지, 2절 규정을 대상군 「주력」 개념으로 넓힐지(그건 새 판별층이라 실행체가 못 만든다)는 사람이 정한다. 두 절이 같은 날 각각 다른 사례(2절=cocacoca, 3절=junonline)를 보고 조작화돼 서로를 확인하지 않은 것이 원인이다. **얽힘이 반대 방향으로도 한 번 더 나타났다** — 3차 실행이 S1 규정에 붙인 괄호 *"(여성 의류 + 같은 대상군용 잡화·신발·가방·속옷)"*는 2절 재작성 예시에 없는 문구이고 출처가 **3절 순서 2**(대표 선정용 「같은 대상군 안의 품목 확장」)다. 포함 범위를 넓히는 쪽이라 2절 제약 위반은 아니고 이번 판정도 안 바꾸지만(ingni·honeys 둘 다 여성 전용), **대표 선정용 개념이 배정 규정으로 넘어온 것**이라 같은 계열이다. 2·3절을 손볼 때 이 괄호를 2절이 직접 정의할지 함께 정한다
  - **⑱ 카드 4절 U-단독의 「또는 같은 패턴」에 판별선이 없다 (2026-08-05 3차 실행 신규).** 이번 회차에 junonline(여성+키즈+반려견)과 cocacoca(여성+남성+키즈)가 둘 다 정의된 무리에 안 맞았는데, **「같은 축 값」을 문자 그대로 보면 서로 다른 값이라 각각 U-단독**이고, **「같은 패턴」(여러 대상군 병행)으로 보면 2곳이라 U-단독이 깨진다.** 하류가 다르다(다음 회차 재시도 / 이번 회차 무리 신설 / 사람 판단). **넓게 읽었을 때 어디로 가는지도 카드가 두 방향을 가리킨다** — 4절 #3이 여러 대상군 병행 값을 U-단독으로 지정하지만 그 지정은 「이 기업뿐」을 전제로 하고, 전제가 깨지면 **2절이 같은 유형의 행선지로 적어 둔 *"둘 이상 무리 판정 또는 U-이질"*이 다시 살아난다.** 즉 이 지점은 **어느 코드가 맞는지가 아니라 카드 안에서 2절 안내와 4절 우선순위 중 무엇이 이기는지가 안 정해진 자리**다 — 카드 4절을 고칠 때 2절 문장까지 함께 본다(`change-reviewer` 2026-08-05 1·2라운드). 실행체는 판별선이 없는 넓은 해석을 적용하지 않고 문자 그대로의 축 값으로 갈랐다 — 넓게 보려면 어느 대상군이 주력이고 어느 것이 부차인지를 봐야 하고 그것이 `SKILL.md` 「멈추는 경계」가 막는 새 판별층이다. **④(코드 겹침)를 우선순위로 해소했더니 그 아래층에서 같은 종류가 한 번 더 나온 것**이라, 카드 4절을 고칠 때 이 자리까지 함께 본다
  - **⑳ 3차 실행이 하류 G4 산출물 1건을 낡게 만들었다 (2026-08-05 3차 실행 신규, `change-reviewer` 2라운드 발견).** `specs/contacts/junonline-jp-rope-picnic.md` 9행이 *"세그먼트 소속 + 대표 여부: **S1(여성 의류) · 소속(대표 아님)** — 출처 `specs/segments/SEG-A-v1.md` **2절**"*로 적고 있는데, 재실행 후 junonline은 그 파일 2절이 아니라 **3절 미분류(U-단독)**에 있다. **그 문장이 그 파일의 G4 탐색 순서 게이트 통과 근거 전체**라(대표 파일이 이미 있으므로 소속을 탐색해도 된다는 논리) 근거째로 무효가 됐다. 같은 디렉터리의 `ingni-store-com.md`(S1 대표)·`honeys-co-jp.md`(S1 소속)는 여전히 맞다 — **깨진 것은 1건뿐**이다. **⑯ 선례(「기존 산출물은 시험 실행 데이터라 소급 수정 안 함」)를 따라 이번에 고치지 않았고**, 지울지·다시 돌릴지·표시만 달지는 사람이 정한다. **이 건은 ⑬(G4 Spec 「입력」에 시험 실행본을 거르는 조건이 없다)이 예고한 위험이 실물로 발현한 것이다** — 시험 실행 산출물이 하류로 흘러가면 상류 재실행이 하류를 조용히 무효로 만든다
  - **⑲ 이번 회차 미분류 비율이 66.7%(4/6)로 카드 5절 잠정 기준(30%)을 크게 넘었다 (2026-08-05 3차 실행 신규).** 2차 실행분은 50%였고 이번에 junonline이 빠지며 더 올랐다. `specs/G2` 「제약」 실패표가 *"미분류가 과다하다 → 사람에게 넘긴다. 스스로 다른 축으로 다시 묶지 않는다"*로 정한 대로 실행체는 축을 바꾸지 않았다. **다만 이 수치는 축이 나쁘다는 신호로 바로 읽히지 않는다** — 표본 6곳이 패션 4·뷰티 1·푸드 1이라 업종축(A)으로 나누면 1곳짜리가 나올 수밖에 없는 분포다. **후보가 더 모이기 전에는 이 비율로 축을 판정하지 않는 것이 맞는지**가 판단 지점이다(카드 6절 「U-단독이 많으면 축이 너무 잘게 나눈다」와 정면으로 걸린다)
  - **⑩ `.claude/README.md`의 "참조는 파일 단위다, 절 단위로 앵커를 걸지 않는다"가 실제 스킬 둘과 어긋난다.** `g7-회신처리`·`g2-세그먼트화` 모두 *"매 실행마다 전체를 다시 읽는다"*를 앞세운 뒤 판정의 자리를 절로 지목한다. 규칙이 막으려던 위험(다른 절 변경을 놓침)은 전체 재독으로 이미 봉쇄돼 있으므로, **규칙 문장을 "전체를 읽되 어느 절이 그 판정의 자리인지는 지목해도 된다"로 정밀화**할지 판단이 필요하다. 지금 문언대로면 선례 둘이 동시에 위반이다
  - **⑪ `.claude/skills/수업/SKILL.md` 대조표에 G2가 빠졌다 — 아래 별도 항목(「60~78행 대조표 갱신」)에 합쳐 처리한다.** G2 한 줄만 끼우면 나머지 낡음(Spec 행이 이력 문서 `32`를 가리킴, Hook 행이 폐기된 트리거 4종, 활용 방식 "미결정", JSON 이관 전 경로)이 갱신된 것처럼 보여 이번엔 손대지 않았다. **같은 표를 두 항목이 각각 등록하고 있었다** — 하나를 처리해도 나머지가 남아 미완으로 보인다(`grading-critic` 2라운드 §3)
  - **⑫ 카드 3절 대표 선정 순서 2·3이 조작화돼 있지 않았다** — 시범 실행이 판별선을 즉석으로 만들었다. **✅ 해결 — 3절에 순서 2·3 판별선 확정(`33` D26), `SKILL.md`의 「잠정 판별선」 절은 카드 인용으로 교체**
  - **⑭ 「실행체는 기준 카드를 고치지 않는다」와 「순수 참조 버그는 고쳐라」가 부딪힌다 (2026-08-05 3라운드 신규 — 사람 판단 필요).** `.claude/agents/harness-builder.md`는 *"Spec·기준 카드·`평가항목.md`를 고치지 않는다"*를 **예외 없이** 닫아 두었는데, 이번 라운드는 사용자 지시로 ②(존재하지 않는 파일을 가리키는 참조)를 실행체가 직접 고쳤다. **지시가 있었으므로 이번 건은 문제가 아니지만, 규칙 문장에는 그 통로가 없다** — 다음 빌드에서 같은 종류(오타·끊긴 경로)를 만나면 실행체는 고칠 수도 없고 「올린다」로 매번 왕복해야 한다. **「판정 문장·임계값은 못 고친다 / 참조 무결성은 고치고 변경 이력에 남긴다」로 갈라 줄지**가 판단 지점이다. 갈라 준다면 `기준/README.md`의 변경 이력 규칙을 실행체가 지키도록 `harness-builder`에 함께 박아야 한다(이번에 놓쳤다)
  - **⑮ 「잠정 판별선」이 판정 문장인데 Skill 본문에 있었다**(배치 규칙 위반). **✅ 해결 — 판별선이 카드로 확정되며 그 절을 SKILL에서 삭제**
  - **⑯ 무리 규정 작성의 최소 제약이 없어 실행체가 즉석으로 배제 조건을 만들었다.** **✅ 해결 — 2절에 "포함 조건만" 신설(`33` D26).** 기존 산출물(`SEG-A-v1.md`)은 시험 실행 데이터라 소급 수정 안 함, 재실행 시 새 규칙 적용
  - **➡ ⑬ G4 빌드로 이관 (2026-08-05).** 고칠 자리가 G4 Spec이므로 이번 G2 라운드에서 손대지 않았다. **G2 쪽에서 한 것은 방어선을 명시한 것뿐이다** — 실행체에 *"산출물 머리의 「시험 실행 — 확정 배정 아님」 표시가 지금은 **유일한** 방어선이며 빠뜨리면 막을 것이 아무것도 없다"*를 적었다. **한 방향 선언이라는 지적은 그대로 살아 있다** — G4를 빌드할 때 소비자 쪽 「입력」에 조건을 넣어야 닫힌다. ↓ 원문
  - **⑬ `specs/G4_연락처확보.md` 「입력」에 시험 실행본을 거르는 조건이 없다 (2026-08-05 2라운드 신규).** `SEG-A-v1.md`와 `specs/README.md`가 *"G4가 확정 배정으로 받으면 안 된다"*고 적었지만 **한 방향 선언이다** — G4 Spec은 *"G2가 정한 순서를 바꾸지 않는다"*만 갖고 있어, 소비자 쪽 Spec만 읽는 실행체는 잠정 축 산출물을 그대로 받는다(`33` D14가 잡으라고 세워 둔 유형)
- [x] **G3 빌드에서 드러난 카드·Spec 공백을 사람이 정리한다 (2026-08-05 `harness-builder` 발견 → 2026-08-06 정리하고 `_검증중/`에서 확정 위치로 승격).** 실행체가 고칠 수 있는 것(①④)은 고쳐서 승격했고, 사람이 정할 값(②③⑤)은 카드에 명시한 채 그대로 보류로 남겼다 — G2와 같은 처리 방식(`CLAUDE.md` 5절)
  - **① ✅ 해결 — 업종 라벨 「코스메틱」→「뷰티」로 통일(G1 정본에 맞춤).** `기준/G3_적합도기준.md` 0·1절, 변경 이력에 반영. **`기준/G2_세그먼트기준.md` 4절의 「코스메틱」은 이번에 안 건드렸다** — G2는 이미 확정·실행된 카드라 별도 판단 필요(잔여 항목)
  - **② ✅ 해결 (2026-08-06, 사용자 지시) — 규모 축 판별선을 상대 판정(하위 사분위, 비교 대상 3곳 미만이면 비움)으로 확정.** 절대 숫자를 안 만든 이유·근거는 `../docs/33_설계근거.md` **D27**
  - **③ ✅ 문서는 정정, 실물 갭은 그대로 — 「한국 진출 신호」·자본금·종업원수 축의 재료 조달 경로.** *"G3이 재방문해 직접 수집"*이라던 자기모순 문구(웹 권한이 없는데 스스로 수집한다고 적혀 있었다)를 `specs/G3_적합도평가.md`·`기준/G3_적합도기준.md`에서 **"재료 조달 경로 없음 — 상시 채점 불가"**로 정정했다. **다리 자체(G1 관찰 항목 복원 또는 다른 경로)는 만들지 않았다** — 그건 사람이 결정할 아키텍처 판단이라 실행체·카드가 스스로 만들 일이 아니다
  - **④ ✅ 해결 — 카드 3절에 「관찰값 `?`」와 「관찰 항목 자체 없음」 두 경우를 표로 분리.** 실행체가 이미 하고 있던 처리를 카드 문장으로 승격
  - **⑤ ✅ 해결 (2026-08-06, 사용자 지시) — 동점 다음 순위는 표준경쟁순위(1·1·1·1·5)로 확정.** 근거는 `../docs/33_설계근거.md` **D27**
  - **⑥ (참고, 재확인 대상) 시험 실행 결과 — ②③이 남아 있는 한 재실행해도 순위가 크게 안 갈릴 것이다.** 세 축 중 둘이 비어 척도가 업종 하나로 축약됐던 것이 시험 실행(2026-08-05)의 관찰이었다. **착수 조건이 와도 가중치 숫자만으론 안 풀린다** — ②의 판별선과 ③의 재료 경로가 먼저다
  - **승격 후 남은 것**: `grading-critic` 재검토(부가 기능이라 급하지 않음), 실제 슬래시 호출(`/g3-적합도평가`)로 `disallowed-tools` 실제 발동 확인 — G1·G2·G4와 같은 미해결 공백 유형
- [ ] **G4 빌드에서 드러난 카드·Spec 공백을 사람이 정리한다 (2026-08-05, `harness-builder` 발견 — 실행체가 스스로 고치지 않고 올린 것). 개수는 아래 항목을 세고 여기서 요약하지 않는다**
  - **⚠ 지금 판정을 실제로 바꾸는 것은 ①④이고**(①은 [메일 없음]이 발동하는지를, ④는 6곳 중 4곳의 탐색 순서를 가른다), **②는 상류 재실행이 하류를 조용히 무효로 만드는 자리**(실물 피해가 이미 G2 ⑳으로 발현), **③은 해결됐다.**
  - **① `기준/G4_연락처유효성기준.md` 1절 「메일 없음」 문언이 실측과 어긋난다 — 전화 병존이 예외가 아니라 기본값이다.** 카드는 조건을 *"이메일 주소 자체가 없고, **웹 문의 폼(입력창+제출 버튼)만** 있음"*으로 적었는데, 시험 실행 3건 중 [메일 없음] 2건(junonline-jp-rope-picnic·honeys-co-jp) 둘 다 **폼 + 전화번호**였다. 글자 그대로 읽으면 *"폼만"*이 아니므로 어느 등급에도 안 걸리고, **일본 EC 사이트에서 전화 병존은 기본값이라 이 상태가 사실상 발동하지 않는다.** 실행체는 같은 절 뒤 문장(*"`mailto:`나 이메일 형식 텍스트가 보이면 최소 「일반」, 안 보이고 폼만 있으면 「메일 없음」"* — **이메일 채널 하나만 놓고 가름**)을 근거로 [메일 없음]으로 판정하고 문언을 스스로 고치지 않았다. **같은 절 「미확보」 행의 괄호 설명(*"창구 자체를 못 찾음"*)도 같은 자리다** — 이메일도 폼도 없이 전화·팩스·SNS만 있는 경우는 문언상 [미확보]에 걸리는데 괄호 설명과 어긋난다. **1절을 「이메일 채널의 유무만 본다(다른 채널 병존은 등급에 영향 없음)」로 명시할지**가 사람이 정할 자리이며, 고치면 「미확보」 괄호까지 함께 본다
  - **② ⑬(`specs/G4_연락처확보.md` 「입력」에 시험 실행본을 거르는 조건이 없다 — G2 빌드에서 이관)은 G4 빌드 후에도 미해결이다.** 실행체는 세그먼트 정의 파일 머리의 「시험 실행 — 확정 배정 아님」 표시를 **산출물의 「세그먼트 소속」 칸과 보고에 그대로 옮기는 것까지만** 한다 — 거르거나 멈추면 Spec에 없는 규칙을 발명하는 것이 되기 때문이다. **표시 전달은 사람이 나중에 알아채게 할 뿐 흘러드는 것 자체를 막지 못한다**는 것이 G2 ⑳(3차 실행이 `specs/contacts/junonline-jp-rope-picnic.md` 1건의 탐색 순서 근거를 무효로 만든 건)으로 실증됐다. **Spec 「입력」에 조건을 넣어야 닫힌다**
  - **③ 시험 실행 3건이 `보류목록.md` append를 실제로 하지 않은 채 자체 점검을 「충족」으로 통과시켰다 (2026-08-05 G4 검토 라운드 발견).** [메일 없음] 2건이 *"`보류목록.md`에 append(2026-08-05 완료)"*라고 적었지만 그 파일에는 플레이스홀더 행만 있었다. **✅ 해결 — 두 줄을 실제로 append했고(`specs/candidates/보류목록.md`), 자체 점검이 자기 서술을 근거로 통과하지 않도록 `SKILL.md` 8번에 「append 대상 파일을 실제로 열어 확인한다」를 박았다.** 산출물 텍스트만 보고 점검하면 쓰기 실패가 통과로 기록된다는 유형이다
  - **④ `specs/G4_연락처확보.md` 「입력」이 미분류(어느 세그먼트에도 안 들어간) 기업의 탐색 순서를 정하지 않았다 (2026-08-05 1라운드 신규).** Spec·카드는 *"각 세그먼트의 대표를 먼저, 그다음 같은 세그먼트 나머지"*까지만 정하는데, **현행 `specs/segments/SEG-A-v1.md`에서 6곳 중 4곳이 미분류(U-단독)라 이 자리가 소수 예외가 아니다.** 실행체는 *"대표 우선 규칙은 무리 안의 순서라 소속이 없는 기업에는 걸리지 않는다"*로 읽어 **그대로 진행**으로 잠정 처리했고 그 사실을 `SKILL.md` 2번 표에 명시했다 — 시험 실행 3건은 전부 S1 대표·소속이라 이 분기를 탄 사례가 아직 없다. **미분류를 뒤로 미룰지·같은 순위로 볼지·아예 G4 대상에서 뺄지**는 사람이 정한다(`change-reviewer` 2026-08-05 지적 A — G2 ⑤⑫⑯과 같은 「실행체가 공백을 메운 자리는 등록한다」 관례)
  - **⑤ 시험 실행 3건이 슬래시 호출이 아니라 `harness-builder` 대행 실행이고, 표본 검사(같은 기업 2회)는 0건이다 (`grading-critic` 2026-08-05, 세 항목 중 **Harness 견고성·재현성**의 최대 감점으로 지목).** G1·G2가 이미 안고 있는 같은 공백을 G4가 그대로 물려받은 것이라(`91` 2026-08-05 항목) **G4 하나를 고쳐 닫히지 않는다** — 사용자가 메인 세션에서 `/g4-연락처확보`를 1회 실제 호출하면 이 기능분은 닫히고, `disallowed-tools`가 실제로 차단하는지(G2 ⑦)는 그것으로도 안 닫힌다

- [ ] **G5 빌드에서 드러난 공백을 사람이 정리한다 (2026-08-06, `harness-builder` 발견 — 실행체가 스스로 고치지 않고 올린 것). 개수는 아래 항목을 세고 여기서 요약하지 않는다**
  - **⚠ 지금 발송을 실제로 막는 것은 ①이고, 그다음이 ④다**(④는 실행체가 재사용 카드의 문장과 글자대로 어긋난 상태라 등급이 낮지 않다). **Spec·카드(③은 `docs/50`)가 정하지 않아 실행체가 자리·문장을 잡은 곳: ②③⑤. 값의 뜻이 안 정해진 곳: ⑨. 테스트 대기: ⑥. G1~G4에서 이어진 같은 공백: ⑦. 문서 동기화 구조: ⑧. 빌드 도구 자신의 규칙에 대한 판단 지점: ⑩.** **항목이 늘면 이 줄도 함께 갱신하고 번호는 물리 순서와 맞춘다**(`change-reviewer` 2026-08-06 2라운드 — 이 줄이 라우팅 문장인데 새로 넣은 셋이 분류되지 않았고 번호도 삽입 위치와 어긋나 있었다)
  - **① 법정 표시 의무 4종 중 둘의 값이 없어 지금은 승인해도 발송할 수 없다 (무관용 항목).** `specs/G5_제안메일제작발송.md` 「제약」이 4종을 모든 메일에 고정 포함하라고 정했는데, **수신 거부를 접수할 메일 주소·URL**은 발송 전용 주소가 없어 채울 수 없고(`12` **Q15** 대기, `docs/50` 1-1절), **송신자의 주소**는 `docs/10_기업조사.md` 1절에 본사 소재지가 「제주」까지만 있고 번지가 없다. 나머지 둘(명칭 / 수신 거부 가능 사실)과 문의·불만 접수처(`https://clod-ai.com/`)는 확인됐다. 실행체는 **추정치로 채우지 않고** `(미확인)`으로 남겨 무관용 판정을 미충족으로 떨어뜨렸다 — 채우면 위법 표시를 우리가 만든 것이 된다. **이 둘은 코드가 아니라 사람이 확보할 값이다**(`specs/README.md` 「발송 도메인 준비」). 실물은 `specs/발송대기/S1-2026-08-06.md` 1절
  - **② `specs/G5` 「출력 형식」이 이름 붙인 자리가 `발송기록/` 하나뿐이다.** 승인 게이트가 실제로 걸리려면 **승인 화면**이, 검증 감사가 남으려면 **검증 입력 기록**이 있어야 하는데 둘 다 Spec에 자리가 없다. 실행체가 `specs/발송대기/{세그먼트ID}-{작성일}.md`와 `specs/발송대기/_검증입력/{기업식별자}-r{n}.md`로 잡았고(G7의 `replies/_검증입력/` 선례와 같은 규칙), `발송기록/`도 G1 산출물과 같은 층인 `specs/` 아래로 두었다. **G2 ①·G3 ⑤와 같은 유형**(출력 자리를 Spec이 안 정한 자리) — 사람이 Spec에 써 넣으면 그쪽이 이긴다
  - **③ `docs/50` 1-1절의 일일 발송 상한이 단일 값이 아니라 범위(잠정 10~15건)다.** 실행체가 하나를 고르면 그 자체가 판별선이 되므로, **하한(10)**을 썼다 — 그 절이 이 값을 예외로 둔 이유가 도메인 평판의 비용 비대칭(넘치는 쪽이 손해)이라 방향이 이미 정해져 있다는 근거다. **새 판별선을 만든 것인지 카드의 원칙에서 도출한 것인지가 갈릴 수 있는 자리**라 등록한다. `12` Q15 답이 오면 단일 값이 되어 이 항목은 사라진다. **함께 볼 것 — 이 문장은 판정 문장인데 Skill 본문에 있다**(`.claude/README.md` 「Skill이 조건 값을 다루는 방법」 배치 규칙 위반, **G2 ⑮와 같은 유형**). G5에는 전용 기준 카드가 없어 내려보낼 카드 자체가 없다는 것이 G2 ⑮와 다른 점이다 — 값의 정본이 `docs/50`이므로 **그 절이 단일 값을 갖게 되면 이 문장은 지워진다**(`change-reviewer` 2026-08-06 1라운드)
  - **④ 재사용한 `기준/G7_회신판정기준.md` 3절에 G7 전용 잔재가 남아 있고, 실행체가 조용히 다르게 돌았다 (2026-08-06 `change-reviewer` 1라운드 발견).** 그 절 「지켜야 할 것 셋」의 3번이 *"재번역 1회 → 두 번째도 안 맞으면 **C8로 내리고** 원문 + 두 번역본 + 안 맞은 지점을 올린다"*인데, **C8(판정 불가)은 회신 분류라 G5 발신 문안에는 대상 자체가 없다.** 2026-08-01에 층3을 G7용/G5용 두 행으로 가른 것과 **정확히 같은 유형**(리스크 31 계열)이 실패 처리 문장에 남은 것이다. `specs/G5` 실패표는 그 문장을 *"(`기준/G7` 3절과 동일 절차)"*로 가리키고, `SKILL.md` 제작 6번은 C8 대신 **"그 건을 발송 대기열에서 뺀다"**로 썼다 — 실질은 맞지만 같은 스킬이 *"문서가 이긴다"*를 선언했으므로 **글자대로는 실행체가 카드를 어긴 상태**다. **실행체가 카드를 고치지 않았다.** 층1 「안 맞으면」 열의 *"원문에 없던 내용이면 삭제"*도 같은 자리다 — `g5-번역검증`은 *"번역을 고쳐 주지 않는다"*라 삭제 주체가 카드에 없다. **층3처럼 행을 하나 더 만들지 / 카드를 고칠지 / `specs/G5` 실패표의 「동일 절차」 참조를 좁힐지**는 카드 소유자(사람)가 정한다
  - **⑤ 승인란 해석 규칙 두 문장이 Spec·카드 어디에도 근거가 없다 (2026-08-06 `change-reviewer` 1라운드 발견).** `SKILL.md` 발송 1번의 *"승인 값·승인자·승인일시 셋 중 하나라도 비었으면 승인으로 보지 않는다"*와 「멈추는 지점」의 *"애매하면 승인이 아니다"*는 실행체가 만든 판정 문장이다. `specs/G5` 「출력 형식」은 승인 기록에 **승인자·승인일시**가 있어야 한다고만 정하고, **무엇을 승인으로 볼지는 정하지 않았다.** 방향이 안전한 쪽(엄격)이라 실질 위험은 낮지만 **G2 ⑤⑫·G4 ④에서 세운 관례(실행체가 공백을 메운 자리는 등록한다)**에 따라 올린다. `13` 7-2절 R2(승인 화면을 실물로 되묻기) 답이 오면 승인 입력의 형태가 정해지므로 그때 함께 확정한다
  - **⑥ 단위 E(확장 생성)는 실제 확장 사례가 0건이라 테스트 대기다.** S1의 소속은 honeys-co-jp 하나뿐인데 G4 상태가 **[메일 없음]**이라 `specs/G5` 「입력」의 [확보] 조건을 못 넘는다. **없는 데이터를 지어내 확장을 시연하지 않았다** — 절차·형식(`g5-문안제작` 모드 E, 인용 근거를 기업마다 바꾸는 규칙, 스킬 제작 3번의 M→E 순서 강제)은 갖춰 두었고 **실제 확장 1건은 G4가 더 돌아 [확보]가 2곳 이상 나올 때 열린다.** 같은 이유로 「인용 근거가 기업마다 다름(템플릿 복사 0건)」 서식 점검도 **대조 대상 0쌍**이라 판정되지 않았다 — 이 성공 기준은 확장이 실제로 일어나야 처음 판정된다
  - **⑦ 시험 실행이 슬래시 호출이 아니라 `harness-builder` 대행 실행이고, 발송 모드는 한 번도 돌지 않았다.** G1·G2·G4가 이미 안고 있는 같은 공백(G4 ⑤)을 그대로 물려받았다 — 사용자가 메인 세션에서 `/g5-제안메일제작발송 제작 S1`을 1회 호출하면 제작 모드분은 닫힌다. **발송 모드는 그것으로도 안 닫힌다** — ①이 해결돼 무관용 5종을 전부 통과하는 건이 하나라도 생겨야 「승인 기록이 있으면 발송 기록이 써지는가」를 관측할 수 있다. **다만 반대 방향(승인 없이는 안 써진다)은 이번에 관측됐다** — 승인란이 빈 채로 실행이 끝났고 `specs/발송기록/`은 생성되지 않았다. (①의 "무관용 5종"은 리스크 40 반영으로 항목이 하나 늘어 이제 6종이다 — 개수는 `specs/G5_제안메일제작발송.md` 「성공 기준」이 정본이고 여기서 다시 세지 않는다)
  - **⑧ `CLAUDE.md` 7절 Build Order 대조표의 ②③이 G4를 빠뜨린 채로 있었다 (이번 라운드에 발견해 함께 갱신).** `.claude/README.md`에는 `g4-연락처확보`가 확정 스킬로 등재돼 있는데 `CLAUDE.md` 쪽은 「확정 3개」에 머물러 있었다 — **두 문서가 같은 사실을 각자 세는 구조라 한쪽만 갱신되면 조용히 어긋난다**(`90` 리스크 26과 같은 유형). **같은 라운드에 같은 사고를 한 번 더 냈다** — 7절 대조표는 채워 맞췄는데 **같은 파일 4절 라우팅 표의 「서브에이전트 12종 / 확정 스킬 5종」은 놓쳤다**(`change-reviewer` 2026-08-06 1라운드). **✅ 구조 쪽으로 해결 — `CLAUDE.md` 4절에서 개수를 지우고 `.claude/README.md`의 절 제목을 정본으로 지목했다.** 같은 숫자를 두 파일에서 세지 않으면 어긋날 자리가 없어진다. **같은 처리를 7절 대조표 ② Skill 행에도 했다**(*"확정 5개"* → *"확정"*, `change-reviewer` 2라운드가 그 잔재를 잡았다). **남은 것은 실행체 「이름」 목록뿐이다** — 이름은 개수와 달리 「무엇이 어느 하네스인가」라는 판단을 담고 있어 지우지 않았고, 그래서 **그쪽은 여전히 손으로 동기화해야 한다.** 개수를 세는 자리는 이제 `.claude/README.md` 절 제목 하나다
  - **⑨ `docs/50` 1-2절 「승인 화면 형태」의 *"문안 3종 미리보기"*가 무엇을 세는 3인지 정해져 있지 않다 (2026-08-06 `change-reviewer` 2라운드 발견 — 실행체 검토자가 고를 자리가 아니라고 명시적으로 넘긴 항목).** 두 읽기가 가능하다 — **(a)** 한국어 의도 / 일본어 제목 / 일본어 본문 **3블록**이면 `specs/발송대기/S1-2026-08-06.md`는 이미 충족이고 더 할 일이 없다. **(b)** `13` 7-2절 R2의 *"메일 초안 3종"* 계열(승인자에게 서로 다른 톤·구성의 초안 3개를 보여 고르게 하는 안)이면 **승인 화면 형태 자체가 미충족**이고 `specs/G5` 「범위」에 「한 기업당 문안 3안 생성」이 들어가야 한다 — 지금 Spec에는 그런 요구가 없다. **실행체는 문안 1건만 만들었고 어느 쪽인지 고르지 않았다.** 이 값은 `docs/50` 소유자와 R2 되묻기 계획에 얽혀 있어 사람이 정한다. 정해지기 전까지 승인 화면이 그 절을 따랐는지는 **판정 불가**다
  - **⑩ 이번 빌드가 언어화한 규칙 하나가 정작 그 판단을 내리는 문서에 도달하지 않는다 (2026-08-06 `change-reviewer` 3라운드가 판단 지점으로 올린 것).** 실행 단위를 가르는 질문 ①(자기검증)이 걸렸을 때 **무엇을 떼는지**가 두 얼굴이라는 것 — ⓐ 검증자가 결론을 보면 안 된다(검증 단위를 뗀다) ⓑ **다시 만드는 쪽이 앞 회차 자기 산출물을 보면 안 된다**(생성 단위를 뗀다) — 은 G5에서 실물로 필요해져 `.claude/README.md`에 적혔다. 그런데 `agents/harness-builder.md`의 질문 ① 근거 칸은 여전히 ⓐ 하나뿐이고, **그 에이전트가 실행 단위를 가르는 1번 단계에서 읽는 것은 자기 표와 G1·G7 실물 4파일이라 `.claude/README.md`는 참조 대상이 아니다.** 즉 다음 빌드에서 이 규칙이 판단에 도달하지 않는다. **올리면 정본이 비대해지고 「질문은 둘」이라는 골격이 흐려지고, 안 올리면 규칙이 죽는다** — `CLAUDE.md` 5절 절차 상한 원칙(지우면 출력이 바뀌는가)으로 사람이 가른다. **실행체가 자기를 만드는 문서를 스스로 고치는 자리라 이번에 손대지 않았다**
- [ ] **G6 빌드에서 드러난 공백을 사람이 정리한다 (2026-08-06, `harness-builder` 발견 — 실행체가 스스로 고치지 않고 올린 것). 개수는 아래 항목을 세고 여기서 요약하지 않는다**
  - **⚠ 지금 G6을 실제로 막는 것은 ①(정식 입력 0건)이고, 판정 결과를 실제로 바꾸는 것은 ②③④⑩이다. Spec·카드가 정하지 않아 실행체가 자리·문장을 잡은 곳: ⑤⑥⑦⑫. 유효기간이 있는 판정: ⑧. 앞 기능들에서 이어진 같은 공백: ⑨. 이번 빌드로 조건이 도래한 것: ⑪.** **한 항목을 두 범주에 넣지 않는다** — ②는 판정을 바꾸므로 앞쪽에만 둔다(G5 라우팅 줄이 냈던 중복을 여기서 반복하지 않는다). **항목이 늘면 이 줄도 함께 갱신하고 번호는 물리 순서와 맞춘다**(G5 항목에서 세운 관례)
  - **① 테스트 대기 — 정식 입력인 G5 발송 기록이 0건이라 이 기능은 한 번도 판정을 내리지 못했다.** `specs/발송기록/` 디렉터리 자체가 없다. 원인은 G6이 아니라 **위 G5 ①**이다(법정 표시 의무 4종 중 둘이 미확인 → 승인해도 발송 모드가 발송 기록을 쓰지 않는다). **없는 발송 기록을 지어내 시연하지 않았다** — 실행체가 실제로 밟은 것은 절차의 첫 게이트 하나뿐이다(입력 자리를 열어 0건을 확인하고 멈추는 경로). **3상태 분류·대기 기간 계산·소프트 바운스 승격·정정 반영은 전부 미검증이다.** **정정 분기는 한 겹 더 막혀 있다** — 정정 신호의 출처인 G7 레코드도 0건이라, G5 ①이 풀려 발송 기록이 생겨도 **회신이 실제로 도착해야** 그 분기가 처음 돈다. **그 레코드의 자리 표기가 문서마다 갈린다는 것도 이번에 드러났다** — `specs/G7_회신해석정리.md` 「출력 형식」과 `.claude/skills/g7-회신처리/SKILL.md`는 접두어 없이 `replies/`인데, 그 뒤에 만들어진 산출물 자리는 전부 `specs/` 아래다(`candidates/`·`segments/`·`scores/`·`contacts/`·`발송대기/`). G7이 아직 한 번도 안 돌아 실물이 없어서 충돌이 표면화되지 않았을 뿐이다 — **G6이 그 디렉터리를 「열지 않는다」로 다루므로 이 기능에는 실질 영향이 없지만**(경로를 몰라도 지켜지는 금지다), G7을 실제로 돌리기 전에 자리를 하나로 정한다
  - **② 「영업일」의 정의가 어디에도 없다.** `기준/G6_판정기준.md` 3절이 대기 기간을 **10영업일**로 정했는데, 주말만 빼는지 공휴일도 빼는지(뺀다면 **어느 나라** 공휴일인지 — 발신은 한국, 수신은 일본이다)가 카드에 없다. 실행체는 **주말만 제외하고 공휴일은 반영하지 않는 것으로 잠정 처리**하고 그 사실을 산출물에 적게 했다. **방향이 안전한 쪽이 아니다** — 공휴일을 빼면 판정이 늦춰지므로, 지금 규칙은 [무응답]을 **며칠 이르게** 낼 수 있고 그만큼 G8 원인 분석·재시도가 일찍 돈다. **함께 볼 것 — 이 문장은 판정 문장인데 Skill 본문에 있다**(`.claude/README.md` 「Skill이 조건 값을 다루는 방법」 배치 규칙 위반, **G2 ⑮·G5 ③과 같은 유형**). G5와 달리 G6에는 전용 카드가 있으므로 **카드 3절에 한 줄 넣으면 그 자리에서 해소되고 Skill 본문의 그 문장은 지워진다**
  - **③ 카드 4절의 「정정 후 상태」가 같은 카드 3절·Spec 「제약」과 글자대로 어긋난다.** 4절은 *"[회신]으로 판정해 둔 것을 **[무응답]으로 되돌린다**"*이면서 동시에 *"자동응답 도착 시점부터 **다시 대기 기간을 센다**"*인데, 다시 세면 그 순간에는 대기 기간이 안 지났으므로 3절(*"지나지 않았으면 [대기중]이지 [무응답]이 아니다"*)과 Spec 「제약」(*"대기 기간이 안 지났는데 무응답으로 확정하지 않는다"*)에 따라 **[대기중]**이어야 한다. 실행체는 **카드 자신의 계산 지시(다시 센다)를 따라 3절을 재통과시키는 쪽**으로 처리하고 카드를 고치지 않았다. **4절의 「[무응답]으로 되돌린다」를 「[회신] 판정을 취소하고 3절로 되돌린다」로 고칠지**가 사람이 정할 자리다 — 지금 문언대로면 자동응답 1건이 즉시 [무응답]으로 확정돼 **G8이 원인 분석을 시작한다**
  - **④ 정정 대상이 「못 찾음」 말고 「둘 이상」·「미확정」일 때가 카드 4절에 없다.** 4절 3번은 *"못 찾으면 적용하지 않고 사람에게 알린다"*만 정하는데, **같은 기업에 발송 건이 여럿이면 식별자 하나로는 어느 발송 건인지 갈리지 않는다**(상태 파일 키는 `{기업식별자}-{발송일시}`인데 G7 레코드가 확정하는 것은 G1 식별자다 — `specs/G7_회신해석정리.md` 「출력 형식」). G7이 **`-미확정`**으로 낸 건도 같다. 실행체는 둘 다 **적용하지 않고 사람에게 알린다**로 처리했다 — 그중 하나를 고르는 것이 곧 같은 절이 금지한 *"추측으로 아무 레코드에나 붙이는 것"*이라는 근거이며, **카드에 이름으로 적혀 있지는 않다.** 재발송이 실제로 도는 순간(소프트 바운스 재시도·G8 재시도) 발송 건이 기업당 여럿이 되므로 잠재 항목이 아니다
  - **⑤ 소프트 바운스 「재시도」의 주체가 정해져 있지 않다.** 카드 2절은 *"잠정 3회, 각 24시간 간격"*으로 횟수만 정하는데, **그 재발송을 누가 하는지가 없다.** G6은 4경계에 **발송 권한이 없어** 스스로 재시도할 수 없고, G5는 「발송 결과 판정은 범위 밖」이라 되받을 자리가 없다. 실행체는 **메일 시스템의 자동 재시도를 조회 회차마다 관측해 세는 것**으로 잠정 처리했다(그래야 발송 권한 없이 카드 2절이 성립한다). **메일 시스템이 알아서 재시도하는 것을 세는 것인지 / 우리가 다시 보내는 것인지**를 정해야 하고, 후자면 그 재발송이 G5의 승인 게이트를 다시 거치는지까지 함께 정한다(같은 문안을 같은 주소로 다시 보내는 것이라 **반자동 요구와 맞닿는다**)
  - **⑥ 메일 시스템 응답의 조달 경로가 Spec에 없다.** `specs/G6_발송결과판정.md` 「입력」이 이 값을 **`← 외부`**로만 적었다. 실행체는 **인자로 붙여넣어 받는 것**으로 잠정 처리했다(G7이 회신 원문을 인자로 받는 선례). **이 저장소에는 메일 인프라가 아직 없다**(`specs/README.md` 「발송 도메인 준비」)는 것이 지금의 실질 이유이며, MCP가 연결되면 그쪽이 정본이 된다 — 그때 ⑧과 함께 본다. **G3 ③(재료 조달 경로가 어디에도 없다)과 같은 유형**이지만, G3과 달리 **경로가 서로 모순되는 것이 아니라 비어 있는 것**이라 실행체가 고를 문제는 아니었다
  - **⑦ `specs/G6` 「출력 형식」이 출력 자리를 `상태/`로만 적었다.** 디렉터리 층이 없어 실행체가 앞 기능 산출물과 같은 층인 **`specs/상태/`**로 잠정 처리했다. **G2 ①·G3 ⑤·G5 ②와 같은 유형**(출력 자리를 Spec이 안 정한 자리) — 사람이 Spec에 써 넣으면 그쪽이 이긴다
  - **⑧ 「스킬 하나」 판정에는 유효기간이 있다 — 메일 시스템 MCP가 연결되면 재판정 대상이다.** 실행 단위를 가르는 질문 ②(닫아야 할 쪽이 열거 불가능한가)를 **지금은 「아니오」로 판정**했다(근거: 4경계의 *"…까지만 / 메일 발송 권한 없음"*은 `specs/` 12개 중 10개가 쓰는 상용구이고, 같은 문구를 G1·G2·G3·G4·G7 다섯이 `disallowed-tools` 거부 목록으로 처리한 선례가 있다). **그러나 거부 목록은 새 도구를 기본 허용한다** — 메일 MCP가 붙으면 그 서버의 발송 도구가 G6에도 기본 허용되고, **G6은 그 MCP를 「바운스 조회만」 필요로 하는 유일한 기능**이라 서버 단위로 통째 거부할 수도 없다. 그때 **허용 목록(서브에이전트 `tools:`)으로 옮길지**를 다시 판정한다. `33` D23이 지목해 둔 「허용 목록이 필요한 후보」의 두 번째 사례다
  - **⑨ 실행체가 슬래시 호출은커녕 대행 실행조차 못 했다 — 실행 0건이다.** G1·G2·G4·G5가 안고 있는 같은 공백(G4 ⑤·G5 ⑦)을 물려받되 **한 단계 더 나쁘다**: 앞 넷은 `harness-builder` 대행으로라도 절차를 한 번 완주했지만 G6은 ① 때문에 판정 자체가 0건이다. **닫는 조건도 다르다** — 사용자가 `/g6-발송결과판정`을 호출하는 것만으로는 안 닫히고, **G5 ①이 풀려 발송 기록이 1건 생기고 그 건에 메일 시스템 응답이 실제로 도착해야** 처음 닫힌다
  - **⑩ 같은 Spec의 「제약」과 4경계 「입력」이 조회 단위를 다르게 적는다 (2026-08-06 `change-reviewer` 1라운드 발견).** 「제약」은 *"여러 건을 한 맥락에서 같이 보되, 한 건의 판정이 다른 건의 판정에 영향을 주지 않는다"*이고 4경계 「입력」은 *"발송 기록 1건 + 그 건의 최신 메일 시스템 응답을 **1건씩**"*이다. **실행체는 4경계를 그대로 두었다**(판정은 1건씩, 다만 여러 건이 한 맥락에 오는 것 자체는 되돌리지 않는다) — 4경계는 넓히지도 좁히지도 않고 옮기는 자리이기 때문이다(`.claude/agents/harness-builder.md` 2번). **처음 초안은 「제약」만 들어 배치 허용으로 썼고 그것이 4경계를 넓힌 것이라 1라운드에 물렸다.** 어느 쪽이 정본인지는 사람이 정하며, **「1건씩만 받고 되돌린다」로 정해지면** `SKILL.md` 「인자」 절과 `CLAUDE.md` 7절 ③ 칸·`.claude/README.md` G6 행·`91` 2026-08-06 줄이 함께 바뀐다
  - **⑪ 이번 빌드로 `Harness/` 패키징 조건이 도래했다 — 실행체가 하지 않고 사실만 올린다.** `Harness/README.md`가 *"MVP 6개 기능(G1·G2·G4·G5·G6·G7) 실행체 빌드가 다 끝나면 한 번에 패키징한다"*로 트리거를 걸어 두었고, **G6이 그 마지막이었다.** 그 README의 G4·G5·G6 칸은 아직 `(미빌드)`로 남아 있다. 패키징은 **제출 범위 결정**이라 실행체가 늘리지 않는다(`.claude/agents/harness-builder.md` 7번). 착수하면 위 **⑨-1의 규칙 3종**(내부 참조 경로 재작성 / 기준카드 확장자 확인 / 운영변수 정본 동봉)과 **G2 ⑬-2**(스킬 경로 실제 실행 0건)를 함께 처리하고, **G6은 실행 0건·테스트 대기 상태 그대로 담긴다는 것을 패키지에 표시**할지 함께 정한다
  - **⑫ 재조회 회차의 표기 규칙 셋이 Spec에 없어 실행체가 잡았다 (2026-08-06 `change-reviewer` 1라운드 발견).** 이 기능은 **같은 발송 건을 시간을 두고 다시 부르는 반복 실행**인데 「출력 형식」이 **1회 조회의 산출물 모양**만 정한다. 실행체가 잡은 것 셋 — ⓐ 재조회 시 **회차별 응답 기록과 정정 이력만 이어받고 이전 상태는 결론으로 이어받지 않는다**(이어받으면 성공 기준 표본 검사가 「첫 조회의 복사」로 통과된다) ⓑ 회차별 응답을 쌓을 **별도 항목이 「출력 형식」에 없어 「판정 근거」 안에 둔다**(⑤와 한 뿌리다 — 카드 2절의 소프트 바운스 승격이 회차 수로 판정되므로 안 쌓으면 승격 자체가 불가능하다) ⓒ **정정 신호만 온 회차의 「조회일시」**에 정정 반영 시각을 적고 그 사실을 근거에 밝힌다(성공 기준 7번이 조회일시를 빈칸으로 두는 것을 허용하지 않는데 그 값의 뜻이 정해져 있지 않다). **ⓐ는 상태 값을 실제로 바꿀 수 있고 ⓑⓒ는 표기다**
- [ ] **G8·G9·G10 빌드에서 드러난 공백을 사람이 정리한다 (2026-08-06, `harness-builder` 발견 — 검토를 사용자 지시로 약식화한 라운드. 세 기능을 한데 묶었고, 개수는 아래 항목을 세고 여기서 요약하지 않는다)**
  - **⚠ `grading-critic`이 아직 셋을 묶어 돌지 않았다 — 아래는 `change-reviewer`(sonnet, 1라운드씩) 기준의 공백이다.** 지금 실제로 실행을 막는 것은 G8·G9의 상류 실물 부재(G6·G4 [미확보] 0건)이고, G10만 실물 1건(tonal-japan-com)이 돌았다.
  - **[G8] ① Spec 「범위」(3종)와 「출력 형식」·「성공 기준」(4종, 판정 불가 포함)이 어긋난다.** 실행체는 4종으로 일관되게 썼다 — Spec을 고치지 않았다. 「범위」 문구를 4종으로 맞추는 것이 사람 몫이다
  - **[G8] ② 「같은 발송 건」의 정의가 없다(`기준/G8` 3절).** 재시도는 새 발송이라 발송일시가 달라져 파일 키(`{기업식별자}-{발송일시}`)가 갈리므로 파일명 단위 반복 횟수 누적이 물리적으로 불가능하다. 실행체는 **기업 식별자 단위 집계**로 잠정 처리하고 그 사실을 산출물에 적었다 — 카드가 정의를 채워야 한다
  - **[G8] ③ 「G3 미가동」의 정의가 갈린다(`기준/G8` 2절).** 카드는 **가동 여부**로 썼지만 실제로 없는 것은 **「잠정 상위 구간」의 선**이고 `specs/scores/`엔 이미 시험 산출물이 있다. 실행체는 「선이 없으면 低 기본」으로 읽었다 — G3이 실제로 가동하는 시점에 이 해석이 유지되는지 사람이 정한다
  - **[G8] ④ 거부 목록의 유효기간 — G6과 대칭이지만 방향이 반대다.** G8은 **어떤 MCP도 필요 없는** 기능이라, 메일 시스템 MCP가 연결되면 오히려 **허용 목록(서브에이전트 `tools:`) 전환 후보**가 된다(G6은 「일부만 필요해서」 재판정 대상)
  - **[G9] ⑤ `specs/G4_연락처확보.md` 「출력 형식」의 *"G9·G10이 MVP로 올라오면 정식 큐로 옮긴다"*가 어느 큐인지 안 적혀 있다.** 연결 정본(`docs/31` 3절)은 G9 입력을 [미확보]로만 적어 실행체는 그것을 따라 [메일 없음]을 경로1에서 되돌렸다 — **그런데 지금 실물 2건이 전부 [메일 없음]이라, 이 한 줄이 G9의 유일한 테스트 가능 입력을 열거나 닫는다**
  - **[G9] ⑥ 재사용 대상 `기준/G3_적합도기준.md`에 적합도 문턱이 아예 없다.** 그 카드는 스스로를 "필터가 아니라 정렬"로 규정하는데 `specs/G9` 「제약」·「성공 기준」은 문턱 미달 배제(0건)를 요구한다. 실행체는 **배제하지 않고 순위 구간만 표시**하고 그 항목을 「판정 불가 — 문턱 미확정」으로 남겼다 — 카드에 문턱 절을 세울지 G9 쪽을 「순서」로 바꿀지 사람이 정한다
  - **[G9] ⑦ 브리프 재실행 규칙이 Spec에 없다.** 유입 경로가 1→2로 바뀌는 일(연락처 확보 후 발송·무응답 → 재시도 가치 高)이 실제로 생기는데, 실행체는 기존 파일을 덮지 않고 **회차를 덧붙이는** 쪽으로 잠정 처리했다
  - **[G10] ⑧ 재조사 코드 표기가 결번과 어긋난다.** `specs/G10` 「제약」이 `O1~O9`로 연속 표기하지만 `기준/G1_기업판정기준.json`은 `O7`을 결번·재사용 금지로 못박았다 — 실행체는 카드를 정본으로 O7을 제외했다. 연속 구간 표기를 그대로 두면 다음 실행자가 결번을 채운다
  - **[G10] ⑨ 「어느 항목이 `?`인지」의 정본이 없다 — 첫 실행에서 실제로 갈렸다.** `specs/G10` 「입력」이 가리키는 것이 G1의 **「다시 볼 항목」 필드**인지 **관찰층의 `?` 값**인지 정해지지 않았고, `tonal-japan-com`에서 관찰층(O3·O4·O6·O8)과 G1이 적은 값(O3·O4·O8, **O6 누락**)이 갈렸다. **`change-reviewer`가 이번 라운드에서 가장 깊은 자리로 지목** — `specs/G1_대상기업발굴.md` 「출력 형식」의 그 필드 정의를 못박아야 닫힌다
  - **[G10] ⑩ G4 [메일 없음]의 G10 유입 자격이 없다 — 실물 2건이 갈 곳 없이 떠 있다.** `specs/G10` 「입력」 6종에 [메일 없음]이 없는데(`docs/31` 3절 정본과 일치), `specs/candidates/보류목록.md`의 junonline·honeys 2건이 그 상태다. 실행체는 6종 중 하나로 억지 라벨을 붙이지 않고 멈추는 자리로 명시했다 — ⑤와 같은 뿌리(G4 「출력 형식」의 미래형 문장 하나가 G9·G10 둘의 입력 자격을 동시에 흔든다)
  - **[G10] ⑪ 「G1 판정 불가분」의 범위가 갈린다.** `specs/G1` 「출력 형식」이 *"`?`가 있으면 재수집 대상 = 예"*로 적어 **후보 판정이 「포함」인 5곳도 재수집 대상으로 표시돼 있다**(전부 O8 `?`) — 좁게 읽으면 1곳, 넓게 읽으면 6곳이다. 실행체는 둘 다 만족하는 tonal 1건으로만 테스트해 해석을 건드리지 않았다
  - **[공통] ⑫ 셋 다 출력 자리에 디렉터리 층이 없다.** `specs/원인분석/`·`specs/위임브리프/`·`specs/재수집대상/`으로 각각 잠정 처리했다 — G2 ①·G3 ⑤·G5 ②·G6 ⑦과 같은 유형, 사람이 Spec에 써 넣으면 그쪽이 이긴다
  - **[공통] ⑬ 스킬 경로로 한 번도 실행되지 않았다 — G1·G2·G4·G6과 같은 자리.** G10만 `harness-builder` 대행으로 실물 1건이 돌았고 G8·G9는 그마저도 없다(상류 실물 0건)
  - **⑭ `grading-critic` 미실행 — 사용자 지시로 G8·G9·G10을 묶어 한 번에 돌 예정(2026-08-06, 토큰 비용 절감).** 「파일이 생겼다」가 완료가 아니므로 그 판정 전까지 세 실행체는 검토 절반 상태다
  - **⑮ 「재수집 대상이 아님」 판정 3종([발송 금지] 제외 / G8 가치 低 / 신호 불명확 보류)의 기록 자리가 Spec에 없다.** `candidates/보류목록.md`는 G4 소유라 `g10-재수집판단`이 쓰지 않는다 — 그 셋은 지금 응답에만 남고 파일로 남지 않는다. 자리가 필요한지는 사람이 정한다
  - **✅ [G8→G9/G10] ⑯ 해결 (2026-08-06, `grading-critic` 최종 검토 발견 → 즉시 수정).** 같은 라운드가 G8 산출물(`specs/원인분석/`)에 대해 **G9는 인자 전달을, G10은 파일 직접 조회를 계약**해 두 스킬이 같은 파일을 다르게 소비하고 있었다(G9의 근거 문장 자체가 작성 시점에 이미 거짓 — G8 자리가 이미 확정돼 있었다). `g9-위임브리프/SKILL.md`를 G10과 같은 **파일 조회 우선(최신 발송일시 건, 없으면 인자로 보완)**으로 통일했고, `g8-원인분석/SKILL.md` 7번 보고에 다음 호출 문장(`/g9-위임브리프 경로2 …` / `/g10-재수집판단 "G8 조사 오류" …`)을 이름으로 명시했다. `g10-재수집판단/SKILL.md`에는 **같은 기업의 원인분석 파일이 둘 이상일 때 최신 발송일시 건을 쓰고 나머지 건의 존재를 산출물에 밝히는 규칙**을 추가했다(발송 건 단위 산출물을 기업 단위 인자로만 받을 때의 공백을 닫았다)
  - **⑰ `run-orchestrator`의 「사람 대기 지점은 둘뿐」 조건이 이번 빌드로 뒤집혔는데 배선이 갱신되지 않았다.** `.claude/agents/run-orchestrator.md`와 `docs/34_사용자흐름.md`가 *"부가 기능이 착수되기 전까지"*를 조건으로 정지점을 둘로 제한해 뒀는데, G9(사람 위임 브리프)가 이제 세 번째 정지점 후보로 존재한다. 정지점 개수·배선 갱신은 사람이 `docs/34` 8절에서 정할 자리이며 실행체가 손대지 않는다
  - **⑱ 거부 목록(`disallowed-tools`)에 `Skill`·`SlashCommand`가 빠져 있어, 웹 권한이 있는 다른 스킬(`g1-기업판정` 등)을 슬래시로 불러 우회할 수 있는 여지가 이론상 남는다.** G2·G6·G8·G9·G10·**G11**(2026-08-06 빌드로 추가) 여섯이 공유하는 구조적 지점이라 이 셋만의 결함이 아니다. **`disallowed-tools`가 이 런타임에서 실제로 어떤 도구 이름까지 막는지 관측된 적이 없다는 것 자체도 함께 확인 대상이다**(`PowerShell`이 유효한 항목명인지도 미확인). 슬래시 호출 1회(⑬)와 같은 자리에서 함께 확인한다
  - **⑲ `Harness/README.md`의 `(미빌드)` 3칸(G4·G5·G6)과 G8·G9·G10 미등재를 이번에 실물과 맞춰 갱신했고(2026-08-06), `specs/README.md`에 `원인분석/`·`위임브리프/`·`연락처후보/`·`재수집대상/` 절을 추가했다 — 순수 표기 동기화라 즉시 반영, 별도 항목으로 남기지 않는다.**
- [x] **G12 빌드에서 남은 것 (2026-08-06, 외부 전달분 반영 후) → 같은 날 `grading-critic` 부분 모드로 채점(부분·부분·미달) → 지적 사항 대부분 반영**
  - **① `grading-critic` 미실행 → ✅ 실행 완료 (2026-08-06 부분 모드, 3항목 부분·부분·미달).** 가장 큰 지적은 "도구 권한으로 원천 차단"이라던 **허위 안전장치 서술**(리스크 41과 같은 유형) — `specs/G12_질의응답.md`·`SKILL.md`·`.claude/README.md` 전부 "Write는 열려 있고 지침으로만 막는다"로 정정. 그 외 조인 대상 목록이 실물과 어긋나던 것(`집계/`는 세그먼트 키, `replies/`는 키 순서 반대, `발송금지.md`는 파일 자체가 없음 등)을 실물 대조로 정정, 산출물 경로 3중 불일치(`specs/질의응답로그/`로 통일), 로그 파일명 충돌 처리(초 단위 + 일련번호), 식별자 파싱 규칙(파일 본문 항목값만 사용, 파일명 파싱 금지), 구 이력 파일·잠정 표시 파일을 가리는 규칙, 인용 재확인 단계, 질문유형→조회범위 매핑 표를 추가했다. `Harness/README.md`에도 G3·G12를 함께 등록(이전엔 둘 다 누락)
  - **② 실제 질문으로 1회도 호출된 적 없다 → ✅ 해결.** `/g12-질의응답 후보 순위 어떻게 돼`를 실제 슬래시 경로로 호출해 `specs/질의응답로그/2026-08-06-150000.md`를 남겼다. **이 실행이 실제로 새 규칙(최신 파일 우선·상충 명시)이 걸리는 실물 사례를 만들어냈다** — `specs/scores/순위표-2026-08-05.md`의 동점 순위 표기("구간 번호")가 같은 날 나중에 확정된 `기준/G3` 표준경쟁순위 규칙보다 오래된 것이었고, G12가 그 상충을 답변에 그대로 남겼다(둘 중 하나로 임의로 확정하지 않음)
  - **③ Spec 자신의 舊 「확인 후 채울 것」 3건 — 2026-08-10 정책 전환으로 갈렸다.** 로그 보존 기간은 임의값(무기한 보존)으로 채워 실사용 중(`docs/93_구현_검증대기.md` 1절 #4). 같은 대화 세션 내 맥락 유지 여부는 4경계 "독립 컨텍스트"로 이미 결정돼 있던 것과 같은 질문이라 중복 제거·정리했다. **`-미확정` 표현 방식만 실제로 남는다** — `specs/G12_질의응답.md` 「지금 쓰는 값」 절의 "남은 것". 화면 설계 단계가 와야 정해지는 운영/UX 결정이라 지금 임의로 채우지 않는다(과도한 사전 결정 지양)
  - **④ (신규, grading-critic 지적) `specs/candidates/`에 구 이력 파일 3개(honeys-co-jp.md 등)와 현행 `후보목록.md`가 한 디렉터리에 섞여 있어, 조회 시 실수로 구 파일을 인용할 위험이 실물로 있다.** G12 쪽 규칙(최신 우선, 구 파일 안 읽음)으로 막았지만, **이 파일들 자체를 별도 `이력/` 디렉터리로 옮기거나 파일 머리에 경고를 붙이는 근본 조치는 G1 소유 영역이라 여기서 하지 않았다** — `specs/README.md`·`g1-기업판정` 쪽 후속 과제로 남긴다
  - **⑤ 인용 실재성 재확인(자기검증)을 별도 실행 단위 없이 같은 실행 안의 재확인 단계로 처리한 판단은 재검토 여지가 있다.** grading-critic이 "조회는 자기검증 문제가 없다"는 기존 판단을 다시 보라고 지적했다 — 지금은 "생성이 아니라 대조라 컨텍스트를 가를 이유가 약하다"는 논리로 가볍게 처리했지만, 실사용 중 허위 인용 사례가 관찰되면 T/V류 분리를 다시 검토한다
- [ ] **G11 빌드에서 드러난 Spec 공백을 사람이 정리한다 (2026-08-06, `harness-builder` 발견 — 실행체가 스스로 고치지 않고 올린 것). 개수는 아래 항목을 세고 여기서 요약하지 않는다**
  - **⚠ 지금 이 기능의 절반을 실제로 못 돌게 하는 것은 ①이다**(조정 제안 절 전체가 여기 걸린다). **Spec이 정하지 않아 실행체가 잠정으로 잡은 곳: ②③⑥. 루프가 안 닫히는 곳: ④. 테스트 대기: ⑦. 여섯 기능이 공유해 여기서 안 닫히는 것: ⑧**(⑱에서 함께 닫는다). ⑤는 이번에 정정 완료. **항목이 늘면 이 줄도 함께 갱신한다**(G5 :184·G6 블록이 세운 관례 — 이번 2라운드에 한 번 놓쳤다가 `change-reviewer`가 잡았다).
  - **① 최소 표본 크기 값이 없어 「조정 제안」(출력 형식 2절) 전체를 낼 수 없다.** `specs/G11_반응집계_기준조정.md` 「제약」은 *"표본 부족 조합은 제안에 넣지 않는다"*를 요구하고 「성공 기준」 (a)는 그것을 **0건**으로 판정하는데, **부족 여부를 가를 값이 없으면 그 0건을 보장할 유일한 방법이 제안을 하나도 내지 않는 것**이다. 실행체는 숫자를 지어내지 않고 2절을 `판정 보류 — 최소 표본 기준 미확정`으로 두고 집계표(1절)만 내게 했다. **같은 이유로 실패표 2행(신호 상충 → "재검토 필요"로 올린다)도 발동할 자리가 없다** — 제안을 안 내는 동안에는 "제안 대신"이 성립하지 않는다. **이 값은 코드가 아니라 실제 반응률 분포를 본 사람이 정한다**(Spec 「확인 후 채울 것」). 정해지면 그 자리는 SKILL 본문이 아니라 `기준/G11_*.md` 카드다(`.claude/README.md` 배치 규칙) — **지금 카드를 만들지 않은 이유가 이것이다: 값이 없는 카드는 파일만 하나 늘린다**
  - **② 「조건 조합」이 이름만 있고 정의된 곳이 없다.** Spec 「범위」·「출력 형식」 1절이 *"세그먼트 × 조건 조합"*을 축으로 쓰는데 **무엇이 조건인지를 어느 절도 정하지 않았다.** 실행체는 발명하지 않고 **상류 Spec이 스스로 표시한 것**을 잠정 축으로 썼다 — `specs/G5_제안메일제작발송.md` 「출력 형식」 표가 소비처를 **G11로 명시한** 세 항목(인용한 근거 / 제안 내용 / 발신 언어·번역 검증 결과)이다. **잠정이라는 사실과 지울 조건을 산출물 머리에 노출한다**(G2 ⑫가 세운 *"적어 두지 않은 판별선은 여전히 즉석 판단"* 관례). **사람이 `specs/G11` 「범위」에 조건 축을 써 넣으면 그쪽이 이긴다**
  - **③ 출력 자리를 Spec이 `집계/`로만 적었다.** `specs/` 아래로 잠정 처리했다 — `specs/G12_질의응답.md`를 실행하는 `g12-질의응답`이 이미 `specs/집계/{집계일}.md`를 조회 대상으로 등재해 둔 것과 맞춘 결과이며 다른 상류 산출물과도 같은 층이다. **G2 ①·G3 ⑤·G5 ②·G10 ①과 같은 유형**(출력 자리를 Spec이 안 정한 자리). **같은 집계일 파일이 이미 있을 때의 처리도 Spec에 없어** 덮어쓰지 않고 멈추게 했다(접미사를 스스로 만들지 않는다)
  - **④ 「기각 이력」을 사람이 적을 칸이 어느 출력 형식에도 없다 — 루프가 닫히지 않는다.** Spec 「범위」는 *"이전 제안의 기각 이력 참조"*를 포함으로 두고 「출력 형식」 2절도 *"이전 기각 이력(있으면)"*을 요구하는데, **집계 파일에 사람이 승인/기각을 기록하는 자리가 정의돼 있지 않다.** 실행체는 이전 `집계/` 파일을 열어 확인하되 **기록 자리가 없으면 `기각 이력 확인 불가 — 기록 자리 없음`으로 적고 "기각 없음"으로 바꿔 적지 않는다.** 실패표 3행(*"기각됐는데 같은 조합이 다시 나오면 이력을 표시하며 다시 제안한다"*)이 통째로 이 칸에 걸려 있다 — **①이 풀려 제안이 열리는 시점에는 이 칸도 함께 있어야 한다**
  - **⑤ ✅ 정정 — 「도구 권한으로 원천 차단」이 거짓 안전장치였다** (`G12` ①과 같은 유형의 반복). Spec 「성공 기준」 (a) 4번과 「실행체 경계 4개」 도구 권한 칸이 *"G2·G3 파일에 쓰기 권한이 없다 / 이 경계가 자동 적용이 아님을 물리적으로 강제한다"*고 적고 있었으나, **집계 파일을 쓰려면 `Write`가 열려 있어야 하고 `disallowed-tools`는 경로 단위가 아니다.** *"쓰는 자리를 집계 출력 하나로 제한하는 지침"*으로 정정했다(`g10-재수집판단`·`g12-질의응답`이 쓰는 문구 패턴). **이 정정은 실행체가 Spec을 직접 고친 것이라 ⑭(「실행체는 Spec·기준 카드를 고치지 않는다」와 「거짓·끊긴 서술은 고쳐라」의 충돌)가 등록해 둔 그 자리가 또 발생한 것이다** — 이번에도 호출자 지시가 있었고, 고친 것은 판정 문장·임계값이 아니라 **기제에 대한 사실 서술**이다. 규칙 문장에 그 통로는 여전히 없다. (**몇 번째 발생인지는 세지 않는다** — 「누가 고쳤는가」가 사례마다 달라 같은 유형으로 묶이는지가 이력 전수 추적 없이는 안 갈린다. `change-reviewer` 2026-08-06 지적)
    - **함께 올린 것 — 「거짓 안전장치」 계열의 묶음이 실제로 맞는지가 확인되지 않았다.** `CLAUDE.md` 7절과 이 문서 G12 항목이 **리스크 41(G5 승인 게이트)을 G12 ①·G11 ⑤와 「같은 유형」으로 묶어** 왔는데, 리스크 41의 실제 내용은 *"승인 판독과 발송 실행이 같은 실행 단위"*(**자기검증 원칙 미적용**)이고 `specs/G5_제안메일제작발송.md`에는 *"원천 차단"·"물리적으로 강제"* 같은 **거짓 서술 자체가 없다**(`change-reviewer` 2026-08-06 2라운드가 문구 검색으로 확인). 즉 **G12 ①·G11 ⑤는 「문서가 거짓 기제를 주장했다」이고 리스크 41은 「기제가 아예 없었다」로 계열이 다를 수 있다.** 묶음을 유지할지 가를지는 이번 변경 하나로 판정할 수 없어(버그 분류 체계 자체를 보는 일이다) `project-critic`이나 사람이 볼 사안으로 남긴다
  - **⑧ `disallowed-tools`에 `Skill`·`SlashCommand`가 빠져 있다 — 위 G8·G9·G10 블록 ⑱과 같은 자리다**(그 항목에 G11을 추가 등록했다). 웹·`Agent`는 닫았지만 웹 권한이 있는 다른 스킬을 슬래시로 불러 우회할 여지가 이론상 남는다. **G11만의 결함이 아니라 여섯 기능이 공유하는 구조적 지점**이라 여기서 따로 처리하지 않고 ⑱에서 함께 닫는다
  - **⑥ Spec 「입력」에 있는데 「출력 형식」에 담을 칸이 없는 것 둘 — G7 회신 성격(C1~C8)과 G8 원인 가설.** 집계표 1절의 열은 반응률 3종뿐이라 이 둘이 들어갈 자리가 없다. 실행체는 **집계표에 열을 새로 만들지 않고** 2절 제안의 근거로만 쓰게 했다(그 2절이 ①로 보류라 실질적으로 대기 상태다). **함께 정한 것 둘** — G6 상태 [대기중] 건은 Spec 「입력」이 넘기는 상태 3종에 없으므로 비율 분모에서 빼되 **뺀 건수를 표에 적고**(안 적으면 분모가 발송 건수와 다른 이유가 설명되지 않는다), `-미확정` 식별자 건은 Spec 「제약」대로 조인에서 빼고 「식별 불가」 건수로만 적는다
  - **⑦ 입력 전부가 0건이라 「테스트 대기」다 — 실행체가 실제로 밟은 것은 첫 게이트 하나뿐이다.** `specs/발송기록/`·`상태/`·`replies/`·`원인분석/` **네 디렉터리가 존재 자체를 하지 않는다**(2026-08-06 확인). G8·G9와 같은 처지이며 뿌리는 G5 ①(법정 표시 2종 미확인으로 발송 0건)이다. **합성 데이터를 만들지 않았다.** 밟은 것은 *"발송 기록이 0건이면 파일을 만들지 않고 보고하고 끝낸다"*는 1번 게이트뿐이고, 그 처리 자체도 Spec에 없어 실행체가 잡은 것이다(행 없는 집계표는 *"아직 안 돌았다"*와 *"돌았는데 반응이 없었다"*를 구분하지 못한다). **G1·G2·G4·G5가 안고 있는 「슬래시 호출 0건」도 그대로 물려받는다**
- [ ] **월 운영비 계산 — 실제 구현 착수 직전에 시뮬레이션으로 확정한다** (사용자 지시, 2026-08-03). 지금은 `50` 3절에 가정 상태로 비워 둔다. 스킬화 등 구현 논의가 나오면 그 전에 이 계산부터 먼저 제안한다
- [ ] **`12` N17 확인 — 일본 발송 적법성** (신규 최우선). 리스크 19. 답에 따라 관찰 항목이 하나 늘어나므로 **설계 변경 사항**이다. 확인 전에는 실제 발송을 전제한 어떤 결정도 확정하지 않는다
- [ ] **`40_검증계획.md` V9 실행 — O8(광고 메일 거부 표시) 판별선 확인** (`12` N17과 묶어 처리). V1 폐기 후 위법 직결 항목만 축소 승계한 것 — 일본 기업 5곳, 두 사람 독립 O/X/? 판정, 갈린 문구만 `기준/G1` 2-2절에 반영. 리스크 16·19
- [ ] **`13` 7-1절 R1 반영 — 국가 선정 3축의 용도 변경** (미반영). 일본을 3축으로 대조해 캘리브레이션하고, 그 3축으로 2차 국가를 제시한다. 이게 없으면 "왜 일본인가"에 답이 "기업이 그랬다"뿐이다. **`40` V3이 이 작업의 검증 항목이다**
- [ ] **`13` 7-2절 R2 반영 — 반자동 범위를 실물로 되묻기** (미반영). 추상 선택지로는 기업이 못 골랐으므로(`13S` U12) 승인 화면 목업 + 메일 초안 3종을 놓고 확인받는다. **세그먼트 2안을 묻는 `12` N19와 같은 자리에서 한다** — 둘 다 실물을 놓고 되묻는 방식이고, 그 답이 `40` **V7**의 입력이다
- [ ] **`40` V7 다섯째 항목("두 사람이 같은 기업을 같은 무리에 넣는가") 실행 — 기업 답 대기 없이 지금 할 수 있다** (`90` 리스크 42①, 2026-08-06 등록). `specs/candidates/후보목록.md`의 같은 6곳(G2 3차 실행이 쓴 표본)을 별도 판정자(다른 세션·다른 사람)가 `기준/G2` 2절 규정만 보고 독립적으로 무리 지어 본 뒤 `specs/segments/SEG-A-v1.md`와 대조 — 갈리면 2절 규정 문장이 모호한 것
- [x] **`specs/G1` 舊 「확인 후 채울 것」 — 2026-08-10 임의값으로 채움(목록 상한=무제한, 관찰 항목 타당성은 `docs/93` 2절 #11로 검증 대기 이관).** 실제 `12` Q9·Q10·N11 답이 오면 `docs/92`에서 교체
- [x] **남은 기능의 Spec 작성 — G1~G11 전 기능 완료 (2026-07-30).** 각 Spec은 **Workflow인가 Agent인가를 반드시 선언**하고(미결정 #8), 자체 기준 검증을 만들면 **V 번호를 `40`에서 받아 간다** (아래는 작성 당시 순서 기록, 이력)
  - [x] **G2 세그먼트화 — 2026-07-30 완료.** `specs/G2_세그먼트화.md` + `기준/G2_세그먼트기준.md`. 브리프 요구 ②의 담당인데 Spec도 검증도 비어 있던 자리다
  - [x] **기존 3개 Spec에 작성 규칙 적용 마무리 — 2026-07-30 완료.** `specs/README.md` 규칙 3(활용 방식 선언)·4(4경계)가 세 Spec 전부에 없던 것을 「제약」에 채웠고, 근거 4건을 `33` D4~D8로 이관, **끊긴 절 번호 참조 14곳**과 **`기준/G2` 관찰 항목 코드 밀림**을 정정. 상세는 `91`
  - [x] **G3 적합도 평가 — 2026-07-30 완료.** `specs/G3_적합도평가.md` + `기준/G3_적합도기준.md`. 작성 중 `31` 6절 #2·#3을 실제로 해소했다 — G2는 입력에서 뺐고(전체 절대 비교), 척도는 넷 다 **단일 척도 + 소비처별 기준선**으로 고정(`33` D9). `40` V8은 *"척도가 하나인가"*에서 *"그 척도의 가중치가 실제 회신과 맞는가"*로 성격이 바뀌었다
  - [x] **G4 연락처 확보 — 2026-07-30 완료.** `specs/G4_연락처확보.md` + `기준/G4_연락처유효성기준.md`. 옵트인 예외 판정은 `기준/G1` 2-2절 거부 문구 목록을 재사용하되, **딥서치로 새로 찾은 주소는 G1이 본 적이 없으므로 그 자리에서 다시 확인한다**는 절차를 이번에 처음 명시했다
  - [x] **G5 제안 메일 제작·발송 — 2026-07-30 완료.** `specs/G5_제안메일제작발송.md`. 요구 ②③의 나머지 절반. **발송 도구 권한을 가진 유일한 기능**이며 승인 기록 없이는 권한을 행사하지 않는 조건으로 반자동을 지킨다. 번역 검증은 `기준/G7` 3절 재사용, 별도 카드 없음
  - [x] **G6 발송 결과 판정 — 2026-07-30 완료.** `specs/G6_발송결과판정.md` + `기준/G6_판정기준.md`. 바운스 코드 3분기, 무응답 판정 대기는 **잠정 10영업일**(구 "7영업일"은 재사용하지 않음 — 리스크 4가 폐기 판정), G7 정정 반영 절차 포함
  - [x] **G8 미응답·미도달 원인 분석 — 2026-07-30 완료.** `specs/G8_원인분석.md` + `기준/G8_원인분석기준.md`. 무시/도달실패를 가르는 신호는 G4 연락처 등급 하나, [주소오류]는 항상 조사오류로 직행(G10)
  - [x] **G9 사람 접촉 위임 브리프 — 2026-07-30 완료.** `specs/G9_위임브리프.md`. 경로 1·2를 하나의 브리프 형식으로 통합, 사람이 찾은 연락처도 `기준/G4` 등급 판정을 그대로 거침(우회 지름길 없음). 별도 기준 카드 없음
  - [x] **G10 재수집 판단 — 2026-07-30 완료.** `specs/G10_재수집판단.md`. 재조사 항목은 G1 코드(O1~O9)로만 지정, 재수집 주기는 수치를 정하지 않음(구 60일·90일 재사용 안 함 — 리스크 4), "특정 항목만 재조사" 성립 여부는 `40` V1 결과 대기로 명시. 별도 기준 카드 없음
  - [x] **G11 반응 집계·기준 조정 — 2026-07-30 완료 (부분 확정).** `specs/G11_반응집계_기준조정.md`. `31`의 "지금 못 쓴다"는 판단을 **구조/값 분리로 부분 철회**(`33` D11, `40` 0절과 같은 논리) — 입출력·집계 형식·조정 제안 구조는 지금 확정, 실제 집계 결과·조정 제안·최소 표본 크기는 발송 데이터 대기. ~~**G2·G3 파일에 쓰기 권한이 없다**는 도구 경계로 "제안이지 자동 적용 아니다"를 물리적으로 강제.~~ **← 2026-08-06 G11 빌드가 이 문장을 무효로 판정했다**(경로 단위 제약이라 도구 권한으로 안 닫힌다 — 3절 G11 ⑤. 이력이라 지우지 않고 정정 표시만 남긴다). **이로써 G1~G11 전 기능의 Spec 작성이 완료됐다**
- [ ] **남은 아홉 기능(G3~G6·G8~G12 — G1·G2·G7은 완료)의 실행체 빌드 — `build-orchestrator`로 돈다.** 빌드 도구 4종(`harness-builder`·`build-orchestrator`·`run-orchestrator`·`grading-critic`)은 2026-08-05 신설됐고(근거 `33` D23), **`harness-builder`는 3회 돌았다 — G2 1차 → G3 → G2 재빌드(2026-08-05 확정 이관)**(위 두 「빌드에서 드러난 공백」 항목이 그 산출물이다 — **실행체 파일보다 그 목록이 이 빌드들의 더 큰 소득이었다**). **`build-orchestrator`와 `run-orchestrator`는 아직 한 번도 돌지 않았다.** 다음 회차에 확인할 것 셋 — ① 「실행 단위 수」 두 질문이 실제로 갈리는지 — **2026-08-06 갈렸다.** G2·G3·G4가 연속으로 「스킬 하나」로 수렴해 `33` D23 뒤집힐 조건 ①(전부 수렴하면 `harness-builder`의 opus 고정을 내린다)이 발동 직전이었으나, **G5에서 처음으로 스킬 1 + 서브에이전트 2로 갈렸다**(자기검증 문제가 Spec 「제약」에 명시 + 기준 카드가 공용을 선언했는데도 방향이 달라 기존 서브에이전트를 재사용할 수 없었던 판단까지 포함). **조건 ①은 발동하지 않는다** ② 앞 기능 산출물이 없어 「테스트 대기」로 끝나는 기능이 몇 개인지 ③ **서브에이전트가 서브에이전트를 부르는 2단 중첩**(`build-orchestrator`→`harness-builder`→`change-reviewer`·`grading-critic`)이 실제로 성립하는지 — 문서 대조로는 판정할 수 없고 한 번 돌려 봐야 안다(`change-reviewer` 2026-08-05 2라운드). **G12는 다른 작업으로 병렬 진행 중이라 산출물이 불완전할 수 있다** — 없으면 대기로 기록하고 추측으로 채우지 않는다
- [ ] **`평가항목.jpg`가 git에 올라가 있다 — 커밋 대상인지 사용자 판단 필요 (2026-08-05, `change-reviewer` 2라운드 발견).** 심사 배점표 이미지이며 `평가항목.md`가 그 전사본이다. 같은 출처의 `[Upstage] … 교육 가이드.docx`는 *"부트캠프 운영 측 배포 자료 — 타인 저작물"*로 `.gitignore`에 등재됐는데 이 이미지는 추적 중이다(`git ls-files`로 확인). `CLAUDE.md` 6절 「타인 저작물은 올리지 않는다」에 걸리면 `.gitignore` 추가 + `git rm --cached`가 필요하고, 이미 올라간 뒤이므로 **히스토리 처리 여부는 사용자 지시로만 한다**(6절). **`평가항목.md`(우리 전사본)는 그대로 둔다** — 표 자체는 사실 정보고, `grading-critic`이 이 파일을 정본으로 읽는다
- [ ] 대안 비교·검증 과정을 발표 자료에 명시적으로 포함
- [ ] **`기준/` 카드 변경 시 자동 모순·형식 검토 (Hook 후보)** — 사람이 `기준/` 카드를 고치거나 G11 조정 제안을 승인 반영할 때, 물어보지 않아도 `change-reviewer`류 검토가 자동으로 걸리게 하는 안. 하네스 A·B 공통으로 비어 있는 Build Order ⑤ Hook 자리를 채우는 것과 같다. 지금은 개념만(`33` D21), Skill·Subagent·Workflow가 더 채워진 뒤 순서대로 본다
- [ ] **`기준/G2`·`기준/G3` 조건을 웹 화면에서 편집하는 인터페이스 (가칭 G13)** — `기준/` 카드를 텍스트 편집 대신 구조화된 입력창(조건 선택 → 추가/수정/삭제)으로 다루고, 그 자리에서 G12(질의응답)에게 근거를 물을 수 있게 하는 안. **의존성상 후순위** — Skill이 `기준/`을 참조하는 규칙(`.claude/README.md`, `33` D21)이 여러 스킬에 실제로 적용돼 안정화된 뒤에 착수한다

### 완료·폐기된 액션 (2026-07-30 정리)

지운 것이 아니라 **왜 더 안 해도 되는지**를 남긴다.

| 액션 | 처리 |
|---|---|
| ~~1절 미결정 8건 확정~~ | **완료.** #1~#5는 `31`에서, #6·#7은 2026-07-30에 확정, #8은 기능별 Spec 항목으로 성격 변경 (1절) |
| ~~코트라 공개 열람 범위 확인 + 대체 소스 비교 조사~~ | **완료 (2026-07-29).** `11_대안탐색_비교.md` — OpenAPI 무료 공개 확인, ITC Trade Map 조건부, UNCTAD 탈락 |
| ~~국가 선정 3축의 지표·임계값 확정~~ | **성격 변경.** 국가를 고르는 용도가 아니게 됐다(일본 지정). 남은 것은 **캘리브레이션**이며 위 R1 항목과 `40` V3으로 넘어갔다 |
| ~~타겟 국가 1개 확정 — 여러 국가 동시 조사 금지~~ | **완료.** 기업이 일본으로 지정했다(`13S` K1). 우리가 고를 사안이 아니었다 |
| ~~국가 선정 기준과 기업 발굴 기준을 별도 문서로 정리~~ | **완료.** 국가 선정 근거는 `11`, 기업 발굴 기준은 `31` G1 + `specs/G1`로 갈려 있다 |
| ~~`40_검증계획.md` V1 실행 — 관찰 판정 일치 검증~~ | **🔴 폐기 (2026-08-03).** 발송 전 사전 게이트가 `31` 1-5절(컨택 기회 확보 1순위)과 부딪히고, 표본 5~10건의 통계적 무의미함을 문서 스스로 인정하면서도 필수 게이트로 세운 것이 자기모순이었다. **대체 근거 중 "G11이 사후 대체한다"는 부분은 `project-critic` 재검토로 사실이 아님이 드러났다**(G11은 G2·G3만 조정) — O4는 확정 규칙으로 리스크를 없앴고, O8(위법 직결)만 V9로 축소 승계했다. 근거·경위는 `91` 2026-08-03(V1 폐기) + 2026-08-03(후속 정정) 항목, 절차 이력은 `40` 1절 |
| ~~기업 미팅 시 실제 실패 사례 확보~~ | **완료 (2026-07-30 설명회).** 4건 확보 — 일본 조사 난항·협력사 의존 / 플랫폼 다수로 전수 탐색 불가 / SNS 계정 정지 / 담당자 정보 미갱신으로 미도달 (`13S` F10~F13) |

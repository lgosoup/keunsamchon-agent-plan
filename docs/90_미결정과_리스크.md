# 90. 미결정 사항 · 리스크 · 다음 액션

> 구현에 들어가기 전에 정해야 할 것과, 정해도 남는 위험을 모아둔 **작업 목록**이다. 결정이 끝난 항목은 `30_해결설계.md` 본문에 반영하고 여기서는 "확정" 표시만 남긴다.
> 출처: 2026-07-28 상담 + 지원 단계 자료 대조(원본은 `../archive/01_지원단계/1지망_수행아이디어_통합정리.md` 5·7절).

---

## 1. 결정해야 할 것 (권고 포함)

| # | 결정 사항 | 선택지 | 권고 | 근거 | 상태 |
|---|---|---|---|---|---|
| 1 | 메일 형식 | (a) 전원 동일 / (b) 타겟 맞춤 / (c) 자사소개 동봉 + 협업 시그널 | (b)+(c) 결합 | (c)는 "신뢰 인식이 비용 논리보다 먼저"라는 문제정의에, (b)는 "고객 특성에 맞춘 제안"이라는 브리프 원문에 대응 | **확정 (2026-07-30)** — `31` G2+G5. **맞춤 단위는 개별 기업이 아니라 세그먼트**이고 자사 자료 동봉은 G5 입력에 포함 |
| 2 | 커뮤니케이션 깊이 | (a) 단발성 / (b) 분석 내용 포함 | (b) | 신호를 수집해 매칭까지 하는 구조를 만들고 결과만 보내면 근거가 메일에 드러나지 않는다 | **확정 (2026-07-30)** — `31` G5 하위 설계에 "G1 관찰층 근거를 본문에 인용해 왜 귀사인가를 설명"으로 반영 (원안 D-4 승계) |
| 3 | 반응 트래킹 범위 | 회신 여부만 / 읽음 여부까지 | 회신 중심 + 읽음은 보조·한계 명시 | 읽음 추적은 픽셀 방식이라 차단·규제로 정확도가 낮다 | **확정 (2026-07-30)** — `31` G6은 **회신 / 무응답 / 주소 오류 3분기**. 기업이 요구한 "안 읽음 vs 주소 오류" 구분은 픽셀이 아니라 **바운스 코드 분류**로 실현 |
| 4 | 메일 외 후속 채널 | 메일 팔로우업만 / 전화 등 확장 | 계획에는 포함, 구현은 메일까지 | 해커톤 범위를 넘는다 | **확정 (2026-07-30)** — `31` G9가 그 자리다. 채널은 "전화"가 아니라 **사람이 주변·협력사를 통해 접촉**이며 기업 실제 자산에 맞춘 형태 |
| 5 | 타겟 국가 | 미정 | 3축 조사 후 확정 | — | **확정 — 일본.** 기업이 지정(`13S` K1). **3축은 국가 선택 도구가 아니라 (i) 일본 캘리브레이션 (ii) 2차 국가 후보 도구로 용도 변경** (`13` 7-1절). ⚠ "가깝다·K문화 적합도"는 기업 발언이 아니라 우리 판단이다 (`31` 1-3절) |
| 6 | 온톨로지·지식그래프 | 유지 / 축소 / 삭제 | **축소 유지 (부가 표시)** | "기술보다 기획" 원칙상 비중을 키우면 역효과 | **확정 — 채택하지 않음 (2026-07-30).** `31`이 기능을 G1~G10으로 확정했고 **그 안에 없다.** 지원 단계 자료(`archive/`)에만 남아 있고 이후 어느 설계 문서에도 들어가지 않았다. `(부가)`로도 넣지 않는 이유: 요구를 담당하는 기능이 이미 열 개인데 여기에 관계 구조화를 얹으면 `CLAUDE.md` 2절이 경고한 *"요청한 걸 안 만들고 하고 싶은 걸 만든"* 비중이 된다. **되살리려면 `91`에 근거를 먼저 쓴다** |
| 7 | 코트라 대체 소스 | 미조사 | **조사 필요 (우선순위 높음)** | 조사 결과가 비교·검증 서술의 핵심 재료가 된다 | **확정 — 조사 완료 (2026-07-29).** 결과는 `11_대안탐색_비교.md`에 있다 — 코트라 국가정보는 **OpenAPI 무료 공개**(축3 확정), 축2는 해외한류실태조사, **ITC Trade Map 조건부**(선진국 계정 등급 미확인), **UNCTAD 지수 탈락**(최신판 2020년). **남은 것은 축1 정량화 하나**이고 그것은 `40` **V3**이 다룬다 |
| **8** | **하네스 B를 Workflow로 만드는가 Agent로 만드는가** | (a) Workflow — 사람이 미리 설계한 고정 순서 / (b) Agent — AI가 스스로 경로를 판단 / (c) 혼합 | **(a) Workflow를 기본, 대화형 조정은 `(부가)`로 분리** | `30` ⑦은 10단계 고정 순서(=Workflow)인데 **같은 절 끝에 "전역 LLM 에이전트와 대화로 조정·자연어로 재분석 요청"(=Agent)이 섞여 있다.** 둘은 **오류 처리가 정반대**다 — Workflow는 실패 시 파이프라인 중단, Agent는 스스로 재시도. 선언하지 않으면 실패 경로가 설계에서 빠진다(실제로 빠져 있었다 → 리스크 17). 브리프가 요구한 "반자동"과 심사의 "판단 근거를 설명할 수 있는가"에도 (a)가 맞는다 | **부분 확정 — 기능별로 선언한다 (2026-07-30).** **Spec이 있는 세 기능 전부가 「제약」에서 "Workflow다"를 선언하고 실패 3종의 처리까지 확정했다** — G1(판정 불가로 기록 후 종료 / 채운 항목은 유지 / 소스 차단 시 수집 범위 기록), G2(미분류 과다는 사람에게, 스스로 축을 바꾸지 않음), G7(판독 불가는 C8 / 검증 불일치는 재번역 1회 후 C8 / 검증층이 비면 사용자에게 보내지 않음). 근거는 `33` **D8**. **전역 결정이 아니라 각 기능 Spec에서 선언하는 항목으로 성격이 바뀌었다** — 기능마다 실패 처리가 다를 수 있기 때문이다. 남은 것은 **G3~G6·G8~G11의 선언**이며 각 Spec 작성 시 채운다 |

> ✅ **2026-07-29 정합성 경고 해소.** 미결정 #1~#5가 `31_기능구조.md`에서 정식 확정됐다(위 표).
>
> ✅ **2026-07-30 — #6·#7 확정, #8 부분 확정. 1절에 남은 미정 항목이 없다.** #8은 미결정이 아니라 **각 기능 Spec이 반드시 채울 항목**으로 성격이 바뀌었다.
>
> ⚠ **확정은 "다시 열지 않는다"는 뜻이 아니다.** #5(타겟 국가)처럼 기업이 정한 것은 우리가 못 바꾸지만, #6·#7은 **우리 판단**이므로 근거가 바뀌면 `91`에 쓰고 되돌릴 수 있다.

## 2. 리스크

> **2026-07-30 상태 점검.** 아래 1~8은 2026-07-28 상담 시점에 등록된 것이고, 그 뒤 조사·설명회·설계 교체를 거치며 **해소되거나 다른 항목으로 승계된 것이 다섯 건**이다. 해소된 것을 그대로 두면 목록 전체가 "안 읽히는 목록"이 된다. **지우지 않고 상태만 표시한다** — 왜 지금 신경 쓰지 않아도 되는지가 근거다.

| # | 내용 | 대응 | 상태 |
|---|---|---|---|
| 1 | 코트라 데이터 실제 접근 범위 미확인 | 공개 열람 범위 확인 + 대체 소스 후보 확보 | **해소 (2026-07-29).** 국가정보는 **OpenAPI로 무료 공개**돼 있고 개발계정 1만 트래픽까지 확인했다(`11` 2절). "특정 데이터셋을 단정하지 않는다"는 제약도 함께 풀린다 — 이제 이름을 적을 수 있다 |
| 2 | 지표명·조사명을 확인 전에 못 박으면 사실 오류 | 3축 표의 지표는 **후보**로만 다루고, 실제 확보한 것만 이름을 적는다 | **유효 — 일반 원칙으로 유지.** 3축은 확정됐으나 같은 실수가 다른 곳에서 난다. 실제로 `31` 1-3절에서 *"가깝다·K문화 적합도"*를 기업 발언으로 잘못 적은 것이 발견됐다 |
| 3 | "읽음 여부 트래킹" 과잉 약속 | 1절 #3대로 회신 중심 + 읽음은 한계 병기 | **해소 (2026-07-30).** `31` G6이 **회신 / 무응답 / 주소 오류 3분기**로 확정됐고 읽음은 분기에서 빠졌다. 기업이 요구한 "안 읽음 vs 주소 오류" 구분은 픽셀이 아니라 **바운스 코드**로 실현된다 |
| 4 | ~~정량 기준 누락 — SKU 50개·7영업일·14일·90일을 빠뜨리지 않는다~~ | — | **🔴 폐기 (2026-07-30). 그대로 두면 위험한 항목이었다.** 여기 적힌 수치가 **전부 근거를 잃었다** — SKU 50개는 폐기된 매칭 로직용이었고(`31` 9절), 60·90일은 수치를 떼어내고 구조만 G10으로 승계했다. 이 리스크는 **폐기된 수치를 "빠뜨리지 말라"고 지시하고 있었다.** <br>**승계본**: *"임계값은 각 기능의 Spec에서 근거와 함께 정하고, 근거 없는 수치를 서술에 남기지 않는다."* 값이 없으면 자리만 만든다(`31` G5 발송 상한이 그 방식) |
| 5 | ~~Clo.D / AI-Studio 매칭 로직 누락~~ | — | **폐기 (2026-07-30).** 제안 서비스가 AI-Studio 하나로 확인돼(`13S` K8) 매칭 자체가 성립하지 않는다. 브리프 요구 ②의 담당은 **`31` G2 + G5**로 이관됐고, **그쪽을 축소하지 않는 것**이 이 리스크의 승계본이다. 검증은 `40` **V7** |
| 6 | 버즈워드 밀도 | AI 기술명 나열보다 판단 기준·근거 서술 비중을 높인다 | **유효.** 발표·제출 시점까지 계속 본다 |
| 7 | 공개 정보만으로는 의사결정권자 연락처 확보가 어려움 | 일반 문의창구로 수렴되는 경우를 전제로 메일 문안·팔로우업을 설계 | **유효 — 설계에 반영됨.** `31` G4가 *"유효 = 존재가 아니라 의미 있는 창구인지"*로 판정하고, 미확보분은 **G9(사람 위임)**로 넘긴다. **기업도 같은 예상을 말했다** — 실제 담당자 도달률을 *"굉장히 낮은 퍼센트"*로 봄(`13S` F15) |
| 8 | 국가별 콜드메일 규제 | 타겟 국가 확정 시 해당국 스팸 규율을 함께 확인 | **발생 → 리스크 19로 승계 (2026-07-30).** 국가가 일본으로 확정됐고 규제를 확인했다. 대응은 **국가 선정 기준이 아니라 발송 적법성 요건**으로 재배치됐다(리스크 15·19) |

### 2-1. 2026-07-29 조사·검토로 새로 등록된 리스크

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 9 | **Clo.D 해외 판매 성립 여부** — 동대문 사입삼촌 구조에 종속. 국내 논리를 해외에 옮기면 "우리 사업을 모른다"로 읽힌다 | **해소, 확정 완료 (2026-07-31).** 2026-07-30 설명회 시점엔 기업이 **AI 비주얼 제작을 1차 목표**로 지목한 정황(`13S` K8)만 있어 "Clo.D 배제"를 단정하지 않았으나, **이후 기업에 직접 재질의해 "AI-Studio 단독, Clo.D 범위 제외"를 명확히 확인했다.** `30` ⑤와 2절 「기업 활용 제안」은 폐기, `00`·`10`·`31`에 확정 반영 | `10` 2절, `12` Q1 |
| 10 | **판별 신호가 지불 고객과 반대 방향일 가능성** — 실제 지불 고객은 전원 "전문" 등급인데 설계는 "비전문"을 타겟한다 | **해소 — 신호 자체가 교체됐다 (2026-07-30).** 1차 판별축이 이미지 전문성이 아니라 **제품 사진 / 커머스 / 업종 / 한국 진출 희망**으로 바뀌었다(`13S` H4, `31` 1-1절). 방향이 문제였던 항목은 **가점 항목으로 내려가** 1차 판별에서 빠졌다. **남은 것**: 가점 항목으로 쓸 때의 방향은 여전히 미확인이며 `12` N2가 그것을 가른다 | `10` 3절 + 검토 |
| 11 | **캐파 초과 리드는 낭비이자 위험** — 제작 캐파를 넘는 발송은 승인 부담·도메인 평판 손실만 남는다 | **2026-08-03 대응 변경.** 상한을 미리 걸지 않는다 — 기업 상황(바쁨, 컨택 자체가 안 됨)상 무제한이 기본이고, 값이 오면(`12` Q9) `docs/50` 1절에 참고 수치로만 기록한다. 감당 못 하면 후보 목록에서 필요한 만큼만 가져다 쓴다. **승인 부담**은 `50` 1-2절, **도메인 평판**은 리스크 12(별도 예외)로 분리했다 | 검토 |
| 12 | **회사 도메인 평판 훼손** — 메인 도메인이 스팸 처리되면 기존 동대문 거래 메일까지 피해 | **이 리스크만 무제한 원칙의 예외다** — 전용 서브도메인(`specs/README.md` "발송 도메인 준비") + 일일 발송 상한(`docs/50` 1-1절, 담당자가 직접 늘려가는 값) + 원클릭 수신거부. `12` Q15 답 대기 | 검토 |
| 13 | **레퍼런스 무단 인용** — GUESS·BARNEYS·CJ ENM을 허가 없이 메일에 쓰면 기업 리스크 | 허가 확인 전 템플릿에서 물리적 제외 (`12` Q7) | 검토 |
| 14 | **회신 이후 경로 공백** — 견적·영문 계약·결제 수단이 없으면 계약 직전에 막힌다 | **리스크 21로 통합 (2026-07-30).** 같은 사안이 두 번 등록돼 있었다. 21이 `31` G7을 **경계로 명시**한 것까지 반영한 최신본이므로 그쪽을 본다. 확인은 `12` **N20**·Q12 | 검토 |
| 15 | **콜드메일 규제를 국가 선정 후에 확인하는 순서 오류** — 옵트인 국가를 1순위로 고르면 국가 선정을 다시 해야 한다 | **🔴 실제로 발생했다 (2026-07-30).** 기업이 지정한 일본이 옵트인 원칙 국가이며, 국가 확정 후에도 규제 확인이 실행되지 않았다. 다만 **국가 선정을 다시 하는 것은 불가능**하다 — 기업이 지정했으므로 게이트 조건으로 쓸 수 없다. → **규제를 국가 선정 기준이 아니라 발송 적법성 요건으로 재배치**했다 (리스크 19) | 검토 → **발생·대응 완료** |
| 16 | **판별 기준의 객관성이 미검증** — 관찰 항목을 "누가 봐도 같은 결론에 이르는 기준"으로 **선언만 했고 확인한 기록이 없다.** 사람끼리 갈리는 기준은 자동화해도 갈린다. 이 전제가 무너지면 목록 신뢰도와 **메일의 맞춤 근거**(*"귀사의 공개 정보에서 이런 점을 확인했다"*)가 함께 무너진다 | **`40` V1으로 실행 처리** (리스크 등록으로 끝내지 않는다). 일본 기업 5~10곳을 두 사람이 독립 판정 → 갈린 항목의 기준 문장 수정 → 실행체와 대조. `01` C4<br>⚠ **2026-07-30 대상 교체** — 신호 4종이 아니라 **`31` G1 관찰층**이다. 무너지는 범위에서 *"⑤ 매칭 입력"*을 뺐다(그 기능이 폐기됐다) | 수업 1일차 |

### 2-2. 2026-07-30 새로 등록된 리스크

> 출처가 둘로 갈린다 — **17·18**은 수업 1·2강 개념 대조에서, **19·20·21**은 `project-critic` 적대적 검토와 일본 법규 확인에서 나왔다. 이전 절 제목이 전자만 가리키고 있었고 18이 21 뒤에 놓여 있어 바로잡았다.

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 17 | **판정 실패 경로가 설계에 없다** — 초기 설계의 제외 조건 3종은 전부 "봤는데 부적합"이고, **"못 봤다"**(사이트 접속 불가 / 스크립트 렌더링으로 상품 목록을 읽을 수 없음 / 로그인 요구 / 상품 수를 셀 수 없음)는 정의돼 있지 않았다. 둘이 한 칸에 섞이면 재수집 때 어느 쪽을 다시 열어야 하는지 알 수 없고, **V1 불일치의 원인 분석도 오염된다**(접근 조건 문제가 기준 문장 문제로 집계된다) | **반영 완료 (2026-07-30).** 판정 불가를 제외와 다른 **별도 상태**로 두고 사유를 남긴다 — `specs/G1` 「성공 기준」, `기준/G1` 3절(? 사유 목록), `40` V1 기록 양식의 전용 행, `31` G1 관찰층. **V1을 돌리면 즉시 부딪히므로 실행 전에 반영했다.** `specs/G1` 「제약」의 Workflow 실패 처리가 한 번 더 고정 | 활용 방식 3분류(Chatbot/Workflow/Agent)의 "오류 발생 시" 축으로 대조 |
| 18 | **기획 하네스(A)에 강제 실행 장치가 없다** — `CLAUDE.md` 5절의 "게이트 전과 미팅·발표 전에 `project-critic`으로 검토한다"는 규칙은 **스스로 판단해 건너뛸 수 있는 형태**다. 규칙이 지켜졌는지 남는 기록도 없다. 자기 점검은 자기 글에 관대해지므로 이 규칙이 건너뛰어지면 적대적 검토가 아예 없는 상태로 게이트를 통과한다 | 선택지는 (a) `.claude/settings.json`에 Hook을 두어 사건에 묶는다 (b) `01` 게이트 표에 "검토 실시 여부"를 기록 항목으로 넣는다 (c) 현행 유지. **권고는 (b) 먼저** — Hook은 자동 실행 동작을 심는 것이라 사용자 확인이 필요하고, (b)만으로도 "건너뛴 사실이 남는다"는 효과가 생긴다. **미대응** | Hook이 Skill로 대체되지 않는 두 이유 중 첫 번째와 대조 |
| 19 | **🔴 일본 특정전자메일법 요건이 설계에 없었다 — 리스크 15가 실제로 발생한 형태** | 일본은 광고 메일에 **사전 동의(옵트인)가 원칙**이고, 웹 공개 주소 예외에도 조건이 붙는다 — **주소 옆에 "광고·선전 메일 거부" 표시가 있으면 예외가 적용되지 않아 발송이 위법**이다. 여기에 표시 의무 4종(송신자명·수신거부 가능 사실·수신거부 접수처·주소와 문의처)이 별도로 붙는다. 국가가 일본으로 확정됐는데 확인이 실행되지 않았다 | **`31`에 반영 완료 (2026-07-30)** — G4 출력에 **[발송 금지]** 상태 신설(제외·판정 불가·미확보와 다른 네 번째 상태, 재수집 대상에서도 제외), G1 관찰층에 "광고 메일 거부 표시" 항목 추가, G5 하위 설계에 표시 의무 4종을 1급 제약으로. **법 해석 자체는 `12` N17로 기업 확인 필요** | 総務省·迷惑メール相談センター 가이드라인 (2026-07-30 확인) |
| 20 | **발송 상한이 어느 기능에도 없었다** | G3은 접촉 **순서**만 정하고 **한 번에 몇 건**을 정하지 않았다. 상한이 없으면 리스크 11(캐파 초과)·12(도메인 평판)가 그대로 실현된다. 이 회사는 SNS에서 같은 유형의 사고를 이미 겪었다(`13S` F12) | **2026-08-03 — 자리를 `docs/50` 1·1-1절로 옮겼다.** 제작 캐파(리스크 11)는 무제한이 기본이라 역산 컷오프를 안 만든다. **일일 발송 상한(리스크 12)만 실제 상한**이고, 그 값은 `50` 1-1절에서 담당자가 직접 조정한다(자동 컷오프 알고리즘 미제작) | `13` 7-3절 R3 |
| 21 | **회신 이후 경로가 플로우 밖이다** (리스크 14 통합) | `31` G7이 사용자 전달로 끝난다. 미팅·견적·영문 계약·결제가 없다. 기업 실무 준비 상태가 미확인(`13S` U7)이라 기능을 만들지 않았으나, **답이 "준비 안 됐다"면 이 프로젝트의 우선순위가 바뀐다** — 리드를 늘리는 것보다 회신 처리 경로를 만드는 것이 먼저가 된다 | `31` G7에 **경계임을 명시**했다(빠뜨린 것과 경계인 것은 다르다). 확인은 `12` **N20**·Q12 | 검토 |

### 2-4. 2026-07-31 `project-critic` 전체 검토로 새로 등록된 리스크

> 치명 1건(기업 비공개 자료 공개 위험)은 발견 즉시 처리 완료(`91` 로그 참조 — `.gitignore`·`docs/10`·`CLAUDE.md` 6절 반영). 아래는 **아직 처리하지 않은** 나머지다.

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 22 | **`docs/20_문제정의.md`·`docs/50_실사용_도입설계.md`가 존재하지 않는다** — `CLAUDE.md` 1절은 ②(진짜 문제)를 ③(해결 설계)보다 앞에 두는데, ②의 산출물 없이 ③④ 산출물(Spec 13개)이 이미 완성됐다. `91:129`가 "가정치로는 지금도 작성 가능"이라 판정하고도 6일간 착수하지 않았다. 재료(`13` Q8 실패 사례 4건, `13S` H1·H2 해석, PPT 가격 구조)는 이미 있다 | **반영 완료 (2026-07-31).** `docs/20_문제정의.md` 신설 — 발굴형 확정(반례 통과), 운영형은 발굴형에 흡수, 전환형은 검증 불가로 보류. `docs/50_실사용_도입설계.md` 신설 — 가정 명시 3표 + G1 실측(리스크 29와 통합) | project-critic 치명 2·12 |
| 23 | **"왜 콜드메일인가"의 채널 대안 비교가 `11`에 없고 공백 목록에도 등록 안 됨** | `11`에 6절 "채널 대안 비교" 신설 — 콜드메일 / 해외 마켓플레이스 입점 / 현지 에이전시 / 인바운드 강화 4축 × 초기비용·월운영·인원요구·기업 실패이력 4열. 관측 사실 3건(크몽 리드·10개언어 사이트·협력사 의존 실패)으로 초안 가능 | project-critic 치명 3 |
| 24 | **`12` Q1 확정(AI-Studio 단독) 이후 `31` 1-1절 "한국 진출 희망" 필수 게이트의 관련성 근거가 재검토되지 않았다** — 근거가 "모집단 축소"뿐인데 그건 편의이지 관련성이 아니다. Clo.D 제외로 이 조건의 원래 근거(동대문 소싱)가 약해졌을 수 있다 | **반영 완료 (2026-08-01).** `12` N22 답 확보 — 기업이 한국 진출 계획 없는 브랜드도 대상에 포함되며 이 조건이 중요하지 않다고 확인. 실측(n=3, 0/3 관찰)도 같은 방향. **필수 게이트에서 G3 적합도 가점으로 하향 확정** — `기준/G1` 1절·2-3절·4절, `specs/G1`, `기준/G2`·`기준/G3`, `40` V1 갱신(`91` 로그) | project-critic 치명 4 |
| 25 | **PPT 확인 산업(Fashion·Cosmetic·Jewelry·Furniture)과 설명회 발언 업종(패션·뷰티·푸드)이 어긋나는데 불일치로 등록 안 됨** — 푸드가 PPT 공식 산업에 없고 주얼리·가구가 발언에 없다. `13S` D1(3순위 업종 미확정)의 판별 재료가 새로 생겼는데 반영 안 됨 | **반영 완료 (2026-07-31).** `13S` D1 갱신(PPT의 Jewelry 명시를 기록, 층위가 다를 수 있음도 명시), `12` N14 갱신 + **N24 신설**로 기업에 1차 타겟 업종 재확인. 답은 대기 | project-critic 중대 11 |
| 26 | **가격 구조를 확보했는데 캐파·최소수주액 "미확인" 표기 7곳이 갱신 안 됨** — `12` Q10, `13S` U4, `기준/G1` 5절, `기준/G3` 4절, `specs/G1`·`specs/G5`, `40` V5 | **반영 완료 (2026-07-31).** 8곳을 "단가 구조는 확인됨(비공개 보유). 남은 것은 최소 수주 금액"으로 좁혔다. **2026-08-03 후속** — 같은 사고를 구조적으로 막기 위해 캐파·최소수주액 등 운영 변수를 `docs/50_실사용_도입설계.md` 단일 출처로 이관했다(`CLAUDE.md` 5절). 이후 이 8곳은 값 갱신 대상이 아니라 `50` 참조만 유지하면 된다 | project-critic 중대 6 |
| 27 | **`CLAUDE.md` 4·7절 라우팅 표 자체 오류 4건** — `31` 행 중복(설명 다름), 기획 본체를 `30`·`31` 두 곳으로 지칭, 기능 수 "G1~G10"(실제 G1~G11), 하네스 B ⑤ Hook을 "○"로 표시(실제로는 리스크 4가 폐기한 수치뿐) | **반영 완료 (2026-07-31).** `CLAUDE.md` 4절 표 정정(중복 병합·기능 수·기획 본체 단일화, `20`·`50` 행 추가), 7절 Hook·Spec 행을 실제 상태로 정정 | project-critic 중대 7·8 |
| 28 | **매출 로드맵 GLOBAL 트랙과 이번 과제가 같은 트랙인지 미검증** — 로드맵 태그(B2B 중계·해외 B2C·PB)는 상품 판매로 읽히고 이번 과제는 AI-Studio 제작 서비스 판매다. `10` 5절이 이미 가설로 낮춰 반례를 병기했다(2026-07-31 처리 완료) | **반영 완료 (2026-07-31).** `12`에 **N21 신설**(같은 트랙인지 질문). 기업 답 오기 전까지 이 로드맵을 Q1 확정의 지지 근거로 인용하지 않는다 | project-critic 중대 9 |
| 29 | **G1 실제 처리량(수백 건 후보 처리 시간·비용)이 어디에도 산정되지 않았다** | **반영 완료 (2026-07-31).** 일본 기업 3곳(INGNI·ROPÉ PICNIC·Honeys) 실측 — `specs/candidates/`, 결과·외삽은 `50_실사용_도입설계.md` 2절. **n=3(목표 5에 못 미침)** — 토큰 비용은 측정 안 하고 웹 요청 횟수·벽시계 시간만 실측. 부수 발견: 3곳 전부 한국 진출 신호 0/3 → 리스크 24와 연결 | project-critic 치명 13 |

### 2-5. 2026-08-01 `project-critic` 감사(불필요한 검증 게이트 점검)로 새로 등록된 리스크

> **사용자 지시의 방향**: 자동화의 목적이 넓게 뿌리는 것인데, 근거 없는 검증·관찰이 후보를 사전에 다 걸러내면 브리프 요구 ①(자동 탐색·목록화)이 성립하지 않는다. **아래 중 A-1·E-1·E-2는 그 방향과 직접 겹쳐 바로 반영했고, 나머지는 후보 물량을 줄이는 게이트가 아니라 남겨 두거나 별도 판단으로 미뤘다.**

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 30 | **O6 규모 신호 중 ⓒ資本金·ⓓ従業員数·ⓔ설립연도가 기업 발언 근거 없이 필수 관찰이었고, 걸러내는 데도 안 쓰이는데 웹 요청을 2배로 늘린 것이 실측됐다** | **반영 완료 (2026-08-01).** ⓐ상품개수·ⓑ채널수만 필수로 남기고 나머지 셋은 선택 관찰로 하향 — `기준/G1` 2-1절·변경이력, `specs/G1` | project-critic A-1 |
| 31 | **G5 번역 검증 3층 중 "어조·방향"이 회신 해석(거절↔관심)의 판정 문장을 그대로 발신 메일의 무관용 기준으로 썼다** — 발신 메일에는 거절·관심이라는 대상이 없어 판정 불가능한 조건이었다 | **반영 완료 (2026-08-01).** `기준/G7` 3절에 G5 전용 판정 문장(정중함 붕괴·단정 뒤집힘) 신설, `specs/G5` 성공 기준 문구 정정 | project-critic E-1 |
| 32 | **V1-a 표본 수가 문서마다 다르다(10곳 vs 5~10곳)** — O7 강등 조건("10곳 모두 관찰률 0")이 실제 계획("5~10곳")과 안 맞아, 강등 여부를 판단할 수 없는 상태였다 | **반영 완료 (2026-08-01).** 리스크 24 처리로 조건4 자체가 삭제돼 이 불일치도 함께 해소 — `기준/판정시트_G1.md`에 "이미 결정됨" 안내로 교체 | project-critic E-2 |
| 33 | **A-2(G8 원인 분석 판정 근거 취약) · B군(G6·G8 근거 없는 임계값 3건) · C-1(국가 확대 전제의 순환 근거) · C-2(G10 트리거 부재) · D-1(G11 브리프 대응표 미표시) · F군(경미 5건)은 이번에 손대지 않았다** | **검토 보류.** 전부 **후보를 사전에 걸러 물량을 줄이는 게이트가 아니다** — 응답 이후 처리 로직(G6·G8)이거나 표시·라벨링 문제(C-1·C-2·D-1)라 이번 우선순위(검증 게이트 축소)와 결이 다르다고 판단했다. 다음에 다룰 때는 `project-critic` 원본 보고(2026-08-01 세션)를 그대로 근거로 쓴다 | project-critic A-2·B·C-1·C-2·D-1·F |

### 2-6. 2026-08-03 MVP 재정의 — G3 이연으로 대체 규칙이 필요해진 지점

| # | 내용 | 대응 | 출처 |
|---|---|---|---|
| 34 | **G3(적합도 순위)을 부가 기능으로 이연하면서, G4 탐색 순서·G5 발송 상한 컷오프가 G3 출력에 의존하던 자리가 비게 됐다** — 대체 규칙 없이 그냥 끄면 두 기능의 성공 기준이 판정 불가능해진다(`90` 리스크 20이 되살아나는 형태) | **반영 완료 (2026-08-03).** G4는 "G2 세그먼트 대표 기업 우선"으로, G5는 "세그먼트별 라운드로빈"으로 대체 — `specs/G4`·`specs/G5`. G8·G9·G10·G11의 G3 의존 항목은 그 넷 자체가 부가 범위라 잠정 규칙(低 기본/소스 제외)으로만 처리. 근거는 `33` D15 | 사용자 결정 + opus 사전 검토 |

### 2-3. 지금 살아 있는 리스크만 추리면

목록이 21건이라 무엇을 보고 있어야 하는지가 묻힌다. **상태별로 갈라 둔다.**

| 상태 | 번호 |
|---|---|
| **살아 있다 — 계속 본다** | **2**(지표명 단정) · **6**(버즈워드) · **7**(의사결정권자 연락처) · **11**(캐파 초과) · **12**(도메인 평판) · **13**(레퍼런스 인용 허가) · **18**(A에 강제 장치 없음) · **21**(회신 이후 경로) |
| **설계에 반영해 대응 완료 — 되돌아가지 않는지만 확인** | **15**·**17**·**19**·**20** |
| **실행 대기** | **16** (→ `40` V1) |
| **해소·폐기·통합** | **1**·**3**·**4**·**5**·**8**·**9**·**10**·**14** |
| **신규 (2026-07-31, `project-critic`) — 반영 완료** | **22**(`20`·`50` 신설) · **23**(`11` 8절 채널 비교) · **24**(N22 답 확보, 필수→적합도 하향, 2026-08-01) · **25**(업종 불일치, N24 신설) · **26**(미확인 표기 8곳 갱신) · **27**(`CLAUDE.md` 라우팅 표 정정) · **28**(N21 신설) · **29**(G1 3곳 실측) |
| **신규 (2026-08-01, `project-critic` 검증 게이트 감사) — 반영 완료** | **30**(O6 3항목 선택 하향) · **31**(G5 층3 판정 문장 신설) · **32**(V1-a 표본 수 불일치 해소) |
| **신규 (2026-08-01) — 검토 보류** | **33**(A-2·B군·C-1·C-2·D-1·F군 — 게이트 축소와 결이 달라 다음 순위로) |
| **신규 (2026-08-03, MVP 재정의) — 반영 완료** | **34**(G4·G5 대체 규칙) |

**살아 있는 8건 중 4건(11·12·13·21)이 기업 답을 기다린다** — 제작 캐파, 발송 도메인, 레퍼런스 인용 허가, 회신 이후 실무 준비. 이 넷은 우리가 더 할 수 있는 것이 없고 `12`의 해당 질문에 걸려 있다.

## 3. 다음 액션

> 이 목록을 어떤 순서로 처리할지는 `01_진행계획.md`의 A~E 단계를 따른다. A2(회사 조사)가 대부분의 선행 조건이다.

- [ ] **`12` N17 확인 — 일본 발송 적법성** (신규 최우선). 리스크 19. 답에 따라 관찰 항목이 하나 늘어나므로 **설계 변경 사항**이다. 확인 전에는 실제 발송을 전제한 어떤 결정도 확정하지 않는다
- [ ] **`40_검증계획.md` V1 실행 — 관찰 판정 일치 검증** (`01` C4). 미팅 답을 기다리지 않고 지금 할 수 있는 유일한 검증이며, 결과가 `31` G1 관찰층 서술의 수정본과 하네스 프롬프트의 판정 규칙이 된다
  - [ ] **판정 대상이 2026-07-30 교체됐다** — 신호 4종이 아니라 `31` G1 관찰층(제품 사진 / 커머스 / 업종 / 규모 신호 / 한국 진출 신호 / 거부 표시). 표본은 **일본**으로 고정
  - [ ] V1-a(사람↔사람) 먼저. 기록 양식에 **판정 불가 행**을 포함할 것 (리스크 17)
  - [ ] **갈릴 것이 예상되는 두 항목**(규모 신호 / 거부 표시)이 이 검증의 실제 목적이다. 갈리면 항목 정의를 쪼개거나 필수에서 적합도로 내린다. (한국 진출 신호는 2026-08-01 별도로 하향 확정 — 리스크 24 참조)
  - [ ] V1-a 결과로 고친 기준 문장 = Build Order ② Skill 초안
  - [x] **V1-b용 실행체 1개 — 2026-07-30 완료.** `.claude/skills/g1-기업판정/`을 `specs/G1` 「제약」의 실행체 경계 4개대로 만들었다(기업 1건씩 / 출력 파일 1개 / 공개 페이지 읽기까지 / 기업 1곳 = 컨텍스트 1단위). **다만 V1-a(사람↔사람 판정)가 아직 안 끝나 V1-b는 여전히 대기다** — 실행체가 생겼다고 검증 순서(V1-a 먼저)가 바뀌지 않는다
  - [ ] **V1-a가 고치는 대상은 `기준/G1_기업판정기준.md`의 판정 문장이다** (2026-07-30 정정 — 이전에는 `31` G1 관찰층을 가리켰다). 고친 이유는 그 카드의 「변경 이력」에 남기고, **그 이력이 곧 검증 기록**이 된다
- [ ] **`13` 7-1절 R1 반영 — 국가 선정 3축의 용도 변경** (미반영). 일본을 3축으로 대조해 캘리브레이션하고, 그 3축으로 2차 국가를 제시한다. 이게 없으면 "왜 일본인가"에 답이 "기업이 그랬다"뿐이다. **`40` V3이 이 작업의 검증 항목이다**
- [ ] **`13` 7-2절 R2 반영 — 반자동 범위를 실물로 되묻기** (미반영). 추상 선택지로는 기업이 못 골랐으므로(`13S` U12) 승인 화면 목업 + 메일 초안 3종을 놓고 확인받는다. **세그먼트 3안을 묻는 `12` N19와 같은 자리에서 한다** — 둘 다 실물을 놓고 되묻는 방식이고, 그 답이 `40` **V7**의 입력이다
- [ ] **`specs/G1` 「확인 후 채울 것」을 채운다** — `12` Q9·Q10·N11 답과 발송 데이터 대기. 그때까지 Checklist는 확정분으로만 운용
- [ ] **남은 기능의 Spec 작성** — 지금 있는 것은 **G1·G7·G2** 셋이다. 각 Spec은 **Workflow인가 Agent인가를 반드시 선언**하고(미결정 #8), 자체 기준 검증을 만들면 **V 번호를 `40`에서 받아 간다**
  - [x] **G2 세그먼트화 — 2026-07-30 완료.** `specs/G2_세그먼트화.md` + `기준/G2_세그먼트기준.md`. 브리프 요구 ②의 담당인데 Spec도 검증도 비어 있던 자리다
  - [x] **기존 3개 Spec에 작성 규칙 적용 마무리 — 2026-07-30 완료.** `specs/README.md` 규칙 3(활용 방식 선언)·4(4경계)가 세 Spec 전부에 없던 것을 「제약」에 채웠고, 근거 4건을 `33` D4~D8로 이관, **끊긴 절 번호 참조 14곳**과 **`기준/G2` 관찰 항목 코드 밀림**을 정정. 상세는 `91`
  - [x] **G3 적합도 평가 — 2026-07-30 완료.** `specs/G3_적합도평가.md` + `기준/G3_적합도기준.md`. 작성 중 `31` 6절 #2·#3을 실제로 해소했다 — G2는 입력에서 뺐고(전체 절대 비교), 척도는 넷 다 **단일 척도 + 소비처별 기준선**으로 고정(`33` D9). `40` V8은 *"척도가 하나인가"*에서 *"그 척도의 가중치가 실제 회신과 맞는가"*로 성격이 바뀌었다
  - [x] **G4 연락처 확보 — 2026-07-30 완료.** `specs/G4_연락처확보.md` + `기준/G4_연락처유효성기준.md`. 옵트인 예외 판정은 `기준/G1` 2-2절 거부 문구 목록을 재사용하되, **딥서치로 새로 찾은 주소는 G1이 본 적이 없으므로 그 자리에서 다시 확인한다**는 절차를 이번에 처음 명시했다
  - [x] **G5 제안 메일 제작·발송 — 2026-07-30 완료.** `specs/G5_제안메일제작발송.md`. 요구 ②③의 나머지 절반. **발송 도구 권한을 가진 유일한 기능**이며 승인 기록 없이는 권한을 행사하지 않는 조건으로 반자동을 지킨다. 번역 검증은 `기준/G7` 3절 재사용, 별도 카드 없음
  - [x] **G6 발송 결과 판정 — 2026-07-30 완료.** `specs/G6_발송결과판정.md` + `기준/G6_판정기준.md`. 바운스 코드 3분기, 무응답 판정 대기는 **잠정 10영업일**(구 "7영업일"은 재사용하지 않음 — 리스크 4가 폐기 판정), G7 정정 반영 절차 포함
  - [x] **G8 미응답·미도달 원인 분석 — 2026-07-30 완료.** `specs/G8_원인분석.md` + `기준/G8_원인분석기준.md`. 무시/도달실패를 가르는 신호는 G4 연락처 등급 하나, [주소오류]는 항상 조사오류로 직행(G10)
  - [x] **G9 사람 접촉 위임 브리프 — 2026-07-30 완료.** `specs/G9_위임브리프.md`. 경로 1·2를 하나의 브리프 형식으로 통합, 사람이 찾은 연락처도 `기준/G4` 등급 판정을 그대로 거침(우회 지름길 없음). 별도 기준 카드 없음
  - [x] **G10 재수집 판단 — 2026-07-30 완료.** `specs/G10_재수집판단.md`. 재조사 항목은 G1 코드(O1~O9)로만 지정, 재수집 주기는 수치를 정하지 않음(구 60일·90일 재사용 안 함 — 리스크 4), "특정 항목만 재조사" 성립 여부는 `40` V1 결과 대기로 명시. 별도 기준 카드 없음
  - [x] **G11 반응 집계·기준 조정 — 2026-07-30 완료 (부분 확정).** `specs/G11_반응집계_기준조정.md`. `31`의 "지금 못 쓴다"는 판단을 **구조/값 분리로 부분 철회**(`33` D11, `40` 0절과 같은 논리) — 입출력·집계 형식·조정 제안 구조는 지금 확정, 실제 집계 결과·조정 제안·최소 표본 크기는 발송 데이터 대기. **G2·G3 파일에 쓰기 권한이 없다**는 도구 경계로 "제안이지 자동 적용 아니다"를 물리적으로 강제. **이로써 G1~G11 전 기능의 Spec 작성이 완료됐다**
- [ ] 대안 비교·검증 과정을 발표 자료에 명시적으로 포함

### 완료·폐기된 액션 (2026-07-30 정리)

지운 것이 아니라 **왜 더 안 해도 되는지**를 남긴다.

| 액션 | 처리 |
|---|---|
| ~~1절 미결정 8건 확정~~ | **완료.** #1~#5는 `31`에서, #6·#7은 2026-07-30에 확정, #8은 기능별 Spec 항목으로 성격 변경 (1절) |
| ~~코트라 공개 열람 범위 확인 + 대체 소스 비교 조사~~ | **완료 (2026-07-29).** `11_대안탐색_비교.md` — OpenAPI 무료 공개 확인, ITC Trade Map 조건부, UNCTAD 탈락 |
| ~~국가 선정 3축의 지표·임계값 확정~~ | **성격 변경.** 국가를 고르는 용도가 아니게 됐다(일본 지정). 남은 것은 **캘리브레이션**이며 위 R1 항목과 `40` V3으로 넘어갔다 |
| ~~타겟 국가 1개 확정 — 여러 국가 동시 조사 금지~~ | **완료.** 기업이 일본으로 지정했다(`13S` K1). 우리가 고를 사안이 아니었다 |
| ~~국가 선정 기준과 기업 발굴 기준을 별도 문서로 정리~~ | **완료.** 국가 선정 근거는 `11`, 기업 발굴 기준은 `31` G1 + `specs/G1`로 갈려 있다 |
| ~~기업 미팅 시 실제 실패 사례 확보~~ | **완료 (2026-07-30 설명회).** 4건 확보 — 일본 조사 난항·협력사 의존 / 플랫폼 다수로 전수 탐색 불가 / SNS 계정 정지 / 담당자 정보 미갱신으로 미도달 (`13S` F10~F13) |

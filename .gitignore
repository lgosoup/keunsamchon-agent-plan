# --- 민감 정보 (절대 커밋 금지) ---
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
credentials.json
service-account*.json
secrets.*
*_token*
*_secret*

# --- 로컬 전용 설정 ---
# 개인 권한 설정이라 팀원과 공유하지 않는다 (사람마다 다르게 두는 파일).
.claude/settings.local.json

# --- OS / 편집기 ---
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# --- 임시 산출물 ---
*.tmp
*.bak
~$*

# --- 타인 저작물 (public 저장소에 올리지 않는다) ---
# 수업 교육 콘텐츠의 지식재산권은 교육 운영 주체에 있고 복제·공개가 금지돼 있다.
# 개념을 우리 말로 재서술한 docs/수업/00_개념정리.md만 공개 대상이다 (CLAUDE.md 6절).
*mission_answer_sheet.pdf
*mission_answer*.pdf
*mission_answer_sheet.zip
*mission_answer*.zip
포멧/
큰아버지의 두아들_데일리리포트_*.pdf

# --- 기업 비공개 자료 (public 저장소에 올리지 않는다) ---
# PPT 자체가 "보안사항 포함"으로 표기돼 있다. 파생 사실은 docs/10에 요약해 남기되
# 원본과 세부 수치(가격표·매출 로드맵·경쟁사 맵)는 올리지 않는다 (CLAUDE.md 6절).
reference/회사소개서_큰삼촌컴퍼니_2026_3Q.pdf

# 부트캠프 운영 측 배포 자료 — 타인 저작물이라 커밋하지 않는다 (CLAUDE.md 6절, 2026-08-05).
\[Upstage\] 제주대학교 x Upstage 부트캠프 교육 가이드.docx
